1/2: Building GenForSome (GenForSome.idr)
2/2: Building Run (Run.idr)
Now compiling the executable: deptycheck-usage-example
define `x1`
-- post defs: ["x1"]

----

define `x2`
-- post defs: ["x2"]

----

define `x3`
-- post defs: ["x3"]

----

define `y1`
-- post defs: ["y1"]

----

define `y2`
-- post defs: ["y2"]

----

define `y3`
-- post defs: ["y3"]

----

define `z1`
-- post defs: ["z1"]

----

define `z2`
-- post defs: ["z2"]

----

define `z3`
-- post defs: ["z3"]

----

define `x1`
define `x2`
-- post defs: ["x2", "x1"]

----

define `x1`
define `x3`
-- post defs: ["x3", "x1"]

----

define `x1`
define `y1`
-- post defs: ["y1", "x1"]

----

define `x1`
define `y2`
-- post defs: ["y2", "x1"]

----

define `x1`
define `y3`
-- post defs: ["y3", "x1"]

----

define `x1`
define `z1`
-- post defs: ["z1", "x1"]

----

define `x1`
define `z2`
-- post defs: ["z2", "x1"]

----

define `x1`
define `z3`
-- post defs: ["z3", "x1"]

----

define `x1`
use `x1`
-- post defs: ["x1"]

----

define `x1`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "x1"]

----

define `x1`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "x1"]

----

define `x1`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "x1"]

----

define `x1`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "x1"]

----

define `x1`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "x1"]

----

define `x1`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "x1"]

----

define `x1`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "x1"]

----

define `x1`
define `x2`
use `x2`
-- post defs: ["x2", "x1"]

----

define `x1`
define `x2`
use `x1`
-- post defs: ["x2", "x1"]

----

define `x1`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "x1"]

----

define `x1`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "x1"]

----

define `x1`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "x1"]

----

define `x1`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "x1"]

----

define `x1`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "x1"]

----

define `x1`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "x1"]

----

define `x1`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "x1"]

----

define `x1`
define `x3`
use `x3`
-- post defs: ["x3", "x1"]

----

define `x1`
define `x3`
use `x1`
-- post defs: ["x3", "x1"]

----

define `x1`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "x1"]

----

define `x1`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "x1"]

----

define `x1`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "x1"]

----

define `x1`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "x1"]

----

define `x1`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "x1"]

----

define `x1`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "x1"]

----

define `x1`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "x1"]

----

define `x1`
define `y1`
use `y1`
-- post defs: ["y1", "x1"]

----

define `x1`
define `y1`
use `x1`
-- post defs: ["y1", "x1"]

----

define `x1`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "x1"]

----

define `x1`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "x1"]

----

define `x1`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "x1"]

----

define `x1`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "x1"]

----

define `x1`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "x1"]

----

define `x1`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "x1"]

----

define `x1`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "x1"]

----

define `x1`
define `y2`
use `y2`
-- post defs: ["y2", "x1"]

----

define `x1`
define `y2`
use `x1`
-- post defs: ["y2", "x1"]

----

define `x1`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "x1"]

----

define `x1`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "x1"]

----

define `x1`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "x1"]

----

define `x1`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "x1"]

----

define `x1`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "x1"]

----

define `x1`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "x1"]

----

define `x1`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "x1"]

----

define `x1`
define `y3`
use `y3`
-- post defs: ["y3", "x1"]

----

define `x1`
define `y3`
use `x1`
-- post defs: ["y3", "x1"]

----

define `x1`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "x1"]

----

define `x1`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "x1"]

----

define `x1`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "x1"]

----

define `x1`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "x1"]

----

define `x1`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "x1"]

----

define `x1`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "x1"]

----

define `x1`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "x1"]

----

define `x1`
define `z1`
use `z1`
-- post defs: ["z1", "x1"]

----

define `x1`
define `z1`
use `x1`
-- post defs: ["z1", "x1"]

----

define `x1`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "x1"]

----

define `x1`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "x1"]

----

define `x1`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "x1"]

----

define `x1`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "x1"]

----

define `x1`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "x1"]

----

define `x1`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "x1"]

----

define `x1`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "x1"]

----

define `x1`
define `z2`
use `z2`
-- post defs: ["z2", "x1"]

----

define `x1`
define `z2`
use `x1`
-- post defs: ["z2", "x1"]

----

define `x1`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "x1"]

----

define `x1`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "x1"]

----

define `x1`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "x1"]

----

define `x1`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "x1"]

----

define `x1`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "x1"]

----

define `x1`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "x1"]

----

define `x1`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "x1"]

----

define `x1`
define `z3`
use `z3`
-- post defs: ["z3", "x1"]

----

define `x1`
define `z3`
use `x1`
-- post defs: ["z3", "x1"]

----

define `x1`
use `x1`
define `x2`
-- post defs: ["x2", "x1"]

----

define `x1`
use `x1`
define `x3`
-- post defs: ["x3", "x1"]

----

define `x1`
use `x1`
define `y1`
-- post defs: ["y1", "x1"]

----

define `x1`
use `x1`
define `y2`
-- post defs: ["y2", "x1"]

----

define `x1`
use `x1`
define `y3`
-- post defs: ["y3", "x1"]

----

define `x1`
use `x1`
define `z1`
-- post defs: ["z1", "x1"]

----

define `x1`
use `x1`
define `z2`
-- post defs: ["z2", "x1"]

----

define `x1`
use `x1`
define `z3`
-- post defs: ["z3", "x1"]

----

define `x1`
use `x1`
use `x1`
-- post defs: ["x1"]

----

define `x2`
define `x1`
-- post defs: ["x1", "x2"]

----

define `x2`
define `x3`
-- post defs: ["x3", "x2"]

----

define `x2`
define `y1`
-- post defs: ["y1", "x2"]

----

define `x2`
define `y2`
-- post defs: ["y2", "x2"]

----

define `x2`
define `y3`
-- post defs: ["y3", "x2"]

----

define `x2`
define `z1`
-- post defs: ["z1", "x2"]

----

define `x2`
define `z2`
-- post defs: ["z2", "x2"]

----

define `x2`
define `z3`
-- post defs: ["z3", "x2"]

----

define `x2`
use `x2`
-- post defs: ["x2"]

----

define `x2`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "x2"]

----

define `x2`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "x2"]

----

define `x2`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "x2"]

----

define `x2`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "x2"]

----

define `x2`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "x2"]

----

define `x2`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "x2"]

----

define `x2`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "x2"]

----

define `x2`
define `x1`
use `x1`
-- post defs: ["x1", "x2"]

----

define `x2`
define `x1`
use `x2`
-- post defs: ["x1", "x2"]

----

define `x2`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "x2"]

----

define `x2`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "x2"]

----

define `x2`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "x2"]

----

define `x2`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "x2"]

----

define `x2`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "x2"]

----

define `x2`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "x2"]

----

define `x2`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "x2"]

----

define `x2`
define `x3`
use `x3`
-- post defs: ["x3", "x2"]

----

define `x2`
define `x3`
use `x2`
-- post defs: ["x3", "x2"]

----

define `x2`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "x2"]

----

define `x2`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "x2"]

----

define `x2`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "x2"]

----

define `x2`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "x2"]

----

define `x2`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "x2"]

----

define `x2`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "x2"]

----

define `x2`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "x2"]

----

define `x2`
define `y1`
use `y1`
-- post defs: ["y1", "x2"]

----

define `x2`
define `y1`
use `x2`
-- post defs: ["y1", "x2"]

----

define `x2`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "x2"]

----

define `x2`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "x2"]

----

define `x2`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "x2"]

----

define `x2`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "x2"]

----

define `x2`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "x2"]

----

define `x2`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "x2"]

----

define `x2`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "x2"]

----

define `x2`
define `y2`
use `y2`
-- post defs: ["y2", "x2"]

----

define `x2`
define `y2`
use `x2`
-- post defs: ["y2", "x2"]

----

define `x2`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "x2"]

----

define `x2`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "x2"]

----

define `x2`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "x2"]

----

define `x2`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "x2"]

----

define `x2`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "x2"]

----

define `x2`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "x2"]

----

define `x2`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "x2"]

----

define `x2`
define `y3`
use `y3`
-- post defs: ["y3", "x2"]

----

define `x2`
define `y3`
use `x2`
-- post defs: ["y3", "x2"]

----

define `x2`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "x2"]

----

define `x2`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "x2"]

----

define `x2`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "x2"]

----

define `x2`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "x2"]

----

define `x2`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "x2"]

----

define `x2`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "x2"]

----

define `x2`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "x2"]

----

define `x2`
define `z1`
use `z1`
-- post defs: ["z1", "x2"]

----

define `x2`
define `z1`
use `x2`
-- post defs: ["z1", "x2"]

----

define `x2`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "x2"]

----

define `x2`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "x2"]

----

define `x2`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "x2"]

----

define `x2`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "x2"]

----

define `x2`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "x2"]

----

define `x2`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "x2"]

----

define `x2`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "x2"]

----

define `x2`
define `z2`
use `z2`
-- post defs: ["z2", "x2"]

----

define `x2`
define `z2`
use `x2`
-- post defs: ["z2", "x2"]

----

define `x2`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "x2"]

----

define `x2`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "x2"]

----

define `x2`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "x2"]

----

define `x2`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "x2"]

----

define `x2`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "x2"]

----

define `x2`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "x2"]

----

define `x2`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "x2"]

----

define `x2`
define `z3`
use `z3`
-- post defs: ["z3", "x2"]

----

define `x2`
define `z3`
use `x2`
-- post defs: ["z3", "x2"]

----

define `x2`
use `x2`
define `x1`
-- post defs: ["x1", "x2"]

----

define `x2`
use `x2`
define `x3`
-- post defs: ["x3", "x2"]

----

define `x2`
use `x2`
define `y1`
-- post defs: ["y1", "x2"]

----

define `x2`
use `x2`
define `y2`
-- post defs: ["y2", "x2"]

----

define `x2`
use `x2`
define `y3`
-- post defs: ["y3", "x2"]

----

define `x2`
use `x2`
define `z1`
-- post defs: ["z1", "x2"]

----

define `x2`
use `x2`
define `z2`
-- post defs: ["z2", "x2"]

----

define `x2`
use `x2`
define `z3`
-- post defs: ["z3", "x2"]

----

define `x2`
use `x2`
use `x2`
-- post defs: ["x2"]

----

define `x3`
define `x1`
-- post defs: ["x1", "x3"]

----

define `x3`
define `x2`
-- post defs: ["x2", "x3"]

----

define `x3`
define `y1`
-- post defs: ["y1", "x3"]

----

define `x3`
define `y2`
-- post defs: ["y2", "x3"]

----

define `x3`
define `y3`
-- post defs: ["y3", "x3"]

----

define `x3`
define `z1`
-- post defs: ["z1", "x3"]

----

define `x3`
define `z2`
-- post defs: ["z2", "x3"]

----

define `x3`
define `z3`
-- post defs: ["z3", "x3"]

----

define `x3`
use `x3`
-- post defs: ["x3"]

----

define `x3`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "x3"]

----

define `x3`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "x3"]

----

define `x3`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "x3"]

----

define `x3`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "x3"]

----

define `x3`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "x3"]

----

define `x3`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "x3"]

----

define `x3`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "x3"]

----

define `x3`
define `x1`
use `x1`
-- post defs: ["x1", "x3"]

----

define `x3`
define `x1`
use `x3`
-- post defs: ["x1", "x3"]

----

define `x3`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "x3"]

----

define `x3`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "x3"]

----

define `x3`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "x3"]

----

define `x3`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "x3"]

----

define `x3`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "x3"]

----

define `x3`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "x3"]

----

define `x3`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "x3"]

----

define `x3`
define `x2`
use `x2`
-- post defs: ["x2", "x3"]

----

define `x3`
define `x2`
use `x3`
-- post defs: ["x2", "x3"]

----

define `x3`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "x3"]

----

define `x3`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "x3"]

----

define `x3`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "x3"]

----

define `x3`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "x3"]

----

define `x3`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "x3"]

----

define `x3`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "x3"]

----

define `x3`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "x3"]

----

define `x3`
define `y1`
use `y1`
-- post defs: ["y1", "x3"]

----

define `x3`
define `y1`
use `x3`
-- post defs: ["y1", "x3"]

----

define `x3`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "x3"]

----

define `x3`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "x3"]

----

define `x3`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "x3"]

----

define `x3`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "x3"]

----

define `x3`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "x3"]

----

define `x3`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "x3"]

----

define `x3`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "x3"]

----

define `x3`
define `y2`
use `y2`
-- post defs: ["y2", "x3"]

----

define `x3`
define `y2`
use `x3`
-- post defs: ["y2", "x3"]

----

define `x3`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "x3"]

----

define `x3`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "x3"]

----

define `x3`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "x3"]

----

define `x3`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "x3"]

----

define `x3`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "x3"]

----

define `x3`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "x3"]

----

define `x3`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "x3"]

----

define `x3`
define `y3`
use `y3`
-- post defs: ["y3", "x3"]

----

define `x3`
define `y3`
use `x3`
-- post defs: ["y3", "x3"]

----

define `x3`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "x3"]

----

define `x3`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "x3"]

----

define `x3`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "x3"]

----

define `x3`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "x3"]

----

define `x3`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "x3"]

----

define `x3`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "x3"]

----

define `x3`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "x3"]

----

define `x3`
define `z1`
use `z1`
-- post defs: ["z1", "x3"]

----

define `x3`
define `z1`
use `x3`
-- post defs: ["z1", "x3"]

----

define `x3`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "x3"]

----

define `x3`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "x3"]

----

define `x3`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "x3"]

----

define `x3`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "x3"]

----

define `x3`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "x3"]

----

define `x3`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "x3"]

----

define `x3`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "x3"]

----

define `x3`
define `z2`
use `z2`
-- post defs: ["z2", "x3"]

----

define `x3`
define `z2`
use `x3`
-- post defs: ["z2", "x3"]

----

define `x3`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "x3"]

----

define `x3`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "x3"]

----

define `x3`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "x3"]

----

define `x3`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "x3"]

----

define `x3`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "x3"]

----

define `x3`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "x3"]

----

define `x3`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "x3"]

----

define `x3`
define `z3`
use `z3`
-- post defs: ["z3", "x3"]

----

define `x3`
define `z3`
use `x3`
-- post defs: ["z3", "x3"]

----

define `x3`
use `x3`
define `x1`
-- post defs: ["x1", "x3"]

----

define `x3`
use `x3`
define `x2`
-- post defs: ["x2", "x3"]

----

define `x3`
use `x3`
define `y1`
-- post defs: ["y1", "x3"]

----

define `x3`
use `x3`
define `y2`
-- post defs: ["y2", "x3"]

----

define `x3`
use `x3`
define `y3`
-- post defs: ["y3", "x3"]

----

define `x3`
use `x3`
define `z1`
-- post defs: ["z1", "x3"]

----

define `x3`
use `x3`
define `z2`
-- post defs: ["z2", "x3"]

----

define `x3`
use `x3`
define `z3`
-- post defs: ["z3", "x3"]

----

define `x3`
use `x3`
use `x3`
-- post defs: ["x3"]

----

define `y1`
define `x1`
-- post defs: ["x1", "y1"]

----

define `y1`
define `x2`
-- post defs: ["x2", "y1"]

----

define `y1`
define `x3`
-- post defs: ["x3", "y1"]

----

define `y1`
define `y2`
-- post defs: ["y2", "y1"]

----

define `y1`
define `y3`
-- post defs: ["y3", "y1"]

----

define `y1`
define `z1`
-- post defs: ["z1", "y1"]

----

define `y1`
define `z2`
-- post defs: ["z2", "y1"]

----

define `y1`
define `z3`
-- post defs: ["z3", "y1"]

----

define `y1`
use `y1`
-- post defs: ["y1"]

----

define `y1`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "y1"]

----

define `y1`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "y1"]

----

define `y1`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "y1"]

----

define `y1`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "y1"]

----

define `y1`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "y1"]

----

define `y1`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "y1"]

----

define `y1`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "y1"]

----

define `y1`
define `x1`
use `x1`
-- post defs: ["x1", "y1"]

----

define `y1`
define `x1`
use `y1`
-- post defs: ["x1", "y1"]

----

define `y1`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "y1"]

----

define `y1`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "y1"]

----

define `y1`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "y1"]

----

define `y1`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "y1"]

----

define `y1`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "y1"]

----

define `y1`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "y1"]

----

define `y1`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "y1"]

----

define `y1`
define `x2`
use `x2`
-- post defs: ["x2", "y1"]

----

define `y1`
define `x2`
use `y1`
-- post defs: ["x2", "y1"]

----

define `y1`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "y1"]

----

define `y1`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "y1"]

----

define `y1`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "y1"]

----

define `y1`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "y1"]

----

define `y1`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "y1"]

----

define `y1`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "y1"]

----

define `y1`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "y1"]

----

define `y1`
define `x3`
use `x3`
-- post defs: ["x3", "y1"]

----

define `y1`
define `x3`
use `y1`
-- post defs: ["x3", "y1"]

----

define `y1`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "y1"]

----

define `y1`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "y1"]

----

define `y1`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "y1"]

----

define `y1`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "y1"]

----

define `y1`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "y1"]

----

define `y1`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "y1"]

----

define `y1`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "y1"]

----

define `y1`
define `y2`
use `y2`
-- post defs: ["y2", "y1"]

----

define `y1`
define `y2`
use `y1`
-- post defs: ["y2", "y1"]

----

define `y1`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "y1"]

----

define `y1`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "y1"]

----

define `y1`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "y1"]

----

define `y1`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "y1"]

----

define `y1`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "y1"]

----

define `y1`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "y1"]

----

define `y1`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "y1"]

----

define `y1`
define `y3`
use `y3`
-- post defs: ["y3", "y1"]

----

define `y1`
define `y3`
use `y1`
-- post defs: ["y3", "y1"]

----

define `y1`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "y1"]

----

define `y1`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "y1"]

----

define `y1`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "y1"]

----

define `y1`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "y1"]

----

define `y1`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "y1"]

----

define `y1`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "y1"]

----

define `y1`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "y1"]

----

define `y1`
define `z1`
use `z1`
-- post defs: ["z1", "y1"]

----

define `y1`
define `z1`
use `y1`
-- post defs: ["z1", "y1"]

----

define `y1`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "y1"]

----

define `y1`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "y1"]

----

define `y1`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "y1"]

----

define `y1`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "y1"]

----

define `y1`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "y1"]

----

define `y1`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "y1"]

----

define `y1`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "y1"]

----

define `y1`
define `z2`
use `z2`
-- post defs: ["z2", "y1"]

----

define `y1`
define `z2`
use `y1`
-- post defs: ["z2", "y1"]

----

define `y1`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "y1"]

----

define `y1`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "y1"]

----

define `y1`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "y1"]

----

define `y1`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "y1"]

----

define `y1`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "y1"]

----

define `y1`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "y1"]

----

define `y1`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "y1"]

----

define `y1`
define `z3`
use `z3`
-- post defs: ["z3", "y1"]

----

define `y1`
define `z3`
use `y1`
-- post defs: ["z3", "y1"]

----

define `y1`
use `y1`
define `x1`
-- post defs: ["x1", "y1"]

----

define `y1`
use `y1`
define `x2`
-- post defs: ["x2", "y1"]

----

define `y1`
use `y1`
define `x3`
-- post defs: ["x3", "y1"]

----

define `y1`
use `y1`
define `y2`
-- post defs: ["y2", "y1"]

----

define `y1`
use `y1`
define `y3`
-- post defs: ["y3", "y1"]

----

define `y1`
use `y1`
define `z1`
-- post defs: ["z1", "y1"]

----

define `y1`
use `y1`
define `z2`
-- post defs: ["z2", "y1"]

----

define `y1`
use `y1`
define `z3`
-- post defs: ["z3", "y1"]

----

define `y1`
use `y1`
use `y1`
-- post defs: ["y1"]

----

define `y2`
define `x1`
-- post defs: ["x1", "y2"]

----

define `y2`
define `x2`
-- post defs: ["x2", "y2"]

----

define `y2`
define `x3`
-- post defs: ["x3", "y2"]

----

define `y2`
define `y1`
-- post defs: ["y1", "y2"]

----

define `y2`
define `y3`
-- post defs: ["y3", "y2"]

----

define `y2`
define `z1`
-- post defs: ["z1", "y2"]

----

define `y2`
define `z2`
-- post defs: ["z2", "y2"]

----

define `y2`
define `z3`
-- post defs: ["z3", "y2"]

----

define `y2`
use `y2`
-- post defs: ["y2"]

----

define `y2`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "y2"]

----

define `y2`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "y2"]

----

define `y2`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "y2"]

----

define `y2`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "y2"]

----

define `y2`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "y2"]

----

define `y2`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "y2"]

----

define `y2`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "y2"]

----

define `y2`
define `x1`
use `x1`
-- post defs: ["x1", "y2"]

----

define `y2`
define `x1`
use `y2`
-- post defs: ["x1", "y2"]

----

define `y2`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "y2"]

----

define `y2`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "y2"]

----

define `y2`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "y2"]

----

define `y2`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "y2"]

----

define `y2`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "y2"]

----

define `y2`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "y2"]

----

define `y2`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "y2"]

----

define `y2`
define `x2`
use `x2`
-- post defs: ["x2", "y2"]

----

define `y2`
define `x2`
use `y2`
-- post defs: ["x2", "y2"]

----

define `y2`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "y2"]

----

define `y2`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "y2"]

----

define `y2`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "y2"]

----

define `y2`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "y2"]

----

define `y2`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "y2"]

----

define `y2`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "y2"]

----

define `y2`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "y2"]

----

define `y2`
define `x3`
use `x3`
-- post defs: ["x3", "y2"]

----

define `y2`
define `x3`
use `y2`
-- post defs: ["x3", "y2"]

----

define `y2`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "y2"]

----

define `y2`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "y2"]

----

define `y2`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "y2"]

----

define `y2`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "y2"]

----

define `y2`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "y2"]

----

define `y2`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "y2"]

----

define `y2`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "y2"]

----

define `y2`
define `y1`
use `y1`
-- post defs: ["y1", "y2"]

----

define `y2`
define `y1`
use `y2`
-- post defs: ["y1", "y2"]

----

define `y2`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "y2"]

----

define `y2`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "y2"]

----

define `y2`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "y2"]

----

define `y2`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "y2"]

----

define `y2`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "y2"]

----

define `y2`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "y2"]

----

define `y2`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "y2"]

----

define `y2`
define `y3`
use `y3`
-- post defs: ["y3", "y2"]

----

define `y2`
define `y3`
use `y2`
-- post defs: ["y3", "y2"]

----

define `y2`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "y2"]

----

define `y2`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "y2"]

----

define `y2`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "y2"]

----

define `y2`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "y2"]

----

define `y2`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "y2"]

----

define `y2`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "y2"]

----

define `y2`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "y2"]

----

define `y2`
define `z1`
use `z1`
-- post defs: ["z1", "y2"]

----

define `y2`
define `z1`
use `y2`
-- post defs: ["z1", "y2"]

----

define `y2`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "y2"]

----

define `y2`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "y2"]

----

define `y2`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "y2"]

----

define `y2`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "y2"]

----

define `y2`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "y2"]

----

define `y2`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "y2"]

----

define `y2`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "y2"]

----

define `y2`
define `z2`
use `z2`
-- post defs: ["z2", "y2"]

----

define `y2`
define `z2`
use `y2`
-- post defs: ["z2", "y2"]

----

define `y2`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "y2"]

----

define `y2`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "y2"]

----

define `y2`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "y2"]

----

define `y2`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "y2"]

----

define `y2`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "y2"]

----

define `y2`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "y2"]

----

define `y2`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "y2"]

----

define `y2`
define `z3`
use `z3`
-- post defs: ["z3", "y2"]

----

define `y2`
define `z3`
use `y2`
-- post defs: ["z3", "y2"]

----

define `y2`
use `y2`
define `x1`
-- post defs: ["x1", "y2"]

----

define `y2`
use `y2`
define `x2`
-- post defs: ["x2", "y2"]

----

define `y2`
use `y2`
define `x3`
-- post defs: ["x3", "y2"]

----

define `y2`
use `y2`
define `y1`
-- post defs: ["y1", "y2"]

----

define `y2`
use `y2`
define `y3`
-- post defs: ["y3", "y2"]

----

define `y2`
use `y2`
define `z1`
-- post defs: ["z1", "y2"]

----

define `y2`
use `y2`
define `z2`
-- post defs: ["z2", "y2"]

----

define `y2`
use `y2`
define `z3`
-- post defs: ["z3", "y2"]

----

define `y2`
use `y2`
use `y2`
-- post defs: ["y2"]

----

define `y3`
define `x1`
-- post defs: ["x1", "y3"]

----

define `y3`
define `x2`
-- post defs: ["x2", "y3"]

----

define `y3`
define `x3`
-- post defs: ["x3", "y3"]

----

define `y3`
define `y1`
-- post defs: ["y1", "y3"]

----

define `y3`
define `y2`
-- post defs: ["y2", "y3"]

----

define `y3`
define `z1`
-- post defs: ["z1", "y3"]

----

define `y3`
define `z2`
-- post defs: ["z2", "y3"]

----

define `y3`
define `z3`
-- post defs: ["z3", "y3"]

----

define `y3`
use `y3`
-- post defs: ["y3"]

----

define `y3`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "y3"]

----

define `y3`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "y3"]

----

define `y3`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "y3"]

----

define `y3`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "y3"]

----

define `y3`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "y3"]

----

define `y3`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "y3"]

----

define `y3`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "y3"]

----

define `y3`
define `x1`
use `x1`
-- post defs: ["x1", "y3"]

----

define `y3`
define `x1`
use `y3`
-- post defs: ["x1", "y3"]

----

define `y3`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "y3"]

----

define `y3`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "y3"]

----

define `y3`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "y3"]

----

define `y3`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "y3"]

----

define `y3`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "y3"]

----

define `y3`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "y3"]

----

define `y3`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "y3"]

----

define `y3`
define `x2`
use `x2`
-- post defs: ["x2", "y3"]

----

define `y3`
define `x2`
use `y3`
-- post defs: ["x2", "y3"]

----

define `y3`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "y3"]

----

define `y3`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "y3"]

----

define `y3`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "y3"]

----

define `y3`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "y3"]

----

define `y3`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "y3"]

----

define `y3`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "y3"]

----

define `y3`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "y3"]

----

define `y3`
define `x3`
use `x3`
-- post defs: ["x3", "y3"]

----

define `y3`
define `x3`
use `y3`
-- post defs: ["x3", "y3"]

----

define `y3`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "y3"]

----

define `y3`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "y3"]

----

define `y3`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "y3"]

----

define `y3`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "y3"]

----

define `y3`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "y3"]

----

define `y3`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "y3"]

----

define `y3`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "y3"]

----

define `y3`
define `y1`
use `y1`
-- post defs: ["y1", "y3"]

----

define `y3`
define `y1`
use `y3`
-- post defs: ["y1", "y3"]

----

define `y3`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "y3"]

----

define `y3`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "y3"]

----

define `y3`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "y3"]

----

define `y3`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "y3"]

----

define `y3`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "y3"]

----

define `y3`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "y3"]

----

define `y3`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "y3"]

----

define `y3`
define `y2`
use `y2`
-- post defs: ["y2", "y3"]

----

define `y3`
define `y2`
use `y3`
-- post defs: ["y2", "y3"]

----

define `y3`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "y3"]

----

define `y3`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "y3"]

----

define `y3`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "y3"]

----

define `y3`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "y3"]

----

define `y3`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "y3"]

----

define `y3`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "y3"]

----

define `y3`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "y3"]

----

define `y3`
define `z1`
use `z1`
-- post defs: ["z1", "y3"]

----

define `y3`
define `z1`
use `y3`
-- post defs: ["z1", "y3"]

----

define `y3`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "y3"]

----

define `y3`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "y3"]

----

define `y3`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "y3"]

----

define `y3`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "y3"]

----

define `y3`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "y3"]

----

define `y3`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "y3"]

----

define `y3`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "y3"]

----

define `y3`
define `z2`
use `z2`
-- post defs: ["z2", "y3"]

----

define `y3`
define `z2`
use `y3`
-- post defs: ["z2", "y3"]

----

define `y3`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "y3"]

----

define `y3`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "y3"]

----

define `y3`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "y3"]

----

define `y3`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "y3"]

----

define `y3`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "y3"]

----

define `y3`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "y3"]

----

define `y3`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "y3"]

----

define `y3`
define `z3`
use `z3`
-- post defs: ["z3", "y3"]

----

define `y3`
define `z3`
use `y3`
-- post defs: ["z3", "y3"]

----

define `y3`
use `y3`
define `x1`
-- post defs: ["x1", "y3"]

----

define `y3`
use `y3`
define `x2`
-- post defs: ["x2", "y3"]

----

define `y3`
use `y3`
define `x3`
-- post defs: ["x3", "y3"]

----

define `y3`
use `y3`
define `y1`
-- post defs: ["y1", "y3"]

----

define `y3`
use `y3`
define `y2`
-- post defs: ["y2", "y3"]

----

define `y3`
use `y3`
define `z1`
-- post defs: ["z1", "y3"]

----

define `y3`
use `y3`
define `z2`
-- post defs: ["z2", "y3"]

----

define `y3`
use `y3`
define `z3`
-- post defs: ["z3", "y3"]

----

define `y3`
use `y3`
use `y3`
-- post defs: ["y3"]

----

define `z1`
define `x1`
-- post defs: ["x1", "z1"]

----

define `z1`
define `x2`
-- post defs: ["x2", "z1"]

----

define `z1`
define `x3`
-- post defs: ["x3", "z1"]

----

define `z1`
define `y1`
-- post defs: ["y1", "z1"]

----

define `z1`
define `y2`
-- post defs: ["y2", "z1"]

----

define `z1`
define `y3`
-- post defs: ["y3", "z1"]

----

define `z1`
define `z2`
-- post defs: ["z2", "z1"]

----

define `z1`
define `z3`
-- post defs: ["z3", "z1"]

----

define `z1`
use `z1`
-- post defs: ["z1"]

----

define `z1`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "z1"]

----

define `z1`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "z1"]

----

define `z1`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "z1"]

----

define `z1`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "z1"]

----

define `z1`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "z1"]

----

define `z1`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "z1"]

----

define `z1`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "z1"]

----

define `z1`
define `x1`
use `x1`
-- post defs: ["x1", "z1"]

----

define `z1`
define `x1`
use `z1`
-- post defs: ["x1", "z1"]

----

define `z1`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "z1"]

----

define `z1`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "z1"]

----

define `z1`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "z1"]

----

define `z1`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "z1"]

----

define `z1`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "z1"]

----

define `z1`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "z1"]

----

define `z1`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "z1"]

----

define `z1`
define `x2`
use `x2`
-- post defs: ["x2", "z1"]

----

define `z1`
define `x2`
use `z1`
-- post defs: ["x2", "z1"]

----

define `z1`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "z1"]

----

define `z1`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "z1"]

----

define `z1`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "z1"]

----

define `z1`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "z1"]

----

define `z1`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "z1"]

----

define `z1`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "z1"]

----

define `z1`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "z1"]

----

define `z1`
define `x3`
use `x3`
-- post defs: ["x3", "z1"]

----

define `z1`
define `x3`
use `z1`
-- post defs: ["x3", "z1"]

----

define `z1`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "z1"]

----

define `z1`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "z1"]

----

define `z1`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "z1"]

----

define `z1`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "z1"]

----

define `z1`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "z1"]

----

define `z1`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "z1"]

----

define `z1`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "z1"]

----

define `z1`
define `y1`
use `y1`
-- post defs: ["y1", "z1"]

----

define `z1`
define `y1`
use `z1`
-- post defs: ["y1", "z1"]

----

define `z1`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "z1"]

----

define `z1`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "z1"]

----

define `z1`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "z1"]

----

define `z1`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "z1"]

----

define `z1`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "z1"]

----

define `z1`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "z1"]

----

define `z1`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "z1"]

----

define `z1`
define `y2`
use `y2`
-- post defs: ["y2", "z1"]

----

define `z1`
define `y2`
use `z1`
-- post defs: ["y2", "z1"]

----

define `z1`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "z1"]

----

define `z1`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "z1"]

----

define `z1`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "z1"]

----

define `z1`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "z1"]

----

define `z1`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "z1"]

----

define `z1`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "z1"]

----

define `z1`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "z1"]

----

define `z1`
define `y3`
use `y3`
-- post defs: ["y3", "z1"]

----

define `z1`
define `y3`
use `z1`
-- post defs: ["y3", "z1"]

----

define `z1`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "z1"]

----

define `z1`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "z1"]

----

define `z1`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "z1"]

----

define `z1`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "z1"]

----

define `z1`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "z1"]

----

define `z1`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "z1"]

----

define `z1`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "z1"]

----

define `z1`
define `z2`
use `z2`
-- post defs: ["z2", "z1"]

----

define `z1`
define `z2`
use `z1`
-- post defs: ["z2", "z1"]

----

define `z1`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "z1"]

----

define `z1`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "z1"]

----

define `z1`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "z1"]

----

define `z1`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "z1"]

----

define `z1`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "z1"]

----

define `z1`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "z1"]

----

define `z1`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "z1"]

----

define `z1`
define `z3`
use `z3`
-- post defs: ["z3", "z1"]

----

define `z1`
define `z3`
use `z1`
-- post defs: ["z3", "z1"]

----

define `z1`
use `z1`
define `x1`
-- post defs: ["x1", "z1"]

----

define `z1`
use `z1`
define `x2`
-- post defs: ["x2", "z1"]

----

define `z1`
use `z1`
define `x3`
-- post defs: ["x3", "z1"]

----

define `z1`
use `z1`
define `y1`
-- post defs: ["y1", "z1"]

----

define `z1`
use `z1`
define `y2`
-- post defs: ["y2", "z1"]

----

define `z1`
use `z1`
define `y3`
-- post defs: ["y3", "z1"]

----

define `z1`
use `z1`
define `z2`
-- post defs: ["z2", "z1"]

----

define `z1`
use `z1`
define `z3`
-- post defs: ["z3", "z1"]

----

define `z1`
use `z1`
use `z1`
-- post defs: ["z1"]

----

define `z2`
define `x1`
-- post defs: ["x1", "z2"]

----

define `z2`
define `x2`
-- post defs: ["x2", "z2"]

----

define `z2`
define `x3`
-- post defs: ["x3", "z2"]

----

define `z2`
define `y1`
-- post defs: ["y1", "z2"]

----

define `z2`
define `y2`
-- post defs: ["y2", "z2"]

----

define `z2`
define `y3`
-- post defs: ["y3", "z2"]

----

define `z2`
define `z1`
-- post defs: ["z1", "z2"]

----

define `z2`
define `z3`
-- post defs: ["z3", "z2"]

----

define `z2`
use `z2`
-- post defs: ["z2"]

----

define `z2`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "z2"]

----

define `z2`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "z2"]

----

define `z2`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "z2"]

----

define `z2`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "z2"]

----

define `z2`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "z2"]

----

define `z2`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "z2"]

----

define `z2`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "z2"]

----

define `z2`
define `x1`
use `x1`
-- post defs: ["x1", "z2"]

----

define `z2`
define `x1`
use `z2`
-- post defs: ["x1", "z2"]

----

define `z2`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "z2"]

----

define `z2`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "z2"]

----

define `z2`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "z2"]

----

define `z2`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "z2"]

----

define `z2`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "z2"]

----

define `z2`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "z2"]

----

define `z2`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "z2"]

----

define `z2`
define `x2`
use `x2`
-- post defs: ["x2", "z2"]

----

define `z2`
define `x2`
use `z2`
-- post defs: ["x2", "z2"]

----

define `z2`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "z2"]

----

define `z2`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "z2"]

----

define `z2`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "z2"]

----

define `z2`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "z2"]

----

define `z2`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "z2"]

----

define `z2`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "z2"]

----

define `z2`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "z2"]

----

define `z2`
define `x3`
use `x3`
-- post defs: ["x3", "z2"]

----

define `z2`
define `x3`
use `z2`
-- post defs: ["x3", "z2"]

----

define `z2`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "z2"]

----

define `z2`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "z2"]

----

define `z2`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "z2"]

----

define `z2`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "z2"]

----

define `z2`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "z2"]

----

define `z2`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "z2"]

----

define `z2`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "z2"]

----

define `z2`
define `y1`
use `y1`
-- post defs: ["y1", "z2"]

----

define `z2`
define `y1`
use `z2`
-- post defs: ["y1", "z2"]

----

define `z2`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "z2"]

----

define `z2`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "z2"]

----

define `z2`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "z2"]

----

define `z2`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "z2"]

----

define `z2`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "z2"]

----

define `z2`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "z2"]

----

define `z2`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "z2"]

----

define `z2`
define `y2`
use `y2`
-- post defs: ["y2", "z2"]

----

define `z2`
define `y2`
use `z2`
-- post defs: ["y2", "z2"]

----

define `z2`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "z2"]

----

define `z2`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "z2"]

----

define `z2`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "z2"]

----

define `z2`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "z2"]

----

define `z2`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "z2"]

----

define `z2`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "z2"]

----

define `z2`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "z2"]

----

define `z2`
define `y3`
use `y3`
-- post defs: ["y3", "z2"]

----

define `z2`
define `y3`
use `z2`
-- post defs: ["y3", "z2"]

----

define `z2`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "z2"]

----

define `z2`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "z2"]

----

define `z2`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "z2"]

----

define `z2`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "z2"]

----

define `z2`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "z2"]

----

define `z2`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "z2"]

----

define `z2`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "z2"]

----

define `z2`
define `z1`
use `z1`
-- post defs: ["z1", "z2"]

----

define `z2`
define `z1`
use `z2`
-- post defs: ["z1", "z2"]

----

define `z2`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "z2"]

----

define `z2`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "z2"]

----

define `z2`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "z2"]

----

define `z2`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "z2"]

----

define `z2`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "z2"]

----

define `z2`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "z2"]

----

define `z2`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "z2"]

----

define `z2`
define `z3`
use `z3`
-- post defs: ["z3", "z2"]

----

define `z2`
define `z3`
use `z2`
-- post defs: ["z3", "z2"]

----

define `z2`
use `z2`
define `x1`
-- post defs: ["x1", "z2"]

----

define `z2`
use `z2`
define `x2`
-- post defs: ["x2", "z2"]

----

define `z2`
use `z2`
define `x3`
-- post defs: ["x3", "z2"]

----

define `z2`
use `z2`
define `y1`
-- post defs: ["y1", "z2"]

----

define `z2`
use `z2`
define `y2`
-- post defs: ["y2", "z2"]

----

define `z2`
use `z2`
define `y3`
-- post defs: ["y3", "z2"]

----

define `z2`
use `z2`
define `z1`
-- post defs: ["z1", "z2"]

----

define `z2`
use `z2`
define `z3`
-- post defs: ["z3", "z2"]

----

define `z2`
use `z2`
use `z2`
-- post defs: ["z2"]

----

define `z3`
define `x1`
-- post defs: ["x1", "z3"]

----

define `z3`
define `x2`
-- post defs: ["x2", "z3"]

----

define `z3`
define `x3`
-- post defs: ["x3", "z3"]

----

define `z3`
define `y1`
-- post defs: ["y1", "z3"]

----

define `z3`
define `y2`
-- post defs: ["y2", "z3"]

----

define `z3`
define `y3`
-- post defs: ["y3", "z3"]

----

define `z3`
define `z1`
-- post defs: ["z1", "z3"]

----

define `z3`
define `z2`
-- post defs: ["z2", "z3"]

----

define `z3`
use `z3`
-- post defs: ["z3"]

----

define `z3`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "z3"]

----

define `z3`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "z3"]

----

define `z3`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "z3"]

----

define `z3`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "z3"]

----

define `z3`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "z3"]

----

define `z3`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "z3"]

----

define `z3`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "z3"]

----

define `z3`
define `x1`
use `x1`
-- post defs: ["x1", "z3"]

----

define `z3`
define `x1`
use `z3`
-- post defs: ["x1", "z3"]

----

define `z3`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "z3"]

----

define `z3`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "z3"]

----

define `z3`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "z3"]

----

define `z3`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "z3"]

----

define `z3`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "z3"]

----

define `z3`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "z3"]

----

define `z3`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "z3"]

----

define `z3`
define `x2`
use `x2`
-- post defs: ["x2", "z3"]

----

define `z3`
define `x2`
use `z3`
-- post defs: ["x2", "z3"]

----

define `z3`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "z3"]

----

define `z3`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "z3"]

----

define `z3`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "z3"]

----

define `z3`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "z3"]

----

define `z3`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "z3"]

----

define `z3`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "z3"]

----

define `z3`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "z3"]

----

define `z3`
define `x3`
use `x3`
-- post defs: ["x3", "z3"]

----

define `z3`
define `x3`
use `z3`
-- post defs: ["x3", "z3"]

----

define `z3`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "z3"]

----

define `z3`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "z3"]

----

define `z3`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "z3"]

----

define `z3`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "z3"]

----

define `z3`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "z3"]

----

define `z3`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "z3"]

----

define `z3`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "z3"]

----

define `z3`
define `y1`
use `y1`
-- post defs: ["y1", "z3"]

----

define `z3`
define `y1`
use `z3`
-- post defs: ["y1", "z3"]

----

define `z3`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "z3"]

----

define `z3`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "z3"]

----

define `z3`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "z3"]

----

define `z3`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "z3"]

----

define `z3`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "z3"]

----

define `z3`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "z3"]

----

define `z3`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "z3"]

----

define `z3`
define `y2`
use `y2`
-- post defs: ["y2", "z3"]

----

define `z3`
define `y2`
use `z3`
-- post defs: ["y2", "z3"]

----

define `z3`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "z3"]

----

define `z3`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "z3"]

----

define `z3`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "z3"]

----

define `z3`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "z3"]

----

define `z3`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "z3"]

----

define `z3`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "z3"]

----

define `z3`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "z3"]

----

define `z3`
define `y3`
use `y3`
-- post defs: ["y3", "z3"]

----

define `z3`
define `y3`
use `z3`
-- post defs: ["y3", "z3"]

----

define `z3`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "z3"]

----

define `z3`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "z3"]

----

define `z3`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "z3"]

----

define `z3`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "z3"]

----

define `z3`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "z3"]

----

define `z3`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "z3"]

----

define `z3`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "z3"]

----

define `z3`
define `z1`
use `z1`
-- post defs: ["z1", "z3"]

----

define `z3`
define `z1`
use `z3`
-- post defs: ["z1", "z3"]

----

define `z3`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "z3"]

----

define `z3`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "z3"]

----

define `z3`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "z3"]

----

define `z3`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "z3"]

----

define `z3`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "z3"]

----

define `z3`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "z3"]

----

define `z3`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "z3"]

----

define `z3`
define `z2`
use `z2`
-- post defs: ["z2", "z3"]

----

define `z3`
define `z2`
use `z3`
-- post defs: ["z2", "z3"]

----

define `z3`
use `z3`
define `x1`
-- post defs: ["x1", "z3"]

----

define `z3`
use `z3`
define `x2`
-- post defs: ["x2", "z3"]

----

define `z3`
use `z3`
define `x3`
-- post defs: ["x3", "z3"]

----

define `z3`
use `z3`
define `y1`
-- post defs: ["y1", "z3"]

----

define `z3`
use `z3`
define `y2`
-- post defs: ["y2", "z3"]

----

define `z3`
use `z3`
define `y3`
-- post defs: ["y3", "z3"]

----

define `z3`
use `z3`
define `z1`
-- post defs: ["z1", "z3"]

----

define `z3`
use `z3`
define `z2`
-- post defs: ["z2", "z3"]

----

define `z3`
use `z3`
use `z3`
-- post defs: ["z3"]

----

define `x1`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "x1"]

----

define `x1`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "x1"]

----

define `x1`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "x1"]

----

define `x1`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "x1"]

----

define `x1`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "x1"]

----

define `x1`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "x1"]

----

define `x1`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "x1"]

----

define `x1`
define `x2`
use `x2`
-- post defs: ["x2", "x1"]

----

define `x1`
define `x2`
use `x1`
-- post defs: ["x2", "x1"]

----

define `x1`
define `x2`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "x2", "x1"]

----

define `x1`
define `x2`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "x2", "x1"]

----

define `x1`
define `x2`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "x2", "x1"]

----

define `x1`
define `x2`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "x2", "x1"]

----

define `x1`
define `x2`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "x2", "x1"]

----

define `x1`
define `x2`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "x2", "x1"]

----

define `x1`
define `x2`
define `x3`
use `x3`
-- post defs: ["x3", "x2", "x1"]

----

define `x1`
define `x2`
define `x3`
use `x2`
-- post defs: ["x3", "x2", "x1"]

----

define `x1`
define `x2`
define `x3`
use `x1`
-- post defs: ["x3", "x2", "x1"]

----

define `x1`
define `x2`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "x2", "x1"]

----

define `x1`
define `x2`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "x2", "x1"]

----

define `x1`
define `x2`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "x2", "x1"]

----

define `x1`
define `x2`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "x2", "x1"]

----

define `x1`
define `x2`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "x2", "x1"]

----

define `x1`
define `x2`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "x2", "x1"]

----

define `x1`
define `x2`
define `y1`
use `y1`
-- post defs: ["y1", "x2", "x1"]

----

define `x1`
define `x2`
define `y1`
use `x2`
-- post defs: ["y1", "x2", "x1"]

----

define `x1`
define `x2`
define `y1`
use `x1`
-- post defs: ["y1", "x2", "x1"]

----

define `x1`
define `x2`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "x2", "x1"]

----

define `x1`
define `x2`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "x2", "x1"]

----

define `x1`
define `x2`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "x2", "x1"]

----

define `x1`
define `x2`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "x2", "x1"]

----

define `x1`
define `x2`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "x2", "x1"]

----

define `x1`
define `x2`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "x2", "x1"]

----

define `x1`
define `x2`
define `y2`
use `y2`
-- post defs: ["y2", "x2", "x1"]

----

define `x1`
define `x2`
define `y2`
use `x2`
-- post defs: ["y2", "x2", "x1"]

----

define `x1`
define `x2`
define `y2`
use `x1`
-- post defs: ["y2", "x2", "x1"]

----

define `x1`
define `x2`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "x2", "x1"]

----

define `x1`
define `x2`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "x2", "x1"]

----

define `x1`
define `x2`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "x2", "x1"]

----

define `x1`
define `x2`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "x2", "x1"]

----

define `x1`
define `x2`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "x2", "x1"]

----

define `x1`
define `x2`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "x2", "x1"]

----

define `x1`
define `x2`
define `y3`
use `y3`
-- post defs: ["y3", "x2", "x1"]

----

define `x1`
define `x2`
define `y3`
use `x2`
-- post defs: ["y3", "x2", "x1"]

----

define `x1`
define `x2`
define `y3`
use `x1`
-- post defs: ["y3", "x2", "x1"]

----

define `x1`
define `x2`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "x2", "x1"]

----

define `x1`
define `x2`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "x2", "x1"]

----

define `x1`
define `x2`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "x2", "x1"]

----

define `x1`
define `x2`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "x2", "x1"]

----

define `x1`
define `x2`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "x2", "x1"]

----

define `x1`
define `x2`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "x2", "x1"]

----

define `x1`
define `x2`
define `z1`
use `z1`
-- post defs: ["z1", "x2", "x1"]

----

define `x1`
define `x2`
define `z1`
use `x2`
-- post defs: ["z1", "x2", "x1"]

----

define `x1`
define `x2`
define `z1`
use `x1`
-- post defs: ["z1", "x2", "x1"]

----

define `x1`
define `x2`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "x2", "x1"]

----

define `x1`
define `x2`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "x2", "x1"]

----

define `x1`
define `x2`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "x2", "x1"]

----

define `x1`
define `x2`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "x2", "x1"]

----

define `x1`
define `x2`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "x2", "x1"]

----

define `x1`
define `x2`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "x2", "x1"]

----

define `x1`
define `x2`
define `z2`
use `z2`
-- post defs: ["z2", "x2", "x1"]

----

define `x1`
define `x2`
define `z2`
use `x2`
-- post defs: ["z2", "x2", "x1"]

----

define `x1`
define `x2`
define `z2`
use `x1`
-- post defs: ["z2", "x2", "x1"]

----

define `x1`
define `x2`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "x2", "x1"]

----

define `x1`
define `x2`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "x2", "x1"]

----

define `x1`
define `x2`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "x2", "x1"]

----

define `x1`
define `x2`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "x2", "x1"]

----

define `x1`
define `x2`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "x2", "x1"]

----

define `x1`
define `x2`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "x2", "x1"]

----

define `x1`
define `x2`
define `z3`
use `z3`
-- post defs: ["z3", "x2", "x1"]

----

define `x1`
define `x2`
define `z3`
use `x2`
-- post defs: ["z3", "x2", "x1"]

----

define `x1`
define `x2`
define `z3`
use `x1`
-- post defs: ["z3", "x2", "x1"]

----

define `x1`
define `x2`
use `x2`
define `x3`
-- post defs: ["x3", "x2", "x1"]

----

define `x1`
define `x2`
use `x2`
define `y1`
-- post defs: ["y1", "x2", "x1"]

----

define `x1`
define `x2`
use `x2`
define `y2`
-- post defs: ["y2", "x2", "x1"]

----

define `x1`
define `x2`
use `x2`
define `y3`
-- post defs: ["y3", "x2", "x1"]

----

define `x1`
define `x2`
use `x2`
define `z1`
-- post defs: ["z1", "x2", "x1"]

----

define `x1`
define `x2`
use `x2`
define `z2`
-- post defs: ["z2", "x2", "x1"]

----

define `x1`
define `x2`
use `x2`
define `z3`
-- post defs: ["z3", "x2", "x1"]

----

define `x1`
define `x2`
use `x2`
use `x2`
-- post defs: ["x2", "x1"]

----

define `x1`
define `x2`
use `x2`
use `x1`
-- post defs: ["x2", "x1"]

----

define `x1`
define `x2`
use `x1`
define `x3`
-- post defs: ["x3", "x2", "x1"]

----

define `x1`
define `x2`
use `x1`
define `y1`
-- post defs: ["y1", "x2", "x1"]

----

define `x1`
define `x2`
use `x1`
define `y2`
-- post defs: ["y2", "x2", "x1"]

----

define `x1`
define `x2`
use `x1`
define `y3`
-- post defs: ["y3", "x2", "x1"]

----

define `x1`
define `x2`
use `x1`
define `z1`
-- post defs: ["z1", "x2", "x1"]

----

define `x1`
define `x2`
use `x1`
define `z2`
-- post defs: ["z2", "x2", "x1"]

----

define `x1`
define `x2`
use `x1`
define `z3`
-- post defs: ["z3", "x2", "x1"]

----

define `x1`
define `x2`
use `x1`
use `x2`
-- post defs: ["x2", "x1"]

----

define `x1`
define `x2`
use `x1`
use `x1`
-- post defs: ["x2", "x1"]

----

define `x1`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "x1"]

----

define `x1`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "x1"]

----

define `x1`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "x1"]

----

define `x1`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "x1"]

----

define `x1`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "x1"]

----

define `x1`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "x1"]

----

define `x1`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "x1"]

----

define `x1`
define `x3`
use `x3`
-- post defs: ["x3", "x1"]

----

define `x1`
define `x3`
use `x1`
-- post defs: ["x3", "x1"]

----

define `x1`
define `x3`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "x3", "x1"]

----

define `x1`
define `x3`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "x3", "x1"]

----

define `x1`
define `x3`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "x3", "x1"]

----

define `x1`
define `x3`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "x3", "x1"]

----

define `x1`
define `x3`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "x3", "x1"]

----

define `x1`
define `x3`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "x3", "x1"]

----

define `x1`
define `x3`
define `x2`
use `x2`
-- post defs: ["x2", "x3", "x1"]

----

define `x1`
define `x3`
define `x2`
use `x3`
-- post defs: ["x2", "x3", "x1"]

----

define `x1`
define `x3`
define `x2`
use `x1`
-- post defs: ["x2", "x3", "x1"]

----

define `x1`
define `x3`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "x3", "x1"]

----

define `x1`
define `x3`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "x3", "x1"]

----

define `x1`
define `x3`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "x3", "x1"]

----

define `x1`
define `x3`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "x3", "x1"]

----

define `x1`
define `x3`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "x3", "x1"]

----

define `x1`
define `x3`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "x3", "x1"]

----

define `x1`
define `x3`
define `y1`
use `y1`
-- post defs: ["y1", "x3", "x1"]

----

define `x1`
define `x3`
define `y1`
use `x3`
-- post defs: ["y1", "x3", "x1"]

----

define `x1`
define `x3`
define `y1`
use `x1`
-- post defs: ["y1", "x3", "x1"]

----

define `x1`
define `x3`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "x3", "x1"]

----

define `x1`
define `x3`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "x3", "x1"]

----

define `x1`
define `x3`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "x3", "x1"]

----

define `x1`
define `x3`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "x3", "x1"]

----

define `x1`
define `x3`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "x3", "x1"]

----

define `x1`
define `x3`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "x3", "x1"]

----

define `x1`
define `x3`
define `y2`
use `y2`
-- post defs: ["y2", "x3", "x1"]

----

define `x1`
define `x3`
define `y2`
use `x3`
-- post defs: ["y2", "x3", "x1"]

----

define `x1`
define `x3`
define `y2`
use `x1`
-- post defs: ["y2", "x3", "x1"]

----

define `x1`
define `x3`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "x3", "x1"]

----

define `x1`
define `x3`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "x3", "x1"]

----

define `x1`
define `x3`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "x3", "x1"]

----

define `x1`
define `x3`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "x3", "x1"]

----

define `x1`
define `x3`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "x3", "x1"]

----

define `x1`
define `x3`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "x3", "x1"]

----

define `x1`
define `x3`
define `y3`
use `y3`
-- post defs: ["y3", "x3", "x1"]

----

define `x1`
define `x3`
define `y3`
use `x3`
-- post defs: ["y3", "x3", "x1"]

----

define `x1`
define `x3`
define `y3`
use `x1`
-- post defs: ["y3", "x3", "x1"]

----

define `x1`
define `x3`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "x3", "x1"]

----

define `x1`
define `x3`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "x3", "x1"]

----

define `x1`
define `x3`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "x3", "x1"]

----

define `x1`
define `x3`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "x3", "x1"]

----

define `x1`
define `x3`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "x3", "x1"]

----

define `x1`
define `x3`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "x3", "x1"]

----

define `x1`
define `x3`
define `z1`
use `z1`
-- post defs: ["z1", "x3", "x1"]

----

define `x1`
define `x3`
define `z1`
use `x3`
-- post defs: ["z1", "x3", "x1"]

----

define `x1`
define `x3`
define `z1`
use `x1`
-- post defs: ["z1", "x3", "x1"]

----

define `x1`
define `x3`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "x3", "x1"]

----

define `x1`
define `x3`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "x3", "x1"]

----

define `x1`
define `x3`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "x3", "x1"]

----

define `x1`
define `x3`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "x3", "x1"]

----

define `x1`
define `x3`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "x3", "x1"]

----

define `x1`
define `x3`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "x3", "x1"]

----

define `x1`
define `x3`
define `z2`
use `z2`
-- post defs: ["z2", "x3", "x1"]

----

define `x1`
define `x3`
define `z2`
use `x3`
-- post defs: ["z2", "x3", "x1"]

----

define `x1`
define `x3`
define `z2`
use `x1`
-- post defs: ["z2", "x3", "x1"]

----

define `x1`
define `x3`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "x3", "x1"]

----

define `x1`
define `x3`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "x3", "x1"]

----

define `x1`
define `x3`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "x3", "x1"]

----

define `x1`
define `x3`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "x3", "x1"]

----

define `x1`
define `x3`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "x3", "x1"]

----

define `x1`
define `x3`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "x3", "x1"]

----

define `x1`
define `x3`
define `z3`
use `z3`
-- post defs: ["z3", "x3", "x1"]

----

define `x1`
define `x3`
define `z3`
use `x3`
-- post defs: ["z3", "x3", "x1"]

----

define `x1`
define `x3`
define `z3`
use `x1`
-- post defs: ["z3", "x3", "x1"]

----

define `x1`
define `x3`
use `x3`
define `x2`
-- post defs: ["x2", "x3", "x1"]

----

define `x1`
define `x3`
use `x3`
define `y1`
-- post defs: ["y1", "x3", "x1"]

----

define `x1`
define `x3`
use `x3`
define `y2`
-- post defs: ["y2", "x3", "x1"]

----

define `x1`
define `x3`
use `x3`
define `y3`
-- post defs: ["y3", "x3", "x1"]

----

define `x1`
define `x3`
use `x3`
define `z1`
-- post defs: ["z1", "x3", "x1"]

----

define `x1`
define `x3`
use `x3`
define `z2`
-- post defs: ["z2", "x3", "x1"]

----

define `x1`
define `x3`
use `x3`
define `z3`
-- post defs: ["z3", "x3", "x1"]

----

define `x1`
define `x3`
use `x3`
use `x3`
-- post defs: ["x3", "x1"]

----

define `x1`
define `x3`
use `x3`
use `x1`
-- post defs: ["x3", "x1"]

----

define `x1`
define `x3`
use `x1`
define `x2`
-- post defs: ["x2", "x3", "x1"]

----

define `x1`
define `x3`
use `x1`
define `y1`
-- post defs: ["y1", "x3", "x1"]

----

define `x1`
define `x3`
use `x1`
define `y2`
-- post defs: ["y2", "x3", "x1"]

----

define `x1`
define `x3`
use `x1`
define `y3`
-- post defs: ["y3", "x3", "x1"]

----

define `x1`
define `x3`
use `x1`
define `z1`
-- post defs: ["z1", "x3", "x1"]

----

define `x1`
define `x3`
use `x1`
define `z2`
-- post defs: ["z2", "x3", "x1"]

----

define `x1`
define `x3`
use `x1`
define `z3`
-- post defs: ["z3", "x3", "x1"]

----

define `x1`
define `x3`
use `x1`
use `x3`
-- post defs: ["x3", "x1"]

----

define `x1`
define `x3`
use `x1`
use `x1`
-- post defs: ["x3", "x1"]

----

define `x1`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "x1"]

----

define `x1`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "x1"]

----

define `x1`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "x1"]

----

define `x1`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "x1"]

----

define `x1`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "x1"]

----

define `x1`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "x1"]

----

define `x1`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "x1"]

----

define `x1`
define `y1`
use `y1`
-- post defs: ["y1", "x1"]

----

define `x1`
define `y1`
use `x1`
-- post defs: ["y1", "x1"]

----

define `x1`
define `y1`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "y1", "x1"]

----

define `x1`
define `y1`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "y1", "x1"]

----

define `x1`
define `y1`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "y1", "x1"]

----

define `x1`
define `y1`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "y1", "x1"]

----

define `x1`
define `y1`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "y1", "x1"]

----

define `x1`
define `y1`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "y1", "x1"]

----

define `x1`
define `y1`
define `x2`
use `x2`
-- post defs: ["x2", "y1", "x1"]

----

define `x1`
define `y1`
define `x2`
use `y1`
-- post defs: ["x2", "y1", "x1"]

----

define `x1`
define `y1`
define `x2`
use `x1`
-- post defs: ["x2", "y1", "x1"]

----

define `x1`
define `y1`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "y1", "x1"]

----

define `x1`
define `y1`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "y1", "x1"]

----

define `x1`
define `y1`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "y1", "x1"]

----

define `x1`
define `y1`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "y1", "x1"]

----

define `x1`
define `y1`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "y1", "x1"]

----

define `x1`
define `y1`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "y1", "x1"]

----

define `x1`
define `y1`
define `x3`
use `x3`
-- post defs: ["x3", "y1", "x1"]

----

define `x1`
define `y1`
define `x3`
use `y1`
-- post defs: ["x3", "y1", "x1"]

----

define `x1`
define `y1`
define `x3`
use `x1`
-- post defs: ["x3", "y1", "x1"]

----

define `x1`
define `y1`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "y1", "x1"]

----

define `x1`
define `y1`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "y1", "x1"]

----

define `x1`
define `y1`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "y1", "x1"]

----

define `x1`
define `y1`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "y1", "x1"]

----

define `x1`
define `y1`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "y1", "x1"]

----

define `x1`
define `y1`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "y1", "x1"]

----

define `x1`
define `y1`
define `y2`
use `y2`
-- post defs: ["y2", "y1", "x1"]

----

define `x1`
define `y1`
define `y2`
use `y1`
-- post defs: ["y2", "y1", "x1"]

----

define `x1`
define `y1`
define `y2`
use `x1`
-- post defs: ["y2", "y1", "x1"]

----

define `x1`
define `y1`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "y1", "x1"]

----

define `x1`
define `y1`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "y1", "x1"]

----

define `x1`
define `y1`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "y1", "x1"]

----

define `x1`
define `y1`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "y1", "x1"]

----

define `x1`
define `y1`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "y1", "x1"]

----

define `x1`
define `y1`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "y1", "x1"]

----

define `x1`
define `y1`
define `y3`
use `y3`
-- post defs: ["y3", "y1", "x1"]

----

define `x1`
define `y1`
define `y3`
use `y1`
-- post defs: ["y3", "y1", "x1"]

----

define `x1`
define `y1`
define `y3`
use `x1`
-- post defs: ["y3", "y1", "x1"]

----

define `x1`
define `y1`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "y1", "x1"]

----

define `x1`
define `y1`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "y1", "x1"]

----

define `x1`
define `y1`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "y1", "x1"]

----

define `x1`
define `y1`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "y1", "x1"]

----

define `x1`
define `y1`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "y1", "x1"]

----

define `x1`
define `y1`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "y1", "x1"]

----

define `x1`
define `y1`
define `z1`
use `z1`
-- post defs: ["z1", "y1", "x1"]

----

define `x1`
define `y1`
define `z1`
use `y1`
-- post defs: ["z1", "y1", "x1"]

----

define `x1`
define `y1`
define `z1`
use `x1`
-- post defs: ["z1", "y1", "x1"]

----

define `x1`
define `y1`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "y1", "x1"]

----

define `x1`
define `y1`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "y1", "x1"]

----

define `x1`
define `y1`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "y1", "x1"]

----

define `x1`
define `y1`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "y1", "x1"]

----

define `x1`
define `y1`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "y1", "x1"]

----

define `x1`
define `y1`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "y1", "x1"]

----

define `x1`
define `y1`
define `z2`
use `z2`
-- post defs: ["z2", "y1", "x1"]

----

define `x1`
define `y1`
define `z2`
use `y1`
-- post defs: ["z2", "y1", "x1"]

----

define `x1`
define `y1`
define `z2`
use `x1`
-- post defs: ["z2", "y1", "x1"]

----

define `x1`
define `y1`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "y1", "x1"]

----

define `x1`
define `y1`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "y1", "x1"]

----

define `x1`
define `y1`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "y1", "x1"]

----

define `x1`
define `y1`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "y1", "x1"]

----

define `x1`
define `y1`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "y1", "x1"]

----

define `x1`
define `y1`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "y1", "x1"]

----

define `x1`
define `y1`
define `z3`
use `z3`
-- post defs: ["z3", "y1", "x1"]

----

define `x1`
define `y1`
define `z3`
use `y1`
-- post defs: ["z3", "y1", "x1"]

----

define `x1`
define `y1`
define `z3`
use `x1`
-- post defs: ["z3", "y1", "x1"]

----

define `x1`
define `y1`
use `y1`
define `x2`
-- post defs: ["x2", "y1", "x1"]

----

define `x1`
define `y1`
use `y1`
define `x3`
-- post defs: ["x3", "y1", "x1"]

----

define `x1`
define `y1`
use `y1`
define `y2`
-- post defs: ["y2", "y1", "x1"]

----

define `x1`
define `y1`
use `y1`
define `y3`
-- post defs: ["y3", "y1", "x1"]

----

define `x1`
define `y1`
use `y1`
define `z1`
-- post defs: ["z1", "y1", "x1"]

----

define `x1`
define `y1`
use `y1`
define `z2`
-- post defs: ["z2", "y1", "x1"]

----

define `x1`
define `y1`
use `y1`
define `z3`
-- post defs: ["z3", "y1", "x1"]

----

define `x1`
define `y1`
use `y1`
use `y1`
-- post defs: ["y1", "x1"]

----

define `x1`
define `y1`
use `y1`
use `x1`
-- post defs: ["y1", "x1"]

----

define `x1`
define `y1`
use `x1`
define `x2`
-- post defs: ["x2", "y1", "x1"]

----

define `x1`
define `y1`
use `x1`
define `x3`
-- post defs: ["x3", "y1", "x1"]

----

define `x1`
define `y1`
use `x1`
define `y2`
-- post defs: ["y2", "y1", "x1"]

----

define `x1`
define `y1`
use `x1`
define `y3`
-- post defs: ["y3", "y1", "x1"]

----

define `x1`
define `y1`
use `x1`
define `z1`
-- post defs: ["z1", "y1", "x1"]

----

define `x1`
define `y1`
use `x1`
define `z2`
-- post defs: ["z2", "y1", "x1"]

----

define `x1`
define `y1`
use `x1`
define `z3`
-- post defs: ["z3", "y1", "x1"]

----

define `x1`
define `y1`
use `x1`
use `y1`
-- post defs: ["y1", "x1"]

----

define `x1`
define `y1`
use `x1`
use `x1`
-- post defs: ["y1", "x1"]

----

define `x1`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "x1"]

----

define `x1`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "x1"]

----

define `x1`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "x1"]

----

define `x1`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "x1"]

----

define `x1`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "x1"]

----

define `x1`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "x1"]

----

define `x1`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "x1"]

----

define `x1`
define `y2`
use `y2`
-- post defs: ["y2", "x1"]

----

define `x1`
define `y2`
use `x1`
-- post defs: ["y2", "x1"]

----

define `x1`
define `y2`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "y2", "x1"]

----

define `x1`
define `y2`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "y2", "x1"]

----

define `x1`
define `y2`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "y2", "x1"]

----

define `x1`
define `y2`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "y2", "x1"]

----

define `x1`
define `y2`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "y2", "x1"]

----

define `x1`
define `y2`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "y2", "x1"]

----

define `x1`
define `y2`
define `x2`
use `x2`
-- post defs: ["x2", "y2", "x1"]

----

define `x1`
define `y2`
define `x2`
use `y2`
-- post defs: ["x2", "y2", "x1"]

----

define `x1`
define `y2`
define `x2`
use `x1`
-- post defs: ["x2", "y2", "x1"]

----

define `x1`
define `y2`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "y2", "x1"]

----

define `x1`
define `y2`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "y2", "x1"]

----

define `x1`
define `y2`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "y2", "x1"]

----

define `x1`
define `y2`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "y2", "x1"]

----

define `x1`
define `y2`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "y2", "x1"]

----

define `x1`
define `y2`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "y2", "x1"]

----

define `x1`
define `y2`
define `x3`
use `x3`
-- post defs: ["x3", "y2", "x1"]

----

define `x1`
define `y2`
define `x3`
use `y2`
-- post defs: ["x3", "y2", "x1"]

----

define `x1`
define `y2`
define `x3`
use `x1`
-- post defs: ["x3", "y2", "x1"]

----

define `x1`
define `y2`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "y2", "x1"]

----

define `x1`
define `y2`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "y2", "x1"]

----

define `x1`
define `y2`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "y2", "x1"]

----

define `x1`
define `y2`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "y2", "x1"]

----

define `x1`
define `y2`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "y2", "x1"]

----

define `x1`
define `y2`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "y2", "x1"]

----

define `x1`
define `y2`
define `y1`
use `y1`
-- post defs: ["y1", "y2", "x1"]

----

define `x1`
define `y2`
define `y1`
use `y2`
-- post defs: ["y1", "y2", "x1"]

----

define `x1`
define `y2`
define `y1`
use `x1`
-- post defs: ["y1", "y2", "x1"]

----

define `x1`
define `y2`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "y2", "x1"]

----

define `x1`
define `y2`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "y2", "x1"]

----

define `x1`
define `y2`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "y2", "x1"]

----

define `x1`
define `y2`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "y2", "x1"]

----

define `x1`
define `y2`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "y2", "x1"]

----

define `x1`
define `y2`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "y2", "x1"]

----

define `x1`
define `y2`
define `y3`
use `y3`
-- post defs: ["y3", "y2", "x1"]

----

define `x1`
define `y2`
define `y3`
use `y2`
-- post defs: ["y3", "y2", "x1"]

----

define `x1`
define `y2`
define `y3`
use `x1`
-- post defs: ["y3", "y2", "x1"]

----

define `x1`
define `y2`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "y2", "x1"]

----

define `x1`
define `y2`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "y2", "x1"]

----

define `x1`
define `y2`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "y2", "x1"]

----

define `x1`
define `y2`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "y2", "x1"]

----

define `x1`
define `y2`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "y2", "x1"]

----

define `x1`
define `y2`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "y2", "x1"]

----

define `x1`
define `y2`
define `z1`
use `z1`
-- post defs: ["z1", "y2", "x1"]

----

define `x1`
define `y2`
define `z1`
use `y2`
-- post defs: ["z1", "y2", "x1"]

----

define `x1`
define `y2`
define `z1`
use `x1`
-- post defs: ["z1", "y2", "x1"]

----

define `x1`
define `y2`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "y2", "x1"]

----

define `x1`
define `y2`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "y2", "x1"]

----

define `x1`
define `y2`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "y2", "x1"]

----

define `x1`
define `y2`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "y2", "x1"]

----

define `x1`
define `y2`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "y2", "x1"]

----

define `x1`
define `y2`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "y2", "x1"]

----

define `x1`
define `y2`
define `z2`
use `z2`
-- post defs: ["z2", "y2", "x1"]

----

define `x1`
define `y2`
define `z2`
use `y2`
-- post defs: ["z2", "y2", "x1"]

----

define `x1`
define `y2`
define `z2`
use `x1`
-- post defs: ["z2", "y2", "x1"]

----

define `x1`
define `y2`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "y2", "x1"]

----

define `x1`
define `y2`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "y2", "x1"]

----

define `x1`
define `y2`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "y2", "x1"]

----

define `x1`
define `y2`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "y2", "x1"]

----

define `x1`
define `y2`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "y2", "x1"]

----

define `x1`
define `y2`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "y2", "x1"]

----

define `x1`
define `y2`
define `z3`
use `z3`
-- post defs: ["z3", "y2", "x1"]

----

define `x1`
define `y2`
define `z3`
use `y2`
-- post defs: ["z3", "y2", "x1"]

----

define `x1`
define `y2`
define `z3`
use `x1`
-- post defs: ["z3", "y2", "x1"]

----

define `x1`
define `y2`
use `y2`
define `x2`
-- post defs: ["x2", "y2", "x1"]

----

define `x1`
define `y2`
use `y2`
define `x3`
-- post defs: ["x3", "y2", "x1"]

----

define `x1`
define `y2`
use `y2`
define `y1`
-- post defs: ["y1", "y2", "x1"]

----

define `x1`
define `y2`
use `y2`
define `y3`
-- post defs: ["y3", "y2", "x1"]

----

define `x1`
define `y2`
use `y2`
define `z1`
-- post defs: ["z1", "y2", "x1"]

----

define `x1`
define `y2`
use `y2`
define `z2`
-- post defs: ["z2", "y2", "x1"]

----

define `x1`
define `y2`
use `y2`
define `z3`
-- post defs: ["z3", "y2", "x1"]

----

define `x1`
define `y2`
use `y2`
use `y2`
-- post defs: ["y2", "x1"]

----

define `x1`
define `y2`
use `y2`
use `x1`
-- post defs: ["y2", "x1"]

----

define `x1`
define `y2`
use `x1`
define `x2`
-- post defs: ["x2", "y2", "x1"]

----

define `x1`
define `y2`
use `x1`
define `x3`
-- post defs: ["x3", "y2", "x1"]

----

define `x1`
define `y2`
use `x1`
define `y1`
-- post defs: ["y1", "y2", "x1"]

----

define `x1`
define `y2`
use `x1`
define `y3`
-- post defs: ["y3", "y2", "x1"]

----

define `x1`
define `y2`
use `x1`
define `z1`
-- post defs: ["z1", "y2", "x1"]

----

define `x1`
define `y2`
use `x1`
define `z2`
-- post defs: ["z2", "y2", "x1"]

----

define `x1`
define `y2`
use `x1`
define `z3`
-- post defs: ["z3", "y2", "x1"]

----

define `x1`
define `y2`
use `x1`
use `y2`
-- post defs: ["y2", "x1"]

----

define `x1`
define `y2`
use `x1`
use `x1`
-- post defs: ["y2", "x1"]

----

define `x1`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "x1"]

----

define `x1`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "x1"]

----

define `x1`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "x1"]

----

define `x1`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "x1"]

----

define `x1`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "x1"]

----

define `x1`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "x1"]

----

define `x1`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "x1"]

----

define `x1`
define `y3`
use `y3`
-- post defs: ["y3", "x1"]

----

define `x1`
define `y3`
use `x1`
-- post defs: ["y3", "x1"]

----

define `x1`
define `y3`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "y3", "x1"]

----

define `x1`
define `y3`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "y3", "x1"]

----

define `x1`
define `y3`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "y3", "x1"]

----

define `x1`
define `y3`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "y3", "x1"]

----

define `x1`
define `y3`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "y3", "x1"]

----

define `x1`
define `y3`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "y3", "x1"]

----

define `x1`
define `y3`
define `x2`
use `x2`
-- post defs: ["x2", "y3", "x1"]

----

define `x1`
define `y3`
define `x2`
use `y3`
-- post defs: ["x2", "y3", "x1"]

----

define `x1`
define `y3`
define `x2`
use `x1`
-- post defs: ["x2", "y3", "x1"]

----

define `x1`
define `y3`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "y3", "x1"]

----

define `x1`
define `y3`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "y3", "x1"]

----

define `x1`
define `y3`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "y3", "x1"]

----

define `x1`
define `y3`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "y3", "x1"]

----

define `x1`
define `y3`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "y3", "x1"]

----

define `x1`
define `y3`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "y3", "x1"]

----

define `x1`
define `y3`
define `x3`
use `x3`
-- post defs: ["x3", "y3", "x1"]

----

define `x1`
define `y3`
define `x3`
use `y3`
-- post defs: ["x3", "y3", "x1"]

----

define `x1`
define `y3`
define `x3`
use `x1`
-- post defs: ["x3", "y3", "x1"]

----

define `x1`
define `y3`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "y3", "x1"]

----

define `x1`
define `y3`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "y3", "x1"]

----

define `x1`
define `y3`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "y3", "x1"]

----

define `x1`
define `y3`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "y3", "x1"]

----

define `x1`
define `y3`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "y3", "x1"]

----

define `x1`
define `y3`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "y3", "x1"]

----

define `x1`
define `y3`
define `y1`
use `y1`
-- post defs: ["y1", "y3", "x1"]

----

define `x1`
define `y3`
define `y1`
use `y3`
-- post defs: ["y1", "y3", "x1"]

----

define `x1`
define `y3`
define `y1`
use `x1`
-- post defs: ["y1", "y3", "x1"]

----

define `x1`
define `y3`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "y3", "x1"]

----

define `x1`
define `y3`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "y3", "x1"]

----

define `x1`
define `y3`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "y3", "x1"]

----

define `x1`
define `y3`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "y3", "x1"]

----

define `x1`
define `y3`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "y3", "x1"]

----

define `x1`
define `y3`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "y3", "x1"]

----

define `x1`
define `y3`
define `y2`
use `y2`
-- post defs: ["y2", "y3", "x1"]

----

define `x1`
define `y3`
define `y2`
use `y3`
-- post defs: ["y2", "y3", "x1"]

----

define `x1`
define `y3`
define `y2`
use `x1`
-- post defs: ["y2", "y3", "x1"]

----

define `x1`
define `y3`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "y3", "x1"]

----

define `x1`
define `y3`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "y3", "x1"]

----

define `x1`
define `y3`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "y3", "x1"]

----

define `x1`
define `y3`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "y3", "x1"]

----

define `x1`
define `y3`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "y3", "x1"]

----

define `x1`
define `y3`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "y3", "x1"]

----

define `x1`
define `y3`
define `z1`
use `z1`
-- post defs: ["z1", "y3", "x1"]

----

define `x1`
define `y3`
define `z1`
use `y3`
-- post defs: ["z1", "y3", "x1"]

----

define `x1`
define `y3`
define `z1`
use `x1`
-- post defs: ["z1", "y3", "x1"]

----

define `x1`
define `y3`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "y3", "x1"]

----

define `x1`
define `y3`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "y3", "x1"]

----

define `x1`
define `y3`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "y3", "x1"]

----

define `x1`
define `y3`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "y3", "x1"]

----

define `x1`
define `y3`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "y3", "x1"]

----

define `x1`
define `y3`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "y3", "x1"]

----

define `x1`
define `y3`
define `z2`
use `z2`
-- post defs: ["z2", "y3", "x1"]

----

define `x1`
define `y3`
define `z2`
use `y3`
-- post defs: ["z2", "y3", "x1"]

----

define `x1`
define `y3`
define `z2`
use `x1`
-- post defs: ["z2", "y3", "x1"]

----

define `x1`
define `y3`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "y3", "x1"]

----

define `x1`
define `y3`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "y3", "x1"]

----

define `x1`
define `y3`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "y3", "x1"]

----

define `x1`
define `y3`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "y3", "x1"]

----

define `x1`
define `y3`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "y3", "x1"]

----

define `x1`
define `y3`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "y3", "x1"]

----

define `x1`
define `y3`
define `z3`
use `z3`
-- post defs: ["z3", "y3", "x1"]

----

define `x1`
define `y3`
define `z3`
use `y3`
-- post defs: ["z3", "y3", "x1"]

----

define `x1`
define `y3`
define `z3`
use `x1`
-- post defs: ["z3", "y3", "x1"]

----

define `x1`
define `y3`
use `y3`
define `x2`
-- post defs: ["x2", "y3", "x1"]

----

define `x1`
define `y3`
use `y3`
define `x3`
-- post defs: ["x3", "y3", "x1"]

----

define `x1`
define `y3`
use `y3`
define `y1`
-- post defs: ["y1", "y3", "x1"]

----

define `x1`
define `y3`
use `y3`
define `y2`
-- post defs: ["y2", "y3", "x1"]

----

define `x1`
define `y3`
use `y3`
define `z1`
-- post defs: ["z1", "y3", "x1"]

----

define `x1`
define `y3`
use `y3`
define `z2`
-- post defs: ["z2", "y3", "x1"]

----

define `x1`
define `y3`
use `y3`
define `z3`
-- post defs: ["z3", "y3", "x1"]

----

define `x1`
define `y3`
use `y3`
use `y3`
-- post defs: ["y3", "x1"]

----

define `x1`
define `y3`
use `y3`
use `x1`
-- post defs: ["y3", "x1"]

----

define `x1`
define `y3`
use `x1`
define `x2`
-- post defs: ["x2", "y3", "x1"]

----

define `x1`
define `y3`
use `x1`
define `x3`
-- post defs: ["x3", "y3", "x1"]

----

define `x1`
define `y3`
use `x1`
define `y1`
-- post defs: ["y1", "y3", "x1"]

----

define `x1`
define `y3`
use `x1`
define `y2`
-- post defs: ["y2", "y3", "x1"]

----

define `x1`
define `y3`
use `x1`
define `z1`
-- post defs: ["z1", "y3", "x1"]

----

define `x1`
define `y3`
use `x1`
define `z2`
-- post defs: ["z2", "y3", "x1"]

----

define `x1`
define `y3`
use `x1`
define `z3`
-- post defs: ["z3", "y3", "x1"]

----

define `x1`
define `y3`
use `x1`
use `y3`
-- post defs: ["y3", "x1"]

----

define `x1`
define `y3`
use `x1`
use `x1`
-- post defs: ["y3", "x1"]

----

define `x1`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "x1"]

----

define `x1`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "x1"]

----

define `x1`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "x1"]

----

define `x1`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "x1"]

----

define `x1`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "x1"]

----

define `x1`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "x1"]

----

define `x1`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "x1"]

----

define `x1`
define `z1`
use `z1`
-- post defs: ["z1", "x1"]

----

define `x1`
define `z1`
use `x1`
-- post defs: ["z1", "x1"]

----

define `x1`
define `z1`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "z1", "x1"]

----

define `x1`
define `z1`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "z1", "x1"]

----

define `x1`
define `z1`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "z1", "x1"]

----

define `x1`
define `z1`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "z1", "x1"]

----

define `x1`
define `z1`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "z1", "x1"]

----

define `x1`
define `z1`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "z1", "x1"]

----

define `x1`
define `z1`
define `x2`
use `x2`
-- post defs: ["x2", "z1", "x1"]

----

define `x1`
define `z1`
define `x2`
use `z1`
-- post defs: ["x2", "z1", "x1"]

----

define `x1`
define `z1`
define `x2`
use `x1`
-- post defs: ["x2", "z1", "x1"]

----

define `x1`
define `z1`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "z1", "x1"]

----

define `x1`
define `z1`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "z1", "x1"]

----

define `x1`
define `z1`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "z1", "x1"]

----

define `x1`
define `z1`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "z1", "x1"]

----

define `x1`
define `z1`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "z1", "x1"]

----

define `x1`
define `z1`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "z1", "x1"]

----

define `x1`
define `z1`
define `x3`
use `x3`
-- post defs: ["x3", "z1", "x1"]

----

define `x1`
define `z1`
define `x3`
use `z1`
-- post defs: ["x3", "z1", "x1"]

----

define `x1`
define `z1`
define `x3`
use `x1`
-- post defs: ["x3", "z1", "x1"]

----

define `x1`
define `z1`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "z1", "x1"]

----

define `x1`
define `z1`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "z1", "x1"]

----

define `x1`
define `z1`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "z1", "x1"]

----

define `x1`
define `z1`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "z1", "x1"]

----

define `x1`
define `z1`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "z1", "x1"]

----

define `x1`
define `z1`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "z1", "x1"]

----

define `x1`
define `z1`
define `y1`
use `y1`
-- post defs: ["y1", "z1", "x1"]

----

define `x1`
define `z1`
define `y1`
use `z1`
-- post defs: ["y1", "z1", "x1"]

----

define `x1`
define `z1`
define `y1`
use `x1`
-- post defs: ["y1", "z1", "x1"]

----

define `x1`
define `z1`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "z1", "x1"]

----

define `x1`
define `z1`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "z1", "x1"]

----

define `x1`
define `z1`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "z1", "x1"]

----

define `x1`
define `z1`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "z1", "x1"]

----

define `x1`
define `z1`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "z1", "x1"]

----

define `x1`
define `z1`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "z1", "x1"]

----

define `x1`
define `z1`
define `y2`
use `y2`
-- post defs: ["y2", "z1", "x1"]

----

define `x1`
define `z1`
define `y2`
use `z1`
-- post defs: ["y2", "z1", "x1"]

----

define `x1`
define `z1`
define `y2`
use `x1`
-- post defs: ["y2", "z1", "x1"]

----

define `x1`
define `z1`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "z1", "x1"]

----

define `x1`
define `z1`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "z1", "x1"]

----

define `x1`
define `z1`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "z1", "x1"]

----

define `x1`
define `z1`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "z1", "x1"]

----

define `x1`
define `z1`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "z1", "x1"]

----

define `x1`
define `z1`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "z1", "x1"]

----

define `x1`
define `z1`
define `y3`
use `y3`
-- post defs: ["y3", "z1", "x1"]

----

define `x1`
define `z1`
define `y3`
use `z1`
-- post defs: ["y3", "z1", "x1"]

----

define `x1`
define `z1`
define `y3`
use `x1`
-- post defs: ["y3", "z1", "x1"]

----

define `x1`
define `z1`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "z1", "x1"]

----

define `x1`
define `z1`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "z1", "x1"]

----

define `x1`
define `z1`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "z1", "x1"]

----

define `x1`
define `z1`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "z1", "x1"]

----

define `x1`
define `z1`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "z1", "x1"]

----

define `x1`
define `z1`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "z1", "x1"]

----

define `x1`
define `z1`
define `z2`
use `z2`
-- post defs: ["z2", "z1", "x1"]

----

define `x1`
define `z1`
define `z2`
use `z1`
-- post defs: ["z2", "z1", "x1"]

----

define `x1`
define `z1`
define `z2`
use `x1`
-- post defs: ["z2", "z1", "x1"]

----

define `x1`
define `z1`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "z1", "x1"]

----

define `x1`
define `z1`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "z1", "x1"]

----

define `x1`
define `z1`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "z1", "x1"]

----

define `x1`
define `z1`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "z1", "x1"]

----

define `x1`
define `z1`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "z1", "x1"]

----

define `x1`
define `z1`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "z1", "x1"]

----

define `x1`
define `z1`
define `z3`
use `z3`
-- post defs: ["z3", "z1", "x1"]

----

define `x1`
define `z1`
define `z3`
use `z1`
-- post defs: ["z3", "z1", "x1"]

----

define `x1`
define `z1`
define `z3`
use `x1`
-- post defs: ["z3", "z1", "x1"]

----

define `x1`
define `z1`
use `z1`
define `x2`
-- post defs: ["x2", "z1", "x1"]

----

define `x1`
define `z1`
use `z1`
define `x3`
-- post defs: ["x3", "z1", "x1"]

----

define `x1`
define `z1`
use `z1`
define `y1`
-- post defs: ["y1", "z1", "x1"]

----

define `x1`
define `z1`
use `z1`
define `y2`
-- post defs: ["y2", "z1", "x1"]

----

define `x1`
define `z1`
use `z1`
define `y3`
-- post defs: ["y3", "z1", "x1"]

----

define `x1`
define `z1`
use `z1`
define `z2`
-- post defs: ["z2", "z1", "x1"]

----

define `x1`
define `z1`
use `z1`
define `z3`
-- post defs: ["z3", "z1", "x1"]

----

define `x1`
define `z1`
use `z1`
use `z1`
-- post defs: ["z1", "x1"]

----

define `x1`
define `z1`
use `z1`
use `x1`
-- post defs: ["z1", "x1"]

----

define `x1`
define `z1`
use `x1`
define `x2`
-- post defs: ["x2", "z1", "x1"]

----

define `x1`
define `z1`
use `x1`
define `x3`
-- post defs: ["x3", "z1", "x1"]

----

define `x1`
define `z1`
use `x1`
define `y1`
-- post defs: ["y1", "z1", "x1"]

----

define `x1`
define `z1`
use `x1`
define `y2`
-- post defs: ["y2", "z1", "x1"]

----

define `x1`
define `z1`
use `x1`
define `y3`
-- post defs: ["y3", "z1", "x1"]

----

define `x1`
define `z1`
use `x1`
define `z2`
-- post defs: ["z2", "z1", "x1"]

----

define `x1`
define `z1`
use `x1`
define `z3`
-- post defs: ["z3", "z1", "x1"]

----

define `x1`
define `z1`
use `x1`
use `z1`
-- post defs: ["z1", "x1"]

----

define `x1`
define `z1`
use `x1`
use `x1`
-- post defs: ["z1", "x1"]

----

define `x1`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "x1"]

----

define `x1`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "x1"]

----

define `x1`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "x1"]

----

define `x1`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "x1"]

----

define `x1`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "x1"]

----

define `x1`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "x1"]

----

define `x1`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "x1"]

----

define `x1`
define `z2`
use `z2`
-- post defs: ["z2", "x1"]

----

define `x1`
define `z2`
use `x1`
-- post defs: ["z2", "x1"]

----

define `x1`
define `z2`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "z2", "x1"]

----

define `x1`
define `z2`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "z2", "x1"]

----

define `x1`
define `z2`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "z2", "x1"]

----

define `x1`
define `z2`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "z2", "x1"]

----

define `x1`
define `z2`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "z2", "x1"]

----

define `x1`
define `z2`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "z2", "x1"]

----

define `x1`
define `z2`
define `x2`
use `x2`
-- post defs: ["x2", "z2", "x1"]

----

define `x1`
define `z2`
define `x2`
use `z2`
-- post defs: ["x2", "z2", "x1"]

----

define `x1`
define `z2`
define `x2`
use `x1`
-- post defs: ["x2", "z2", "x1"]

----

define `x1`
define `z2`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "z2", "x1"]

----

define `x1`
define `z2`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "z2", "x1"]

----

define `x1`
define `z2`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "z2", "x1"]

----

define `x1`
define `z2`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "z2", "x1"]

----

define `x1`
define `z2`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "z2", "x1"]

----

define `x1`
define `z2`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "z2", "x1"]

----

define `x1`
define `z2`
define `x3`
use `x3`
-- post defs: ["x3", "z2", "x1"]

----

define `x1`
define `z2`
define `x3`
use `z2`
-- post defs: ["x3", "z2", "x1"]

----

define `x1`
define `z2`
define `x3`
use `x1`
-- post defs: ["x3", "z2", "x1"]

----

define `x1`
define `z2`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "z2", "x1"]

----

define `x1`
define `z2`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "z2", "x1"]

----

define `x1`
define `z2`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "z2", "x1"]

----

define `x1`
define `z2`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "z2", "x1"]

----

define `x1`
define `z2`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "z2", "x1"]

----

define `x1`
define `z2`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "z2", "x1"]

----

define `x1`
define `z2`
define `y1`
use `y1`
-- post defs: ["y1", "z2", "x1"]

----

define `x1`
define `z2`
define `y1`
use `z2`
-- post defs: ["y1", "z2", "x1"]

----

define `x1`
define `z2`
define `y1`
use `x1`
-- post defs: ["y1", "z2", "x1"]

----

define `x1`
define `z2`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "z2", "x1"]

----

define `x1`
define `z2`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "z2", "x1"]

----

define `x1`
define `z2`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "z2", "x1"]

----

define `x1`
define `z2`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "z2", "x1"]

----

define `x1`
define `z2`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "z2", "x1"]

----

define `x1`
define `z2`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "z2", "x1"]

----

define `x1`
define `z2`
define `y2`
use `y2`
-- post defs: ["y2", "z2", "x1"]

----

define `x1`
define `z2`
define `y2`
use `z2`
-- post defs: ["y2", "z2", "x1"]

----

define `x1`
define `z2`
define `y2`
use `x1`
-- post defs: ["y2", "z2", "x1"]

----

define `x1`
define `z2`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "z2", "x1"]

----

define `x1`
define `z2`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "z2", "x1"]

----

define `x1`
define `z2`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "z2", "x1"]

----

define `x1`
define `z2`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "z2", "x1"]

----

define `x1`
define `z2`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "z2", "x1"]

----

define `x1`
define `z2`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "z2", "x1"]

----

define `x1`
define `z2`
define `y3`
use `y3`
-- post defs: ["y3", "z2", "x1"]

----

define `x1`
define `z2`
define `y3`
use `z2`
-- post defs: ["y3", "z2", "x1"]

----

define `x1`
define `z2`
define `y3`
use `x1`
-- post defs: ["y3", "z2", "x1"]

----

define `x1`
define `z2`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "z2", "x1"]

----

define `x1`
define `z2`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "z2", "x1"]

----

define `x1`
define `z2`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "z2", "x1"]

----

define `x1`
define `z2`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "z2", "x1"]

----

define `x1`
define `z2`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "z2", "x1"]

----

define `x1`
define `z2`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "z2", "x1"]

----

define `x1`
define `z2`
define `z1`
use `z1`
-- post defs: ["z1", "z2", "x1"]

----

define `x1`
define `z2`
define `z1`
use `z2`
-- post defs: ["z1", "z2", "x1"]

----

define `x1`
define `z2`
define `z1`
use `x1`
-- post defs: ["z1", "z2", "x1"]

----

define `x1`
define `z2`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "z2", "x1"]

----

define `x1`
define `z2`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "z2", "x1"]

----

define `x1`
define `z2`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "z2", "x1"]

----

define `x1`
define `z2`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "z2", "x1"]

----

define `x1`
define `z2`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "z2", "x1"]

----

define `x1`
define `z2`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "z2", "x1"]

----

define `x1`
define `z2`
define `z3`
use `z3`
-- post defs: ["z3", "z2", "x1"]

----

define `x1`
define `z2`
define `z3`
use `z2`
-- post defs: ["z3", "z2", "x1"]

----

define `x1`
define `z2`
define `z3`
use `x1`
-- post defs: ["z3", "z2", "x1"]

----

define `x1`
define `z2`
use `z2`
define `x2`
-- post defs: ["x2", "z2", "x1"]

----

define `x1`
define `z2`
use `z2`
define `x3`
-- post defs: ["x3", "z2", "x1"]

----

define `x1`
define `z2`
use `z2`
define `y1`
-- post defs: ["y1", "z2", "x1"]

----

define `x1`
define `z2`
use `z2`
define `y2`
-- post defs: ["y2", "z2", "x1"]

----

define `x1`
define `z2`
use `z2`
define `y3`
-- post defs: ["y3", "z2", "x1"]

----

define `x1`
define `z2`
use `z2`
define `z1`
-- post defs: ["z1", "z2", "x1"]

----

define `x1`
define `z2`
use `z2`
define `z3`
-- post defs: ["z3", "z2", "x1"]

----

define `x1`
define `z2`
use `z2`
use `z2`
-- post defs: ["z2", "x1"]

----

define `x1`
define `z2`
use `z2`
use `x1`
-- post defs: ["z2", "x1"]

----

define `x1`
define `z2`
use `x1`
define `x2`
-- post defs: ["x2", "z2", "x1"]

----

define `x1`
define `z2`
use `x1`
define `x3`
-- post defs: ["x3", "z2", "x1"]

----

define `x1`
define `z2`
use `x1`
define `y1`
-- post defs: ["y1", "z2", "x1"]

----

define `x1`
define `z2`
use `x1`
define `y2`
-- post defs: ["y2", "z2", "x1"]

----

define `x1`
define `z2`
use `x1`
define `y3`
-- post defs: ["y3", "z2", "x1"]

----

define `x1`
define `z2`
use `x1`
define `z1`
-- post defs: ["z1", "z2", "x1"]

----

define `x1`
define `z2`
use `x1`
define `z3`
-- post defs: ["z3", "z2", "x1"]

----

define `x1`
define `z2`
use `x1`
use `z2`
-- post defs: ["z2", "x1"]

----

define `x1`
define `z2`
use `x1`
use `x1`
-- post defs: ["z2", "x1"]

----

define `x1`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "x1"]

----

define `x1`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "x1"]

----

define `x1`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "x1"]

----

define `x1`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "x1"]

----

define `x1`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "x1"]

----

define `x1`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "x1"]

----

define `x1`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "x1"]

----

define `x1`
define `z3`
use `z3`
-- post defs: ["z3", "x1"]

----

define `x1`
define `z3`
use `x1`
-- post defs: ["z3", "x1"]

----

define `x1`
define `z3`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "z3", "x1"]

----

define `x1`
define `z3`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "z3", "x1"]

----

define `x1`
define `z3`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "z3", "x1"]

----

define `x1`
define `z3`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "z3", "x1"]

----

define `x1`
define `z3`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "z3", "x1"]

----

define `x1`
define `z3`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "z3", "x1"]

----

define `x1`
define `z3`
define `x2`
use `x2`
-- post defs: ["x2", "z3", "x1"]

----

define `x1`
define `z3`
define `x2`
use `z3`
-- post defs: ["x2", "z3", "x1"]

----

define `x1`
define `z3`
define `x2`
use `x1`
-- post defs: ["x2", "z3", "x1"]

----

define `x1`
define `z3`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "z3", "x1"]

----

define `x1`
define `z3`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "z3", "x1"]

----

define `x1`
define `z3`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "z3", "x1"]

----

define `x1`
define `z3`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "z3", "x1"]

----

define `x1`
define `z3`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "z3", "x1"]

----

define `x1`
define `z3`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "z3", "x1"]

----

define `x1`
define `z3`
define `x3`
use `x3`
-- post defs: ["x3", "z3", "x1"]

----

define `x1`
define `z3`
define `x3`
use `z3`
-- post defs: ["x3", "z3", "x1"]

----

define `x1`
define `z3`
define `x3`
use `x1`
-- post defs: ["x3", "z3", "x1"]

----

define `x1`
define `z3`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "z3", "x1"]

----

define `x1`
define `z3`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "z3", "x1"]

----

define `x1`
define `z3`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "z3", "x1"]

----

define `x1`
define `z3`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "z3", "x1"]

----

define `x1`
define `z3`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "z3", "x1"]

----

define `x1`
define `z3`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "z3", "x1"]

----

define `x1`
define `z3`
define `y1`
use `y1`
-- post defs: ["y1", "z3", "x1"]

----

define `x1`
define `z3`
define `y1`
use `z3`
-- post defs: ["y1", "z3", "x1"]

----

define `x1`
define `z3`
define `y1`
use `x1`
-- post defs: ["y1", "z3", "x1"]

----

define `x1`
define `z3`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "z3", "x1"]

----

define `x1`
define `z3`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "z3", "x1"]

----

define `x1`
define `z3`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "z3", "x1"]

----

define `x1`
define `z3`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "z3", "x1"]

----

define `x1`
define `z3`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "z3", "x1"]

----

define `x1`
define `z3`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "z3", "x1"]

----

define `x1`
define `z3`
define `y2`
use `y2`
-- post defs: ["y2", "z3", "x1"]

----

define `x1`
define `z3`
define `y2`
use `z3`
-- post defs: ["y2", "z3", "x1"]

----

define `x1`
define `z3`
define `y2`
use `x1`
-- post defs: ["y2", "z3", "x1"]

----

define `x1`
define `z3`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "z3", "x1"]

----

define `x1`
define `z3`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "z3", "x1"]

----

define `x1`
define `z3`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "z3", "x1"]

----

define `x1`
define `z3`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "z3", "x1"]

----

define `x1`
define `z3`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "z3", "x1"]

----

define `x1`
define `z3`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "z3", "x1"]

----

define `x1`
define `z3`
define `y3`
use `y3`
-- post defs: ["y3", "z3", "x1"]

----

define `x1`
define `z3`
define `y3`
use `z3`
-- post defs: ["y3", "z3", "x1"]

----

define `x1`
define `z3`
define `y3`
use `x1`
-- post defs: ["y3", "z3", "x1"]

----

define `x1`
define `z3`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "z3", "x1"]

----

define `x1`
define `z3`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "z3", "x1"]

----

define `x1`
define `z3`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "z3", "x1"]

----

define `x1`
define `z3`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "z3", "x1"]

----

define `x1`
define `z3`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "z3", "x1"]

----

define `x1`
define `z3`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "z3", "x1"]

----

define `x1`
define `z3`
define `z1`
use `z1`
-- post defs: ["z1", "z3", "x1"]

----

define `x1`
define `z3`
define `z1`
use `z3`
-- post defs: ["z1", "z3", "x1"]

----

define `x1`
define `z3`
define `z1`
use `x1`
-- post defs: ["z1", "z3", "x1"]

----

define `x1`
define `z3`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "z3", "x1"]

----

define `x1`
define `z3`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "z3", "x1"]

----

define `x1`
define `z3`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "z3", "x1"]

----

define `x1`
define `z3`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "z3", "x1"]

----

define `x1`
define `z3`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "z3", "x1"]

----

define `x1`
define `z3`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "z3", "x1"]

----

define `x1`
define `z3`
define `z2`
use `z2`
-- post defs: ["z2", "z3", "x1"]

----

define `x1`
define `z3`
define `z2`
use `z3`
-- post defs: ["z2", "z3", "x1"]

----

define `x1`
define `z3`
define `z2`
use `x1`
-- post defs: ["z2", "z3", "x1"]

----

define `x1`
define `z3`
use `z3`
define `x2`
-- post defs: ["x2", "z3", "x1"]

----

define `x1`
define `z3`
use `z3`
define `x3`
-- post defs: ["x3", "z3", "x1"]

----

define `x1`
define `z3`
use `z3`
define `y1`
-- post defs: ["y1", "z3", "x1"]

----

define `x1`
define `z3`
use `z3`
define `y2`
-- post defs: ["y2", "z3", "x1"]

----

define `x1`
define `z3`
use `z3`
define `y3`
-- post defs: ["y3", "z3", "x1"]

----

define `x1`
define `z3`
use `z3`
define `z1`
-- post defs: ["z1", "z3", "x1"]

----

define `x1`
define `z3`
use `z3`
define `z2`
-- post defs: ["z2", "z3", "x1"]

----

define `x1`
define `z3`
use `z3`
use `z3`
-- post defs: ["z3", "x1"]

----

define `x1`
define `z3`
use `z3`
use `x1`
-- post defs: ["z3", "x1"]

----

define `x1`
define `z3`
use `x1`
define `x2`
-- post defs: ["x2", "z3", "x1"]

----

define `x1`
define `z3`
use `x1`
define `x3`
-- post defs: ["x3", "z3", "x1"]

----

define `x1`
define `z3`
use `x1`
define `y1`
-- post defs: ["y1", "z3", "x1"]

----

define `x1`
define `z3`
use `x1`
define `y2`
-- post defs: ["y2", "z3", "x1"]

----

define `x1`
define `z3`
use `x1`
define `y3`
-- post defs: ["y3", "z3", "x1"]

----

define `x1`
define `z3`
use `x1`
define `z1`
-- post defs: ["z1", "z3", "x1"]

----

define `x1`
define `z3`
use `x1`
define `z2`
-- post defs: ["z2", "z3", "x1"]

----

define `x1`
define `z3`
use `x1`
use `z3`
-- post defs: ["z3", "x1"]

----

define `x1`
define `z3`
use `x1`
use `x1`
-- post defs: ["z3", "x1"]

----

define `x1`
use `x1`
define `x2`
-- post defs: ["x2", "x1"]

----

define `x1`
use `x1`
define `x3`
-- post defs: ["x3", "x1"]

----

define `x1`
use `x1`
define `y1`
-- post defs: ["y1", "x1"]

----

define `x1`
use `x1`
define `y2`
-- post defs: ["y2", "x1"]

----

define `x1`
use `x1`
define `y3`
-- post defs: ["y3", "x1"]

----

define `x1`
use `x1`
define `z1`
-- post defs: ["z1", "x1"]

----

define `x1`
use `x1`
define `z2`
-- post defs: ["z2", "x1"]

----

define `x1`
use `x1`
define `z3`
-- post defs: ["z3", "x1"]

----

define `x1`
use `x1`
use `x1`
-- post defs: ["x1"]

----

define `x1`
use `x1`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "x1"]

----

define `x1`
use `x1`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "x1"]

----

define `x1`
use `x1`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "x1"]

----

define `x1`
use `x1`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "x1"]

----

define `x1`
use `x1`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "x1"]

----

define `x1`
use `x1`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "x1"]

----

define `x1`
use `x1`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "x1"]

----

define `x1`
use `x1`
define `x2`
use `x2`
-- post defs: ["x2", "x1"]

----

define `x1`
use `x1`
define `x2`
use `x1`
-- post defs: ["x2", "x1"]

----

define `x1`
use `x1`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "x1"]

----

define `x1`
use `x1`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "x1"]

----

define `x1`
use `x1`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "x1"]

----

define `x1`
use `x1`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "x1"]

----

define `x1`
use `x1`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "x1"]

----

define `x1`
use `x1`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "x1"]

----

define `x1`
use `x1`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "x1"]

----

define `x1`
use `x1`
define `x3`
use `x3`
-- post defs: ["x3", "x1"]

----

define `x1`
use `x1`
define `x3`
use `x1`
-- post defs: ["x3", "x1"]

----

define `x1`
use `x1`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "x1"]

----

define `x1`
use `x1`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "x1"]

----

define `x1`
use `x1`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "x1"]

----

define `x1`
use `x1`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "x1"]

----

define `x1`
use `x1`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "x1"]

----

define `x1`
use `x1`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "x1"]

----

define `x1`
use `x1`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "x1"]

----

define `x1`
use `x1`
define `y1`
use `y1`
-- post defs: ["y1", "x1"]

----

define `x1`
use `x1`
define `y1`
use `x1`
-- post defs: ["y1", "x1"]

----

define `x1`
use `x1`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "x1"]

----

define `x1`
use `x1`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "x1"]

----

define `x1`
use `x1`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "x1"]

----

define `x1`
use `x1`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "x1"]

----

define `x1`
use `x1`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "x1"]

----

define `x1`
use `x1`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "x1"]

----

define `x1`
use `x1`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "x1"]

----

define `x1`
use `x1`
define `y2`
use `y2`
-- post defs: ["y2", "x1"]

----

define `x1`
use `x1`
define `y2`
use `x1`
-- post defs: ["y2", "x1"]

----

define `x1`
use `x1`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "x1"]

----

define `x1`
use `x1`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "x1"]

----

define `x1`
use `x1`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "x1"]

----

define `x1`
use `x1`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "x1"]

----

define `x1`
use `x1`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "x1"]

----

define `x1`
use `x1`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "x1"]

----

define `x1`
use `x1`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "x1"]

----

define `x1`
use `x1`
define `y3`
use `y3`
-- post defs: ["y3", "x1"]

----

define `x1`
use `x1`
define `y3`
use `x1`
-- post defs: ["y3", "x1"]

----

define `x1`
use `x1`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "x1"]

----

define `x1`
use `x1`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "x1"]

----

define `x1`
use `x1`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "x1"]

----

define `x1`
use `x1`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "x1"]

----

define `x1`
use `x1`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "x1"]

----

define `x1`
use `x1`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "x1"]

----

define `x1`
use `x1`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "x1"]

----

define `x1`
use `x1`
define `z1`
use `z1`
-- post defs: ["z1", "x1"]

----

define `x1`
use `x1`
define `z1`
use `x1`
-- post defs: ["z1", "x1"]

----

define `x1`
use `x1`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "x1"]

----

define `x1`
use `x1`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "x1"]

----

define `x1`
use `x1`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "x1"]

----

define `x1`
use `x1`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "x1"]

----

define `x1`
use `x1`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "x1"]

----

define `x1`
use `x1`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "x1"]

----

define `x1`
use `x1`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "x1"]

----

define `x1`
use `x1`
define `z2`
use `z2`
-- post defs: ["z2", "x1"]

----

define `x1`
use `x1`
define `z2`
use `x1`
-- post defs: ["z2", "x1"]

----

define `x1`
use `x1`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "x1"]

----

define `x1`
use `x1`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "x1"]

----

define `x1`
use `x1`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "x1"]

----

define `x1`
use `x1`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "x1"]

----

define `x1`
use `x1`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "x1"]

----

define `x1`
use `x1`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "x1"]

----

define `x1`
use `x1`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "x1"]

----

define `x1`
use `x1`
define `z3`
use `z3`
-- post defs: ["z3", "x1"]

----

define `x1`
use `x1`
define `z3`
use `x1`
-- post defs: ["z3", "x1"]

----

define `x1`
use `x1`
use `x1`
define `x2`
-- post defs: ["x2", "x1"]

----

define `x1`
use `x1`
use `x1`
define `x3`
-- post defs: ["x3", "x1"]

----

define `x1`
use `x1`
use `x1`
define `y1`
-- post defs: ["y1", "x1"]

----

define `x1`
use `x1`
use `x1`
define `y2`
-- post defs: ["y2", "x1"]

----

define `x1`
use `x1`
use `x1`
define `y3`
-- post defs: ["y3", "x1"]

----

define `x1`
use `x1`
use `x1`
define `z1`
-- post defs: ["z1", "x1"]

----

define `x1`
use `x1`
use `x1`
define `z2`
-- post defs: ["z2", "x1"]

----

define `x1`
use `x1`
use `x1`
define `z3`
-- post defs: ["z3", "x1"]

----

define `x1`
use `x1`
use `x1`
use `x1`
-- post defs: ["x1"]

----

define `x2`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "x2"]

----

define `x2`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "x2"]

----

define `x2`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "x2"]

----

define `x2`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "x2"]

----

define `x2`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "x2"]

----

define `x2`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "x2"]

----

define `x2`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "x2"]

----

define `x2`
define `x1`
use `x1`
-- post defs: ["x1", "x2"]

----

define `x2`
define `x1`
use `x2`
-- post defs: ["x1", "x2"]

----

define `x2`
define `x1`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "x1", "x2"]

----

define `x2`
define `x1`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "x1", "x2"]

----

define `x2`
define `x1`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "x1", "x2"]

----

define `x2`
define `x1`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "x1", "x2"]

----

define `x2`
define `x1`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "x1", "x2"]

----

define `x2`
define `x1`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "x1", "x2"]

----

define `x2`
define `x1`
define `x3`
use `x3`
-- post defs: ["x3", "x1", "x2"]

----

define `x2`
define `x1`
define `x3`
use `x1`
-- post defs: ["x3", "x1", "x2"]

----

define `x2`
define `x1`
define `x3`
use `x2`
-- post defs: ["x3", "x1", "x2"]

----

define `x2`
define `x1`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "x1", "x2"]

----

define `x2`
define `x1`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "x1", "x2"]

----

define `x2`
define `x1`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "x1", "x2"]

----

define `x2`
define `x1`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "x1", "x2"]

----

define `x2`
define `x1`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "x1", "x2"]

----

define `x2`
define `x1`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "x1", "x2"]

----

define `x2`
define `x1`
define `y1`
use `y1`
-- post defs: ["y1", "x1", "x2"]

----

define `x2`
define `x1`
define `y1`
use `x1`
-- post defs: ["y1", "x1", "x2"]

----

define `x2`
define `x1`
define `y1`
use `x2`
-- post defs: ["y1", "x1", "x2"]

----

define `x2`
define `x1`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "x1", "x2"]

----

define `x2`
define `x1`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "x1", "x2"]

----

define `x2`
define `x1`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "x1", "x2"]

----

define `x2`
define `x1`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "x1", "x2"]

----

define `x2`
define `x1`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "x1", "x2"]

----

define `x2`
define `x1`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "x1", "x2"]

----

define `x2`
define `x1`
define `y2`
use `y2`
-- post defs: ["y2", "x1", "x2"]

----

define `x2`
define `x1`
define `y2`
use `x1`
-- post defs: ["y2", "x1", "x2"]

----

define `x2`
define `x1`
define `y2`
use `x2`
-- post defs: ["y2", "x1", "x2"]

----

define `x2`
define `x1`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "x1", "x2"]

----

define `x2`
define `x1`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "x1", "x2"]

----

define `x2`
define `x1`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "x1", "x2"]

----

define `x2`
define `x1`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "x1", "x2"]

----

define `x2`
define `x1`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "x1", "x2"]

----

define `x2`
define `x1`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "x1", "x2"]

----

define `x2`
define `x1`
define `y3`
use `y3`
-- post defs: ["y3", "x1", "x2"]

----

define `x2`
define `x1`
define `y3`
use `x1`
-- post defs: ["y3", "x1", "x2"]

----

define `x2`
define `x1`
define `y3`
use `x2`
-- post defs: ["y3", "x1", "x2"]

----

define `x2`
define `x1`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "x1", "x2"]

----

define `x2`
define `x1`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "x1", "x2"]

----

define `x2`
define `x1`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "x1", "x2"]

----

define `x2`
define `x1`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "x1", "x2"]

----

define `x2`
define `x1`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "x1", "x2"]

----

define `x2`
define `x1`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "x1", "x2"]

----

define `x2`
define `x1`
define `z1`
use `z1`
-- post defs: ["z1", "x1", "x2"]

----

define `x2`
define `x1`
define `z1`
use `x1`
-- post defs: ["z1", "x1", "x2"]

----

define `x2`
define `x1`
define `z1`
use `x2`
-- post defs: ["z1", "x1", "x2"]

----

define `x2`
define `x1`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "x1", "x2"]

----

define `x2`
define `x1`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "x1", "x2"]

----

define `x2`
define `x1`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "x1", "x2"]

----

define `x2`
define `x1`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "x1", "x2"]

----

define `x2`
define `x1`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "x1", "x2"]

----

define `x2`
define `x1`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "x1", "x2"]

----

define `x2`
define `x1`
define `z2`
use `z2`
-- post defs: ["z2", "x1", "x2"]

----

define `x2`
define `x1`
define `z2`
use `x1`
-- post defs: ["z2", "x1", "x2"]

----

define `x2`
define `x1`
define `z2`
use `x2`
-- post defs: ["z2", "x1", "x2"]

----

define `x2`
define `x1`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "x1", "x2"]

----

define `x2`
define `x1`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "x1", "x2"]

----

define `x2`
define `x1`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "x1", "x2"]

----

define `x2`
define `x1`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "x1", "x2"]

----

define `x2`
define `x1`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "x1", "x2"]

----

define `x2`
define `x1`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "x1", "x2"]

----

define `x2`
define `x1`
define `z3`
use `z3`
-- post defs: ["z3", "x1", "x2"]

----

define `x2`
define `x1`
define `z3`
use `x1`
-- post defs: ["z3", "x1", "x2"]

----

define `x2`
define `x1`
define `z3`
use `x2`
-- post defs: ["z3", "x1", "x2"]

----

define `x2`
define `x1`
use `x1`
define `x3`
-- post defs: ["x3", "x1", "x2"]

----

define `x2`
define `x1`
use `x1`
define `y1`
-- post defs: ["y1", "x1", "x2"]

----

define `x2`
define `x1`
use `x1`
define `y2`
-- post defs: ["y2", "x1", "x2"]

----

define `x2`
define `x1`
use `x1`
define `y3`
-- post defs: ["y3", "x1", "x2"]

----

define `x2`
define `x1`
use `x1`
define `z1`
-- post defs: ["z1", "x1", "x2"]

----

define `x2`
define `x1`
use `x1`
define `z2`
-- post defs: ["z2", "x1", "x2"]

----

define `x2`
define `x1`
use `x1`
define `z3`
-- post defs: ["z3", "x1", "x2"]

----

define `x2`
define `x1`
use `x1`
use `x1`
-- post defs: ["x1", "x2"]

----

define `x2`
define `x1`
use `x1`
use `x2`
-- post defs: ["x1", "x2"]

----

define `x2`
define `x1`
use `x2`
define `x3`
-- post defs: ["x3", "x1", "x2"]

----

define `x2`
define `x1`
use `x2`
define `y1`
-- post defs: ["y1", "x1", "x2"]

----

define `x2`
define `x1`
use `x2`
define `y2`
-- post defs: ["y2", "x1", "x2"]

----

define `x2`
define `x1`
use `x2`
define `y3`
-- post defs: ["y3", "x1", "x2"]

----

define `x2`
define `x1`
use `x2`
define `z1`
-- post defs: ["z1", "x1", "x2"]

----

define `x2`
define `x1`
use `x2`
define `z2`
-- post defs: ["z2", "x1", "x2"]

----

define `x2`
define `x1`
use `x2`
define `z3`
-- post defs: ["z3", "x1", "x2"]

----

define `x2`
define `x1`
use `x2`
use `x1`
-- post defs: ["x1", "x2"]

----

define `x2`
define `x1`
use `x2`
use `x2`
-- post defs: ["x1", "x2"]

----

define `x2`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "x2"]

----

define `x2`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "x2"]

----

define `x2`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "x2"]

----

define `x2`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "x2"]

----

define `x2`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "x2"]

----

define `x2`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "x2"]

----

define `x2`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "x2"]

----

define `x2`
define `x3`
use `x3`
-- post defs: ["x3", "x2"]

----

define `x2`
define `x3`
use `x2`
-- post defs: ["x3", "x2"]

----

define `x2`
define `x3`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "x3", "x2"]

----

define `x2`
define `x3`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "x3", "x2"]

----

define `x2`
define `x3`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "x3", "x2"]

----

define `x2`
define `x3`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "x3", "x2"]

----

define `x2`
define `x3`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "x3", "x2"]

----

define `x2`
define `x3`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "x3", "x2"]

----

define `x2`
define `x3`
define `x1`
use `x1`
-- post defs: ["x1", "x3", "x2"]

----

define `x2`
define `x3`
define `x1`
use `x3`
-- post defs: ["x1", "x3", "x2"]

----

define `x2`
define `x3`
define `x1`
use `x2`
-- post defs: ["x1", "x3", "x2"]

----

define `x2`
define `x3`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "x3", "x2"]

----

define `x2`
define `x3`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "x3", "x2"]

----

define `x2`
define `x3`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "x3", "x2"]

----

define `x2`
define `x3`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "x3", "x2"]

----

define `x2`
define `x3`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "x3", "x2"]

----

define `x2`
define `x3`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "x3", "x2"]

----

define `x2`
define `x3`
define `y1`
use `y1`
-- post defs: ["y1", "x3", "x2"]

----

define `x2`
define `x3`
define `y1`
use `x3`
-- post defs: ["y1", "x3", "x2"]

----

define `x2`
define `x3`
define `y1`
use `x2`
-- post defs: ["y1", "x3", "x2"]

----

define `x2`
define `x3`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "x3", "x2"]

----

define `x2`
define `x3`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "x3", "x2"]

----

define `x2`
define `x3`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "x3", "x2"]

----

define `x2`
define `x3`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "x3", "x2"]

----

define `x2`
define `x3`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "x3", "x2"]

----

define `x2`
define `x3`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "x3", "x2"]

----

define `x2`
define `x3`
define `y2`
use `y2`
-- post defs: ["y2", "x3", "x2"]

----

define `x2`
define `x3`
define `y2`
use `x3`
-- post defs: ["y2", "x3", "x2"]

----

define `x2`
define `x3`
define `y2`
use `x2`
-- post defs: ["y2", "x3", "x2"]

----

define `x2`
define `x3`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "x3", "x2"]

----

define `x2`
define `x3`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "x3", "x2"]

----

define `x2`
define `x3`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "x3", "x2"]

----

define `x2`
define `x3`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "x3", "x2"]

----

define `x2`
define `x3`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "x3", "x2"]

----

define `x2`
define `x3`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "x3", "x2"]

----

define `x2`
define `x3`
define `y3`
use `y3`
-- post defs: ["y3", "x3", "x2"]

----

define `x2`
define `x3`
define `y3`
use `x3`
-- post defs: ["y3", "x3", "x2"]

----

define `x2`
define `x3`
define `y3`
use `x2`
-- post defs: ["y3", "x3", "x2"]

----

define `x2`
define `x3`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "x3", "x2"]

----

define `x2`
define `x3`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "x3", "x2"]

----

define `x2`
define `x3`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "x3", "x2"]

----

define `x2`
define `x3`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "x3", "x2"]

----

define `x2`
define `x3`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "x3", "x2"]

----

define `x2`
define `x3`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "x3", "x2"]

----

define `x2`
define `x3`
define `z1`
use `z1`
-- post defs: ["z1", "x3", "x2"]

----

define `x2`
define `x3`
define `z1`
use `x3`
-- post defs: ["z1", "x3", "x2"]

----

define `x2`
define `x3`
define `z1`
use `x2`
-- post defs: ["z1", "x3", "x2"]

----

define `x2`
define `x3`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "x3", "x2"]

----

define `x2`
define `x3`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "x3", "x2"]

----

define `x2`
define `x3`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "x3", "x2"]

----

define `x2`
define `x3`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "x3", "x2"]

----

define `x2`
define `x3`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "x3", "x2"]

----

define `x2`
define `x3`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "x3", "x2"]

----

define `x2`
define `x3`
define `z2`
use `z2`
-- post defs: ["z2", "x3", "x2"]

----

define `x2`
define `x3`
define `z2`
use `x3`
-- post defs: ["z2", "x3", "x2"]

----

define `x2`
define `x3`
define `z2`
use `x2`
-- post defs: ["z2", "x3", "x2"]

----

define `x2`
define `x3`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "x3", "x2"]

----

define `x2`
define `x3`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "x3", "x2"]

----

define `x2`
define `x3`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "x3", "x2"]

----

define `x2`
define `x3`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "x3", "x2"]

----

define `x2`
define `x3`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "x3", "x2"]

----

define `x2`
define `x3`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "x3", "x2"]

----

define `x2`
define `x3`
define `z3`
use `z3`
-- post defs: ["z3", "x3", "x2"]

----

define `x2`
define `x3`
define `z3`
use `x3`
-- post defs: ["z3", "x3", "x2"]

----

define `x2`
define `x3`
define `z3`
use `x2`
-- post defs: ["z3", "x3", "x2"]

----

define `x2`
define `x3`
use `x3`
define `x1`
-- post defs: ["x1", "x3", "x2"]

----

define `x2`
define `x3`
use `x3`
define `y1`
-- post defs: ["y1", "x3", "x2"]

----

define `x2`
define `x3`
use `x3`
define `y2`
-- post defs: ["y2", "x3", "x2"]

----

define `x2`
define `x3`
use `x3`
define `y3`
-- post defs: ["y3", "x3", "x2"]

----

define `x2`
define `x3`
use `x3`
define `z1`
-- post defs: ["z1", "x3", "x2"]

----

define `x2`
define `x3`
use `x3`
define `z2`
-- post defs: ["z2", "x3", "x2"]

----

define `x2`
define `x3`
use `x3`
define `z3`
-- post defs: ["z3", "x3", "x2"]

----

define `x2`
define `x3`
use `x3`
use `x3`
-- post defs: ["x3", "x2"]

----

define `x2`
define `x3`
use `x3`
use `x2`
-- post defs: ["x3", "x2"]

----

define `x2`
define `x3`
use `x2`
define `x1`
-- post defs: ["x1", "x3", "x2"]

----

define `x2`
define `x3`
use `x2`
define `y1`
-- post defs: ["y1", "x3", "x2"]

----

define `x2`
define `x3`
use `x2`
define `y2`
-- post defs: ["y2", "x3", "x2"]

----

define `x2`
define `x3`
use `x2`
define `y3`
-- post defs: ["y3", "x3", "x2"]

----

define `x2`
define `x3`
use `x2`
define `z1`
-- post defs: ["z1", "x3", "x2"]

----

define `x2`
define `x3`
use `x2`
define `z2`
-- post defs: ["z2", "x3", "x2"]

----

define `x2`
define `x3`
use `x2`
define `z3`
-- post defs: ["z3", "x3", "x2"]

----

define `x2`
define `x3`
use `x2`
use `x3`
-- post defs: ["x3", "x2"]

----

define `x2`
define `x3`
use `x2`
use `x2`
-- post defs: ["x3", "x2"]

----

define `x2`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "x2"]

----

define `x2`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "x2"]

----

define `x2`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "x2"]

----

define `x2`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "x2"]

----

define `x2`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "x2"]

----

define `x2`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "x2"]

----

define `x2`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "x2"]

----

define `x2`
define `y1`
use `y1`
-- post defs: ["y1", "x2"]

----

define `x2`
define `y1`
use `x2`
-- post defs: ["y1", "x2"]

----

define `x2`
define `y1`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "y1", "x2"]

----

define `x2`
define `y1`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "y1", "x2"]

----

define `x2`
define `y1`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "y1", "x2"]

----

define `x2`
define `y1`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "y1", "x2"]

----

define `x2`
define `y1`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "y1", "x2"]

----

define `x2`
define `y1`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "y1", "x2"]

----

define `x2`
define `y1`
define `x1`
use `x1`
-- post defs: ["x1", "y1", "x2"]

----

define `x2`
define `y1`
define `x1`
use `y1`
-- post defs: ["x1", "y1", "x2"]

----

define `x2`
define `y1`
define `x1`
use `x2`
-- post defs: ["x1", "y1", "x2"]

----

define `x2`
define `y1`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "y1", "x2"]

----

define `x2`
define `y1`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "y1", "x2"]

----

define `x2`
define `y1`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "y1", "x2"]

----

define `x2`
define `y1`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "y1", "x2"]

----

define `x2`
define `y1`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "y1", "x2"]

----

define `x2`
define `y1`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "y1", "x2"]

----

define `x2`
define `y1`
define `x3`
use `x3`
-- post defs: ["x3", "y1", "x2"]

----

define `x2`
define `y1`
define `x3`
use `y1`
-- post defs: ["x3", "y1", "x2"]

----

define `x2`
define `y1`
define `x3`
use `x2`
-- post defs: ["x3", "y1", "x2"]

----

define `x2`
define `y1`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "y1", "x2"]

----

define `x2`
define `y1`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "y1", "x2"]

----

define `x2`
define `y1`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "y1", "x2"]

----

define `x2`
define `y1`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "y1", "x2"]

----

define `x2`
define `y1`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "y1", "x2"]

----

define `x2`
define `y1`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "y1", "x2"]

----

define `x2`
define `y1`
define `y2`
use `y2`
-- post defs: ["y2", "y1", "x2"]

----

define `x2`
define `y1`
define `y2`
use `y1`
-- post defs: ["y2", "y1", "x2"]

----

define `x2`
define `y1`
define `y2`
use `x2`
-- post defs: ["y2", "y1", "x2"]

----

define `x2`
define `y1`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "y1", "x2"]

----

define `x2`
define `y1`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "y1", "x2"]

----

define `x2`
define `y1`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "y1", "x2"]

----

define `x2`
define `y1`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "y1", "x2"]

----

define `x2`
define `y1`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "y1", "x2"]

----

define `x2`
define `y1`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "y1", "x2"]

----

define `x2`
define `y1`
define `y3`
use `y3`
-- post defs: ["y3", "y1", "x2"]

----

define `x2`
define `y1`
define `y3`
use `y1`
-- post defs: ["y3", "y1", "x2"]

----

define `x2`
define `y1`
define `y3`
use `x2`
-- post defs: ["y3", "y1", "x2"]

----

define `x2`
define `y1`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "y1", "x2"]

----

define `x2`
define `y1`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "y1", "x2"]

----

define `x2`
define `y1`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "y1", "x2"]

----

define `x2`
define `y1`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "y1", "x2"]

----

define `x2`
define `y1`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "y1", "x2"]

----

define `x2`
define `y1`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "y1", "x2"]

----

define `x2`
define `y1`
define `z1`
use `z1`
-- post defs: ["z1", "y1", "x2"]

----

define `x2`
define `y1`
define `z1`
use `y1`
-- post defs: ["z1", "y1", "x2"]

----

define `x2`
define `y1`
define `z1`
use `x2`
-- post defs: ["z1", "y1", "x2"]

----

define `x2`
define `y1`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "y1", "x2"]

----

define `x2`
define `y1`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "y1", "x2"]

----

define `x2`
define `y1`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "y1", "x2"]

----

define `x2`
define `y1`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "y1", "x2"]

----

define `x2`
define `y1`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "y1", "x2"]

----

define `x2`
define `y1`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "y1", "x2"]

----

define `x2`
define `y1`
define `z2`
use `z2`
-- post defs: ["z2", "y1", "x2"]

----

define `x2`
define `y1`
define `z2`
use `y1`
-- post defs: ["z2", "y1", "x2"]

----

define `x2`
define `y1`
define `z2`
use `x2`
-- post defs: ["z2", "y1", "x2"]

----

define `x2`
define `y1`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "y1", "x2"]

----

define `x2`
define `y1`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "y1", "x2"]

----

define `x2`
define `y1`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "y1", "x2"]

----

define `x2`
define `y1`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "y1", "x2"]

----

define `x2`
define `y1`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "y1", "x2"]

----

define `x2`
define `y1`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "y1", "x2"]

----

define `x2`
define `y1`
define `z3`
use `z3`
-- post defs: ["z3", "y1", "x2"]

----

define `x2`
define `y1`
define `z3`
use `y1`
-- post defs: ["z3", "y1", "x2"]

----

define `x2`
define `y1`
define `z3`
use `x2`
-- post defs: ["z3", "y1", "x2"]

----

define `x2`
define `y1`
use `y1`
define `x1`
-- post defs: ["x1", "y1", "x2"]

----

define `x2`
define `y1`
use `y1`
define `x3`
-- post defs: ["x3", "y1", "x2"]

----

define `x2`
define `y1`
use `y1`
define `y2`
-- post defs: ["y2", "y1", "x2"]

----

define `x2`
define `y1`
use `y1`
define `y3`
-- post defs: ["y3", "y1", "x2"]

----

define `x2`
define `y1`
use `y1`
define `z1`
-- post defs: ["z1", "y1", "x2"]

----

define `x2`
define `y1`
use `y1`
define `z2`
-- post defs: ["z2", "y1", "x2"]

----

define `x2`
define `y1`
use `y1`
define `z3`
-- post defs: ["z3", "y1", "x2"]

----

define `x2`
define `y1`
use `y1`
use `y1`
-- post defs: ["y1", "x2"]

----

define `x2`
define `y1`
use `y1`
use `x2`
-- post defs: ["y1", "x2"]

----

define `x2`
define `y1`
use `x2`
define `x1`
-- post defs: ["x1", "y1", "x2"]

----

define `x2`
define `y1`
use `x2`
define `x3`
-- post defs: ["x3", "y1", "x2"]

----

define `x2`
define `y1`
use `x2`
define `y2`
-- post defs: ["y2", "y1", "x2"]

----

define `x2`
define `y1`
use `x2`
define `y3`
-- post defs: ["y3", "y1", "x2"]

----

define `x2`
define `y1`
use `x2`
define `z1`
-- post defs: ["z1", "y1", "x2"]

----

define `x2`
define `y1`
use `x2`
define `z2`
-- post defs: ["z2", "y1", "x2"]

----

define `x2`
define `y1`
use `x2`
define `z3`
-- post defs: ["z3", "y1", "x2"]

----

define `x2`
define `y1`
use `x2`
use `y1`
-- post defs: ["y1", "x2"]

----

define `x2`
define `y1`
use `x2`
use `x2`
-- post defs: ["y1", "x2"]

----

define `x2`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "x2"]

----

define `x2`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "x2"]

----

define `x2`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "x2"]

----

define `x2`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "x2"]

----

define `x2`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "x2"]

----

define `x2`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "x2"]

----

define `x2`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "x2"]

----

define `x2`
define `y2`
use `y2`
-- post defs: ["y2", "x2"]

----

define `x2`
define `y2`
use `x2`
-- post defs: ["y2", "x2"]

----

define `x2`
define `y2`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "y2", "x2"]

----

define `x2`
define `y2`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "y2", "x2"]

----

define `x2`
define `y2`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "y2", "x2"]

----

define `x2`
define `y2`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "y2", "x2"]

----

define `x2`
define `y2`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "y2", "x2"]

----

define `x2`
define `y2`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "y2", "x2"]

----

define `x2`
define `y2`
define `x1`
use `x1`
-- post defs: ["x1", "y2", "x2"]

----

define `x2`
define `y2`
define `x1`
use `y2`
-- post defs: ["x1", "y2", "x2"]

----

define `x2`
define `y2`
define `x1`
use `x2`
-- post defs: ["x1", "y2", "x2"]

----

define `x2`
define `y2`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "y2", "x2"]

----

define `x2`
define `y2`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "y2", "x2"]

----

define `x2`
define `y2`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "y2", "x2"]

----

define `x2`
define `y2`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "y2", "x2"]

----

define `x2`
define `y2`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "y2", "x2"]

----

define `x2`
define `y2`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "y2", "x2"]

----

define `x2`
define `y2`
define `x3`
use `x3`
-- post defs: ["x3", "y2", "x2"]

----

define `x2`
define `y2`
define `x3`
use `y2`
-- post defs: ["x3", "y2", "x2"]

----

define `x2`
define `y2`
define `x3`
use `x2`
-- post defs: ["x3", "y2", "x2"]

----

define `x2`
define `y2`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "y2", "x2"]

----

define `x2`
define `y2`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "y2", "x2"]

----

define `x2`
define `y2`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "y2", "x2"]

----

define `x2`
define `y2`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "y2", "x2"]

----

define `x2`
define `y2`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "y2", "x2"]

----

define `x2`
define `y2`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "y2", "x2"]

----

define `x2`
define `y2`
define `y1`
use `y1`
-- post defs: ["y1", "y2", "x2"]

----

define `x2`
define `y2`
define `y1`
use `y2`
-- post defs: ["y1", "y2", "x2"]

----

define `x2`
define `y2`
define `y1`
use `x2`
-- post defs: ["y1", "y2", "x2"]

----

define `x2`
define `y2`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "y2", "x2"]

----

define `x2`
define `y2`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "y2", "x2"]

----

define `x2`
define `y2`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "y2", "x2"]

----

define `x2`
define `y2`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "y2", "x2"]

----

define `x2`
define `y2`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "y2", "x2"]

----

define `x2`
define `y2`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "y2", "x2"]

----

define `x2`
define `y2`
define `y3`
use `y3`
-- post defs: ["y3", "y2", "x2"]

----

define `x2`
define `y2`
define `y3`
use `y2`
-- post defs: ["y3", "y2", "x2"]

----

define `x2`
define `y2`
define `y3`
use `x2`
-- post defs: ["y3", "y2", "x2"]

----

define `x2`
define `y2`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "y2", "x2"]

----

define `x2`
define `y2`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "y2", "x2"]

----

define `x2`
define `y2`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "y2", "x2"]

----

define `x2`
define `y2`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "y2", "x2"]

----

define `x2`
define `y2`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "y2", "x2"]

----

define `x2`
define `y2`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "y2", "x2"]

----

define `x2`
define `y2`
define `z1`
use `z1`
-- post defs: ["z1", "y2", "x2"]

----

define `x2`
define `y2`
define `z1`
use `y2`
-- post defs: ["z1", "y2", "x2"]

----

define `x2`
define `y2`
define `z1`
use `x2`
-- post defs: ["z1", "y2", "x2"]

----

define `x2`
define `y2`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "y2", "x2"]

----

define `x2`
define `y2`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "y2", "x2"]

----

define `x2`
define `y2`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "y2", "x2"]

----

define `x2`
define `y2`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "y2", "x2"]

----

define `x2`
define `y2`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "y2", "x2"]

----

define `x2`
define `y2`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "y2", "x2"]

----

define `x2`
define `y2`
define `z2`
use `z2`
-- post defs: ["z2", "y2", "x2"]

----

define `x2`
define `y2`
define `z2`
use `y2`
-- post defs: ["z2", "y2", "x2"]

----

define `x2`
define `y2`
define `z2`
use `x2`
-- post defs: ["z2", "y2", "x2"]

----

define `x2`
define `y2`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "y2", "x2"]

----

define `x2`
define `y2`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "y2", "x2"]

----

define `x2`
define `y2`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "y2", "x2"]

----

define `x2`
define `y2`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "y2", "x2"]

----

define `x2`
define `y2`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "y2", "x2"]

----

define `x2`
define `y2`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "y2", "x2"]

----

define `x2`
define `y2`
define `z3`
use `z3`
-- post defs: ["z3", "y2", "x2"]

----

define `x2`
define `y2`
define `z3`
use `y2`
-- post defs: ["z3", "y2", "x2"]

----

define `x2`
define `y2`
define `z3`
use `x2`
-- post defs: ["z3", "y2", "x2"]

----

define `x2`
define `y2`
use `y2`
define `x1`
-- post defs: ["x1", "y2", "x2"]

----

define `x2`
define `y2`
use `y2`
define `x3`
-- post defs: ["x3", "y2", "x2"]

----

define `x2`
define `y2`
use `y2`
define `y1`
-- post defs: ["y1", "y2", "x2"]

----

define `x2`
define `y2`
use `y2`
define `y3`
-- post defs: ["y3", "y2", "x2"]

----

define `x2`
define `y2`
use `y2`
define `z1`
-- post defs: ["z1", "y2", "x2"]

----

define `x2`
define `y2`
use `y2`
define `z2`
-- post defs: ["z2", "y2", "x2"]

----

define `x2`
define `y2`
use `y2`
define `z3`
-- post defs: ["z3", "y2", "x2"]

----

define `x2`
define `y2`
use `y2`
use `y2`
-- post defs: ["y2", "x2"]

----

define `x2`
define `y2`
use `y2`
use `x2`
-- post defs: ["y2", "x2"]

----

define `x2`
define `y2`
use `x2`
define `x1`
-- post defs: ["x1", "y2", "x2"]

----

define `x2`
define `y2`
use `x2`
define `x3`
-- post defs: ["x3", "y2", "x2"]

----

define `x2`
define `y2`
use `x2`
define `y1`
-- post defs: ["y1", "y2", "x2"]

----

define `x2`
define `y2`
use `x2`
define `y3`
-- post defs: ["y3", "y2", "x2"]

----

define `x2`
define `y2`
use `x2`
define `z1`
-- post defs: ["z1", "y2", "x2"]

----

define `x2`
define `y2`
use `x2`
define `z2`
-- post defs: ["z2", "y2", "x2"]

----

define `x2`
define `y2`
use `x2`
define `z3`
-- post defs: ["z3", "y2", "x2"]

----

define `x2`
define `y2`
use `x2`
use `y2`
-- post defs: ["y2", "x2"]

----

define `x2`
define `y2`
use `x2`
use `x2`
-- post defs: ["y2", "x2"]

----

define `x2`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "x2"]

----

define `x2`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "x2"]

----

define `x2`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "x2"]

----

define `x2`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "x2"]

----

define `x2`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "x2"]

----

define `x2`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "x2"]

----

define `x2`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "x2"]

----

define `x2`
define `y3`
use `y3`
-- post defs: ["y3", "x2"]

----

define `x2`
define `y3`
use `x2`
-- post defs: ["y3", "x2"]

----

define `x2`
define `y3`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "y3", "x2"]

----

define `x2`
define `y3`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "y3", "x2"]

----

define `x2`
define `y3`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "y3", "x2"]

----

define `x2`
define `y3`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "y3", "x2"]

----

define `x2`
define `y3`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "y3", "x2"]

----

define `x2`
define `y3`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "y3", "x2"]

----

define `x2`
define `y3`
define `x1`
use `x1`
-- post defs: ["x1", "y3", "x2"]

----

define `x2`
define `y3`
define `x1`
use `y3`
-- post defs: ["x1", "y3", "x2"]

----

define `x2`
define `y3`
define `x1`
use `x2`
-- post defs: ["x1", "y3", "x2"]

----

define `x2`
define `y3`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "y3", "x2"]

----

define `x2`
define `y3`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "y3", "x2"]

----

define `x2`
define `y3`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "y3", "x2"]

----

define `x2`
define `y3`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "y3", "x2"]

----

define `x2`
define `y3`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "y3", "x2"]

----

define `x2`
define `y3`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "y3", "x2"]

----

define `x2`
define `y3`
define `x3`
use `x3`
-- post defs: ["x3", "y3", "x2"]

----

define `x2`
define `y3`
define `x3`
use `y3`
-- post defs: ["x3", "y3", "x2"]

----

define `x2`
define `y3`
define `x3`
use `x2`
-- post defs: ["x3", "y3", "x2"]

----

define `x2`
define `y3`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "y3", "x2"]

----

define `x2`
define `y3`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "y3", "x2"]

----

define `x2`
define `y3`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "y3", "x2"]

----

define `x2`
define `y3`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "y3", "x2"]

----

define `x2`
define `y3`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "y3", "x2"]

----

define `x2`
define `y3`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "y3", "x2"]

----

define `x2`
define `y3`
define `y1`
use `y1`
-- post defs: ["y1", "y3", "x2"]

----

define `x2`
define `y3`
define `y1`
use `y3`
-- post defs: ["y1", "y3", "x2"]

----

define `x2`
define `y3`
define `y1`
use `x2`
-- post defs: ["y1", "y3", "x2"]

----

define `x2`
define `y3`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "y3", "x2"]

----

define `x2`
define `y3`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "y3", "x2"]

----

define `x2`
define `y3`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "y3", "x2"]

----

define `x2`
define `y3`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "y3", "x2"]

----

define `x2`
define `y3`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "y3", "x2"]

----

define `x2`
define `y3`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "y3", "x2"]

----

define `x2`
define `y3`
define `y2`
use `y2`
-- post defs: ["y2", "y3", "x2"]

----

define `x2`
define `y3`
define `y2`
use `y3`
-- post defs: ["y2", "y3", "x2"]

----

define `x2`
define `y3`
define `y2`
use `x2`
-- post defs: ["y2", "y3", "x2"]

----

define `x2`
define `y3`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "y3", "x2"]

----

define `x2`
define `y3`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "y3", "x2"]

----

define `x2`
define `y3`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "y3", "x2"]

----

define `x2`
define `y3`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "y3", "x2"]

----

define `x2`
define `y3`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "y3", "x2"]

----

define `x2`
define `y3`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "y3", "x2"]

----

define `x2`
define `y3`
define `z1`
use `z1`
-- post defs: ["z1", "y3", "x2"]

----

define `x2`
define `y3`
define `z1`
use `y3`
-- post defs: ["z1", "y3", "x2"]

----

define `x2`
define `y3`
define `z1`
use `x2`
-- post defs: ["z1", "y3", "x2"]

----

define `x2`
define `y3`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "y3", "x2"]

----

define `x2`
define `y3`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "y3", "x2"]

----

define `x2`
define `y3`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "y3", "x2"]

----

define `x2`
define `y3`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "y3", "x2"]

----

define `x2`
define `y3`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "y3", "x2"]

----

define `x2`
define `y3`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "y3", "x2"]

----

define `x2`
define `y3`
define `z2`
use `z2`
-- post defs: ["z2", "y3", "x2"]

----

define `x2`
define `y3`
define `z2`
use `y3`
-- post defs: ["z2", "y3", "x2"]

----

define `x2`
define `y3`
define `z2`
use `x2`
-- post defs: ["z2", "y3", "x2"]

----

define `x2`
define `y3`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "y3", "x2"]

----

define `x2`
define `y3`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "y3", "x2"]

----

define `x2`
define `y3`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "y3", "x2"]

----

define `x2`
define `y3`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "y3", "x2"]

----

define `x2`
define `y3`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "y3", "x2"]

----

define `x2`
define `y3`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "y3", "x2"]

----

define `x2`
define `y3`
define `z3`
use `z3`
-- post defs: ["z3", "y3", "x2"]

----

define `x2`
define `y3`
define `z3`
use `y3`
-- post defs: ["z3", "y3", "x2"]

----

define `x2`
define `y3`
define `z3`
use `x2`
-- post defs: ["z3", "y3", "x2"]

----

define `x2`
define `y3`
use `y3`
define `x1`
-- post defs: ["x1", "y3", "x2"]

----

define `x2`
define `y3`
use `y3`
define `x3`
-- post defs: ["x3", "y3", "x2"]

----

define `x2`
define `y3`
use `y3`
define `y1`
-- post defs: ["y1", "y3", "x2"]

----

define `x2`
define `y3`
use `y3`
define `y2`
-- post defs: ["y2", "y3", "x2"]

----

define `x2`
define `y3`
use `y3`
define `z1`
-- post defs: ["z1", "y3", "x2"]

----

define `x2`
define `y3`
use `y3`
define `z2`
-- post defs: ["z2", "y3", "x2"]

----

define `x2`
define `y3`
use `y3`
define `z3`
-- post defs: ["z3", "y3", "x2"]

----

define `x2`
define `y3`
use `y3`
use `y3`
-- post defs: ["y3", "x2"]

----

define `x2`
define `y3`
use `y3`
use `x2`
-- post defs: ["y3", "x2"]

----

define `x2`
define `y3`
use `x2`
define `x1`
-- post defs: ["x1", "y3", "x2"]

----

define `x2`
define `y3`
use `x2`
define `x3`
-- post defs: ["x3", "y3", "x2"]

----

define `x2`
define `y3`
use `x2`
define `y1`
-- post defs: ["y1", "y3", "x2"]

----

define `x2`
define `y3`
use `x2`
define `y2`
-- post defs: ["y2", "y3", "x2"]

----

define `x2`
define `y3`
use `x2`
define `z1`
-- post defs: ["z1", "y3", "x2"]

----

define `x2`
define `y3`
use `x2`
define `z2`
-- post defs: ["z2", "y3", "x2"]

----

define `x2`
define `y3`
use `x2`
define `z3`
-- post defs: ["z3", "y3", "x2"]

----

define `x2`
define `y3`
use `x2`
use `y3`
-- post defs: ["y3", "x2"]

----

define `x2`
define `y3`
use `x2`
use `x2`
-- post defs: ["y3", "x2"]

----

define `x2`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "x2"]

----

define `x2`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "x2"]

----

define `x2`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "x2"]

----

define `x2`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "x2"]

----

define `x2`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "x2"]

----

define `x2`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "x2"]

----

define `x2`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "x2"]

----

define `x2`
define `z1`
use `z1`
-- post defs: ["z1", "x2"]

----

define `x2`
define `z1`
use `x2`
-- post defs: ["z1", "x2"]

----

define `x2`
define `z1`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "z1", "x2"]

----

define `x2`
define `z1`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "z1", "x2"]

----

define `x2`
define `z1`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "z1", "x2"]

----

define `x2`
define `z1`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "z1", "x2"]

----

define `x2`
define `z1`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "z1", "x2"]

----

define `x2`
define `z1`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "z1", "x2"]

----

define `x2`
define `z1`
define `x1`
use `x1`
-- post defs: ["x1", "z1", "x2"]

----

define `x2`
define `z1`
define `x1`
use `z1`
-- post defs: ["x1", "z1", "x2"]

----

define `x2`
define `z1`
define `x1`
use `x2`
-- post defs: ["x1", "z1", "x2"]

----

define `x2`
define `z1`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "z1", "x2"]

----

define `x2`
define `z1`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "z1", "x2"]

----

define `x2`
define `z1`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "z1", "x2"]

----

define `x2`
define `z1`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "z1", "x2"]

----

define `x2`
define `z1`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "z1", "x2"]

----

define `x2`
define `z1`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "z1", "x2"]

----

define `x2`
define `z1`
define `x3`
use `x3`
-- post defs: ["x3", "z1", "x2"]

----

define `x2`
define `z1`
define `x3`
use `z1`
-- post defs: ["x3", "z1", "x2"]

----

define `x2`
define `z1`
define `x3`
use `x2`
-- post defs: ["x3", "z1", "x2"]

----

define `x2`
define `z1`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "z1", "x2"]

----

define `x2`
define `z1`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "z1", "x2"]

----

define `x2`
define `z1`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "z1", "x2"]

----

define `x2`
define `z1`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "z1", "x2"]

----

define `x2`
define `z1`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "z1", "x2"]

----

define `x2`
define `z1`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "z1", "x2"]

----

define `x2`
define `z1`
define `y1`
use `y1`
-- post defs: ["y1", "z1", "x2"]

----

define `x2`
define `z1`
define `y1`
use `z1`
-- post defs: ["y1", "z1", "x2"]

----

define `x2`
define `z1`
define `y1`
use `x2`
-- post defs: ["y1", "z1", "x2"]

----

define `x2`
define `z1`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "z1", "x2"]

----

define `x2`
define `z1`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "z1", "x2"]

----

define `x2`
define `z1`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "z1", "x2"]

----

define `x2`
define `z1`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "z1", "x2"]

----

define `x2`
define `z1`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "z1", "x2"]

----

define `x2`
define `z1`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "z1", "x2"]

----

define `x2`
define `z1`
define `y2`
use `y2`
-- post defs: ["y2", "z1", "x2"]

----

define `x2`
define `z1`
define `y2`
use `z1`
-- post defs: ["y2", "z1", "x2"]

----

define `x2`
define `z1`
define `y2`
use `x2`
-- post defs: ["y2", "z1", "x2"]

----

define `x2`
define `z1`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "z1", "x2"]

----

define `x2`
define `z1`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "z1", "x2"]

----

define `x2`
define `z1`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "z1", "x2"]

----

define `x2`
define `z1`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "z1", "x2"]

----

define `x2`
define `z1`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "z1", "x2"]

----

define `x2`
define `z1`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "z1", "x2"]

----

define `x2`
define `z1`
define `y3`
use `y3`
-- post defs: ["y3", "z1", "x2"]

----

define `x2`
define `z1`
define `y3`
use `z1`
-- post defs: ["y3", "z1", "x2"]

----

define `x2`
define `z1`
define `y3`
use `x2`
-- post defs: ["y3", "z1", "x2"]

----

define `x2`
define `z1`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "z1", "x2"]

----

define `x2`
define `z1`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "z1", "x2"]

----

define `x2`
define `z1`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "z1", "x2"]

----

define `x2`
define `z1`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "z1", "x2"]

----

define `x2`
define `z1`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "z1", "x2"]

----

define `x2`
define `z1`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "z1", "x2"]

----

define `x2`
define `z1`
define `z2`
use `z2`
-- post defs: ["z2", "z1", "x2"]

----

define `x2`
define `z1`
define `z2`
use `z1`
-- post defs: ["z2", "z1", "x2"]

----

define `x2`
define `z1`
define `z2`
use `x2`
-- post defs: ["z2", "z1", "x2"]

----

define `x2`
define `z1`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "z1", "x2"]

----

define `x2`
define `z1`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "z1", "x2"]

----

define `x2`
define `z1`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "z1", "x2"]

----

define `x2`
define `z1`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "z1", "x2"]

----

define `x2`
define `z1`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "z1", "x2"]

----

define `x2`
define `z1`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "z1", "x2"]

----

define `x2`
define `z1`
define `z3`
use `z3`
-- post defs: ["z3", "z1", "x2"]

----

define `x2`
define `z1`
define `z3`
use `z1`
-- post defs: ["z3", "z1", "x2"]

----

define `x2`
define `z1`
define `z3`
use `x2`
-- post defs: ["z3", "z1", "x2"]

----

define `x2`
define `z1`
use `z1`
define `x1`
-- post defs: ["x1", "z1", "x2"]

----

define `x2`
define `z1`
use `z1`
define `x3`
-- post defs: ["x3", "z1", "x2"]

----

define `x2`
define `z1`
use `z1`
define `y1`
-- post defs: ["y1", "z1", "x2"]

----

define `x2`
define `z1`
use `z1`
define `y2`
-- post defs: ["y2", "z1", "x2"]

----

define `x2`
define `z1`
use `z1`
define `y3`
-- post defs: ["y3", "z1", "x2"]

----

define `x2`
define `z1`
use `z1`
define `z2`
-- post defs: ["z2", "z1", "x2"]

----

define `x2`
define `z1`
use `z1`
define `z3`
-- post defs: ["z3", "z1", "x2"]

----

define `x2`
define `z1`
use `z1`
use `z1`
-- post defs: ["z1", "x2"]

----

define `x2`
define `z1`
use `z1`
use `x2`
-- post defs: ["z1", "x2"]

----

define `x2`
define `z1`
use `x2`
define `x1`
-- post defs: ["x1", "z1", "x2"]

----

define `x2`
define `z1`
use `x2`
define `x3`
-- post defs: ["x3", "z1", "x2"]

----

define `x2`
define `z1`
use `x2`
define `y1`
-- post defs: ["y1", "z1", "x2"]

----

define `x2`
define `z1`
use `x2`
define `y2`
-- post defs: ["y2", "z1", "x2"]

----

define `x2`
define `z1`
use `x2`
define `y3`
-- post defs: ["y3", "z1", "x2"]

----

define `x2`
define `z1`
use `x2`
define `z2`
-- post defs: ["z2", "z1", "x2"]

----

define `x2`
define `z1`
use `x2`
define `z3`
-- post defs: ["z3", "z1", "x2"]

----

define `x2`
define `z1`
use `x2`
use `z1`
-- post defs: ["z1", "x2"]

----

define `x2`
define `z1`
use `x2`
use `x2`
-- post defs: ["z1", "x2"]

----

define `x2`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "x2"]

----

define `x2`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "x2"]

----

define `x2`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "x2"]

----

define `x2`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "x2"]

----

define `x2`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "x2"]

----

define `x2`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "x2"]

----

define `x2`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "x2"]

----

define `x2`
define `z2`
use `z2`
-- post defs: ["z2", "x2"]

----

define `x2`
define `z2`
use `x2`
-- post defs: ["z2", "x2"]

----

define `x2`
define `z2`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "z2", "x2"]

----

define `x2`
define `z2`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "z2", "x2"]

----

define `x2`
define `z2`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "z2", "x2"]

----

define `x2`
define `z2`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "z2", "x2"]

----

define `x2`
define `z2`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "z2", "x2"]

----

define `x2`
define `z2`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "z2", "x2"]

----

define `x2`
define `z2`
define `x1`
use `x1`
-- post defs: ["x1", "z2", "x2"]

----

define `x2`
define `z2`
define `x1`
use `z2`
-- post defs: ["x1", "z2", "x2"]

----

define `x2`
define `z2`
define `x1`
use `x2`
-- post defs: ["x1", "z2", "x2"]

----

define `x2`
define `z2`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "z2", "x2"]

----

define `x2`
define `z2`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "z2", "x2"]

----

define `x2`
define `z2`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "z2", "x2"]

----

define `x2`
define `z2`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "z2", "x2"]

----

define `x2`
define `z2`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "z2", "x2"]

----

define `x2`
define `z2`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "z2", "x2"]

----

define `x2`
define `z2`
define `x3`
use `x3`
-- post defs: ["x3", "z2", "x2"]

----

define `x2`
define `z2`
define `x3`
use `z2`
-- post defs: ["x3", "z2", "x2"]

----

define `x2`
define `z2`
define `x3`
use `x2`
-- post defs: ["x3", "z2", "x2"]

----

define `x2`
define `z2`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "z2", "x2"]

----

define `x2`
define `z2`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "z2", "x2"]

----

define `x2`
define `z2`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "z2", "x2"]

----

define `x2`
define `z2`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "z2", "x2"]

----

define `x2`
define `z2`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "z2", "x2"]

----

define `x2`
define `z2`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "z2", "x2"]

----

define `x2`
define `z2`
define `y1`
use `y1`
-- post defs: ["y1", "z2", "x2"]

----

define `x2`
define `z2`
define `y1`
use `z2`
-- post defs: ["y1", "z2", "x2"]

----

define `x2`
define `z2`
define `y1`
use `x2`
-- post defs: ["y1", "z2", "x2"]

----

define `x2`
define `z2`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "z2", "x2"]

----

define `x2`
define `z2`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "z2", "x2"]

----

define `x2`
define `z2`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "z2", "x2"]

----

define `x2`
define `z2`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "z2", "x2"]

----

define `x2`
define `z2`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "z2", "x2"]

----

define `x2`
define `z2`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "z2", "x2"]

----

define `x2`
define `z2`
define `y2`
use `y2`
-- post defs: ["y2", "z2", "x2"]

----

define `x2`
define `z2`
define `y2`
use `z2`
-- post defs: ["y2", "z2", "x2"]

----

define `x2`
define `z2`
define `y2`
use `x2`
-- post defs: ["y2", "z2", "x2"]

----

define `x2`
define `z2`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "z2", "x2"]

----

define `x2`
define `z2`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "z2", "x2"]

----

define `x2`
define `z2`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "z2", "x2"]

----

define `x2`
define `z2`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "z2", "x2"]

----

define `x2`
define `z2`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "z2", "x2"]

----

define `x2`
define `z2`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "z2", "x2"]

----

define `x2`
define `z2`
define `y3`
use `y3`
-- post defs: ["y3", "z2", "x2"]

----

define `x2`
define `z2`
define `y3`
use `z2`
-- post defs: ["y3", "z2", "x2"]

----

define `x2`
define `z2`
define `y3`
use `x2`
-- post defs: ["y3", "z2", "x2"]

----

define `x2`
define `z2`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "z2", "x2"]

----

define `x2`
define `z2`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "z2", "x2"]

----

define `x2`
define `z2`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "z2", "x2"]

----

define `x2`
define `z2`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "z2", "x2"]

----

define `x2`
define `z2`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "z2", "x2"]

----

define `x2`
define `z2`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "z2", "x2"]

----

define `x2`
define `z2`
define `z1`
use `z1`
-- post defs: ["z1", "z2", "x2"]

----

define `x2`
define `z2`
define `z1`
use `z2`
-- post defs: ["z1", "z2", "x2"]

----

define `x2`
define `z2`
define `z1`
use `x2`
-- post defs: ["z1", "z2", "x2"]

----

define `x2`
define `z2`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "z2", "x2"]

----

define `x2`
define `z2`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "z2", "x2"]

----

define `x2`
define `z2`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "z2", "x2"]

----

define `x2`
define `z2`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "z2", "x2"]

----

define `x2`
define `z2`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "z2", "x2"]

----

define `x2`
define `z2`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "z2", "x2"]

----

define `x2`
define `z2`
define `z3`
use `z3`
-- post defs: ["z3", "z2", "x2"]

----

define `x2`
define `z2`
define `z3`
use `z2`
-- post defs: ["z3", "z2", "x2"]

----

define `x2`
define `z2`
define `z3`
use `x2`
-- post defs: ["z3", "z2", "x2"]

----

define `x2`
define `z2`
use `z2`
define `x1`
-- post defs: ["x1", "z2", "x2"]

----

define `x2`
define `z2`
use `z2`
define `x3`
-- post defs: ["x3", "z2", "x2"]

----

define `x2`
define `z2`
use `z2`
define `y1`
-- post defs: ["y1", "z2", "x2"]

----

define `x2`
define `z2`
use `z2`
define `y2`
-- post defs: ["y2", "z2", "x2"]

----

define `x2`
define `z2`
use `z2`
define `y3`
-- post defs: ["y3", "z2", "x2"]

----

define `x2`
define `z2`
use `z2`
define `z1`
-- post defs: ["z1", "z2", "x2"]

----

define `x2`
define `z2`
use `z2`
define `z3`
-- post defs: ["z3", "z2", "x2"]

----

define `x2`
define `z2`
use `z2`
use `z2`
-- post defs: ["z2", "x2"]

----

define `x2`
define `z2`
use `z2`
use `x2`
-- post defs: ["z2", "x2"]

----

define `x2`
define `z2`
use `x2`
define `x1`
-- post defs: ["x1", "z2", "x2"]

----

define `x2`
define `z2`
use `x2`
define `x3`
-- post defs: ["x3", "z2", "x2"]

----

define `x2`
define `z2`
use `x2`
define `y1`
-- post defs: ["y1", "z2", "x2"]

----

define `x2`
define `z2`
use `x2`
define `y2`
-- post defs: ["y2", "z2", "x2"]

----

define `x2`
define `z2`
use `x2`
define `y3`
-- post defs: ["y3", "z2", "x2"]

----

define `x2`
define `z2`
use `x2`
define `z1`
-- post defs: ["z1", "z2", "x2"]

----

define `x2`
define `z2`
use `x2`
define `z3`
-- post defs: ["z3", "z2", "x2"]

----

define `x2`
define `z2`
use `x2`
use `z2`
-- post defs: ["z2", "x2"]

----

define `x2`
define `z2`
use `x2`
use `x2`
-- post defs: ["z2", "x2"]

----

define `x2`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "x2"]

----

define `x2`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "x2"]

----

define `x2`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "x2"]

----

define `x2`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "x2"]

----

define `x2`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "x2"]

----

define `x2`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "x2"]

----

define `x2`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "x2"]

----

define `x2`
define `z3`
use `z3`
-- post defs: ["z3", "x2"]

----

define `x2`
define `z3`
use `x2`
-- post defs: ["z3", "x2"]

----

define `x2`
define `z3`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "z3", "x2"]

----

define `x2`
define `z3`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "z3", "x2"]

----

define `x2`
define `z3`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "z3", "x2"]

----

define `x2`
define `z3`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "z3", "x2"]

----

define `x2`
define `z3`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "z3", "x2"]

----

define `x2`
define `z3`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "z3", "x2"]

----

define `x2`
define `z3`
define `x1`
use `x1`
-- post defs: ["x1", "z3", "x2"]

----

define `x2`
define `z3`
define `x1`
use `z3`
-- post defs: ["x1", "z3", "x2"]

----

define `x2`
define `z3`
define `x1`
use `x2`
-- post defs: ["x1", "z3", "x2"]

----

define `x2`
define `z3`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "z3", "x2"]

----

define `x2`
define `z3`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "z3", "x2"]

----

define `x2`
define `z3`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "z3", "x2"]

----

define `x2`
define `z3`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "z3", "x2"]

----

define `x2`
define `z3`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "z3", "x2"]

----

define `x2`
define `z3`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "z3", "x2"]

----

define `x2`
define `z3`
define `x3`
use `x3`
-- post defs: ["x3", "z3", "x2"]

----

define `x2`
define `z3`
define `x3`
use `z3`
-- post defs: ["x3", "z3", "x2"]

----

define `x2`
define `z3`
define `x3`
use `x2`
-- post defs: ["x3", "z3", "x2"]

----

define `x2`
define `z3`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "z3", "x2"]

----

define `x2`
define `z3`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "z3", "x2"]

----

define `x2`
define `z3`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "z3", "x2"]

----

define `x2`
define `z3`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "z3", "x2"]

----

define `x2`
define `z3`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "z3", "x2"]

----

define `x2`
define `z3`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "z3", "x2"]

----

define `x2`
define `z3`
define `y1`
use `y1`
-- post defs: ["y1", "z3", "x2"]

----

define `x2`
define `z3`
define `y1`
use `z3`
-- post defs: ["y1", "z3", "x2"]

----

define `x2`
define `z3`
define `y1`
use `x2`
-- post defs: ["y1", "z3", "x2"]

----

define `x2`
define `z3`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "z3", "x2"]

----

define `x2`
define `z3`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "z3", "x2"]

----

define `x2`
define `z3`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "z3", "x2"]

----

define `x2`
define `z3`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "z3", "x2"]

----

define `x2`
define `z3`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "z3", "x2"]

----

define `x2`
define `z3`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "z3", "x2"]

----

define `x2`
define `z3`
define `y2`
use `y2`
-- post defs: ["y2", "z3", "x2"]

----

define `x2`
define `z3`
define `y2`
use `z3`
-- post defs: ["y2", "z3", "x2"]

----

define `x2`
define `z3`
define `y2`
use `x2`
-- post defs: ["y2", "z3", "x2"]

----

define `x2`
define `z3`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "z3", "x2"]

----

define `x2`
define `z3`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "z3", "x2"]

----

define `x2`
define `z3`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "z3", "x2"]

----

define `x2`
define `z3`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "z3", "x2"]

----

define `x2`
define `z3`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "z3", "x2"]

----

define `x2`
define `z3`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "z3", "x2"]

----

define `x2`
define `z3`
define `y3`
use `y3`
-- post defs: ["y3", "z3", "x2"]

----

define `x2`
define `z3`
define `y3`
use `z3`
-- post defs: ["y3", "z3", "x2"]

----

define `x2`
define `z3`
define `y3`
use `x2`
-- post defs: ["y3", "z3", "x2"]

----

define `x2`
define `z3`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "z3", "x2"]

----

define `x2`
define `z3`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "z3", "x2"]

----

define `x2`
define `z3`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "z3", "x2"]

----

define `x2`
define `z3`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "z3", "x2"]

----

define `x2`
define `z3`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "z3", "x2"]

----

define `x2`
define `z3`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "z3", "x2"]

----

define `x2`
define `z3`
define `z1`
use `z1`
-- post defs: ["z1", "z3", "x2"]

----

define `x2`
define `z3`
define `z1`
use `z3`
-- post defs: ["z1", "z3", "x2"]

----

define `x2`
define `z3`
define `z1`
use `x2`
-- post defs: ["z1", "z3", "x2"]

----

define `x2`
define `z3`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "z3", "x2"]

----

define `x2`
define `z3`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "z3", "x2"]

----

define `x2`
define `z3`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "z3", "x2"]

----

define `x2`
define `z3`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "z3", "x2"]

----

define `x2`
define `z3`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "z3", "x2"]

----

define `x2`
define `z3`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "z3", "x2"]

----

define `x2`
define `z3`
define `z2`
use `z2`
-- post defs: ["z2", "z3", "x2"]

----

define `x2`
define `z3`
define `z2`
use `z3`
-- post defs: ["z2", "z3", "x2"]

----

define `x2`
define `z3`
define `z2`
use `x2`
-- post defs: ["z2", "z3", "x2"]

----

define `x2`
define `z3`
use `z3`
define `x1`
-- post defs: ["x1", "z3", "x2"]

----

define `x2`
define `z3`
use `z3`
define `x3`
-- post defs: ["x3", "z3", "x2"]

----

define `x2`
define `z3`
use `z3`
define `y1`
-- post defs: ["y1", "z3", "x2"]

----

define `x2`
define `z3`
use `z3`
define `y2`
-- post defs: ["y2", "z3", "x2"]

----

define `x2`
define `z3`
use `z3`
define `y3`
-- post defs: ["y3", "z3", "x2"]

----

define `x2`
define `z3`
use `z3`
define `z1`
-- post defs: ["z1", "z3", "x2"]

----

define `x2`
define `z3`
use `z3`
define `z2`
-- post defs: ["z2", "z3", "x2"]

----

define `x2`
define `z3`
use `z3`
use `z3`
-- post defs: ["z3", "x2"]

----

define `x2`
define `z3`
use `z3`
use `x2`
-- post defs: ["z3", "x2"]

----

define `x2`
define `z3`
use `x2`
define `x1`
-- post defs: ["x1", "z3", "x2"]

----

define `x2`
define `z3`
use `x2`
define `x3`
-- post defs: ["x3", "z3", "x2"]

----

define `x2`
define `z3`
use `x2`
define `y1`
-- post defs: ["y1", "z3", "x2"]

----

define `x2`
define `z3`
use `x2`
define `y2`
-- post defs: ["y2", "z3", "x2"]

----

define `x2`
define `z3`
use `x2`
define `y3`
-- post defs: ["y3", "z3", "x2"]

----

define `x2`
define `z3`
use `x2`
define `z1`
-- post defs: ["z1", "z3", "x2"]

----

define `x2`
define `z3`
use `x2`
define `z2`
-- post defs: ["z2", "z3", "x2"]

----

define `x2`
define `z3`
use `x2`
use `z3`
-- post defs: ["z3", "x2"]

----

define `x2`
define `z3`
use `x2`
use `x2`
-- post defs: ["z3", "x2"]

----

define `x2`
use `x2`
define `x1`
-- post defs: ["x1", "x2"]

----

define `x2`
use `x2`
define `x3`
-- post defs: ["x3", "x2"]

----

define `x2`
use `x2`
define `y1`
-- post defs: ["y1", "x2"]

----

define `x2`
use `x2`
define `y2`
-- post defs: ["y2", "x2"]

----

define `x2`
use `x2`
define `y3`
-- post defs: ["y3", "x2"]

----

define `x2`
use `x2`
define `z1`
-- post defs: ["z1", "x2"]

----

define `x2`
use `x2`
define `z2`
-- post defs: ["z2", "x2"]

----

define `x2`
use `x2`
define `z3`
-- post defs: ["z3", "x2"]

----

define `x2`
use `x2`
use `x2`
-- post defs: ["x2"]

----

define `x2`
use `x2`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "x2"]

----

define `x2`
use `x2`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "x2"]

----

define `x2`
use `x2`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "x2"]

----

define `x2`
use `x2`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "x2"]

----

define `x2`
use `x2`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "x2"]

----

define `x2`
use `x2`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "x2"]

----

define `x2`
use `x2`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "x2"]

----

define `x2`
use `x2`
define `x1`
use `x1`
-- post defs: ["x1", "x2"]

----

define `x2`
use `x2`
define `x1`
use `x2`
-- post defs: ["x1", "x2"]

----

define `x2`
use `x2`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "x2"]

----

define `x2`
use `x2`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "x2"]

----

define `x2`
use `x2`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "x2"]

----

define `x2`
use `x2`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "x2"]

----

define `x2`
use `x2`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "x2"]

----

define `x2`
use `x2`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "x2"]

----

define `x2`
use `x2`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "x2"]

----

define `x2`
use `x2`
define `x3`
use `x3`
-- post defs: ["x3", "x2"]

----

define `x2`
use `x2`
define `x3`
use `x2`
-- post defs: ["x3", "x2"]

----

define `x2`
use `x2`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "x2"]

----

define `x2`
use `x2`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "x2"]

----

define `x2`
use `x2`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "x2"]

----

define `x2`
use `x2`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "x2"]

----

define `x2`
use `x2`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "x2"]

----

define `x2`
use `x2`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "x2"]

----

define `x2`
use `x2`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "x2"]

----

define `x2`
use `x2`
define `y1`
use `y1`
-- post defs: ["y1", "x2"]

----

define `x2`
use `x2`
define `y1`
use `x2`
-- post defs: ["y1", "x2"]

----

define `x2`
use `x2`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "x2"]

----

define `x2`
use `x2`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "x2"]

----

define `x2`
use `x2`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "x2"]

----

define `x2`
use `x2`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "x2"]

----

define `x2`
use `x2`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "x2"]

----

define `x2`
use `x2`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "x2"]

----

define `x2`
use `x2`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "x2"]

----

define `x2`
use `x2`
define `y2`
use `y2`
-- post defs: ["y2", "x2"]

----

define `x2`
use `x2`
define `y2`
use `x2`
-- post defs: ["y2", "x2"]

----

define `x2`
use `x2`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "x2"]

----

define `x2`
use `x2`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "x2"]

----

define `x2`
use `x2`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "x2"]

----

define `x2`
use `x2`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "x2"]

----

define `x2`
use `x2`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "x2"]

----

define `x2`
use `x2`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "x2"]

----

define `x2`
use `x2`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "x2"]

----

define `x2`
use `x2`
define `y3`
use `y3`
-- post defs: ["y3", "x2"]

----

define `x2`
use `x2`
define `y3`
use `x2`
-- post defs: ["y3", "x2"]

----

define `x2`
use `x2`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "x2"]

----

define `x2`
use `x2`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "x2"]

----

define `x2`
use `x2`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "x2"]

----

define `x2`
use `x2`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "x2"]

----

define `x2`
use `x2`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "x2"]

----

define `x2`
use `x2`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "x2"]

----

define `x2`
use `x2`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "x2"]

----

define `x2`
use `x2`
define `z1`
use `z1`
-- post defs: ["z1", "x2"]

----

define `x2`
use `x2`
define `z1`
use `x2`
-- post defs: ["z1", "x2"]

----

define `x2`
use `x2`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "x2"]

----

define `x2`
use `x2`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "x2"]

----

define `x2`
use `x2`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "x2"]

----

define `x2`
use `x2`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "x2"]

----

define `x2`
use `x2`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "x2"]

----

define `x2`
use `x2`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "x2"]

----

define `x2`
use `x2`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "x2"]

----

define `x2`
use `x2`
define `z2`
use `z2`
-- post defs: ["z2", "x2"]

----

define `x2`
use `x2`
define `z2`
use `x2`
-- post defs: ["z2", "x2"]

----

define `x2`
use `x2`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "x2"]

----

define `x2`
use `x2`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "x2"]

----

define `x2`
use `x2`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "x2"]

----

define `x2`
use `x2`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "x2"]

----

define `x2`
use `x2`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "x2"]

----

define `x2`
use `x2`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "x2"]

----

define `x2`
use `x2`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "x2"]

----

define `x2`
use `x2`
define `z3`
use `z3`
-- post defs: ["z3", "x2"]

----

define `x2`
use `x2`
define `z3`
use `x2`
-- post defs: ["z3", "x2"]

----

define `x2`
use `x2`
use `x2`
define `x1`
-- post defs: ["x1", "x2"]

----

define `x2`
use `x2`
use `x2`
define `x3`
-- post defs: ["x3", "x2"]

----

define `x2`
use `x2`
use `x2`
define `y1`
-- post defs: ["y1", "x2"]

----

define `x2`
use `x2`
use `x2`
define `y2`
-- post defs: ["y2", "x2"]

----

define `x2`
use `x2`
use `x2`
define `y3`
-- post defs: ["y3", "x2"]

----

define `x2`
use `x2`
use `x2`
define `z1`
-- post defs: ["z1", "x2"]

----

define `x2`
use `x2`
use `x2`
define `z2`
-- post defs: ["z2", "x2"]

----

define `x2`
use `x2`
use `x2`
define `z3`
-- post defs: ["z3", "x2"]

----

define `x2`
use `x2`
use `x2`
use `x2`
-- post defs: ["x2"]

----

define `x3`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "x3"]

----

define `x3`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "x3"]

----

define `x3`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "x3"]

----

define `x3`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "x3"]

----

define `x3`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "x3"]

----

define `x3`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "x3"]

----

define `x3`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "x3"]

----

define `x3`
define `x1`
use `x1`
-- post defs: ["x1", "x3"]

----

define `x3`
define `x1`
use `x3`
-- post defs: ["x1", "x3"]

----

define `x3`
define `x1`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "x1", "x3"]

----

define `x3`
define `x1`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "x1", "x3"]

----

define `x3`
define `x1`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "x1", "x3"]

----

define `x3`
define `x1`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "x1", "x3"]

----

define `x3`
define `x1`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "x1", "x3"]

----

define `x3`
define `x1`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "x1", "x3"]

----

define `x3`
define `x1`
define `x2`
use `x2`
-- post defs: ["x2", "x1", "x3"]

----

define `x3`
define `x1`
define `x2`
use `x1`
-- post defs: ["x2", "x1", "x3"]

----

define `x3`
define `x1`
define `x2`
use `x3`
-- post defs: ["x2", "x1", "x3"]

----

define `x3`
define `x1`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "x1", "x3"]

----

define `x3`
define `x1`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "x1", "x3"]

----

define `x3`
define `x1`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "x1", "x3"]

----

define `x3`
define `x1`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "x1", "x3"]

----

define `x3`
define `x1`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "x1", "x3"]

----

define `x3`
define `x1`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "x1", "x3"]

----

define `x3`
define `x1`
define `y1`
use `y1`
-- post defs: ["y1", "x1", "x3"]

----

define `x3`
define `x1`
define `y1`
use `x1`
-- post defs: ["y1", "x1", "x3"]

----

define `x3`
define `x1`
define `y1`
use `x3`
-- post defs: ["y1", "x1", "x3"]

----

define `x3`
define `x1`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "x1", "x3"]

----

define `x3`
define `x1`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "x1", "x3"]

----

define `x3`
define `x1`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "x1", "x3"]

----

define `x3`
define `x1`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "x1", "x3"]

----

define `x3`
define `x1`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "x1", "x3"]

----

define `x3`
define `x1`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "x1", "x3"]

----

define `x3`
define `x1`
define `y2`
use `y2`
-- post defs: ["y2", "x1", "x3"]

----

define `x3`
define `x1`
define `y2`
use `x1`
-- post defs: ["y2", "x1", "x3"]

----

define `x3`
define `x1`
define `y2`
use `x3`
-- post defs: ["y2", "x1", "x3"]

----

define `x3`
define `x1`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "x1", "x3"]

----

define `x3`
define `x1`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "x1", "x3"]

----

define `x3`
define `x1`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "x1", "x3"]

----

define `x3`
define `x1`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "x1", "x3"]

----

define `x3`
define `x1`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "x1", "x3"]

----

define `x3`
define `x1`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "x1", "x3"]

----

define `x3`
define `x1`
define `y3`
use `y3`
-- post defs: ["y3", "x1", "x3"]

----

define `x3`
define `x1`
define `y3`
use `x1`
-- post defs: ["y3", "x1", "x3"]

----

define `x3`
define `x1`
define `y3`
use `x3`
-- post defs: ["y3", "x1", "x3"]

----

define `x3`
define `x1`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "x1", "x3"]

----

define `x3`
define `x1`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "x1", "x3"]

----

define `x3`
define `x1`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "x1", "x3"]

----

define `x3`
define `x1`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "x1", "x3"]

----

define `x3`
define `x1`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "x1", "x3"]

----

define `x3`
define `x1`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "x1", "x3"]

----

define `x3`
define `x1`
define `z1`
use `z1`
-- post defs: ["z1", "x1", "x3"]

----

define `x3`
define `x1`
define `z1`
use `x1`
-- post defs: ["z1", "x1", "x3"]

----

define `x3`
define `x1`
define `z1`
use `x3`
-- post defs: ["z1", "x1", "x3"]

----

define `x3`
define `x1`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "x1", "x3"]

----

define `x3`
define `x1`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "x1", "x3"]

----

define `x3`
define `x1`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "x1", "x3"]

----

define `x3`
define `x1`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "x1", "x3"]

----

define `x3`
define `x1`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "x1", "x3"]

----

define `x3`
define `x1`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "x1", "x3"]

----

define `x3`
define `x1`
define `z2`
use `z2`
-- post defs: ["z2", "x1", "x3"]

----

define `x3`
define `x1`
define `z2`
use `x1`
-- post defs: ["z2", "x1", "x3"]

----

define `x3`
define `x1`
define `z2`
use `x3`
-- post defs: ["z2", "x1", "x3"]

----

define `x3`
define `x1`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "x1", "x3"]

----

define `x3`
define `x1`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "x1", "x3"]

----

define `x3`
define `x1`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "x1", "x3"]

----

define `x3`
define `x1`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "x1", "x3"]

----

define `x3`
define `x1`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "x1", "x3"]

----

define `x3`
define `x1`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "x1", "x3"]

----

define `x3`
define `x1`
define `z3`
use `z3`
-- post defs: ["z3", "x1", "x3"]

----

define `x3`
define `x1`
define `z3`
use `x1`
-- post defs: ["z3", "x1", "x3"]

----

define `x3`
define `x1`
define `z3`
use `x3`
-- post defs: ["z3", "x1", "x3"]

----

define `x3`
define `x1`
use `x1`
define `x2`
-- post defs: ["x2", "x1", "x3"]

----

define `x3`
define `x1`
use `x1`
define `y1`
-- post defs: ["y1", "x1", "x3"]

----

define `x3`
define `x1`
use `x1`
define `y2`
-- post defs: ["y2", "x1", "x3"]

----

define `x3`
define `x1`
use `x1`
define `y3`
-- post defs: ["y3", "x1", "x3"]

----

define `x3`
define `x1`
use `x1`
define `z1`
-- post defs: ["z1", "x1", "x3"]

----

define `x3`
define `x1`
use `x1`
define `z2`
-- post defs: ["z2", "x1", "x3"]

----

define `x3`
define `x1`
use `x1`
define `z3`
-- post defs: ["z3", "x1", "x3"]

----

define `x3`
define `x1`
use `x1`
use `x1`
-- post defs: ["x1", "x3"]

----

define `x3`
define `x1`
use `x1`
use `x3`
-- post defs: ["x1", "x3"]

----

define `x3`
define `x1`
use `x3`
define `x2`
-- post defs: ["x2", "x1", "x3"]

----

define `x3`
define `x1`
use `x3`
define `y1`
-- post defs: ["y1", "x1", "x3"]

----

define `x3`
define `x1`
use `x3`
define `y2`
-- post defs: ["y2", "x1", "x3"]

----

define `x3`
define `x1`
use `x3`
define `y3`
-- post defs: ["y3", "x1", "x3"]

----

define `x3`
define `x1`
use `x3`
define `z1`
-- post defs: ["z1", "x1", "x3"]

----

define `x3`
define `x1`
use `x3`
define `z2`
-- post defs: ["z2", "x1", "x3"]

----

define `x3`
define `x1`
use `x3`
define `z3`
-- post defs: ["z3", "x1", "x3"]

----

define `x3`
define `x1`
use `x3`
use `x1`
-- post defs: ["x1", "x3"]

----

define `x3`
define `x1`
use `x3`
use `x3`
-- post defs: ["x1", "x3"]

----

define `x3`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "x3"]

----

define `x3`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "x3"]

----

define `x3`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "x3"]

----

define `x3`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "x3"]

----

define `x3`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "x3"]

----

define `x3`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "x3"]

----

define `x3`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "x3"]

----

define `x3`
define `x2`
use `x2`
-- post defs: ["x2", "x3"]

----

define `x3`
define `x2`
use `x3`
-- post defs: ["x2", "x3"]

----

define `x3`
define `x2`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "x2", "x3"]

----

define `x3`
define `x2`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "x2", "x3"]

----

define `x3`
define `x2`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "x2", "x3"]

----

define `x3`
define `x2`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "x2", "x3"]

----

define `x3`
define `x2`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "x2", "x3"]

----

define `x3`
define `x2`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "x2", "x3"]

----

define `x3`
define `x2`
define `x1`
use `x1`
-- post defs: ["x1", "x2", "x3"]

----

define `x3`
define `x2`
define `x1`
use `x2`
-- post defs: ["x1", "x2", "x3"]

----

define `x3`
define `x2`
define `x1`
use `x3`
-- post defs: ["x1", "x2", "x3"]

----

define `x3`
define `x2`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "x2", "x3"]

----

define `x3`
define `x2`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "x2", "x3"]

----

define `x3`
define `x2`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "x2", "x3"]

----

define `x3`
define `x2`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "x2", "x3"]

----

define `x3`
define `x2`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "x2", "x3"]

----

define `x3`
define `x2`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "x2", "x3"]

----

define `x3`
define `x2`
define `y1`
use `y1`
-- post defs: ["y1", "x2", "x3"]

----

define `x3`
define `x2`
define `y1`
use `x2`
-- post defs: ["y1", "x2", "x3"]

----

define `x3`
define `x2`
define `y1`
use `x3`
-- post defs: ["y1", "x2", "x3"]

----

define `x3`
define `x2`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "x2", "x3"]

----

define `x3`
define `x2`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "x2", "x3"]

----

define `x3`
define `x2`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "x2", "x3"]

----

define `x3`
define `x2`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "x2", "x3"]

----

define `x3`
define `x2`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "x2", "x3"]

----

define `x3`
define `x2`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "x2", "x3"]

----

define `x3`
define `x2`
define `y2`
use `y2`
-- post defs: ["y2", "x2", "x3"]

----

define `x3`
define `x2`
define `y2`
use `x2`
-- post defs: ["y2", "x2", "x3"]

----

define `x3`
define `x2`
define `y2`
use `x3`
-- post defs: ["y2", "x2", "x3"]

----

define `x3`
define `x2`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "x2", "x3"]

----

define `x3`
define `x2`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "x2", "x3"]

----

define `x3`
define `x2`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "x2", "x3"]

----

define `x3`
define `x2`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "x2", "x3"]

----

define `x3`
define `x2`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "x2", "x3"]

----

define `x3`
define `x2`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "x2", "x3"]

----

define `x3`
define `x2`
define `y3`
use `y3`
-- post defs: ["y3", "x2", "x3"]

----

define `x3`
define `x2`
define `y3`
use `x2`
-- post defs: ["y3", "x2", "x3"]

----

define `x3`
define `x2`
define `y3`
use `x3`
-- post defs: ["y3", "x2", "x3"]

----

define `x3`
define `x2`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "x2", "x3"]

----

define `x3`
define `x2`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "x2", "x3"]

----

define `x3`
define `x2`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "x2", "x3"]

----

define `x3`
define `x2`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "x2", "x3"]

----

define `x3`
define `x2`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "x2", "x3"]

----

define `x3`
define `x2`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "x2", "x3"]

----

define `x3`
define `x2`
define `z1`
use `z1`
-- post defs: ["z1", "x2", "x3"]

----

define `x3`
define `x2`
define `z1`
use `x2`
-- post defs: ["z1", "x2", "x3"]

----

define `x3`
define `x2`
define `z1`
use `x3`
-- post defs: ["z1", "x2", "x3"]

----

define `x3`
define `x2`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "x2", "x3"]

----

define `x3`
define `x2`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "x2", "x3"]

----

define `x3`
define `x2`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "x2", "x3"]

----

define `x3`
define `x2`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "x2", "x3"]

----

define `x3`
define `x2`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "x2", "x3"]

----

define `x3`
define `x2`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "x2", "x3"]

----

define `x3`
define `x2`
define `z2`
use `z2`
-- post defs: ["z2", "x2", "x3"]

----

define `x3`
define `x2`
define `z2`
use `x2`
-- post defs: ["z2", "x2", "x3"]

----

define `x3`
define `x2`
define `z2`
use `x3`
-- post defs: ["z2", "x2", "x3"]

----

define `x3`
define `x2`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "x2", "x3"]

----

define `x3`
define `x2`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "x2", "x3"]

----

define `x3`
define `x2`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "x2", "x3"]

----

define `x3`
define `x2`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "x2", "x3"]

----

define `x3`
define `x2`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "x2", "x3"]

----

define `x3`
define `x2`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "x2", "x3"]

----

define `x3`
define `x2`
define `z3`
use `z3`
-- post defs: ["z3", "x2", "x3"]

----

define `x3`
define `x2`
define `z3`
use `x2`
-- post defs: ["z3", "x2", "x3"]

----

define `x3`
define `x2`
define `z3`
use `x3`
-- post defs: ["z3", "x2", "x3"]

----

define `x3`
define `x2`
use `x2`
define `x1`
-- post defs: ["x1", "x2", "x3"]

----

define `x3`
define `x2`
use `x2`
define `y1`
-- post defs: ["y1", "x2", "x3"]

----

define `x3`
define `x2`
use `x2`
define `y2`
-- post defs: ["y2", "x2", "x3"]

----

define `x3`
define `x2`
use `x2`
define `y3`
-- post defs: ["y3", "x2", "x3"]

----

define `x3`
define `x2`
use `x2`
define `z1`
-- post defs: ["z1", "x2", "x3"]

----

define `x3`
define `x2`
use `x2`
define `z2`
-- post defs: ["z2", "x2", "x3"]

----

define `x3`
define `x2`
use `x2`
define `z3`
-- post defs: ["z3", "x2", "x3"]

----

define `x3`
define `x2`
use `x2`
use `x2`
-- post defs: ["x2", "x3"]

----

define `x3`
define `x2`
use `x2`
use `x3`
-- post defs: ["x2", "x3"]

----

define `x3`
define `x2`
use `x3`
define `x1`
-- post defs: ["x1", "x2", "x3"]

----

define `x3`
define `x2`
use `x3`
define `y1`
-- post defs: ["y1", "x2", "x3"]

----

define `x3`
define `x2`
use `x3`
define `y2`
-- post defs: ["y2", "x2", "x3"]

----

define `x3`
define `x2`
use `x3`
define `y3`
-- post defs: ["y3", "x2", "x3"]

----

define `x3`
define `x2`
use `x3`
define `z1`
-- post defs: ["z1", "x2", "x3"]

----

define `x3`
define `x2`
use `x3`
define `z2`
-- post defs: ["z2", "x2", "x3"]

----

define `x3`
define `x2`
use `x3`
define `z3`
-- post defs: ["z3", "x2", "x3"]

----

define `x3`
define `x2`
use `x3`
use `x2`
-- post defs: ["x2", "x3"]

----

define `x3`
define `x2`
use `x3`
use `x3`
-- post defs: ["x2", "x3"]

----

define `x3`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "x3"]

----

define `x3`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "x3"]

----

define `x3`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "x3"]

----

define `x3`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "x3"]

----

define `x3`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "x3"]

----

define `x3`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "x3"]

----

define `x3`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "x3"]

----

define `x3`
define `y1`
use `y1`
-- post defs: ["y1", "x3"]

----

define `x3`
define `y1`
use `x3`
-- post defs: ["y1", "x3"]

----

define `x3`
define `y1`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "y1", "x3"]

----

define `x3`
define `y1`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "y1", "x3"]

----

define `x3`
define `y1`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "y1", "x3"]

----

define `x3`
define `y1`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "y1", "x3"]

----

define `x3`
define `y1`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "y1", "x3"]

----

define `x3`
define `y1`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "y1", "x3"]

----

define `x3`
define `y1`
define `x1`
use `x1`
-- post defs: ["x1", "y1", "x3"]

----

define `x3`
define `y1`
define `x1`
use `y1`
-- post defs: ["x1", "y1", "x3"]

----

define `x3`
define `y1`
define `x1`
use `x3`
-- post defs: ["x1", "y1", "x3"]

----

define `x3`
define `y1`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "y1", "x3"]

----

define `x3`
define `y1`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "y1", "x3"]

----

define `x3`
define `y1`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "y1", "x3"]

----

define `x3`
define `y1`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "y1", "x3"]

----

define `x3`
define `y1`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "y1", "x3"]

----

define `x3`
define `y1`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "y1", "x3"]

----

define `x3`
define `y1`
define `x2`
use `x2`
-- post defs: ["x2", "y1", "x3"]

----

define `x3`
define `y1`
define `x2`
use `y1`
-- post defs: ["x2", "y1", "x3"]

----

define `x3`
define `y1`
define `x2`
use `x3`
-- post defs: ["x2", "y1", "x3"]

----

define `x3`
define `y1`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "y1", "x3"]

----

define `x3`
define `y1`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "y1", "x3"]

----

define `x3`
define `y1`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "y1", "x3"]

----

define `x3`
define `y1`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "y1", "x3"]

----

define `x3`
define `y1`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "y1", "x3"]

----

define `x3`
define `y1`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "y1", "x3"]

----

define `x3`
define `y1`
define `y2`
use `y2`
-- post defs: ["y2", "y1", "x3"]

----

define `x3`
define `y1`
define `y2`
use `y1`
-- post defs: ["y2", "y1", "x3"]

----

define `x3`
define `y1`
define `y2`
use `x3`
-- post defs: ["y2", "y1", "x3"]

----

define `x3`
define `y1`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "y1", "x3"]

----

define `x3`
define `y1`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "y1", "x3"]

----

define `x3`
define `y1`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "y1", "x3"]

----

define `x3`
define `y1`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "y1", "x3"]

----

define `x3`
define `y1`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "y1", "x3"]

----

define `x3`
define `y1`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "y1", "x3"]

----

define `x3`
define `y1`
define `y3`
use `y3`
-- post defs: ["y3", "y1", "x3"]

----

define `x3`
define `y1`
define `y3`
use `y1`
-- post defs: ["y3", "y1", "x3"]

----

define `x3`
define `y1`
define `y3`
use `x3`
-- post defs: ["y3", "y1", "x3"]

----

define `x3`
define `y1`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "y1", "x3"]

----

define `x3`
define `y1`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "y1", "x3"]

----

define `x3`
define `y1`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "y1", "x3"]

----

define `x3`
define `y1`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "y1", "x3"]

----

define `x3`
define `y1`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "y1", "x3"]

----

define `x3`
define `y1`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "y1", "x3"]

----

define `x3`
define `y1`
define `z1`
use `z1`
-- post defs: ["z1", "y1", "x3"]

----

define `x3`
define `y1`
define `z1`
use `y1`
-- post defs: ["z1", "y1", "x3"]

----

define `x3`
define `y1`
define `z1`
use `x3`
-- post defs: ["z1", "y1", "x3"]

----

define `x3`
define `y1`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "y1", "x3"]

----

define `x3`
define `y1`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "y1", "x3"]

----

define `x3`
define `y1`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "y1", "x3"]

----

define `x3`
define `y1`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "y1", "x3"]

----

define `x3`
define `y1`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "y1", "x3"]

----

define `x3`
define `y1`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "y1", "x3"]

----

define `x3`
define `y1`
define `z2`
use `z2`
-- post defs: ["z2", "y1", "x3"]

----

define `x3`
define `y1`
define `z2`
use `y1`
-- post defs: ["z2", "y1", "x3"]

----

define `x3`
define `y1`
define `z2`
use `x3`
-- post defs: ["z2", "y1", "x3"]

----

define `x3`
define `y1`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "y1", "x3"]

----

define `x3`
define `y1`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "y1", "x3"]

----

define `x3`
define `y1`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "y1", "x3"]

----

define `x3`
define `y1`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "y1", "x3"]

----

define `x3`
define `y1`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "y1", "x3"]

----

define `x3`
define `y1`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "y1", "x3"]

----

define `x3`
define `y1`
define `z3`
use `z3`
-- post defs: ["z3", "y1", "x3"]

----

define `x3`
define `y1`
define `z3`
use `y1`
-- post defs: ["z3", "y1", "x3"]

----

define `x3`
define `y1`
define `z3`
use `x3`
-- post defs: ["z3", "y1", "x3"]

----

define `x3`
define `y1`
use `y1`
define `x1`
-- post defs: ["x1", "y1", "x3"]

----

define `x3`
define `y1`
use `y1`
define `x2`
-- post defs: ["x2", "y1", "x3"]

----

define `x3`
define `y1`
use `y1`
define `y2`
-- post defs: ["y2", "y1", "x3"]

----

define `x3`
define `y1`
use `y1`
define `y3`
-- post defs: ["y3", "y1", "x3"]

----

define `x3`
define `y1`
use `y1`
define `z1`
-- post defs: ["z1", "y1", "x3"]

----

define `x3`
define `y1`
use `y1`
define `z2`
-- post defs: ["z2", "y1", "x3"]

----

define `x3`
define `y1`
use `y1`
define `z3`
-- post defs: ["z3", "y1", "x3"]

----

define `x3`
define `y1`
use `y1`
use `y1`
-- post defs: ["y1", "x3"]

----

define `x3`
define `y1`
use `y1`
use `x3`
-- post defs: ["y1", "x3"]

----

define `x3`
define `y1`
use `x3`
define `x1`
-- post defs: ["x1", "y1", "x3"]

----

define `x3`
define `y1`
use `x3`
define `x2`
-- post defs: ["x2", "y1", "x3"]

----

define `x3`
define `y1`
use `x3`
define `y2`
-- post defs: ["y2", "y1", "x3"]

----

define `x3`
define `y1`
use `x3`
define `y3`
-- post defs: ["y3", "y1", "x3"]

----

define `x3`
define `y1`
use `x3`
define `z1`
-- post defs: ["z1", "y1", "x3"]

----

define `x3`
define `y1`
use `x3`
define `z2`
-- post defs: ["z2", "y1", "x3"]

----

define `x3`
define `y1`
use `x3`
define `z3`
-- post defs: ["z3", "y1", "x3"]

----

define `x3`
define `y1`
use `x3`
use `y1`
-- post defs: ["y1", "x3"]

----

define `x3`
define `y1`
use `x3`
use `x3`
-- post defs: ["y1", "x3"]

----

define `x3`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "x3"]

----

define `x3`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "x3"]

----

define `x3`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "x3"]

----

define `x3`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "x3"]

----

define `x3`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "x3"]

----

define `x3`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "x3"]

----

define `x3`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "x3"]

----

define `x3`
define `y2`
use `y2`
-- post defs: ["y2", "x3"]

----

define `x3`
define `y2`
use `x3`
-- post defs: ["y2", "x3"]

----

define `x3`
define `y2`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "y2", "x3"]

----

define `x3`
define `y2`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "y2", "x3"]

----

define `x3`
define `y2`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "y2", "x3"]

----

define `x3`
define `y2`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "y2", "x3"]

----

define `x3`
define `y2`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "y2", "x3"]

----

define `x3`
define `y2`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "y2", "x3"]

----

define `x3`
define `y2`
define `x1`
use `x1`
-- post defs: ["x1", "y2", "x3"]

----

define `x3`
define `y2`
define `x1`
use `y2`
-- post defs: ["x1", "y2", "x3"]

----

define `x3`
define `y2`
define `x1`
use `x3`
-- post defs: ["x1", "y2", "x3"]

----

define `x3`
define `y2`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "y2", "x3"]

----

define `x3`
define `y2`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "y2", "x3"]

----

define `x3`
define `y2`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "y2", "x3"]

----

define `x3`
define `y2`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "y2", "x3"]

----

define `x3`
define `y2`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "y2", "x3"]

----

define `x3`
define `y2`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "y2", "x3"]

----

define `x3`
define `y2`
define `x2`
use `x2`
-- post defs: ["x2", "y2", "x3"]

----

define `x3`
define `y2`
define `x2`
use `y2`
-- post defs: ["x2", "y2", "x3"]

----

define `x3`
define `y2`
define `x2`
use `x3`
-- post defs: ["x2", "y2", "x3"]

----

define `x3`
define `y2`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "y2", "x3"]

----

define `x3`
define `y2`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "y2", "x3"]

----

define `x3`
define `y2`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "y2", "x3"]

----

define `x3`
define `y2`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "y2", "x3"]

----

define `x3`
define `y2`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "y2", "x3"]

----

define `x3`
define `y2`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "y2", "x3"]

----

define `x3`
define `y2`
define `y1`
use `y1`
-- post defs: ["y1", "y2", "x3"]

----

define `x3`
define `y2`
define `y1`
use `y2`
-- post defs: ["y1", "y2", "x3"]

----

define `x3`
define `y2`
define `y1`
use `x3`
-- post defs: ["y1", "y2", "x3"]

----

define `x3`
define `y2`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "y2", "x3"]

----

define `x3`
define `y2`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "y2", "x3"]

----

define `x3`
define `y2`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "y2", "x3"]

----

define `x3`
define `y2`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "y2", "x3"]

----

define `x3`
define `y2`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "y2", "x3"]

----

define `x3`
define `y2`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "y2", "x3"]

----

define `x3`
define `y2`
define `y3`
use `y3`
-- post defs: ["y3", "y2", "x3"]

----

define `x3`
define `y2`
define `y3`
use `y2`
-- post defs: ["y3", "y2", "x3"]

----

define `x3`
define `y2`
define `y3`
use `x3`
-- post defs: ["y3", "y2", "x3"]

----

define `x3`
define `y2`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "y2", "x3"]

----

define `x3`
define `y2`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "y2", "x3"]

----

define `x3`
define `y2`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "y2", "x3"]

----

define `x3`
define `y2`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "y2", "x3"]

----

define `x3`
define `y2`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "y2", "x3"]

----

define `x3`
define `y2`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "y2", "x3"]

----

define `x3`
define `y2`
define `z1`
use `z1`
-- post defs: ["z1", "y2", "x3"]

----

define `x3`
define `y2`
define `z1`
use `y2`
-- post defs: ["z1", "y2", "x3"]

----

define `x3`
define `y2`
define `z1`
use `x3`
-- post defs: ["z1", "y2", "x3"]

----

define `x3`
define `y2`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "y2", "x3"]

----

define `x3`
define `y2`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "y2", "x3"]

----

define `x3`
define `y2`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "y2", "x3"]

----

define `x3`
define `y2`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "y2", "x3"]

----

define `x3`
define `y2`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "y2", "x3"]

----

define `x3`
define `y2`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "y2", "x3"]

----

define `x3`
define `y2`
define `z2`
use `z2`
-- post defs: ["z2", "y2", "x3"]

----

define `x3`
define `y2`
define `z2`
use `y2`
-- post defs: ["z2", "y2", "x3"]

----

define `x3`
define `y2`
define `z2`
use `x3`
-- post defs: ["z2", "y2", "x3"]

----

define `x3`
define `y2`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "y2", "x3"]

----

define `x3`
define `y2`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "y2", "x3"]

----

define `x3`
define `y2`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "y2", "x3"]

----

define `x3`
define `y2`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "y2", "x3"]

----

define `x3`
define `y2`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "y2", "x3"]

----

define `x3`
define `y2`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "y2", "x3"]

----

define `x3`
define `y2`
define `z3`
use `z3`
-- post defs: ["z3", "y2", "x3"]

----

define `x3`
define `y2`
define `z3`
use `y2`
-- post defs: ["z3", "y2", "x3"]

----

define `x3`
define `y2`
define `z3`
use `x3`
-- post defs: ["z3", "y2", "x3"]

----

define `x3`
define `y2`
use `y2`
define `x1`
-- post defs: ["x1", "y2", "x3"]

----

define `x3`
define `y2`
use `y2`
define `x2`
-- post defs: ["x2", "y2", "x3"]

----

define `x3`
define `y2`
use `y2`
define `y1`
-- post defs: ["y1", "y2", "x3"]

----

define `x3`
define `y2`
use `y2`
define `y3`
-- post defs: ["y3", "y2", "x3"]

----

define `x3`
define `y2`
use `y2`
define `z1`
-- post defs: ["z1", "y2", "x3"]

----

define `x3`
define `y2`
use `y2`
define `z2`
-- post defs: ["z2", "y2", "x3"]

----

define `x3`
define `y2`
use `y2`
define `z3`
-- post defs: ["z3", "y2", "x3"]

----

define `x3`
define `y2`
use `y2`
use `y2`
-- post defs: ["y2", "x3"]

----

define `x3`
define `y2`
use `y2`
use `x3`
-- post defs: ["y2", "x3"]

----

define `x3`
define `y2`
use `x3`
define `x1`
-- post defs: ["x1", "y2", "x3"]

----

define `x3`
define `y2`
use `x3`
define `x2`
-- post defs: ["x2", "y2", "x3"]

----

define `x3`
define `y2`
use `x3`
define `y1`
-- post defs: ["y1", "y2", "x3"]

----

define `x3`
define `y2`
use `x3`
define `y3`
-- post defs: ["y3", "y2", "x3"]

----

define `x3`
define `y2`
use `x3`
define `z1`
-- post defs: ["z1", "y2", "x3"]

----

define `x3`
define `y2`
use `x3`
define `z2`
-- post defs: ["z2", "y2", "x3"]

----

define `x3`
define `y2`
use `x3`
define `z3`
-- post defs: ["z3", "y2", "x3"]

----

define `x3`
define `y2`
use `x3`
use `y2`
-- post defs: ["y2", "x3"]

----

define `x3`
define `y2`
use `x3`
use `x3`
-- post defs: ["y2", "x3"]

----

define `x3`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "x3"]

----

define `x3`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "x3"]

----

define `x3`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "x3"]

----

define `x3`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "x3"]

----

define `x3`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "x3"]

----

define `x3`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "x3"]

----

define `x3`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "x3"]

----

define `x3`
define `y3`
use `y3`
-- post defs: ["y3", "x3"]

----

define `x3`
define `y3`
use `x3`
-- post defs: ["y3", "x3"]

----

define `x3`
define `y3`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "y3", "x3"]

----

define `x3`
define `y3`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "y3", "x3"]

----

define `x3`
define `y3`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "y3", "x3"]

----

define `x3`
define `y3`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "y3", "x3"]

----

define `x3`
define `y3`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "y3", "x3"]

----

define `x3`
define `y3`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "y3", "x3"]

----

define `x3`
define `y3`
define `x1`
use `x1`
-- post defs: ["x1", "y3", "x3"]

----

define `x3`
define `y3`
define `x1`
use `y3`
-- post defs: ["x1", "y3", "x3"]

----

define `x3`
define `y3`
define `x1`
use `x3`
-- post defs: ["x1", "y3", "x3"]

----

define `x3`
define `y3`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "y3", "x3"]

----

define `x3`
define `y3`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "y3", "x3"]

----

define `x3`
define `y3`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "y3", "x3"]

----

define `x3`
define `y3`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "y3", "x3"]

----

define `x3`
define `y3`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "y3", "x3"]

----

define `x3`
define `y3`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "y3", "x3"]

----

define `x3`
define `y3`
define `x2`
use `x2`
-- post defs: ["x2", "y3", "x3"]

----

define `x3`
define `y3`
define `x2`
use `y3`
-- post defs: ["x2", "y3", "x3"]

----

define `x3`
define `y3`
define `x2`
use `x3`
-- post defs: ["x2", "y3", "x3"]

----

define `x3`
define `y3`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "y3", "x3"]

----

define `x3`
define `y3`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "y3", "x3"]

----

define `x3`
define `y3`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "y3", "x3"]

----

define `x3`
define `y3`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "y3", "x3"]

----

define `x3`
define `y3`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "y3", "x3"]

----

define `x3`
define `y3`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "y3", "x3"]

----

define `x3`
define `y3`
define `y1`
use `y1`
-- post defs: ["y1", "y3", "x3"]

----

define `x3`
define `y3`
define `y1`
use `y3`
-- post defs: ["y1", "y3", "x3"]

----

define `x3`
define `y3`
define `y1`
use `x3`
-- post defs: ["y1", "y3", "x3"]

----

define `x3`
define `y3`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "y3", "x3"]

----

define `x3`
define `y3`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "y3", "x3"]

----

define `x3`
define `y3`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "y3", "x3"]

----

define `x3`
define `y3`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "y3", "x3"]

----

define `x3`
define `y3`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "y3", "x3"]

----

define `x3`
define `y3`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "y3", "x3"]

----

define `x3`
define `y3`
define `y2`
use `y2`
-- post defs: ["y2", "y3", "x3"]

----

define `x3`
define `y3`
define `y2`
use `y3`
-- post defs: ["y2", "y3", "x3"]

----

define `x3`
define `y3`
define `y2`
use `x3`
-- post defs: ["y2", "y3", "x3"]

----

define `x3`
define `y3`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "y3", "x3"]

----

define `x3`
define `y3`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "y3", "x3"]

----

define `x3`
define `y3`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "y3", "x3"]

----

define `x3`
define `y3`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "y3", "x3"]

----

define `x3`
define `y3`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "y3", "x3"]

----

define `x3`
define `y3`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "y3", "x3"]

----

define `x3`
define `y3`
define `z1`
use `z1`
-- post defs: ["z1", "y3", "x3"]

----

define `x3`
define `y3`
define `z1`
use `y3`
-- post defs: ["z1", "y3", "x3"]

----

define `x3`
define `y3`
define `z1`
use `x3`
-- post defs: ["z1", "y3", "x3"]

----

define `x3`
define `y3`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "y3", "x3"]

----

define `x3`
define `y3`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "y3", "x3"]

----

define `x3`
define `y3`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "y3", "x3"]

----

define `x3`
define `y3`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "y3", "x3"]

----

define `x3`
define `y3`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "y3", "x3"]

----

define `x3`
define `y3`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "y3", "x3"]

----

define `x3`
define `y3`
define `z2`
use `z2`
-- post defs: ["z2", "y3", "x3"]

----

define `x3`
define `y3`
define `z2`
use `y3`
-- post defs: ["z2", "y3", "x3"]

----

define `x3`
define `y3`
define `z2`
use `x3`
-- post defs: ["z2", "y3", "x3"]

----

define `x3`
define `y3`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "y3", "x3"]

----

define `x3`
define `y3`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "y3", "x3"]

----

define `x3`
define `y3`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "y3", "x3"]

----

define `x3`
define `y3`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "y3", "x3"]

----

define `x3`
define `y3`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "y3", "x3"]

----

define `x3`
define `y3`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "y3", "x3"]

----

define `x3`
define `y3`
define `z3`
use `z3`
-- post defs: ["z3", "y3", "x3"]

----

define `x3`
define `y3`
define `z3`
use `y3`
-- post defs: ["z3", "y3", "x3"]

----

define `x3`
define `y3`
define `z3`
use `x3`
-- post defs: ["z3", "y3", "x3"]

----

define `x3`
define `y3`
use `y3`
define `x1`
-- post defs: ["x1", "y3", "x3"]

----

define `x3`
define `y3`
use `y3`
define `x2`
-- post defs: ["x2", "y3", "x3"]

----

define `x3`
define `y3`
use `y3`
define `y1`
-- post defs: ["y1", "y3", "x3"]

----

define `x3`
define `y3`
use `y3`
define `y2`
-- post defs: ["y2", "y3", "x3"]

----

define `x3`
define `y3`
use `y3`
define `z1`
-- post defs: ["z1", "y3", "x3"]

----

define `x3`
define `y3`
use `y3`
define `z2`
-- post defs: ["z2", "y3", "x3"]

----

define `x3`
define `y3`
use `y3`
define `z3`
-- post defs: ["z3", "y3", "x3"]

----

define `x3`
define `y3`
use `y3`
use `y3`
-- post defs: ["y3", "x3"]

----

define `x3`
define `y3`
use `y3`
use `x3`
-- post defs: ["y3", "x3"]

----

define `x3`
define `y3`
use `x3`
define `x1`
-- post defs: ["x1", "y3", "x3"]

----

define `x3`
define `y3`
use `x3`
define `x2`
-- post defs: ["x2", "y3", "x3"]

----

define `x3`
define `y3`
use `x3`
define `y1`
-- post defs: ["y1", "y3", "x3"]

----

define `x3`
define `y3`
use `x3`
define `y2`
-- post defs: ["y2", "y3", "x3"]

----

define `x3`
define `y3`
use `x3`
define `z1`
-- post defs: ["z1", "y3", "x3"]

----

define `x3`
define `y3`
use `x3`
define `z2`
-- post defs: ["z2", "y3", "x3"]

----

define `x3`
define `y3`
use `x3`
define `z3`
-- post defs: ["z3", "y3", "x3"]

----

define `x3`
define `y3`
use `x3`
use `y3`
-- post defs: ["y3", "x3"]

----

define `x3`
define `y3`
use `x3`
use `x3`
-- post defs: ["y3", "x3"]

----

define `x3`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "x3"]

----

define `x3`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "x3"]

----

define `x3`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "x3"]

----

define `x3`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "x3"]

----

define `x3`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "x3"]

----

define `x3`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "x3"]

----

define `x3`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "x3"]

----

define `x3`
define `z1`
use `z1`
-- post defs: ["z1", "x3"]

----

define `x3`
define `z1`
use `x3`
-- post defs: ["z1", "x3"]

----

define `x3`
define `z1`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "z1", "x3"]

----

define `x3`
define `z1`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "z1", "x3"]

----

define `x3`
define `z1`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "z1", "x3"]

----

define `x3`
define `z1`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "z1", "x3"]

----

define `x3`
define `z1`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "z1", "x3"]

----

define `x3`
define `z1`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "z1", "x3"]

----

define `x3`
define `z1`
define `x1`
use `x1`
-- post defs: ["x1", "z1", "x3"]

----

define `x3`
define `z1`
define `x1`
use `z1`
-- post defs: ["x1", "z1", "x3"]

----

define `x3`
define `z1`
define `x1`
use `x3`
-- post defs: ["x1", "z1", "x3"]

----

define `x3`
define `z1`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "z1", "x3"]

----

define `x3`
define `z1`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "z1", "x3"]

----

define `x3`
define `z1`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "z1", "x3"]

----

define `x3`
define `z1`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "z1", "x3"]

----

define `x3`
define `z1`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "z1", "x3"]

----

define `x3`
define `z1`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "z1", "x3"]

----

define `x3`
define `z1`
define `x2`
use `x2`
-- post defs: ["x2", "z1", "x3"]

----

define `x3`
define `z1`
define `x2`
use `z1`
-- post defs: ["x2", "z1", "x3"]

----

define `x3`
define `z1`
define `x2`
use `x3`
-- post defs: ["x2", "z1", "x3"]

----

define `x3`
define `z1`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "z1", "x3"]

----

define `x3`
define `z1`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "z1", "x3"]

----

define `x3`
define `z1`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "z1", "x3"]

----

define `x3`
define `z1`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "z1", "x3"]

----

define `x3`
define `z1`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "z1", "x3"]

----

define `x3`
define `z1`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "z1", "x3"]

----

define `x3`
define `z1`
define `y1`
use `y1`
-- post defs: ["y1", "z1", "x3"]

----

define `x3`
define `z1`
define `y1`
use `z1`
-- post defs: ["y1", "z1", "x3"]

----

define `x3`
define `z1`
define `y1`
use `x3`
-- post defs: ["y1", "z1", "x3"]

----

define `x3`
define `z1`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "z1", "x3"]

----

define `x3`
define `z1`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "z1", "x3"]

----

define `x3`
define `z1`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "z1", "x3"]

----

define `x3`
define `z1`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "z1", "x3"]

----

define `x3`
define `z1`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "z1", "x3"]

----

define `x3`
define `z1`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "z1", "x3"]

----

define `x3`
define `z1`
define `y2`
use `y2`
-- post defs: ["y2", "z1", "x3"]

----

define `x3`
define `z1`
define `y2`
use `z1`
-- post defs: ["y2", "z1", "x3"]

----

define `x3`
define `z1`
define `y2`
use `x3`
-- post defs: ["y2", "z1", "x3"]

----

define `x3`
define `z1`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "z1", "x3"]

----

define `x3`
define `z1`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "z1", "x3"]

----

define `x3`
define `z1`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "z1", "x3"]

----

define `x3`
define `z1`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "z1", "x3"]

----

define `x3`
define `z1`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "z1", "x3"]

----

define `x3`
define `z1`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "z1", "x3"]

----

define `x3`
define `z1`
define `y3`
use `y3`
-- post defs: ["y3", "z1", "x3"]

----

define `x3`
define `z1`
define `y3`
use `z1`
-- post defs: ["y3", "z1", "x3"]

----

define `x3`
define `z1`
define `y3`
use `x3`
-- post defs: ["y3", "z1", "x3"]

----

define `x3`
define `z1`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "z1", "x3"]

----

define `x3`
define `z1`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "z1", "x3"]

----

define `x3`
define `z1`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "z1", "x3"]

----

define `x3`
define `z1`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "z1", "x3"]

----

define `x3`
define `z1`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "z1", "x3"]

----

define `x3`
define `z1`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "z1", "x3"]

----

define `x3`
define `z1`
define `z2`
use `z2`
-- post defs: ["z2", "z1", "x3"]

----

define `x3`
define `z1`
define `z2`
use `z1`
-- post defs: ["z2", "z1", "x3"]

----

define `x3`
define `z1`
define `z2`
use `x3`
-- post defs: ["z2", "z1", "x3"]

----

define `x3`
define `z1`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "z1", "x3"]

----

define `x3`
define `z1`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "z1", "x3"]

----

define `x3`
define `z1`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "z1", "x3"]

----

define `x3`
define `z1`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "z1", "x3"]

----

define `x3`
define `z1`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "z1", "x3"]

----

define `x3`
define `z1`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "z1", "x3"]

----

define `x3`
define `z1`
define `z3`
use `z3`
-- post defs: ["z3", "z1", "x3"]

----

define `x3`
define `z1`
define `z3`
use `z1`
-- post defs: ["z3", "z1", "x3"]

----

define `x3`
define `z1`
define `z3`
use `x3`
-- post defs: ["z3", "z1", "x3"]

----

define `x3`
define `z1`
use `z1`
define `x1`
-- post defs: ["x1", "z1", "x3"]

----

define `x3`
define `z1`
use `z1`
define `x2`
-- post defs: ["x2", "z1", "x3"]

----

define `x3`
define `z1`
use `z1`
define `y1`
-- post defs: ["y1", "z1", "x3"]

----

define `x3`
define `z1`
use `z1`
define `y2`
-- post defs: ["y2", "z1", "x3"]

----

define `x3`
define `z1`
use `z1`
define `y3`
-- post defs: ["y3", "z1", "x3"]

----

define `x3`
define `z1`
use `z1`
define `z2`
-- post defs: ["z2", "z1", "x3"]

----

define `x3`
define `z1`
use `z1`
define `z3`
-- post defs: ["z3", "z1", "x3"]

----

define `x3`
define `z1`
use `z1`
use `z1`
-- post defs: ["z1", "x3"]

----

define `x3`
define `z1`
use `z1`
use `x3`
-- post defs: ["z1", "x3"]

----

define `x3`
define `z1`
use `x3`
define `x1`
-- post defs: ["x1", "z1", "x3"]

----

define `x3`
define `z1`
use `x3`
define `x2`
-- post defs: ["x2", "z1", "x3"]

----

define `x3`
define `z1`
use `x3`
define `y1`
-- post defs: ["y1", "z1", "x3"]

----

define `x3`
define `z1`
use `x3`
define `y2`
-- post defs: ["y2", "z1", "x3"]

----

define `x3`
define `z1`
use `x3`
define `y3`
-- post defs: ["y3", "z1", "x3"]

----

define `x3`
define `z1`
use `x3`
define `z2`
-- post defs: ["z2", "z1", "x3"]

----

define `x3`
define `z1`
use `x3`
define `z3`
-- post defs: ["z3", "z1", "x3"]

----

define `x3`
define `z1`
use `x3`
use `z1`
-- post defs: ["z1", "x3"]

----

define `x3`
define `z1`
use `x3`
use `x3`
-- post defs: ["z1", "x3"]

----

define `x3`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "x3"]

----

define `x3`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "x3"]

----

define `x3`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "x3"]

----

define `x3`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "x3"]

----

define `x3`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "x3"]

----

define `x3`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "x3"]

----

define `x3`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "x3"]

----

define `x3`
define `z2`
use `z2`
-- post defs: ["z2", "x3"]

----

define `x3`
define `z2`
use `x3`
-- post defs: ["z2", "x3"]

----

define `x3`
define `z2`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "z2", "x3"]

----

define `x3`
define `z2`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "z2", "x3"]

----

define `x3`
define `z2`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "z2", "x3"]

----

define `x3`
define `z2`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "z2", "x3"]

----

define `x3`
define `z2`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "z2", "x3"]

----

define `x3`
define `z2`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "z2", "x3"]

----

define `x3`
define `z2`
define `x1`
use `x1`
-- post defs: ["x1", "z2", "x3"]

----

define `x3`
define `z2`
define `x1`
use `z2`
-- post defs: ["x1", "z2", "x3"]

----

define `x3`
define `z2`
define `x1`
use `x3`
-- post defs: ["x1", "z2", "x3"]

----

define `x3`
define `z2`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "z2", "x3"]

----

define `x3`
define `z2`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "z2", "x3"]

----

define `x3`
define `z2`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "z2", "x3"]

----

define `x3`
define `z2`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "z2", "x3"]

----

define `x3`
define `z2`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "z2", "x3"]

----

define `x3`
define `z2`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "z2", "x3"]

----

define `x3`
define `z2`
define `x2`
use `x2`
-- post defs: ["x2", "z2", "x3"]

----

define `x3`
define `z2`
define `x2`
use `z2`
-- post defs: ["x2", "z2", "x3"]

----

define `x3`
define `z2`
define `x2`
use `x3`
-- post defs: ["x2", "z2", "x3"]

----

define `x3`
define `z2`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "z2", "x3"]

----

define `x3`
define `z2`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "z2", "x3"]

----

define `x3`
define `z2`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "z2", "x3"]

----

define `x3`
define `z2`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "z2", "x3"]

----

define `x3`
define `z2`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "z2", "x3"]

----

define `x3`
define `z2`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "z2", "x3"]

----

define `x3`
define `z2`
define `y1`
use `y1`
-- post defs: ["y1", "z2", "x3"]

----

define `x3`
define `z2`
define `y1`
use `z2`
-- post defs: ["y1", "z2", "x3"]

----

define `x3`
define `z2`
define `y1`
use `x3`
-- post defs: ["y1", "z2", "x3"]

----

define `x3`
define `z2`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "z2", "x3"]

----

define `x3`
define `z2`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "z2", "x3"]

----

define `x3`
define `z2`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "z2", "x3"]

----

define `x3`
define `z2`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "z2", "x3"]

----

define `x3`
define `z2`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "z2", "x3"]

----

define `x3`
define `z2`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "z2", "x3"]

----

define `x3`
define `z2`
define `y2`
use `y2`
-- post defs: ["y2", "z2", "x3"]

----

define `x3`
define `z2`
define `y2`
use `z2`
-- post defs: ["y2", "z2", "x3"]

----

define `x3`
define `z2`
define `y2`
use `x3`
-- post defs: ["y2", "z2", "x3"]

----

define `x3`
define `z2`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "z2", "x3"]

----

define `x3`
define `z2`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "z2", "x3"]

----

define `x3`
define `z2`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "z2", "x3"]

----

define `x3`
define `z2`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "z2", "x3"]

----

define `x3`
define `z2`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "z2", "x3"]

----

define `x3`
define `z2`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "z2", "x3"]

----

define `x3`
define `z2`
define `y3`
use `y3`
-- post defs: ["y3", "z2", "x3"]

----

define `x3`
define `z2`
define `y3`
use `z2`
-- post defs: ["y3", "z2", "x3"]

----

define `x3`
define `z2`
define `y3`
use `x3`
-- post defs: ["y3", "z2", "x3"]

----

define `x3`
define `z2`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "z2", "x3"]

----

define `x3`
define `z2`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "z2", "x3"]

----

define `x3`
define `z2`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "z2", "x3"]

----

define `x3`
define `z2`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "z2", "x3"]

----

define `x3`
define `z2`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "z2", "x3"]

----

define `x3`
define `z2`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "z2", "x3"]

----

define `x3`
define `z2`
define `z1`
use `z1`
-- post defs: ["z1", "z2", "x3"]

----

define `x3`
define `z2`
define `z1`
use `z2`
-- post defs: ["z1", "z2", "x3"]

----

define `x3`
define `z2`
define `z1`
use `x3`
-- post defs: ["z1", "z2", "x3"]

----

define `x3`
define `z2`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "z2", "x3"]

----

define `x3`
define `z2`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "z2", "x3"]

----

define `x3`
define `z2`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "z2", "x3"]

----

define `x3`
define `z2`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "z2", "x3"]

----

define `x3`
define `z2`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "z2", "x3"]

----

define `x3`
define `z2`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "z2", "x3"]

----

define `x3`
define `z2`
define `z3`
use `z3`
-- post defs: ["z3", "z2", "x3"]

----

define `x3`
define `z2`
define `z3`
use `z2`
-- post defs: ["z3", "z2", "x3"]

----

define `x3`
define `z2`
define `z3`
use `x3`
-- post defs: ["z3", "z2", "x3"]

----

define `x3`
define `z2`
use `z2`
define `x1`
-- post defs: ["x1", "z2", "x3"]

----

define `x3`
define `z2`
use `z2`
define `x2`
-- post defs: ["x2", "z2", "x3"]

----

define `x3`
define `z2`
use `z2`
define `y1`
-- post defs: ["y1", "z2", "x3"]

----

define `x3`
define `z2`
use `z2`
define `y2`
-- post defs: ["y2", "z2", "x3"]

----

define `x3`
define `z2`
use `z2`
define `y3`
-- post defs: ["y3", "z2", "x3"]

----

define `x3`
define `z2`
use `z2`
define `z1`
-- post defs: ["z1", "z2", "x3"]

----

define `x3`
define `z2`
use `z2`
define `z3`
-- post defs: ["z3", "z2", "x3"]

----

define `x3`
define `z2`
use `z2`
use `z2`
-- post defs: ["z2", "x3"]

----

define `x3`
define `z2`
use `z2`
use `x3`
-- post defs: ["z2", "x3"]

----

define `x3`
define `z2`
use `x3`
define `x1`
-- post defs: ["x1", "z2", "x3"]

----

define `x3`
define `z2`
use `x3`
define `x2`
-- post defs: ["x2", "z2", "x3"]

----

define `x3`
define `z2`
use `x3`
define `y1`
-- post defs: ["y1", "z2", "x3"]

----

define `x3`
define `z2`
use `x3`
define `y2`
-- post defs: ["y2", "z2", "x3"]

----

define `x3`
define `z2`
use `x3`
define `y3`
-- post defs: ["y3", "z2", "x3"]

----

define `x3`
define `z2`
use `x3`
define `z1`
-- post defs: ["z1", "z2", "x3"]

----

define `x3`
define `z2`
use `x3`
define `z3`
-- post defs: ["z3", "z2", "x3"]

----

define `x3`
define `z2`
use `x3`
use `z2`
-- post defs: ["z2", "x3"]

----

define `x3`
define `z2`
use `x3`
use `x3`
-- post defs: ["z2", "x3"]

----

define `x3`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "x3"]

----

define `x3`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "x3"]

----

define `x3`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "x3"]

----

define `x3`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "x3"]

----

define `x3`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "x3"]

----

define `x3`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "x3"]

----

define `x3`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "x3"]

----

define `x3`
define `z3`
use `z3`
-- post defs: ["z3", "x3"]

----

define `x3`
define `z3`
use `x3`
-- post defs: ["z3", "x3"]

----

define `x3`
define `z3`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "z3", "x3"]

----

define `x3`
define `z3`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "z3", "x3"]

----

define `x3`
define `z3`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "z3", "x3"]

----

define `x3`
define `z3`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "z3", "x3"]

----

define `x3`
define `z3`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "z3", "x3"]

----

define `x3`
define `z3`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "z3", "x3"]

----

define `x3`
define `z3`
define `x1`
use `x1`
-- post defs: ["x1", "z3", "x3"]

----

define `x3`
define `z3`
define `x1`
use `z3`
-- post defs: ["x1", "z3", "x3"]

----

define `x3`
define `z3`
define `x1`
use `x3`
-- post defs: ["x1", "z3", "x3"]

----

define `x3`
define `z3`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "z3", "x3"]

----

define `x3`
define `z3`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "z3", "x3"]

----

define `x3`
define `z3`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "z3", "x3"]

----

define `x3`
define `z3`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "z3", "x3"]

----

define `x3`
define `z3`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "z3", "x3"]

----

define `x3`
define `z3`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "z3", "x3"]

----

define `x3`
define `z3`
define `x2`
use `x2`
-- post defs: ["x2", "z3", "x3"]

----

define `x3`
define `z3`
define `x2`
use `z3`
-- post defs: ["x2", "z3", "x3"]

----

define `x3`
define `z3`
define `x2`
use `x3`
-- post defs: ["x2", "z3", "x3"]

----

define `x3`
define `z3`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "z3", "x3"]

----

define `x3`
define `z3`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "z3", "x3"]

----

define `x3`
define `z3`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "z3", "x3"]

----

define `x3`
define `z3`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "z3", "x3"]

----

define `x3`
define `z3`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "z3", "x3"]

----

define `x3`
define `z3`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "z3", "x3"]

----

define `x3`
define `z3`
define `y1`
use `y1`
-- post defs: ["y1", "z3", "x3"]

----

define `x3`
define `z3`
define `y1`
use `z3`
-- post defs: ["y1", "z3", "x3"]

----

define `x3`
define `z3`
define `y1`
use `x3`
-- post defs: ["y1", "z3", "x3"]

----

define `x3`
define `z3`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "z3", "x3"]

----

define `x3`
define `z3`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "z3", "x3"]

----

define `x3`
define `z3`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "z3", "x3"]

----

define `x3`
define `z3`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "z3", "x3"]

----

define `x3`
define `z3`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "z3", "x3"]

----

define `x3`
define `z3`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "z3", "x3"]

----

define `x3`
define `z3`
define `y2`
use `y2`
-- post defs: ["y2", "z3", "x3"]

----

define `x3`
define `z3`
define `y2`
use `z3`
-- post defs: ["y2", "z3", "x3"]

----

define `x3`
define `z3`
define `y2`
use `x3`
-- post defs: ["y2", "z3", "x3"]

----

define `x3`
define `z3`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "z3", "x3"]

----

define `x3`
define `z3`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "z3", "x3"]

----

define `x3`
define `z3`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "z3", "x3"]

----

define `x3`
define `z3`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "z3", "x3"]

----

define `x3`
define `z3`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "z3", "x3"]

----

define `x3`
define `z3`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "z3", "x3"]

----

define `x3`
define `z3`
define `y3`
use `y3`
-- post defs: ["y3", "z3", "x3"]

----

define `x3`
define `z3`
define `y3`
use `z3`
-- post defs: ["y3", "z3", "x3"]

----

define `x3`
define `z3`
define `y3`
use `x3`
-- post defs: ["y3", "z3", "x3"]

----

define `x3`
define `z3`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "z3", "x3"]

----

define `x3`
define `z3`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "z3", "x3"]

----

define `x3`
define `z3`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "z3", "x3"]

----

define `x3`
define `z3`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "z3", "x3"]

----

define `x3`
define `z3`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "z3", "x3"]

----

define `x3`
define `z3`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "z3", "x3"]

----

define `x3`
define `z3`
define `z1`
use `z1`
-- post defs: ["z1", "z3", "x3"]

----

define `x3`
define `z3`
define `z1`
use `z3`
-- post defs: ["z1", "z3", "x3"]

----

define `x3`
define `z3`
define `z1`
use `x3`
-- post defs: ["z1", "z3", "x3"]

----

define `x3`
define `z3`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "z3", "x3"]

----

define `x3`
define `z3`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "z3", "x3"]

----

define `x3`
define `z3`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "z3", "x3"]

----

define `x3`
define `z3`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "z3", "x3"]

----

define `x3`
define `z3`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "z3", "x3"]

----

define `x3`
define `z3`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "z3", "x3"]

----

define `x3`
define `z3`
define `z2`
use `z2`
-- post defs: ["z2", "z3", "x3"]

----

define `x3`
define `z3`
define `z2`
use `z3`
-- post defs: ["z2", "z3", "x3"]

----

define `x3`
define `z3`
define `z2`
use `x3`
-- post defs: ["z2", "z3", "x3"]

----

define `x3`
define `z3`
use `z3`
define `x1`
-- post defs: ["x1", "z3", "x3"]

----

define `x3`
define `z3`
use `z3`
define `x2`
-- post defs: ["x2", "z3", "x3"]

----

define `x3`
define `z3`
use `z3`
define `y1`
-- post defs: ["y1", "z3", "x3"]

----

define `x3`
define `z3`
use `z3`
define `y2`
-- post defs: ["y2", "z3", "x3"]

----

define `x3`
define `z3`
use `z3`
define `y3`
-- post defs: ["y3", "z3", "x3"]

----

define `x3`
define `z3`
use `z3`
define `z1`
-- post defs: ["z1", "z3", "x3"]

----

define `x3`
define `z3`
use `z3`
define `z2`
-- post defs: ["z2", "z3", "x3"]

----

define `x3`
define `z3`
use `z3`
use `z3`
-- post defs: ["z3", "x3"]

----

define `x3`
define `z3`
use `z3`
use `x3`
-- post defs: ["z3", "x3"]

----

define `x3`
define `z3`
use `x3`
define `x1`
-- post defs: ["x1", "z3", "x3"]

----

define `x3`
define `z3`
use `x3`
define `x2`
-- post defs: ["x2", "z3", "x3"]

----

define `x3`
define `z3`
use `x3`
define `y1`
-- post defs: ["y1", "z3", "x3"]

----

define `x3`
define `z3`
use `x3`
define `y2`
-- post defs: ["y2", "z3", "x3"]

----

define `x3`
define `z3`
use `x3`
define `y3`
-- post defs: ["y3", "z3", "x3"]

----

define `x3`
define `z3`
use `x3`
define `z1`
-- post defs: ["z1", "z3", "x3"]

----

define `x3`
define `z3`
use `x3`
define `z2`
-- post defs: ["z2", "z3", "x3"]

----

define `x3`
define `z3`
use `x3`
use `z3`
-- post defs: ["z3", "x3"]

----

define `x3`
define `z3`
use `x3`
use `x3`
-- post defs: ["z3", "x3"]

----

define `x3`
use `x3`
define `x1`
-- post defs: ["x1", "x3"]

----

define `x3`
use `x3`
define `x2`
-- post defs: ["x2", "x3"]

----

define `x3`
use `x3`
define `y1`
-- post defs: ["y1", "x3"]

----

define `x3`
use `x3`
define `y2`
-- post defs: ["y2", "x3"]

----

define `x3`
use `x3`
define `y3`
-- post defs: ["y3", "x3"]

----

define `x3`
use `x3`
define `z1`
-- post defs: ["z1", "x3"]

----

define `x3`
use `x3`
define `z2`
-- post defs: ["z2", "x3"]

----

define `x3`
use `x3`
define `z3`
-- post defs: ["z3", "x3"]

----

define `x3`
use `x3`
use `x3`
-- post defs: ["x3"]

----

define `x3`
use `x3`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "x3"]

----

define `x3`
use `x3`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "x3"]

----

define `x3`
use `x3`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "x3"]

----

define `x3`
use `x3`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "x3"]

----

define `x3`
use `x3`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "x3"]

----

define `x3`
use `x3`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "x3"]

----

define `x3`
use `x3`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "x3"]

----

define `x3`
use `x3`
define `x1`
use `x1`
-- post defs: ["x1", "x3"]

----

define `x3`
use `x3`
define `x1`
use `x3`
-- post defs: ["x1", "x3"]

----

define `x3`
use `x3`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "x3"]

----

define `x3`
use `x3`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "x3"]

----

define `x3`
use `x3`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "x3"]

----

define `x3`
use `x3`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "x3"]

----

define `x3`
use `x3`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "x3"]

----

define `x3`
use `x3`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "x3"]

----

define `x3`
use `x3`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "x3"]

----

define `x3`
use `x3`
define `x2`
use `x2`
-- post defs: ["x2", "x3"]

----

define `x3`
use `x3`
define `x2`
use `x3`
-- post defs: ["x2", "x3"]

----

define `x3`
use `x3`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "x3"]

----

define `x3`
use `x3`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "x3"]

----

define `x3`
use `x3`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "x3"]

----

define `x3`
use `x3`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "x3"]

----

define `x3`
use `x3`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "x3"]

----

define `x3`
use `x3`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "x3"]

----

define `x3`
use `x3`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "x3"]

----

define `x3`
use `x3`
define `y1`
use `y1`
-- post defs: ["y1", "x3"]

----

define `x3`
use `x3`
define `y1`
use `x3`
-- post defs: ["y1", "x3"]

----

define `x3`
use `x3`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "x3"]

----

define `x3`
use `x3`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "x3"]

----

define `x3`
use `x3`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "x3"]

----

define `x3`
use `x3`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "x3"]

----

define `x3`
use `x3`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "x3"]

----

define `x3`
use `x3`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "x3"]

----

define `x3`
use `x3`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "x3"]

----

define `x3`
use `x3`
define `y2`
use `y2`
-- post defs: ["y2", "x3"]

----

define `x3`
use `x3`
define `y2`
use `x3`
-- post defs: ["y2", "x3"]

----

define `x3`
use `x3`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "x3"]

----

define `x3`
use `x3`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "x3"]

----

define `x3`
use `x3`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "x3"]

----

define `x3`
use `x3`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "x3"]

----

define `x3`
use `x3`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "x3"]

----

define `x3`
use `x3`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "x3"]

----

define `x3`
use `x3`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "x3"]

----

define `x3`
use `x3`
define `y3`
use `y3`
-- post defs: ["y3", "x3"]

----

define `x3`
use `x3`
define `y3`
use `x3`
-- post defs: ["y3", "x3"]

----

define `x3`
use `x3`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "x3"]

----

define `x3`
use `x3`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "x3"]

----

define `x3`
use `x3`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "x3"]

----

define `x3`
use `x3`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "x3"]

----

define `x3`
use `x3`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "x3"]

----

define `x3`
use `x3`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "x3"]

----

define `x3`
use `x3`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "x3"]

----

define `x3`
use `x3`
define `z1`
use `z1`
-- post defs: ["z1", "x3"]

----

define `x3`
use `x3`
define `z1`
use `x3`
-- post defs: ["z1", "x3"]

----

define `x3`
use `x3`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "x3"]

----

define `x3`
use `x3`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "x3"]

----

define `x3`
use `x3`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "x3"]

----

define `x3`
use `x3`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "x3"]

----

define `x3`
use `x3`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "x3"]

----

define `x3`
use `x3`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "x3"]

----

define `x3`
use `x3`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "x3"]

----

define `x3`
use `x3`
define `z2`
use `z2`
-- post defs: ["z2", "x3"]

----

define `x3`
use `x3`
define `z2`
use `x3`
-- post defs: ["z2", "x3"]

----

define `x3`
use `x3`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "x3"]

----

define `x3`
use `x3`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "x3"]

----

define `x3`
use `x3`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "x3"]

----

define `x3`
use `x3`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "x3"]

----

define `x3`
use `x3`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "x3"]

----

define `x3`
use `x3`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "x3"]

----

define `x3`
use `x3`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "x3"]

----

define `x3`
use `x3`
define `z3`
use `z3`
-- post defs: ["z3", "x3"]

----

define `x3`
use `x3`
define `z3`
use `x3`
-- post defs: ["z3", "x3"]

----

define `x3`
use `x3`
use `x3`
define `x1`
-- post defs: ["x1", "x3"]

----

define `x3`
use `x3`
use `x3`
define `x2`
-- post defs: ["x2", "x3"]

----

define `x3`
use `x3`
use `x3`
define `y1`
-- post defs: ["y1", "x3"]

----

define `x3`
use `x3`
use `x3`
define `y2`
-- post defs: ["y2", "x3"]

----

define `x3`
use `x3`
use `x3`
define `y3`
-- post defs: ["y3", "x3"]

----

define `x3`
use `x3`
use `x3`
define `z1`
-- post defs: ["z1", "x3"]

----

define `x3`
use `x3`
use `x3`
define `z2`
-- post defs: ["z2", "x3"]

----

define `x3`
use `x3`
use `x3`
define `z3`
-- post defs: ["z3", "x3"]

----

define `x3`
use `x3`
use `x3`
use `x3`
-- post defs: ["x3"]

----

define `y1`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "y1"]

----

define `y1`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "y1"]

----

define `y1`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "y1"]

----

define `y1`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "y1"]

----

define `y1`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "y1"]

----

define `y1`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "y1"]

----

define `y1`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "y1"]

----

define `y1`
define `x1`
use `x1`
-- post defs: ["x1", "y1"]

----

define `y1`
define `x1`
use `y1`
-- post defs: ["x1", "y1"]

----

define `y1`
define `x1`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "x1", "y1"]

----

define `y1`
define `x1`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "x1", "y1"]

----

define `y1`
define `x1`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "x1", "y1"]

----

define `y1`
define `x1`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "x1", "y1"]

----

define `y1`
define `x1`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "x1", "y1"]

----

define `y1`
define `x1`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "x1", "y1"]

----

define `y1`
define `x1`
define `x2`
use `x2`
-- post defs: ["x2", "x1", "y1"]

----

define `y1`
define `x1`
define `x2`
use `x1`
-- post defs: ["x2", "x1", "y1"]

----

define `y1`
define `x1`
define `x2`
use `y1`
-- post defs: ["x2", "x1", "y1"]

----

define `y1`
define `x1`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "x1", "y1"]

----

define `y1`
define `x1`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "x1", "y1"]

----

define `y1`
define `x1`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "x1", "y1"]

----

define `y1`
define `x1`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "x1", "y1"]

----

define `y1`
define `x1`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "x1", "y1"]

----

define `y1`
define `x1`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "x1", "y1"]

----

define `y1`
define `x1`
define `x3`
use `x3`
-- post defs: ["x3", "x1", "y1"]

----

define `y1`
define `x1`
define `x3`
use `x1`
-- post defs: ["x3", "x1", "y1"]

----

define `y1`
define `x1`
define `x3`
use `y1`
-- post defs: ["x3", "x1", "y1"]

----

define `y1`
define `x1`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "x1", "y1"]

----

define `y1`
define `x1`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "x1", "y1"]

----

define `y1`
define `x1`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "x1", "y1"]

----

define `y1`
define `x1`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "x1", "y1"]

----

define `y1`
define `x1`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "x1", "y1"]

----

define `y1`
define `x1`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "x1", "y1"]

----

define `y1`
define `x1`
define `y2`
use `y2`
-- post defs: ["y2", "x1", "y1"]

----

define `y1`
define `x1`
define `y2`
use `x1`
-- post defs: ["y2", "x1", "y1"]

----

define `y1`
define `x1`
define `y2`
use `y1`
-- post defs: ["y2", "x1", "y1"]

----

define `y1`
define `x1`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "x1", "y1"]

----

define `y1`
define `x1`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "x1", "y1"]

----

define `y1`
define `x1`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "x1", "y1"]

----

define `y1`
define `x1`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "x1", "y1"]

----

define `y1`
define `x1`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "x1", "y1"]

----

define `y1`
define `x1`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "x1", "y1"]

----

define `y1`
define `x1`
define `y3`
use `y3`
-- post defs: ["y3", "x1", "y1"]

----

define `y1`
define `x1`
define `y3`
use `x1`
-- post defs: ["y3", "x1", "y1"]

----

define `y1`
define `x1`
define `y3`
use `y1`
-- post defs: ["y3", "x1", "y1"]

----

define `y1`
define `x1`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "x1", "y1"]

----

define `y1`
define `x1`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "x1", "y1"]

----

define `y1`
define `x1`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "x1", "y1"]

----

define `y1`
define `x1`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "x1", "y1"]

----

define `y1`
define `x1`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "x1", "y1"]

----

define `y1`
define `x1`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "x1", "y1"]

----

define `y1`
define `x1`
define `z1`
use `z1`
-- post defs: ["z1", "x1", "y1"]

----

define `y1`
define `x1`
define `z1`
use `x1`
-- post defs: ["z1", "x1", "y1"]

----

define `y1`
define `x1`
define `z1`
use `y1`
-- post defs: ["z1", "x1", "y1"]

----

define `y1`
define `x1`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "x1", "y1"]

----

define `y1`
define `x1`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "x1", "y1"]

----

define `y1`
define `x1`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "x1", "y1"]

----

define `y1`
define `x1`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "x1", "y1"]

----

define `y1`
define `x1`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "x1", "y1"]

----

define `y1`
define `x1`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "x1", "y1"]

----

define `y1`
define `x1`
define `z2`
use `z2`
-- post defs: ["z2", "x1", "y1"]

----

define `y1`
define `x1`
define `z2`
use `x1`
-- post defs: ["z2", "x1", "y1"]

----

define `y1`
define `x1`
define `z2`
use `y1`
-- post defs: ["z2", "x1", "y1"]

----

define `y1`
define `x1`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "x1", "y1"]

----

define `y1`
define `x1`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "x1", "y1"]

----

define `y1`
define `x1`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "x1", "y1"]

----

define `y1`
define `x1`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "x1", "y1"]

----

define `y1`
define `x1`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "x1", "y1"]

----

define `y1`
define `x1`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "x1", "y1"]

----

define `y1`
define `x1`
define `z3`
use `z3`
-- post defs: ["z3", "x1", "y1"]

----

define `y1`
define `x1`
define `z3`
use `x1`
-- post defs: ["z3", "x1", "y1"]

----

define `y1`
define `x1`
define `z3`
use `y1`
-- post defs: ["z3", "x1", "y1"]

----

define `y1`
define `x1`
use `x1`
define `x2`
-- post defs: ["x2", "x1", "y1"]

----

define `y1`
define `x1`
use `x1`
define `x3`
-- post defs: ["x3", "x1", "y1"]

----

define `y1`
define `x1`
use `x1`
define `y2`
-- post defs: ["y2", "x1", "y1"]

----

define `y1`
define `x1`
use `x1`
define `y3`
-- post defs: ["y3", "x1", "y1"]

----

define `y1`
define `x1`
use `x1`
define `z1`
-- post defs: ["z1", "x1", "y1"]

----

define `y1`
define `x1`
use `x1`
define `z2`
-- post defs: ["z2", "x1", "y1"]

----

define `y1`
define `x1`
use `x1`
define `z3`
-- post defs: ["z3", "x1", "y1"]

----

define `y1`
define `x1`
use `x1`
use `x1`
-- post defs: ["x1", "y1"]

----

define `y1`
define `x1`
use `x1`
use `y1`
-- post defs: ["x1", "y1"]

----

define `y1`
define `x1`
use `y1`
define `x2`
-- post defs: ["x2", "x1", "y1"]

----

define `y1`
define `x1`
use `y1`
define `x3`
-- post defs: ["x3", "x1", "y1"]

----

define `y1`
define `x1`
use `y1`
define `y2`
-- post defs: ["y2", "x1", "y1"]

----

define `y1`
define `x1`
use `y1`
define `y3`
-- post defs: ["y3", "x1", "y1"]

----

define `y1`
define `x1`
use `y1`
define `z1`
-- post defs: ["z1", "x1", "y1"]

----

define `y1`
define `x1`
use `y1`
define `z2`
-- post defs: ["z2", "x1", "y1"]

----

define `y1`
define `x1`
use `y1`
define `z3`
-- post defs: ["z3", "x1", "y1"]

----

define `y1`
define `x1`
use `y1`
use `x1`
-- post defs: ["x1", "y1"]

----

define `y1`
define `x1`
use `y1`
use `y1`
-- post defs: ["x1", "y1"]

----

define `y1`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "y1"]

----

define `y1`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "y1"]

----

define `y1`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "y1"]

----

define `y1`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "y1"]

----

define `y1`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "y1"]

----

define `y1`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "y1"]

----

define `y1`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "y1"]

----

define `y1`
define `x2`
use `x2`
-- post defs: ["x2", "y1"]

----

define `y1`
define `x2`
use `y1`
-- post defs: ["x2", "y1"]

----

define `y1`
define `x2`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "x2", "y1"]

----

define `y1`
define `x2`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "x2", "y1"]

----

define `y1`
define `x2`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "x2", "y1"]

----

define `y1`
define `x2`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "x2", "y1"]

----

define `y1`
define `x2`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "x2", "y1"]

----

define `y1`
define `x2`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "x2", "y1"]

----

define `y1`
define `x2`
define `x1`
use `x1`
-- post defs: ["x1", "x2", "y1"]

----

define `y1`
define `x2`
define `x1`
use `x2`
-- post defs: ["x1", "x2", "y1"]

----

define `y1`
define `x2`
define `x1`
use `y1`
-- post defs: ["x1", "x2", "y1"]

----

define `y1`
define `x2`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "x2", "y1"]

----

define `y1`
define `x2`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "x2", "y1"]

----

define `y1`
define `x2`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "x2", "y1"]

----

define `y1`
define `x2`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "x2", "y1"]

----

define `y1`
define `x2`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "x2", "y1"]

----

define `y1`
define `x2`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "x2", "y1"]

----

define `y1`
define `x2`
define `x3`
use `x3`
-- post defs: ["x3", "x2", "y1"]

----

define `y1`
define `x2`
define `x3`
use `x2`
-- post defs: ["x3", "x2", "y1"]

----

define `y1`
define `x2`
define `x3`
use `y1`
-- post defs: ["x3", "x2", "y1"]

----

define `y1`
define `x2`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "x2", "y1"]

----

define `y1`
define `x2`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "x2", "y1"]

----

define `y1`
define `x2`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "x2", "y1"]

----

define `y1`
define `x2`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "x2", "y1"]

----

define `y1`
define `x2`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "x2", "y1"]

----

define `y1`
define `x2`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "x2", "y1"]

----

define `y1`
define `x2`
define `y2`
use `y2`
-- post defs: ["y2", "x2", "y1"]

----

define `y1`
define `x2`
define `y2`
use `x2`
-- post defs: ["y2", "x2", "y1"]

----

define `y1`
define `x2`
define `y2`
use `y1`
-- post defs: ["y2", "x2", "y1"]

----

define `y1`
define `x2`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "x2", "y1"]

----

define `y1`
define `x2`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "x2", "y1"]

----

define `y1`
define `x2`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "x2", "y1"]

----

define `y1`
define `x2`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "x2", "y1"]

----

define `y1`
define `x2`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "x2", "y1"]

----

define `y1`
define `x2`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "x2", "y1"]

----

define `y1`
define `x2`
define `y3`
use `y3`
-- post defs: ["y3", "x2", "y1"]

----

define `y1`
define `x2`
define `y3`
use `x2`
-- post defs: ["y3", "x2", "y1"]

----

define `y1`
define `x2`
define `y3`
use `y1`
-- post defs: ["y3", "x2", "y1"]

----

define `y1`
define `x2`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "x2", "y1"]

----

define `y1`
define `x2`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "x2", "y1"]

----

define `y1`
define `x2`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "x2", "y1"]

----

define `y1`
define `x2`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "x2", "y1"]

----

define `y1`
define `x2`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "x2", "y1"]

----

define `y1`
define `x2`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "x2", "y1"]

----

define `y1`
define `x2`
define `z1`
use `z1`
-- post defs: ["z1", "x2", "y1"]

----

define `y1`
define `x2`
define `z1`
use `x2`
-- post defs: ["z1", "x2", "y1"]

----

define `y1`
define `x2`
define `z1`
use `y1`
-- post defs: ["z1", "x2", "y1"]

----

define `y1`
define `x2`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "x2", "y1"]

----

define `y1`
define `x2`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "x2", "y1"]

----

define `y1`
define `x2`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "x2", "y1"]

----

define `y1`
define `x2`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "x2", "y1"]

----

define `y1`
define `x2`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "x2", "y1"]

----

define `y1`
define `x2`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "x2", "y1"]

----

define `y1`
define `x2`
define `z2`
use `z2`
-- post defs: ["z2", "x2", "y1"]

----

define `y1`
define `x2`
define `z2`
use `x2`
-- post defs: ["z2", "x2", "y1"]

----

define `y1`
define `x2`
define `z2`
use `y1`
-- post defs: ["z2", "x2", "y1"]

----

define `y1`
define `x2`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "x2", "y1"]

----

define `y1`
define `x2`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "x2", "y1"]

----

define `y1`
define `x2`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "x2", "y1"]

----

define `y1`
define `x2`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "x2", "y1"]

----

define `y1`
define `x2`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "x2", "y1"]

----

define `y1`
define `x2`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "x2", "y1"]

----

define `y1`
define `x2`
define `z3`
use `z3`
-- post defs: ["z3", "x2", "y1"]

----

define `y1`
define `x2`
define `z3`
use `x2`
-- post defs: ["z3", "x2", "y1"]

----

define `y1`
define `x2`
define `z3`
use `y1`
-- post defs: ["z3", "x2", "y1"]

----

define `y1`
define `x2`
use `x2`
define `x1`
-- post defs: ["x1", "x2", "y1"]

----

define `y1`
define `x2`
use `x2`
define `x3`
-- post defs: ["x3", "x2", "y1"]

----

define `y1`
define `x2`
use `x2`
define `y2`
-- post defs: ["y2", "x2", "y1"]

----

define `y1`
define `x2`
use `x2`
define `y3`
-- post defs: ["y3", "x2", "y1"]

----

define `y1`
define `x2`
use `x2`
define `z1`
-- post defs: ["z1", "x2", "y1"]

----

define `y1`
define `x2`
use `x2`
define `z2`
-- post defs: ["z2", "x2", "y1"]

----

define `y1`
define `x2`
use `x2`
define `z3`
-- post defs: ["z3", "x2", "y1"]

----

define `y1`
define `x2`
use `x2`
use `x2`
-- post defs: ["x2", "y1"]

----

define `y1`
define `x2`
use `x2`
use `y1`
-- post defs: ["x2", "y1"]

----

define `y1`
define `x2`
use `y1`
define `x1`
-- post defs: ["x1", "x2", "y1"]

----

define `y1`
define `x2`
use `y1`
define `x3`
-- post defs: ["x3", "x2", "y1"]

----

define `y1`
define `x2`
use `y1`
define `y2`
-- post defs: ["y2", "x2", "y1"]

----

define `y1`
define `x2`
use `y1`
define `y3`
-- post defs: ["y3", "x2", "y1"]

----

define `y1`
define `x2`
use `y1`
define `z1`
-- post defs: ["z1", "x2", "y1"]

----

define `y1`
define `x2`
use `y1`
define `z2`
-- post defs: ["z2", "x2", "y1"]

----

define `y1`
define `x2`
use `y1`
define `z3`
-- post defs: ["z3", "x2", "y1"]

----

define `y1`
define `x2`
use `y1`
use `x2`
-- post defs: ["x2", "y1"]

----

define `y1`
define `x2`
use `y1`
use `y1`
-- post defs: ["x2", "y1"]

----

define `y1`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "y1"]

----

define `y1`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "y1"]

----

define `y1`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "y1"]

----

define `y1`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "y1"]

----

define `y1`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "y1"]

----

define `y1`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "y1"]

----

define `y1`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "y1"]

----

define `y1`
define `x3`
use `x3`
-- post defs: ["x3", "y1"]

----

define `y1`
define `x3`
use `y1`
-- post defs: ["x3", "y1"]

----

define `y1`
define `x3`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "x3", "y1"]

----

define `y1`
define `x3`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "x3", "y1"]

----

define `y1`
define `x3`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "x3", "y1"]

----

define `y1`
define `x3`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "x3", "y1"]

----

define `y1`
define `x3`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "x3", "y1"]

----

define `y1`
define `x3`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "x3", "y1"]

----

define `y1`
define `x3`
define `x1`
use `x1`
-- post defs: ["x1", "x3", "y1"]

----

define `y1`
define `x3`
define `x1`
use `x3`
-- post defs: ["x1", "x3", "y1"]

----

define `y1`
define `x3`
define `x1`
use `y1`
-- post defs: ["x1", "x3", "y1"]

----

define `y1`
define `x3`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "x3", "y1"]

----

define `y1`
define `x3`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "x3", "y1"]

----

define `y1`
define `x3`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "x3", "y1"]

----

define `y1`
define `x3`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "x3", "y1"]

----

define `y1`
define `x3`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "x3", "y1"]

----

define `y1`
define `x3`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "x3", "y1"]

----

define `y1`
define `x3`
define `x2`
use `x2`
-- post defs: ["x2", "x3", "y1"]

----

define `y1`
define `x3`
define `x2`
use `x3`
-- post defs: ["x2", "x3", "y1"]

----

define `y1`
define `x3`
define `x2`
use `y1`
-- post defs: ["x2", "x3", "y1"]

----

define `y1`
define `x3`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "x3", "y1"]

----

define `y1`
define `x3`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "x3", "y1"]

----

define `y1`
define `x3`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "x3", "y1"]

----

define `y1`
define `x3`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "x3", "y1"]

----

define `y1`
define `x3`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "x3", "y1"]

----

define `y1`
define `x3`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "x3", "y1"]

----

define `y1`
define `x3`
define `y2`
use `y2`
-- post defs: ["y2", "x3", "y1"]

----

define `y1`
define `x3`
define `y2`
use `x3`
-- post defs: ["y2", "x3", "y1"]

----

define `y1`
define `x3`
define `y2`
use `y1`
-- post defs: ["y2", "x3", "y1"]

----

define `y1`
define `x3`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "x3", "y1"]

----

define `y1`
define `x3`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "x3", "y1"]

----

define `y1`
define `x3`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "x3", "y1"]

----

define `y1`
define `x3`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "x3", "y1"]

----

define `y1`
define `x3`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "x3", "y1"]

----

define `y1`
define `x3`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "x3", "y1"]

----

define `y1`
define `x3`
define `y3`
use `y3`
-- post defs: ["y3", "x3", "y1"]

----

define `y1`
define `x3`
define `y3`
use `x3`
-- post defs: ["y3", "x3", "y1"]

----

define `y1`
define `x3`
define `y3`
use `y1`
-- post defs: ["y3", "x3", "y1"]

----

define `y1`
define `x3`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "x3", "y1"]

----

define `y1`
define `x3`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "x3", "y1"]

----

define `y1`
define `x3`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "x3", "y1"]

----

define `y1`
define `x3`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "x3", "y1"]

----

define `y1`
define `x3`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "x3", "y1"]

----

define `y1`
define `x3`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "x3", "y1"]

----

define `y1`
define `x3`
define `z1`
use `z1`
-- post defs: ["z1", "x3", "y1"]

----

define `y1`
define `x3`
define `z1`
use `x3`
-- post defs: ["z1", "x3", "y1"]

----

define `y1`
define `x3`
define `z1`
use `y1`
-- post defs: ["z1", "x3", "y1"]

----

define `y1`
define `x3`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "x3", "y1"]

----

define `y1`
define `x3`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "x3", "y1"]

----

define `y1`
define `x3`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "x3", "y1"]

----

define `y1`
define `x3`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "x3", "y1"]

----

define `y1`
define `x3`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "x3", "y1"]

----

define `y1`
define `x3`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "x3", "y1"]

----

define `y1`
define `x3`
define `z2`
use `z2`
-- post defs: ["z2", "x3", "y1"]

----

define `y1`
define `x3`
define `z2`
use `x3`
-- post defs: ["z2", "x3", "y1"]

----

define `y1`
define `x3`
define `z2`
use `y1`
-- post defs: ["z2", "x3", "y1"]

----

define `y1`
define `x3`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "x3", "y1"]

----

define `y1`
define `x3`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "x3", "y1"]

----

define `y1`
define `x3`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "x3", "y1"]

----

define `y1`
define `x3`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "x3", "y1"]

----

define `y1`
define `x3`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "x3", "y1"]

----

define `y1`
define `x3`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "x3", "y1"]

----

define `y1`
define `x3`
define `z3`
use `z3`
-- post defs: ["z3", "x3", "y1"]

----

define `y1`
define `x3`
define `z3`
use `x3`
-- post defs: ["z3", "x3", "y1"]

----

define `y1`
define `x3`
define `z3`
use `y1`
-- post defs: ["z3", "x3", "y1"]

----

define `y1`
define `x3`
use `x3`
define `x1`
-- post defs: ["x1", "x3", "y1"]

----

define `y1`
define `x3`
use `x3`
define `x2`
-- post defs: ["x2", "x3", "y1"]

----

define `y1`
define `x3`
use `x3`
define `y2`
-- post defs: ["y2", "x3", "y1"]

----

define `y1`
define `x3`
use `x3`
define `y3`
-- post defs: ["y3", "x3", "y1"]

----

define `y1`
define `x3`
use `x3`
define `z1`
-- post defs: ["z1", "x3", "y1"]

----

define `y1`
define `x3`
use `x3`
define `z2`
-- post defs: ["z2", "x3", "y1"]

----

define `y1`
define `x3`
use `x3`
define `z3`
-- post defs: ["z3", "x3", "y1"]

----

define `y1`
define `x3`
use `x3`
use `x3`
-- post defs: ["x3", "y1"]

----

define `y1`
define `x3`
use `x3`
use `y1`
-- post defs: ["x3", "y1"]

----

define `y1`
define `x3`
use `y1`
define `x1`
-- post defs: ["x1", "x3", "y1"]

----

define `y1`
define `x3`
use `y1`
define `x2`
-- post defs: ["x2", "x3", "y1"]

----

define `y1`
define `x3`
use `y1`
define `y2`
-- post defs: ["y2", "x3", "y1"]

----

define `y1`
define `x3`
use `y1`
define `y3`
-- post defs: ["y3", "x3", "y1"]

----

define `y1`
define `x3`
use `y1`
define `z1`
-- post defs: ["z1", "x3", "y1"]

----

define `y1`
define `x3`
use `y1`
define `z2`
-- post defs: ["z2", "x3", "y1"]

----

define `y1`
define `x3`
use `y1`
define `z3`
-- post defs: ["z3", "x3", "y1"]

----

define `y1`
define `x3`
use `y1`
use `x3`
-- post defs: ["x3", "y1"]

----

define `y1`
define `x3`
use `y1`
use `y1`
-- post defs: ["x3", "y1"]

----

define `y1`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "y1"]

----

define `y1`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "y1"]

----

define `y1`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "y1"]

----

define `y1`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "y1"]

----

define `y1`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "y1"]

----

define `y1`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "y1"]

----

define `y1`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "y1"]

----

define `y1`
define `y2`
use `y2`
-- post defs: ["y2", "y1"]

----

define `y1`
define `y2`
use `y1`
-- post defs: ["y2", "y1"]

----

define `y1`
define `y2`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "y2", "y1"]

----

define `y1`
define `y2`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "y2", "y1"]

----

define `y1`
define `y2`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "y2", "y1"]

----

define `y1`
define `y2`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "y2", "y1"]

----

define `y1`
define `y2`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "y2", "y1"]

----

define `y1`
define `y2`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "y2", "y1"]

----

define `y1`
define `y2`
define `x1`
use `x1`
-- post defs: ["x1", "y2", "y1"]

----

define `y1`
define `y2`
define `x1`
use `y2`
-- post defs: ["x1", "y2", "y1"]

----

define `y1`
define `y2`
define `x1`
use `y1`
-- post defs: ["x1", "y2", "y1"]

----

define `y1`
define `y2`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "y2", "y1"]

----

define `y1`
define `y2`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "y2", "y1"]

----

define `y1`
define `y2`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "y2", "y1"]

----

define `y1`
define `y2`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "y2", "y1"]

----

define `y1`
define `y2`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "y2", "y1"]

----

define `y1`
define `y2`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "y2", "y1"]

----

define `y1`
define `y2`
define `x2`
use `x2`
-- post defs: ["x2", "y2", "y1"]

----

define `y1`
define `y2`
define `x2`
use `y2`
-- post defs: ["x2", "y2", "y1"]

----

define `y1`
define `y2`
define `x2`
use `y1`
-- post defs: ["x2", "y2", "y1"]

----

define `y1`
define `y2`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "y2", "y1"]

----

define `y1`
define `y2`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "y2", "y1"]

----

define `y1`
define `y2`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "y2", "y1"]

----

define `y1`
define `y2`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "y2", "y1"]

----

define `y1`
define `y2`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "y2", "y1"]

----

define `y1`
define `y2`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "y2", "y1"]

----

define `y1`
define `y2`
define `x3`
use `x3`
-- post defs: ["x3", "y2", "y1"]

----

define `y1`
define `y2`
define `x3`
use `y2`
-- post defs: ["x3", "y2", "y1"]

----

define `y1`
define `y2`
define `x3`
use `y1`
-- post defs: ["x3", "y2", "y1"]

----

define `y1`
define `y2`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "y2", "y1"]

----

define `y1`
define `y2`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "y2", "y1"]

----

define `y1`
define `y2`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "y2", "y1"]

----

define `y1`
define `y2`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "y2", "y1"]

----

define `y1`
define `y2`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "y2", "y1"]

----

define `y1`
define `y2`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "y2", "y1"]

----

define `y1`
define `y2`
define `y3`
use `y3`
-- post defs: ["y3", "y2", "y1"]

----

define `y1`
define `y2`
define `y3`
use `y2`
-- post defs: ["y3", "y2", "y1"]

----

define `y1`
define `y2`
define `y3`
use `y1`
-- post defs: ["y3", "y2", "y1"]

----

define `y1`
define `y2`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "y2", "y1"]

----

define `y1`
define `y2`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "y2", "y1"]

----

define `y1`
define `y2`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "y2", "y1"]

----

define `y1`
define `y2`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "y2", "y1"]

----

define `y1`
define `y2`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "y2", "y1"]

----

define `y1`
define `y2`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "y2", "y1"]

----

define `y1`
define `y2`
define `z1`
use `z1`
-- post defs: ["z1", "y2", "y1"]

----

define `y1`
define `y2`
define `z1`
use `y2`
-- post defs: ["z1", "y2", "y1"]

----

define `y1`
define `y2`
define `z1`
use `y1`
-- post defs: ["z1", "y2", "y1"]

----

define `y1`
define `y2`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "y2", "y1"]

----

define `y1`
define `y2`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "y2", "y1"]

----

define `y1`
define `y2`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "y2", "y1"]

----

define `y1`
define `y2`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "y2", "y1"]

----

define `y1`
define `y2`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "y2", "y1"]

----

define `y1`
define `y2`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "y2", "y1"]

----

define `y1`
define `y2`
define `z2`
use `z2`
-- post defs: ["z2", "y2", "y1"]

----

define `y1`
define `y2`
define `z2`
use `y2`
-- post defs: ["z2", "y2", "y1"]

----

define `y1`
define `y2`
define `z2`
use `y1`
-- post defs: ["z2", "y2", "y1"]

----

define `y1`
define `y2`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "y2", "y1"]

----

define `y1`
define `y2`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "y2", "y1"]

----

define `y1`
define `y2`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "y2", "y1"]

----

define `y1`
define `y2`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "y2", "y1"]

----

define `y1`
define `y2`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "y2", "y1"]

----

define `y1`
define `y2`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "y2", "y1"]

----

define `y1`
define `y2`
define `z3`
use `z3`
-- post defs: ["z3", "y2", "y1"]

----

define `y1`
define `y2`
define `z3`
use `y2`
-- post defs: ["z3", "y2", "y1"]

----

define `y1`
define `y2`
define `z3`
use `y1`
-- post defs: ["z3", "y2", "y1"]

----

define `y1`
define `y2`
use `y2`
define `x1`
-- post defs: ["x1", "y2", "y1"]

----

define `y1`
define `y2`
use `y2`
define `x2`
-- post defs: ["x2", "y2", "y1"]

----

define `y1`
define `y2`
use `y2`
define `x3`
-- post defs: ["x3", "y2", "y1"]

----

define `y1`
define `y2`
use `y2`
define `y3`
-- post defs: ["y3", "y2", "y1"]

----

define `y1`
define `y2`
use `y2`
define `z1`
-- post defs: ["z1", "y2", "y1"]

----

define `y1`
define `y2`
use `y2`
define `z2`
-- post defs: ["z2", "y2", "y1"]

----

define `y1`
define `y2`
use `y2`
define `z3`
-- post defs: ["z3", "y2", "y1"]

----

define `y1`
define `y2`
use `y2`
use `y2`
-- post defs: ["y2", "y1"]

----

define `y1`
define `y2`
use `y2`
use `y1`
-- post defs: ["y2", "y1"]

----

define `y1`
define `y2`
use `y1`
define `x1`
-- post defs: ["x1", "y2", "y1"]

----

define `y1`
define `y2`
use `y1`
define `x2`
-- post defs: ["x2", "y2", "y1"]

----

define `y1`
define `y2`
use `y1`
define `x3`
-- post defs: ["x3", "y2", "y1"]

----

define `y1`
define `y2`
use `y1`
define `y3`
-- post defs: ["y3", "y2", "y1"]

----

define `y1`
define `y2`
use `y1`
define `z1`
-- post defs: ["z1", "y2", "y1"]

----

define `y1`
define `y2`
use `y1`
define `z2`
-- post defs: ["z2", "y2", "y1"]

----

define `y1`
define `y2`
use `y1`
define `z3`
-- post defs: ["z3", "y2", "y1"]

----

define `y1`
define `y2`
use `y1`
use `y2`
-- post defs: ["y2", "y1"]

----

define `y1`
define `y2`
use `y1`
use `y1`
-- post defs: ["y2", "y1"]

----

define `y1`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "y1"]

----

define `y1`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "y1"]

----

define `y1`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "y1"]

----

define `y1`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "y1"]

----

define `y1`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "y1"]

----

define `y1`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "y1"]

----

define `y1`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "y1"]

----

define `y1`
define `y3`
use `y3`
-- post defs: ["y3", "y1"]

----

define `y1`
define `y3`
use `y1`
-- post defs: ["y3", "y1"]

----

define `y1`
define `y3`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "y3", "y1"]

----

define `y1`
define `y3`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "y3", "y1"]

----

define `y1`
define `y3`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "y3", "y1"]

----

define `y1`
define `y3`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "y3", "y1"]

----

define `y1`
define `y3`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "y3", "y1"]

----

define `y1`
define `y3`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "y3", "y1"]

----

define `y1`
define `y3`
define `x1`
use `x1`
-- post defs: ["x1", "y3", "y1"]

----

define `y1`
define `y3`
define `x1`
use `y3`
-- post defs: ["x1", "y3", "y1"]

----

define `y1`
define `y3`
define `x1`
use `y1`
-- post defs: ["x1", "y3", "y1"]

----

define `y1`
define `y3`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "y3", "y1"]

----

define `y1`
define `y3`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "y3", "y1"]

----

define `y1`
define `y3`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "y3", "y1"]

----

define `y1`
define `y3`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "y3", "y1"]

----

define `y1`
define `y3`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "y3", "y1"]

----

define `y1`
define `y3`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "y3", "y1"]

----

define `y1`
define `y3`
define `x2`
use `x2`
-- post defs: ["x2", "y3", "y1"]

----

define `y1`
define `y3`
define `x2`
use `y3`
-- post defs: ["x2", "y3", "y1"]

----

define `y1`
define `y3`
define `x2`
use `y1`
-- post defs: ["x2", "y3", "y1"]

----

define `y1`
define `y3`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "y3", "y1"]

----

define `y1`
define `y3`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "y3", "y1"]

----

define `y1`
define `y3`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "y3", "y1"]

----

define `y1`
define `y3`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "y3", "y1"]

----

define `y1`
define `y3`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "y3", "y1"]

----

define `y1`
define `y3`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "y3", "y1"]

----

define `y1`
define `y3`
define `x3`
use `x3`
-- post defs: ["x3", "y3", "y1"]

----

define `y1`
define `y3`
define `x3`
use `y3`
-- post defs: ["x3", "y3", "y1"]

----

define `y1`
define `y3`
define `x3`
use `y1`
-- post defs: ["x3", "y3", "y1"]

----

define `y1`
define `y3`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "y3", "y1"]

----

define `y1`
define `y3`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "y3", "y1"]

----

define `y1`
define `y3`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "y3", "y1"]

----

define `y1`
define `y3`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "y3", "y1"]

----

define `y1`
define `y3`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "y3", "y1"]

----

define `y1`
define `y3`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "y3", "y1"]

----

define `y1`
define `y3`
define `y2`
use `y2`
-- post defs: ["y2", "y3", "y1"]

----

define `y1`
define `y3`
define `y2`
use `y3`
-- post defs: ["y2", "y3", "y1"]

----

define `y1`
define `y3`
define `y2`
use `y1`
-- post defs: ["y2", "y3", "y1"]

----

define `y1`
define `y3`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "y3", "y1"]

----

define `y1`
define `y3`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "y3", "y1"]

----

define `y1`
define `y3`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "y3", "y1"]

----

define `y1`
define `y3`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "y3", "y1"]

----

define `y1`
define `y3`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "y3", "y1"]

----

define `y1`
define `y3`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "y3", "y1"]

----

define `y1`
define `y3`
define `z1`
use `z1`
-- post defs: ["z1", "y3", "y1"]

----

define `y1`
define `y3`
define `z1`
use `y3`
-- post defs: ["z1", "y3", "y1"]

----

define `y1`
define `y3`
define `z1`
use `y1`
-- post defs: ["z1", "y3", "y1"]

----

define `y1`
define `y3`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "y3", "y1"]

----

define `y1`
define `y3`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "y3", "y1"]

----

define `y1`
define `y3`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "y3", "y1"]

----

define `y1`
define `y3`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "y3", "y1"]

----

define `y1`
define `y3`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "y3", "y1"]

----

define `y1`
define `y3`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "y3", "y1"]

----

define `y1`
define `y3`
define `z2`
use `z2`
-- post defs: ["z2", "y3", "y1"]

----

define `y1`
define `y3`
define `z2`
use `y3`
-- post defs: ["z2", "y3", "y1"]

----

define `y1`
define `y3`
define `z2`
use `y1`
-- post defs: ["z2", "y3", "y1"]

----

define `y1`
define `y3`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "y3", "y1"]

----

define `y1`
define `y3`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "y3", "y1"]

----

define `y1`
define `y3`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "y3", "y1"]

----

define `y1`
define `y3`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "y3", "y1"]

----

define `y1`
define `y3`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "y3", "y1"]

----

define `y1`
define `y3`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "y3", "y1"]

----

define `y1`
define `y3`
define `z3`
use `z3`
-- post defs: ["z3", "y3", "y1"]

----

define `y1`
define `y3`
define `z3`
use `y3`
-- post defs: ["z3", "y3", "y1"]

----

define `y1`
define `y3`
define `z3`
use `y1`
-- post defs: ["z3", "y3", "y1"]

----

define `y1`
define `y3`
use `y3`
define `x1`
-- post defs: ["x1", "y3", "y1"]

----

define `y1`
define `y3`
use `y3`
define `x2`
-- post defs: ["x2", "y3", "y1"]

----

define `y1`
define `y3`
use `y3`
define `x3`
-- post defs: ["x3", "y3", "y1"]

----

define `y1`
define `y3`
use `y3`
define `y2`
-- post defs: ["y2", "y3", "y1"]

----

define `y1`
define `y3`
use `y3`
define `z1`
-- post defs: ["z1", "y3", "y1"]

----

define `y1`
define `y3`
use `y3`
define `z2`
-- post defs: ["z2", "y3", "y1"]

----

define `y1`
define `y3`
use `y3`
define `z3`
-- post defs: ["z3", "y3", "y1"]

----

define `y1`
define `y3`
use `y3`
use `y3`
-- post defs: ["y3", "y1"]

----

define `y1`
define `y3`
use `y3`
use `y1`
-- post defs: ["y3", "y1"]

----

define `y1`
define `y3`
use `y1`
define `x1`
-- post defs: ["x1", "y3", "y1"]

----

define `y1`
define `y3`
use `y1`
define `x2`
-- post defs: ["x2", "y3", "y1"]

----

define `y1`
define `y3`
use `y1`
define `x3`
-- post defs: ["x3", "y3", "y1"]

----

define `y1`
define `y3`
use `y1`
define `y2`
-- post defs: ["y2", "y3", "y1"]

----

define `y1`
define `y3`
use `y1`
define `z1`
-- post defs: ["z1", "y3", "y1"]

----

define `y1`
define `y3`
use `y1`
define `z2`
-- post defs: ["z2", "y3", "y1"]

----

define `y1`
define `y3`
use `y1`
define `z3`
-- post defs: ["z3", "y3", "y1"]

----

define `y1`
define `y3`
use `y1`
use `y3`
-- post defs: ["y3", "y1"]

----

define `y1`
define `y3`
use `y1`
use `y1`
-- post defs: ["y3", "y1"]

----

define `y1`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "y1"]

----

define `y1`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "y1"]

----

define `y1`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "y1"]

----

define `y1`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "y1"]

----

define `y1`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "y1"]

----

define `y1`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "y1"]

----

define `y1`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "y1"]

----

define `y1`
define `z1`
use `z1`
-- post defs: ["z1", "y1"]

----

define `y1`
define `z1`
use `y1`
-- post defs: ["z1", "y1"]

----

define `y1`
define `z1`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "z1", "y1"]

----

define `y1`
define `z1`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "z1", "y1"]

----

define `y1`
define `z1`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "z1", "y1"]

----

define `y1`
define `z1`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "z1", "y1"]

----

define `y1`
define `z1`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "z1", "y1"]

----

define `y1`
define `z1`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "z1", "y1"]

----

define `y1`
define `z1`
define `x1`
use `x1`
-- post defs: ["x1", "z1", "y1"]

----

define `y1`
define `z1`
define `x1`
use `z1`
-- post defs: ["x1", "z1", "y1"]

----

define `y1`
define `z1`
define `x1`
use `y1`
-- post defs: ["x1", "z1", "y1"]

----

define `y1`
define `z1`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "z1", "y1"]

----

define `y1`
define `z1`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "z1", "y1"]

----

define `y1`
define `z1`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "z1", "y1"]

----

define `y1`
define `z1`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "z1", "y1"]

----

define `y1`
define `z1`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "z1", "y1"]

----

define `y1`
define `z1`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "z1", "y1"]

----

define `y1`
define `z1`
define `x2`
use `x2`
-- post defs: ["x2", "z1", "y1"]

----

define `y1`
define `z1`
define `x2`
use `z1`
-- post defs: ["x2", "z1", "y1"]

----

define `y1`
define `z1`
define `x2`
use `y1`
-- post defs: ["x2", "z1", "y1"]

----

define `y1`
define `z1`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "z1", "y1"]

----

define `y1`
define `z1`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "z1", "y1"]

----

define `y1`
define `z1`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "z1", "y1"]

----

define `y1`
define `z1`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "z1", "y1"]

----

define `y1`
define `z1`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "z1", "y1"]

----

define `y1`
define `z1`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "z1", "y1"]

----

define `y1`
define `z1`
define `x3`
use `x3`
-- post defs: ["x3", "z1", "y1"]

----

define `y1`
define `z1`
define `x3`
use `z1`
-- post defs: ["x3", "z1", "y1"]

----

define `y1`
define `z1`
define `x3`
use `y1`
-- post defs: ["x3", "z1", "y1"]

----

define `y1`
define `z1`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "z1", "y1"]

----

define `y1`
define `z1`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "z1", "y1"]

----

define `y1`
define `z1`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "z1", "y1"]

----

define `y1`
define `z1`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "z1", "y1"]

----

define `y1`
define `z1`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "z1", "y1"]

----

define `y1`
define `z1`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "z1", "y1"]

----

define `y1`
define `z1`
define `y2`
use `y2`
-- post defs: ["y2", "z1", "y1"]

----

define `y1`
define `z1`
define `y2`
use `z1`
-- post defs: ["y2", "z1", "y1"]

----

define `y1`
define `z1`
define `y2`
use `y1`
-- post defs: ["y2", "z1", "y1"]

----

define `y1`
define `z1`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "z1", "y1"]

----

define `y1`
define `z1`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "z1", "y1"]

----

define `y1`
define `z1`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "z1", "y1"]

----

define `y1`
define `z1`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "z1", "y1"]

----

define `y1`
define `z1`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "z1", "y1"]

----

define `y1`
define `z1`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "z1", "y1"]

----

define `y1`
define `z1`
define `y3`
use `y3`
-- post defs: ["y3", "z1", "y1"]

----

define `y1`
define `z1`
define `y3`
use `z1`
-- post defs: ["y3", "z1", "y1"]

----

define `y1`
define `z1`
define `y3`
use `y1`
-- post defs: ["y3", "z1", "y1"]

----

define `y1`
define `z1`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "z1", "y1"]

----

define `y1`
define `z1`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "z1", "y1"]

----

define `y1`
define `z1`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "z1", "y1"]

----

define `y1`
define `z1`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "z1", "y1"]

----

define `y1`
define `z1`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "z1", "y1"]

----

define `y1`
define `z1`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "z1", "y1"]

----

define `y1`
define `z1`
define `z2`
use `z2`
-- post defs: ["z2", "z1", "y1"]

----

define `y1`
define `z1`
define `z2`
use `z1`
-- post defs: ["z2", "z1", "y1"]

----

define `y1`
define `z1`
define `z2`
use `y1`
-- post defs: ["z2", "z1", "y1"]

----

define `y1`
define `z1`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "z1", "y1"]

----

define `y1`
define `z1`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "z1", "y1"]

----

define `y1`
define `z1`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "z1", "y1"]

----

define `y1`
define `z1`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "z1", "y1"]

----

define `y1`
define `z1`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "z1", "y1"]

----

define `y1`
define `z1`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "z1", "y1"]

----

define `y1`
define `z1`
define `z3`
use `z3`
-- post defs: ["z3", "z1", "y1"]

----

define `y1`
define `z1`
define `z3`
use `z1`
-- post defs: ["z3", "z1", "y1"]

----

define `y1`
define `z1`
define `z3`
use `y1`
-- post defs: ["z3", "z1", "y1"]

----

define `y1`
define `z1`
use `z1`
define `x1`
-- post defs: ["x1", "z1", "y1"]

----

define `y1`
define `z1`
use `z1`
define `x2`
-- post defs: ["x2", "z1", "y1"]

----

define `y1`
define `z1`
use `z1`
define `x3`
-- post defs: ["x3", "z1", "y1"]

----

define `y1`
define `z1`
use `z1`
define `y2`
-- post defs: ["y2", "z1", "y1"]

----

define `y1`
define `z1`
use `z1`
define `y3`
-- post defs: ["y3", "z1", "y1"]

----

define `y1`
define `z1`
use `z1`
define `z2`
-- post defs: ["z2", "z1", "y1"]

----

define `y1`
define `z1`
use `z1`
define `z3`
-- post defs: ["z3", "z1", "y1"]

----

define `y1`
define `z1`
use `z1`
use `z1`
-- post defs: ["z1", "y1"]

----

define `y1`
define `z1`
use `z1`
use `y1`
-- post defs: ["z1", "y1"]

----

define `y1`
define `z1`
use `y1`
define `x1`
-- post defs: ["x1", "z1", "y1"]

----

define `y1`
define `z1`
use `y1`
define `x2`
-- post defs: ["x2", "z1", "y1"]

----

define `y1`
define `z1`
use `y1`
define `x3`
-- post defs: ["x3", "z1", "y1"]

----

define `y1`
define `z1`
use `y1`
define `y2`
-- post defs: ["y2", "z1", "y1"]

----

define `y1`
define `z1`
use `y1`
define `y3`
-- post defs: ["y3", "z1", "y1"]

----

define `y1`
define `z1`
use `y1`
define `z2`
-- post defs: ["z2", "z1", "y1"]

----

define `y1`
define `z1`
use `y1`
define `z3`
-- post defs: ["z3", "z1", "y1"]

----

define `y1`
define `z1`
use `y1`
use `z1`
-- post defs: ["z1", "y1"]

----

define `y1`
define `z1`
use `y1`
use `y1`
-- post defs: ["z1", "y1"]

----

define `y1`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "y1"]

----

define `y1`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "y1"]

----

define `y1`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "y1"]

----

define `y1`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "y1"]

----

define `y1`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "y1"]

----

define `y1`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "y1"]

----

define `y1`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "y1"]

----

define `y1`
define `z2`
use `z2`
-- post defs: ["z2", "y1"]

----

define `y1`
define `z2`
use `y1`
-- post defs: ["z2", "y1"]

----

define `y1`
define `z2`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "z2", "y1"]

----

define `y1`
define `z2`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "z2", "y1"]

----

define `y1`
define `z2`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "z2", "y1"]

----

define `y1`
define `z2`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "z2", "y1"]

----

define `y1`
define `z2`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "z2", "y1"]

----

define `y1`
define `z2`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "z2", "y1"]

----

define `y1`
define `z2`
define `x1`
use `x1`
-- post defs: ["x1", "z2", "y1"]

----

define `y1`
define `z2`
define `x1`
use `z2`
-- post defs: ["x1", "z2", "y1"]

----

define `y1`
define `z2`
define `x1`
use `y1`
-- post defs: ["x1", "z2", "y1"]

----

define `y1`
define `z2`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "z2", "y1"]

----

define `y1`
define `z2`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "z2", "y1"]

----

define `y1`
define `z2`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "z2", "y1"]

----

define `y1`
define `z2`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "z2", "y1"]

----

define `y1`
define `z2`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "z2", "y1"]

----

define `y1`
define `z2`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "z2", "y1"]

----

define `y1`
define `z2`
define `x2`
use `x2`
-- post defs: ["x2", "z2", "y1"]

----

define `y1`
define `z2`
define `x2`
use `z2`
-- post defs: ["x2", "z2", "y1"]

----

define `y1`
define `z2`
define `x2`
use `y1`
-- post defs: ["x2", "z2", "y1"]

----

define `y1`
define `z2`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "z2", "y1"]

----

define `y1`
define `z2`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "z2", "y1"]

----

define `y1`
define `z2`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "z2", "y1"]

----

define `y1`
define `z2`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "z2", "y1"]

----

define `y1`
define `z2`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "z2", "y1"]

----

define `y1`
define `z2`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "z2", "y1"]

----

define `y1`
define `z2`
define `x3`
use `x3`
-- post defs: ["x3", "z2", "y1"]

----

define `y1`
define `z2`
define `x3`
use `z2`
-- post defs: ["x3", "z2", "y1"]

----

define `y1`
define `z2`
define `x3`
use `y1`
-- post defs: ["x3", "z2", "y1"]

----

define `y1`
define `z2`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "z2", "y1"]

----

define `y1`
define `z2`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "z2", "y1"]

----

define `y1`
define `z2`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "z2", "y1"]

----

define `y1`
define `z2`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "z2", "y1"]

----

define `y1`
define `z2`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "z2", "y1"]

----

define `y1`
define `z2`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "z2", "y1"]

----

define `y1`
define `z2`
define `y2`
use `y2`
-- post defs: ["y2", "z2", "y1"]

----

define `y1`
define `z2`
define `y2`
use `z2`
-- post defs: ["y2", "z2", "y1"]

----

define `y1`
define `z2`
define `y2`
use `y1`
-- post defs: ["y2", "z2", "y1"]

----

define `y1`
define `z2`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "z2", "y1"]

----

define `y1`
define `z2`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "z2", "y1"]

----

define `y1`
define `z2`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "z2", "y1"]

----

define `y1`
define `z2`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "z2", "y1"]

----

define `y1`
define `z2`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "z2", "y1"]

----

define `y1`
define `z2`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "z2", "y1"]

----

define `y1`
define `z2`
define `y3`
use `y3`
-- post defs: ["y3", "z2", "y1"]

----

define `y1`
define `z2`
define `y3`
use `z2`
-- post defs: ["y3", "z2", "y1"]

----

define `y1`
define `z2`
define `y3`
use `y1`
-- post defs: ["y3", "z2", "y1"]

----

define `y1`
define `z2`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "z2", "y1"]

----

define `y1`
define `z2`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "z2", "y1"]

----

define `y1`
define `z2`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "z2", "y1"]

----

define `y1`
define `z2`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "z2", "y1"]

----

define `y1`
define `z2`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "z2", "y1"]

----

define `y1`
define `z2`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "z2", "y1"]

----

define `y1`
define `z2`
define `z1`
use `z1`
-- post defs: ["z1", "z2", "y1"]

----

define `y1`
define `z2`
define `z1`
use `z2`
-- post defs: ["z1", "z2", "y1"]

----

define `y1`
define `z2`
define `z1`
use `y1`
-- post defs: ["z1", "z2", "y1"]

----

define `y1`
define `z2`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "z2", "y1"]

----

define `y1`
define `z2`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "z2", "y1"]

----

define `y1`
define `z2`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "z2", "y1"]

----

define `y1`
define `z2`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "z2", "y1"]

----

define `y1`
define `z2`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "z2", "y1"]

----

define `y1`
define `z2`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "z2", "y1"]

----

define `y1`
define `z2`
define `z3`
use `z3`
-- post defs: ["z3", "z2", "y1"]

----

define `y1`
define `z2`
define `z3`
use `z2`
-- post defs: ["z3", "z2", "y1"]

----

define `y1`
define `z2`
define `z3`
use `y1`
-- post defs: ["z3", "z2", "y1"]

----

define `y1`
define `z2`
use `z2`
define `x1`
-- post defs: ["x1", "z2", "y1"]

----

define `y1`
define `z2`
use `z2`
define `x2`
-- post defs: ["x2", "z2", "y1"]

----

define `y1`
define `z2`
use `z2`
define `x3`
-- post defs: ["x3", "z2", "y1"]

----

define `y1`
define `z2`
use `z2`
define `y2`
-- post defs: ["y2", "z2", "y1"]

----

define `y1`
define `z2`
use `z2`
define `y3`
-- post defs: ["y3", "z2", "y1"]

----

define `y1`
define `z2`
use `z2`
define `z1`
-- post defs: ["z1", "z2", "y1"]

----

define `y1`
define `z2`
use `z2`
define `z3`
-- post defs: ["z3", "z2", "y1"]

----

define `y1`
define `z2`
use `z2`
use `z2`
-- post defs: ["z2", "y1"]

----

define `y1`
define `z2`
use `z2`
use `y1`
-- post defs: ["z2", "y1"]

----

define `y1`
define `z2`
use `y1`
define `x1`
-- post defs: ["x1", "z2", "y1"]

----

define `y1`
define `z2`
use `y1`
define `x2`
-- post defs: ["x2", "z2", "y1"]

----

define `y1`
define `z2`
use `y1`
define `x3`
-- post defs: ["x3", "z2", "y1"]

----

define `y1`
define `z2`
use `y1`
define `y2`
-- post defs: ["y2", "z2", "y1"]

----

define `y1`
define `z2`
use `y1`
define `y3`
-- post defs: ["y3", "z2", "y1"]

----

define `y1`
define `z2`
use `y1`
define `z1`
-- post defs: ["z1", "z2", "y1"]

----

define `y1`
define `z2`
use `y1`
define `z3`
-- post defs: ["z3", "z2", "y1"]

----

define `y1`
define `z2`
use `y1`
use `z2`
-- post defs: ["z2", "y1"]

----

define `y1`
define `z2`
use `y1`
use `y1`
-- post defs: ["z2", "y1"]

----

define `y1`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "y1"]

----

define `y1`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "y1"]

----

define `y1`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "y1"]

----

define `y1`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "y1"]

----

define `y1`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "y1"]

----

define `y1`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "y1"]

----

define `y1`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "y1"]

----

define `y1`
define `z3`
use `z3`
-- post defs: ["z3", "y1"]

----

define `y1`
define `z3`
use `y1`
-- post defs: ["z3", "y1"]

----

define `y1`
define `z3`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "z3", "y1"]

----

define `y1`
define `z3`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "z3", "y1"]

----

define `y1`
define `z3`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "z3", "y1"]

----

define `y1`
define `z3`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "z3", "y1"]

----

define `y1`
define `z3`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "z3", "y1"]

----

define `y1`
define `z3`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "z3", "y1"]

----

define `y1`
define `z3`
define `x1`
use `x1`
-- post defs: ["x1", "z3", "y1"]

----

define `y1`
define `z3`
define `x1`
use `z3`
-- post defs: ["x1", "z3", "y1"]

----

define `y1`
define `z3`
define `x1`
use `y1`
-- post defs: ["x1", "z3", "y1"]

----

define `y1`
define `z3`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "z3", "y1"]

----

define `y1`
define `z3`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "z3", "y1"]

----

define `y1`
define `z3`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "z3", "y1"]

----

define `y1`
define `z3`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "z3", "y1"]

----

define `y1`
define `z3`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "z3", "y1"]

----

define `y1`
define `z3`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "z3", "y1"]

----

define `y1`
define `z3`
define `x2`
use `x2`
-- post defs: ["x2", "z3", "y1"]

----

define `y1`
define `z3`
define `x2`
use `z3`
-- post defs: ["x2", "z3", "y1"]

----

define `y1`
define `z3`
define `x2`
use `y1`
-- post defs: ["x2", "z3", "y1"]

----

define `y1`
define `z3`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "z3", "y1"]

----

define `y1`
define `z3`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "z3", "y1"]

----

define `y1`
define `z3`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "z3", "y1"]

----

define `y1`
define `z3`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "z3", "y1"]

----

define `y1`
define `z3`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "z3", "y1"]

----

define `y1`
define `z3`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "z3", "y1"]

----

define `y1`
define `z3`
define `x3`
use `x3`
-- post defs: ["x3", "z3", "y1"]

----

define `y1`
define `z3`
define `x3`
use `z3`
-- post defs: ["x3", "z3", "y1"]

----

define `y1`
define `z3`
define `x3`
use `y1`
-- post defs: ["x3", "z3", "y1"]

----

define `y1`
define `z3`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "z3", "y1"]

----

define `y1`
define `z3`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "z3", "y1"]

----

define `y1`
define `z3`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "z3", "y1"]

----

define `y1`
define `z3`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "z3", "y1"]

----

define `y1`
define `z3`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "z3", "y1"]

----

define `y1`
define `z3`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "z3", "y1"]

----

define `y1`
define `z3`
define `y2`
use `y2`
-- post defs: ["y2", "z3", "y1"]

----

define `y1`
define `z3`
define `y2`
use `z3`
-- post defs: ["y2", "z3", "y1"]

----

define `y1`
define `z3`
define `y2`
use `y1`
-- post defs: ["y2", "z3", "y1"]

----

define `y1`
define `z3`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "z3", "y1"]

----

define `y1`
define `z3`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "z3", "y1"]

----

define `y1`
define `z3`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "z3", "y1"]

----

define `y1`
define `z3`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "z3", "y1"]

----

define `y1`
define `z3`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "z3", "y1"]

----

define `y1`
define `z3`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "z3", "y1"]

----

define `y1`
define `z3`
define `y3`
use `y3`
-- post defs: ["y3", "z3", "y1"]

----

define `y1`
define `z3`
define `y3`
use `z3`
-- post defs: ["y3", "z3", "y1"]

----

define `y1`
define `z3`
define `y3`
use `y1`
-- post defs: ["y3", "z3", "y1"]

----

define `y1`
define `z3`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "z3", "y1"]

----

define `y1`
define `z3`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "z3", "y1"]

----

define `y1`
define `z3`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "z3", "y1"]

----

define `y1`
define `z3`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "z3", "y1"]

----

define `y1`
define `z3`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "z3", "y1"]

----

define `y1`
define `z3`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "z3", "y1"]

----

define `y1`
define `z3`
define `z1`
use `z1`
-- post defs: ["z1", "z3", "y1"]

----

define `y1`
define `z3`
define `z1`
use `z3`
-- post defs: ["z1", "z3", "y1"]

----

define `y1`
define `z3`
define `z1`
use `y1`
-- post defs: ["z1", "z3", "y1"]

----

define `y1`
define `z3`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "z3", "y1"]

----

define `y1`
define `z3`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "z3", "y1"]

----

define `y1`
define `z3`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "z3", "y1"]

----

define `y1`
define `z3`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "z3", "y1"]

----

define `y1`
define `z3`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "z3", "y1"]

----

define `y1`
define `z3`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "z3", "y1"]

----

define `y1`
define `z3`
define `z2`
use `z2`
-- post defs: ["z2", "z3", "y1"]

----

define `y1`
define `z3`
define `z2`
use `z3`
-- post defs: ["z2", "z3", "y1"]

----

define `y1`
define `z3`
define `z2`
use `y1`
-- post defs: ["z2", "z3", "y1"]

----

define `y1`
define `z3`
use `z3`
define `x1`
-- post defs: ["x1", "z3", "y1"]

----

define `y1`
define `z3`
use `z3`
define `x2`
-- post defs: ["x2", "z3", "y1"]

----

define `y1`
define `z3`
use `z3`
define `x3`
-- post defs: ["x3", "z3", "y1"]

----

define `y1`
define `z3`
use `z3`
define `y2`
-- post defs: ["y2", "z3", "y1"]

----

define `y1`
define `z3`
use `z3`
define `y3`
-- post defs: ["y3", "z3", "y1"]

----

define `y1`
define `z3`
use `z3`
define `z1`
-- post defs: ["z1", "z3", "y1"]

----

define `y1`
define `z3`
use `z3`
define `z2`
-- post defs: ["z2", "z3", "y1"]

----

define `y1`
define `z3`
use `z3`
use `z3`
-- post defs: ["z3", "y1"]

----

define `y1`
define `z3`
use `z3`
use `y1`
-- post defs: ["z3", "y1"]

----

define `y1`
define `z3`
use `y1`
define `x1`
-- post defs: ["x1", "z3", "y1"]

----

define `y1`
define `z3`
use `y1`
define `x2`
-- post defs: ["x2", "z3", "y1"]

----

define `y1`
define `z3`
use `y1`
define `x3`
-- post defs: ["x3", "z3", "y1"]

----

define `y1`
define `z3`
use `y1`
define `y2`
-- post defs: ["y2", "z3", "y1"]

----

define `y1`
define `z3`
use `y1`
define `y3`
-- post defs: ["y3", "z3", "y1"]

----

define `y1`
define `z3`
use `y1`
define `z1`
-- post defs: ["z1", "z3", "y1"]

----

define `y1`
define `z3`
use `y1`
define `z2`
-- post defs: ["z2", "z3", "y1"]

----

define `y1`
define `z3`
use `y1`
use `z3`
-- post defs: ["z3", "y1"]

----

define `y1`
define `z3`
use `y1`
use `y1`
-- post defs: ["z3", "y1"]

----

define `y1`
use `y1`
define `x1`
-- post defs: ["x1", "y1"]

----

define `y1`
use `y1`
define `x2`
-- post defs: ["x2", "y1"]

----

define `y1`
use `y1`
define `x3`
-- post defs: ["x3", "y1"]

----

define `y1`
use `y1`
define `y2`
-- post defs: ["y2", "y1"]

----

define `y1`
use `y1`
define `y3`
-- post defs: ["y3", "y1"]

----

define `y1`
use `y1`
define `z1`
-- post defs: ["z1", "y1"]

----

define `y1`
use `y1`
define `z2`
-- post defs: ["z2", "y1"]

----

define `y1`
use `y1`
define `z3`
-- post defs: ["z3", "y1"]

----

define `y1`
use `y1`
use `y1`
-- post defs: ["y1"]

----

define `y1`
use `y1`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "y1"]

----

define `y1`
use `y1`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "y1"]

----

define `y1`
use `y1`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "y1"]

----

define `y1`
use `y1`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "y1"]

----

define `y1`
use `y1`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "y1"]

----

define `y1`
use `y1`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "y1"]

----

define `y1`
use `y1`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "y1"]

----

define `y1`
use `y1`
define `x1`
use `x1`
-- post defs: ["x1", "y1"]

----

define `y1`
use `y1`
define `x1`
use `y1`
-- post defs: ["x1", "y1"]

----

define `y1`
use `y1`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "y1"]

----

define `y1`
use `y1`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "y1"]

----

define `y1`
use `y1`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "y1"]

----

define `y1`
use `y1`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "y1"]

----

define `y1`
use `y1`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "y1"]

----

define `y1`
use `y1`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "y1"]

----

define `y1`
use `y1`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "y1"]

----

define `y1`
use `y1`
define `x2`
use `x2`
-- post defs: ["x2", "y1"]

----

define `y1`
use `y1`
define `x2`
use `y1`
-- post defs: ["x2", "y1"]

----

define `y1`
use `y1`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "y1"]

----

define `y1`
use `y1`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "y1"]

----

define `y1`
use `y1`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "y1"]

----

define `y1`
use `y1`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "y1"]

----

define `y1`
use `y1`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "y1"]

----

define `y1`
use `y1`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "y1"]

----

define `y1`
use `y1`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "y1"]

----

define `y1`
use `y1`
define `x3`
use `x3`
-- post defs: ["x3", "y1"]

----

define `y1`
use `y1`
define `x3`
use `y1`
-- post defs: ["x3", "y1"]

----

define `y1`
use `y1`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "y1"]

----

define `y1`
use `y1`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "y1"]

----

define `y1`
use `y1`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "y1"]

----

define `y1`
use `y1`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "y1"]

----

define `y1`
use `y1`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "y1"]

----

define `y1`
use `y1`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "y1"]

----

define `y1`
use `y1`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "y1"]

----

define `y1`
use `y1`
define `y2`
use `y2`
-- post defs: ["y2", "y1"]

----

define `y1`
use `y1`
define `y2`
use `y1`
-- post defs: ["y2", "y1"]

----

define `y1`
use `y1`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "y1"]

----

define `y1`
use `y1`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "y1"]

----

define `y1`
use `y1`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "y1"]

----

define `y1`
use `y1`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "y1"]

----

define `y1`
use `y1`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "y1"]

----

define `y1`
use `y1`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "y1"]

----

define `y1`
use `y1`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "y1"]

----

define `y1`
use `y1`
define `y3`
use `y3`
-- post defs: ["y3", "y1"]

----

define `y1`
use `y1`
define `y3`
use `y1`
-- post defs: ["y3", "y1"]

----

define `y1`
use `y1`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "y1"]

----

define `y1`
use `y1`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "y1"]

----

define `y1`
use `y1`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "y1"]

----

define `y1`
use `y1`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "y1"]

----

define `y1`
use `y1`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "y1"]

----

define `y1`
use `y1`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "y1"]

----

define `y1`
use `y1`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "y1"]

----

define `y1`
use `y1`
define `z1`
use `z1`
-- post defs: ["z1", "y1"]

----

define `y1`
use `y1`
define `z1`
use `y1`
-- post defs: ["z1", "y1"]

----

define `y1`
use `y1`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "y1"]

----

define `y1`
use `y1`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "y1"]

----

define `y1`
use `y1`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "y1"]

----

define `y1`
use `y1`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "y1"]

----

define `y1`
use `y1`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "y1"]

----

define `y1`
use `y1`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "y1"]

----

define `y1`
use `y1`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "y1"]

----

define `y1`
use `y1`
define `z2`
use `z2`
-- post defs: ["z2", "y1"]

----

define `y1`
use `y1`
define `z2`
use `y1`
-- post defs: ["z2", "y1"]

----

define `y1`
use `y1`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "y1"]

----

define `y1`
use `y1`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "y1"]

----

define `y1`
use `y1`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "y1"]

----

define `y1`
use `y1`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "y1"]

----

define `y1`
use `y1`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "y1"]

----

define `y1`
use `y1`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "y1"]

----

define `y1`
use `y1`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "y1"]

----

define `y1`
use `y1`
define `z3`
use `z3`
-- post defs: ["z3", "y1"]

----

define `y1`
use `y1`
define `z3`
use `y1`
-- post defs: ["z3", "y1"]

----

define `y1`
use `y1`
use `y1`
define `x1`
-- post defs: ["x1", "y1"]

----

define `y1`
use `y1`
use `y1`
define `x2`
-- post defs: ["x2", "y1"]

----

define `y1`
use `y1`
use `y1`
define `x3`
-- post defs: ["x3", "y1"]

----

define `y1`
use `y1`
use `y1`
define `y2`
-- post defs: ["y2", "y1"]

----

define `y1`
use `y1`
use `y1`
define `y3`
-- post defs: ["y3", "y1"]

----

define `y1`
use `y1`
use `y1`
define `z1`
-- post defs: ["z1", "y1"]

----

define `y1`
use `y1`
use `y1`
define `z2`
-- post defs: ["z2", "y1"]

----

define `y1`
use `y1`
use `y1`
define `z3`
-- post defs: ["z3", "y1"]

----

define `y1`
use `y1`
use `y1`
use `y1`
-- post defs: ["y1"]

----

define `y2`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "y2"]

----

define `y2`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "y2"]

----

define `y2`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "y2"]

----

define `y2`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "y2"]

----

define `y2`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "y2"]

----

define `y2`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "y2"]

----

define `y2`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "y2"]

----

define `y2`
define `x1`
use `x1`
-- post defs: ["x1", "y2"]

----

define `y2`
define `x1`
use `y2`
-- post defs: ["x1", "y2"]

----

define `y2`
define `x1`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "x1", "y2"]

----

define `y2`
define `x1`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "x1", "y2"]

----

define `y2`
define `x1`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "x1", "y2"]

----

define `y2`
define `x1`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "x1", "y2"]

----

define `y2`
define `x1`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "x1", "y2"]

----

define `y2`
define `x1`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "x1", "y2"]

----

define `y2`
define `x1`
define `x2`
use `x2`
-- post defs: ["x2", "x1", "y2"]

----

define `y2`
define `x1`
define `x2`
use `x1`
-- post defs: ["x2", "x1", "y2"]

----

define `y2`
define `x1`
define `x2`
use `y2`
-- post defs: ["x2", "x1", "y2"]

----

define `y2`
define `x1`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "x1", "y2"]

----

define `y2`
define `x1`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "x1", "y2"]

----

define `y2`
define `x1`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "x1", "y2"]

----

define `y2`
define `x1`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "x1", "y2"]

----

define `y2`
define `x1`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "x1", "y2"]

----

define `y2`
define `x1`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "x1", "y2"]

----

define `y2`
define `x1`
define `x3`
use `x3`
-- post defs: ["x3", "x1", "y2"]

----

define `y2`
define `x1`
define `x3`
use `x1`
-- post defs: ["x3", "x1", "y2"]

----

define `y2`
define `x1`
define `x3`
use `y2`
-- post defs: ["x3", "x1", "y2"]

----

define `y2`
define `x1`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "x1", "y2"]

----

define `y2`
define `x1`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "x1", "y2"]

----

define `y2`
define `x1`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "x1", "y2"]

----

define `y2`
define `x1`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "x1", "y2"]

----

define `y2`
define `x1`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "x1", "y2"]

----

define `y2`
define `x1`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "x1", "y2"]

----

define `y2`
define `x1`
define `y1`
use `y1`
-- post defs: ["y1", "x1", "y2"]

----

define `y2`
define `x1`
define `y1`
use `x1`
-- post defs: ["y1", "x1", "y2"]

----

define `y2`
define `x1`
define `y1`
use `y2`
-- post defs: ["y1", "x1", "y2"]

----

define `y2`
define `x1`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "x1", "y2"]

----

define `y2`
define `x1`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "x1", "y2"]

----

define `y2`
define `x1`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "x1", "y2"]

----

define `y2`
define `x1`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "x1", "y2"]

----

define `y2`
define `x1`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "x1", "y2"]

----

define `y2`
define `x1`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "x1", "y2"]

----

define `y2`
define `x1`
define `y3`
use `y3`
-- post defs: ["y3", "x1", "y2"]

----

define `y2`
define `x1`
define `y3`
use `x1`
-- post defs: ["y3", "x1", "y2"]

----

define `y2`
define `x1`
define `y3`
use `y2`
-- post defs: ["y3", "x1", "y2"]

----

define `y2`
define `x1`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "x1", "y2"]

----

define `y2`
define `x1`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "x1", "y2"]

----

define `y2`
define `x1`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "x1", "y2"]

----

define `y2`
define `x1`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "x1", "y2"]

----

define `y2`
define `x1`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "x1", "y2"]

----

define `y2`
define `x1`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "x1", "y2"]

----

define `y2`
define `x1`
define `z1`
use `z1`
-- post defs: ["z1", "x1", "y2"]

----

define `y2`
define `x1`
define `z1`
use `x1`
-- post defs: ["z1", "x1", "y2"]

----

define `y2`
define `x1`
define `z1`
use `y2`
-- post defs: ["z1", "x1", "y2"]

----

define `y2`
define `x1`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "x1", "y2"]

----

define `y2`
define `x1`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "x1", "y2"]

----

define `y2`
define `x1`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "x1", "y2"]

----

define `y2`
define `x1`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "x1", "y2"]

----

define `y2`
define `x1`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "x1", "y2"]

----

define `y2`
define `x1`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "x1", "y2"]

----

define `y2`
define `x1`
define `z2`
use `z2`
-- post defs: ["z2", "x1", "y2"]

----

define `y2`
define `x1`
define `z2`
use `x1`
-- post defs: ["z2", "x1", "y2"]

----

define `y2`
define `x1`
define `z2`
use `y2`
-- post defs: ["z2", "x1", "y2"]

----

define `y2`
define `x1`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "x1", "y2"]

----

define `y2`
define `x1`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "x1", "y2"]

----

define `y2`
define `x1`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "x1", "y2"]

----

define `y2`
define `x1`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "x1", "y2"]

----

define `y2`
define `x1`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "x1", "y2"]

----

define `y2`
define `x1`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "x1", "y2"]

----

define `y2`
define `x1`
define `z3`
use `z3`
-- post defs: ["z3", "x1", "y2"]

----

define `y2`
define `x1`
define `z3`
use `x1`
-- post defs: ["z3", "x1", "y2"]

----

define `y2`
define `x1`
define `z3`
use `y2`
-- post defs: ["z3", "x1", "y2"]

----

define `y2`
define `x1`
use `x1`
define `x2`
-- post defs: ["x2", "x1", "y2"]

----

define `y2`
define `x1`
use `x1`
define `x3`
-- post defs: ["x3", "x1", "y2"]

----

define `y2`
define `x1`
use `x1`
define `y1`
-- post defs: ["y1", "x1", "y2"]

----

define `y2`
define `x1`
use `x1`
define `y3`
-- post defs: ["y3", "x1", "y2"]

----

define `y2`
define `x1`
use `x1`
define `z1`
-- post defs: ["z1", "x1", "y2"]

----

define `y2`
define `x1`
use `x1`
define `z2`
-- post defs: ["z2", "x1", "y2"]

----

define `y2`
define `x1`
use `x1`
define `z3`
-- post defs: ["z3", "x1", "y2"]

----

define `y2`
define `x1`
use `x1`
use `x1`
-- post defs: ["x1", "y2"]

----

define `y2`
define `x1`
use `x1`
use `y2`
-- post defs: ["x1", "y2"]

----

define `y2`
define `x1`
use `y2`
define `x2`
-- post defs: ["x2", "x1", "y2"]

----

define `y2`
define `x1`
use `y2`
define `x3`
-- post defs: ["x3", "x1", "y2"]

----

define `y2`
define `x1`
use `y2`
define `y1`
-- post defs: ["y1", "x1", "y2"]

----

define `y2`
define `x1`
use `y2`
define `y3`
-- post defs: ["y3", "x1", "y2"]

----

define `y2`
define `x1`
use `y2`
define `z1`
-- post defs: ["z1", "x1", "y2"]

----

define `y2`
define `x1`
use `y2`
define `z2`
-- post defs: ["z2", "x1", "y2"]

----

define `y2`
define `x1`
use `y2`
define `z3`
-- post defs: ["z3", "x1", "y2"]

----

define `y2`
define `x1`
use `y2`
use `x1`
-- post defs: ["x1", "y2"]

----

define `y2`
define `x1`
use `y2`
use `y2`
-- post defs: ["x1", "y2"]

----

define `y2`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "y2"]

----

define `y2`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "y2"]

----

define `y2`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "y2"]

----

define `y2`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "y2"]

----

define `y2`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "y2"]

----

define `y2`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "y2"]

----

define `y2`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "y2"]

----

define `y2`
define `x2`
use `x2`
-- post defs: ["x2", "y2"]

----

define `y2`
define `x2`
use `y2`
-- post defs: ["x2", "y2"]

----

define `y2`
define `x2`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "x2", "y2"]

----

define `y2`
define `x2`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "x2", "y2"]

----

define `y2`
define `x2`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "x2", "y2"]

----

define `y2`
define `x2`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "x2", "y2"]

----

define `y2`
define `x2`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "x2", "y2"]

----

define `y2`
define `x2`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "x2", "y2"]

----

define `y2`
define `x2`
define `x1`
use `x1`
-- post defs: ["x1", "x2", "y2"]

----

define `y2`
define `x2`
define `x1`
use `x2`
-- post defs: ["x1", "x2", "y2"]

----

define `y2`
define `x2`
define `x1`
use `y2`
-- post defs: ["x1", "x2", "y2"]

----

define `y2`
define `x2`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "x2", "y2"]

----

define `y2`
define `x2`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "x2", "y2"]

----

define `y2`
define `x2`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "x2", "y2"]

----

define `y2`
define `x2`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "x2", "y2"]

----

define `y2`
define `x2`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "x2", "y2"]

----

define `y2`
define `x2`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "x2", "y2"]

----

define `y2`
define `x2`
define `x3`
use `x3`
-- post defs: ["x3", "x2", "y2"]

----

define `y2`
define `x2`
define `x3`
use `x2`
-- post defs: ["x3", "x2", "y2"]

----

define `y2`
define `x2`
define `x3`
use `y2`
-- post defs: ["x3", "x2", "y2"]

----

define `y2`
define `x2`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "x2", "y2"]

----

define `y2`
define `x2`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "x2", "y2"]

----

define `y2`
define `x2`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "x2", "y2"]

----

define `y2`
define `x2`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "x2", "y2"]

----

define `y2`
define `x2`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "x2", "y2"]

----

define `y2`
define `x2`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "x2", "y2"]

----

define `y2`
define `x2`
define `y1`
use `y1`
-- post defs: ["y1", "x2", "y2"]

----

define `y2`
define `x2`
define `y1`
use `x2`
-- post defs: ["y1", "x2", "y2"]

----

define `y2`
define `x2`
define `y1`
use `y2`
-- post defs: ["y1", "x2", "y2"]

----

define `y2`
define `x2`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "x2", "y2"]

----

define `y2`
define `x2`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "x2", "y2"]

----

define `y2`
define `x2`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "x2", "y2"]

----

define `y2`
define `x2`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "x2", "y2"]

----

define `y2`
define `x2`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "x2", "y2"]

----

define `y2`
define `x2`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "x2", "y2"]

----

define `y2`
define `x2`
define `y3`
use `y3`
-- post defs: ["y3", "x2", "y2"]

----

define `y2`
define `x2`
define `y3`
use `x2`
-- post defs: ["y3", "x2", "y2"]

----

define `y2`
define `x2`
define `y3`
use `y2`
-- post defs: ["y3", "x2", "y2"]

----

define `y2`
define `x2`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "x2", "y2"]

----

define `y2`
define `x2`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "x2", "y2"]

----

define `y2`
define `x2`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "x2", "y2"]

----

define `y2`
define `x2`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "x2", "y2"]

----

define `y2`
define `x2`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "x2", "y2"]

----

define `y2`
define `x2`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "x2", "y2"]

----

define `y2`
define `x2`
define `z1`
use `z1`
-- post defs: ["z1", "x2", "y2"]

----

define `y2`
define `x2`
define `z1`
use `x2`
-- post defs: ["z1", "x2", "y2"]

----

define `y2`
define `x2`
define `z1`
use `y2`
-- post defs: ["z1", "x2", "y2"]

----

define `y2`
define `x2`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "x2", "y2"]

----

define `y2`
define `x2`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "x2", "y2"]

----

define `y2`
define `x2`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "x2", "y2"]

----

define `y2`
define `x2`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "x2", "y2"]

----

define `y2`
define `x2`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "x2", "y2"]

----

define `y2`
define `x2`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "x2", "y2"]

----

define `y2`
define `x2`
define `z2`
use `z2`
-- post defs: ["z2", "x2", "y2"]

----

define `y2`
define `x2`
define `z2`
use `x2`
-- post defs: ["z2", "x2", "y2"]

----

define `y2`
define `x2`
define `z2`
use `y2`
-- post defs: ["z2", "x2", "y2"]

----

define `y2`
define `x2`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "x2", "y2"]

----

define `y2`
define `x2`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "x2", "y2"]

----

define `y2`
define `x2`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "x2", "y2"]

----

define `y2`
define `x2`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "x2", "y2"]

----

define `y2`
define `x2`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "x2", "y2"]

----

define `y2`
define `x2`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "x2", "y2"]

----

define `y2`
define `x2`
define `z3`
use `z3`
-- post defs: ["z3", "x2", "y2"]

----

define `y2`
define `x2`
define `z3`
use `x2`
-- post defs: ["z3", "x2", "y2"]

----

define `y2`
define `x2`
define `z3`
use `y2`
-- post defs: ["z3", "x2", "y2"]

----

define `y2`
define `x2`
use `x2`
define `x1`
-- post defs: ["x1", "x2", "y2"]

----

define `y2`
define `x2`
use `x2`
define `x3`
-- post defs: ["x3", "x2", "y2"]

----

define `y2`
define `x2`
use `x2`
define `y1`
-- post defs: ["y1", "x2", "y2"]

----

define `y2`
define `x2`
use `x2`
define `y3`
-- post defs: ["y3", "x2", "y2"]

----

define `y2`
define `x2`
use `x2`
define `z1`
-- post defs: ["z1", "x2", "y2"]

----

define `y2`
define `x2`
use `x2`
define `z2`
-- post defs: ["z2", "x2", "y2"]

----

define `y2`
define `x2`
use `x2`
define `z3`
-- post defs: ["z3", "x2", "y2"]

----

define `y2`
define `x2`
use `x2`
use `x2`
-- post defs: ["x2", "y2"]

----

define `y2`
define `x2`
use `x2`
use `y2`
-- post defs: ["x2", "y2"]

----

define `y2`
define `x2`
use `y2`
define `x1`
-- post defs: ["x1", "x2", "y2"]

----

define `y2`
define `x2`
use `y2`
define `x3`
-- post defs: ["x3", "x2", "y2"]

----

define `y2`
define `x2`
use `y2`
define `y1`
-- post defs: ["y1", "x2", "y2"]

----

define `y2`
define `x2`
use `y2`
define `y3`
-- post defs: ["y3", "x2", "y2"]

----

define `y2`
define `x2`
use `y2`
define `z1`
-- post defs: ["z1", "x2", "y2"]

----

define `y2`
define `x2`
use `y2`
define `z2`
-- post defs: ["z2", "x2", "y2"]

----

define `y2`
define `x2`
use `y2`
define `z3`
-- post defs: ["z3", "x2", "y2"]

----

define `y2`
define `x2`
use `y2`
use `x2`
-- post defs: ["x2", "y2"]

----

define `y2`
define `x2`
use `y2`
use `y2`
-- post defs: ["x2", "y2"]

----

define `y2`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "y2"]

----

define `y2`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "y2"]

----

define `y2`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "y2"]

----

define `y2`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "y2"]

----

define `y2`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "y2"]

----

define `y2`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "y2"]

----

define `y2`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "y2"]

----

define `y2`
define `x3`
use `x3`
-- post defs: ["x3", "y2"]

----

define `y2`
define `x3`
use `y2`
-- post defs: ["x3", "y2"]

----

define `y2`
define `x3`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "x3", "y2"]

----

define `y2`
define `x3`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "x3", "y2"]

----

define `y2`
define `x3`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "x3", "y2"]

----

define `y2`
define `x3`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "x3", "y2"]

----

define `y2`
define `x3`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "x3", "y2"]

----

define `y2`
define `x3`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "x3", "y2"]

----

define `y2`
define `x3`
define `x1`
use `x1`
-- post defs: ["x1", "x3", "y2"]

----

define `y2`
define `x3`
define `x1`
use `x3`
-- post defs: ["x1", "x3", "y2"]

----

define `y2`
define `x3`
define `x1`
use `y2`
-- post defs: ["x1", "x3", "y2"]

----

define `y2`
define `x3`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "x3", "y2"]

----

define `y2`
define `x3`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "x3", "y2"]

----

define `y2`
define `x3`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "x3", "y2"]

----

define `y2`
define `x3`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "x3", "y2"]

----

define `y2`
define `x3`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "x3", "y2"]

----

define `y2`
define `x3`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "x3", "y2"]

----

define `y2`
define `x3`
define `x2`
use `x2`
-- post defs: ["x2", "x3", "y2"]

----

define `y2`
define `x3`
define `x2`
use `x3`
-- post defs: ["x2", "x3", "y2"]

----

define `y2`
define `x3`
define `x2`
use `y2`
-- post defs: ["x2", "x3", "y2"]

----

define `y2`
define `x3`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "x3", "y2"]

----

define `y2`
define `x3`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "x3", "y2"]

----

define `y2`
define `x3`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "x3", "y2"]

----

define `y2`
define `x3`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "x3", "y2"]

----

define `y2`
define `x3`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "x3", "y2"]

----

define `y2`
define `x3`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "x3", "y2"]

----

define `y2`
define `x3`
define `y1`
use `y1`
-- post defs: ["y1", "x3", "y2"]

----

define `y2`
define `x3`
define `y1`
use `x3`
-- post defs: ["y1", "x3", "y2"]

----

define `y2`
define `x3`
define `y1`
use `y2`
-- post defs: ["y1", "x3", "y2"]

----

define `y2`
define `x3`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "x3", "y2"]

----

define `y2`
define `x3`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "x3", "y2"]

----

define `y2`
define `x3`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "x3", "y2"]

----

define `y2`
define `x3`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "x3", "y2"]

----

define `y2`
define `x3`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "x3", "y2"]

----

define `y2`
define `x3`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "x3", "y2"]

----

define `y2`
define `x3`
define `y3`
use `y3`
-- post defs: ["y3", "x3", "y2"]

----

define `y2`
define `x3`
define `y3`
use `x3`
-- post defs: ["y3", "x3", "y2"]

----

define `y2`
define `x3`
define `y3`
use `y2`
-- post defs: ["y3", "x3", "y2"]

----

define `y2`
define `x3`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "x3", "y2"]

----

define `y2`
define `x3`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "x3", "y2"]

----

define `y2`
define `x3`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "x3", "y2"]

----

define `y2`
define `x3`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "x3", "y2"]

----

define `y2`
define `x3`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "x3", "y2"]

----

define `y2`
define `x3`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "x3", "y2"]

----

define `y2`
define `x3`
define `z1`
use `z1`
-- post defs: ["z1", "x3", "y2"]

----

define `y2`
define `x3`
define `z1`
use `x3`
-- post defs: ["z1", "x3", "y2"]

----

define `y2`
define `x3`
define `z1`
use `y2`
-- post defs: ["z1", "x3", "y2"]

----

define `y2`
define `x3`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "x3", "y2"]

----

define `y2`
define `x3`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "x3", "y2"]

----

define `y2`
define `x3`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "x3", "y2"]

----

define `y2`
define `x3`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "x3", "y2"]

----

define `y2`
define `x3`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "x3", "y2"]

----

define `y2`
define `x3`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "x3", "y2"]

----

define `y2`
define `x3`
define `z2`
use `z2`
-- post defs: ["z2", "x3", "y2"]

----

define `y2`
define `x3`
define `z2`
use `x3`
-- post defs: ["z2", "x3", "y2"]

----

define `y2`
define `x3`
define `z2`
use `y2`
-- post defs: ["z2", "x3", "y2"]

----

define `y2`
define `x3`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "x3", "y2"]

----

define `y2`
define `x3`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "x3", "y2"]

----

define `y2`
define `x3`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "x3", "y2"]

----

define `y2`
define `x3`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "x3", "y2"]

----

define `y2`
define `x3`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "x3", "y2"]

----

define `y2`
define `x3`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "x3", "y2"]

----

define `y2`
define `x3`
define `z3`
use `z3`
-- post defs: ["z3", "x3", "y2"]

----

define `y2`
define `x3`
define `z3`
use `x3`
-- post defs: ["z3", "x3", "y2"]

----

define `y2`
define `x3`
define `z3`
use `y2`
-- post defs: ["z3", "x3", "y2"]

----

define `y2`
define `x3`
use `x3`
define `x1`
-- post defs: ["x1", "x3", "y2"]

----

define `y2`
define `x3`
use `x3`
define `x2`
-- post defs: ["x2", "x3", "y2"]

----

define `y2`
define `x3`
use `x3`
define `y1`
-- post defs: ["y1", "x3", "y2"]

----

define `y2`
define `x3`
use `x3`
define `y3`
-- post defs: ["y3", "x3", "y2"]

----

define `y2`
define `x3`
use `x3`
define `z1`
-- post defs: ["z1", "x3", "y2"]

----

define `y2`
define `x3`
use `x3`
define `z2`
-- post defs: ["z2", "x3", "y2"]

----

define `y2`
define `x3`
use `x3`
define `z3`
-- post defs: ["z3", "x3", "y2"]

----

define `y2`
define `x3`
use `x3`
use `x3`
-- post defs: ["x3", "y2"]

----

define `y2`
define `x3`
use `x3`
use `y2`
-- post defs: ["x3", "y2"]

----

define `y2`
define `x3`
use `y2`
define `x1`
-- post defs: ["x1", "x3", "y2"]

----

define `y2`
define `x3`
use `y2`
define `x2`
-- post defs: ["x2", "x3", "y2"]

----

define `y2`
define `x3`
use `y2`
define `y1`
-- post defs: ["y1", "x3", "y2"]

----

define `y2`
define `x3`
use `y2`
define `y3`
-- post defs: ["y3", "x3", "y2"]

----

define `y2`
define `x3`
use `y2`
define `z1`
-- post defs: ["z1", "x3", "y2"]

----

define `y2`
define `x3`
use `y2`
define `z2`
-- post defs: ["z2", "x3", "y2"]

----

define `y2`
define `x3`
use `y2`
define `z3`
-- post defs: ["z3", "x3", "y2"]

----

define `y2`
define `x3`
use `y2`
use `x3`
-- post defs: ["x3", "y2"]

----

define `y2`
define `x3`
use `y2`
use `y2`
-- post defs: ["x3", "y2"]

----

define `y2`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "y2"]

----

define `y2`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "y2"]

----

define `y2`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "y2"]

----

define `y2`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "y2"]

----

define `y2`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "y2"]

----

define `y2`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "y2"]

----

define `y2`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "y2"]

----

define `y2`
define `y1`
use `y1`
-- post defs: ["y1", "y2"]

----

define `y2`
define `y1`
use `y2`
-- post defs: ["y1", "y2"]

----

define `y2`
define `y1`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "y1", "y2"]

----

define `y2`
define `y1`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "y1", "y2"]

----

define `y2`
define `y1`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "y1", "y2"]

----

define `y2`
define `y1`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "y1", "y2"]

----

define `y2`
define `y1`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "y1", "y2"]

----

define `y2`
define `y1`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "y1", "y2"]

----

define `y2`
define `y1`
define `x1`
use `x1`
-- post defs: ["x1", "y1", "y2"]

----

define `y2`
define `y1`
define `x1`
use `y1`
-- post defs: ["x1", "y1", "y2"]

----

define `y2`
define `y1`
define `x1`
use `y2`
-- post defs: ["x1", "y1", "y2"]

----

define `y2`
define `y1`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "y1", "y2"]

----

define `y2`
define `y1`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "y1", "y2"]

----

define `y2`
define `y1`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "y1", "y2"]

----

define `y2`
define `y1`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "y1", "y2"]

----

define `y2`
define `y1`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "y1", "y2"]

----

define `y2`
define `y1`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "y1", "y2"]

----

define `y2`
define `y1`
define `x2`
use `x2`
-- post defs: ["x2", "y1", "y2"]

----

define `y2`
define `y1`
define `x2`
use `y1`
-- post defs: ["x2", "y1", "y2"]

----

define `y2`
define `y1`
define `x2`
use `y2`
-- post defs: ["x2", "y1", "y2"]

----

define `y2`
define `y1`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "y1", "y2"]

----

define `y2`
define `y1`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "y1", "y2"]

----

define `y2`
define `y1`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "y1", "y2"]

----

define `y2`
define `y1`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "y1", "y2"]

----

define `y2`
define `y1`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "y1", "y2"]

----

define `y2`
define `y1`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "y1", "y2"]

----

define `y2`
define `y1`
define `x3`
use `x3`
-- post defs: ["x3", "y1", "y2"]

----

define `y2`
define `y1`
define `x3`
use `y1`
-- post defs: ["x3", "y1", "y2"]

----

define `y2`
define `y1`
define `x3`
use `y2`
-- post defs: ["x3", "y1", "y2"]

----

define `y2`
define `y1`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "y1", "y2"]

----

define `y2`
define `y1`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "y1", "y2"]

----

define `y2`
define `y1`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "y1", "y2"]

----

define `y2`
define `y1`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "y1", "y2"]

----

define `y2`
define `y1`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "y1", "y2"]

----

define `y2`
define `y1`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "y1", "y2"]

----

define `y2`
define `y1`
define `y3`
use `y3`
-- post defs: ["y3", "y1", "y2"]

----

define `y2`
define `y1`
define `y3`
use `y1`
-- post defs: ["y3", "y1", "y2"]

----

define `y2`
define `y1`
define `y3`
use `y2`
-- post defs: ["y3", "y1", "y2"]

----

define `y2`
define `y1`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "y1", "y2"]

----

define `y2`
define `y1`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "y1", "y2"]

----

define `y2`
define `y1`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "y1", "y2"]

----

define `y2`
define `y1`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "y1", "y2"]

----

define `y2`
define `y1`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "y1", "y2"]

----

define `y2`
define `y1`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "y1", "y2"]

----

define `y2`
define `y1`
define `z1`
use `z1`
-- post defs: ["z1", "y1", "y2"]

----

define `y2`
define `y1`
define `z1`
use `y1`
-- post defs: ["z1", "y1", "y2"]

----

define `y2`
define `y1`
define `z1`
use `y2`
-- post defs: ["z1", "y1", "y2"]

----

define `y2`
define `y1`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "y1", "y2"]

----

define `y2`
define `y1`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "y1", "y2"]

----

define `y2`
define `y1`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "y1", "y2"]

----

define `y2`
define `y1`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "y1", "y2"]

----

define `y2`
define `y1`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "y1", "y2"]

----

define `y2`
define `y1`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "y1", "y2"]

----

define `y2`
define `y1`
define `z2`
use `z2`
-- post defs: ["z2", "y1", "y2"]

----

define `y2`
define `y1`
define `z2`
use `y1`
-- post defs: ["z2", "y1", "y2"]

----

define `y2`
define `y1`
define `z2`
use `y2`
-- post defs: ["z2", "y1", "y2"]

----

define `y2`
define `y1`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "y1", "y2"]

----

define `y2`
define `y1`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "y1", "y2"]

----

define `y2`
define `y1`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "y1", "y2"]

----

define `y2`
define `y1`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "y1", "y2"]

----

define `y2`
define `y1`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "y1", "y2"]

----

define `y2`
define `y1`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "y1", "y2"]

----

define `y2`
define `y1`
define `z3`
use `z3`
-- post defs: ["z3", "y1", "y2"]

----

define `y2`
define `y1`
define `z3`
use `y1`
-- post defs: ["z3", "y1", "y2"]

----

define `y2`
define `y1`
define `z3`
use `y2`
-- post defs: ["z3", "y1", "y2"]

----

define `y2`
define `y1`
use `y1`
define `x1`
-- post defs: ["x1", "y1", "y2"]

----

define `y2`
define `y1`
use `y1`
define `x2`
-- post defs: ["x2", "y1", "y2"]

----

define `y2`
define `y1`
use `y1`
define `x3`
-- post defs: ["x3", "y1", "y2"]

----

define `y2`
define `y1`
use `y1`
define `y3`
-- post defs: ["y3", "y1", "y2"]

----

define `y2`
define `y1`
use `y1`
define `z1`
-- post defs: ["z1", "y1", "y2"]

----

define `y2`
define `y1`
use `y1`
define `z2`
-- post defs: ["z2", "y1", "y2"]

----

define `y2`
define `y1`
use `y1`
define `z3`
-- post defs: ["z3", "y1", "y2"]

----

define `y2`
define `y1`
use `y1`
use `y1`
-- post defs: ["y1", "y2"]

----

define `y2`
define `y1`
use `y1`
use `y2`
-- post defs: ["y1", "y2"]

----

define `y2`
define `y1`
use `y2`
define `x1`
-- post defs: ["x1", "y1", "y2"]

----

define `y2`
define `y1`
use `y2`
define `x2`
-- post defs: ["x2", "y1", "y2"]

----

define `y2`
define `y1`
use `y2`
define `x3`
-- post defs: ["x3", "y1", "y2"]

----

define `y2`
define `y1`
use `y2`
define `y3`
-- post defs: ["y3", "y1", "y2"]

----

define `y2`
define `y1`
use `y2`
define `z1`
-- post defs: ["z1", "y1", "y2"]

----

define `y2`
define `y1`
use `y2`
define `z2`
-- post defs: ["z2", "y1", "y2"]

----

define `y2`
define `y1`
use `y2`
define `z3`
-- post defs: ["z3", "y1", "y2"]

----

define `y2`
define `y1`
use `y2`
use `y1`
-- post defs: ["y1", "y2"]

----

define `y2`
define `y1`
use `y2`
use `y2`
-- post defs: ["y1", "y2"]

----

define `y2`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "y2"]

----

define `y2`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "y2"]

----

define `y2`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "y2"]

----

define `y2`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "y2"]

----

define `y2`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "y2"]

----

define `y2`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "y2"]

----

define `y2`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "y2"]

----

define `y2`
define `y3`
use `y3`
-- post defs: ["y3", "y2"]

----

define `y2`
define `y3`
use `y2`
-- post defs: ["y3", "y2"]

----

define `y2`
define `y3`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "y3", "y2"]

----

define `y2`
define `y3`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "y3", "y2"]

----

define `y2`
define `y3`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "y3", "y2"]

----

define `y2`
define `y3`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "y3", "y2"]

----

define `y2`
define `y3`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "y3", "y2"]

----

define `y2`
define `y3`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "y3", "y2"]

----

define `y2`
define `y3`
define `x1`
use `x1`
-- post defs: ["x1", "y3", "y2"]

----

define `y2`
define `y3`
define `x1`
use `y3`
-- post defs: ["x1", "y3", "y2"]

----

define `y2`
define `y3`
define `x1`
use `y2`
-- post defs: ["x1", "y3", "y2"]

----

define `y2`
define `y3`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "y3", "y2"]

----

define `y2`
define `y3`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "y3", "y2"]

----

define `y2`
define `y3`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "y3", "y2"]

----

define `y2`
define `y3`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "y3", "y2"]

----

define `y2`
define `y3`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "y3", "y2"]

----

define `y2`
define `y3`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "y3", "y2"]

----

define `y2`
define `y3`
define `x2`
use `x2`
-- post defs: ["x2", "y3", "y2"]

----

define `y2`
define `y3`
define `x2`
use `y3`
-- post defs: ["x2", "y3", "y2"]

----

define `y2`
define `y3`
define `x2`
use `y2`
-- post defs: ["x2", "y3", "y2"]

----

define `y2`
define `y3`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "y3", "y2"]

----

define `y2`
define `y3`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "y3", "y2"]

----

define `y2`
define `y3`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "y3", "y2"]

----

define `y2`
define `y3`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "y3", "y2"]

----

define `y2`
define `y3`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "y3", "y2"]

----

define `y2`
define `y3`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "y3", "y2"]

----

define `y2`
define `y3`
define `x3`
use `x3`
-- post defs: ["x3", "y3", "y2"]

----

define `y2`
define `y3`
define `x3`
use `y3`
-- post defs: ["x3", "y3", "y2"]

----

define `y2`
define `y3`
define `x3`
use `y2`
-- post defs: ["x3", "y3", "y2"]

----

define `y2`
define `y3`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "y3", "y2"]

----

define `y2`
define `y3`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "y3", "y2"]

----

define `y2`
define `y3`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "y3", "y2"]

----

define `y2`
define `y3`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "y3", "y2"]

----

define `y2`
define `y3`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "y3", "y2"]

----

define `y2`
define `y3`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "y3", "y2"]

----

define `y2`
define `y3`
define `y1`
use `y1`
-- post defs: ["y1", "y3", "y2"]

----

define `y2`
define `y3`
define `y1`
use `y3`
-- post defs: ["y1", "y3", "y2"]

----

define `y2`
define `y3`
define `y1`
use `y2`
-- post defs: ["y1", "y3", "y2"]

----

define `y2`
define `y3`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "y3", "y2"]

----

define `y2`
define `y3`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "y3", "y2"]

----

define `y2`
define `y3`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "y3", "y2"]

----

define `y2`
define `y3`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "y3", "y2"]

----

define `y2`
define `y3`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "y3", "y2"]

----

define `y2`
define `y3`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "y3", "y2"]

----

define `y2`
define `y3`
define `z1`
use `z1`
-- post defs: ["z1", "y3", "y2"]

----

define `y2`
define `y3`
define `z1`
use `y3`
-- post defs: ["z1", "y3", "y2"]

----

define `y2`
define `y3`
define `z1`
use `y2`
-- post defs: ["z1", "y3", "y2"]

----

define `y2`
define `y3`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "y3", "y2"]

----

define `y2`
define `y3`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "y3", "y2"]

----

define `y2`
define `y3`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "y3", "y2"]

----

define `y2`
define `y3`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "y3", "y2"]

----

define `y2`
define `y3`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "y3", "y2"]

----

define `y2`
define `y3`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "y3", "y2"]

----

define `y2`
define `y3`
define `z2`
use `z2`
-- post defs: ["z2", "y3", "y2"]

----

define `y2`
define `y3`
define `z2`
use `y3`
-- post defs: ["z2", "y3", "y2"]

----

define `y2`
define `y3`
define `z2`
use `y2`
-- post defs: ["z2", "y3", "y2"]

----

define `y2`
define `y3`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "y3", "y2"]

----

define `y2`
define `y3`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "y3", "y2"]

----

define `y2`
define `y3`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "y3", "y2"]

----

define `y2`
define `y3`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "y3", "y2"]

----

define `y2`
define `y3`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "y3", "y2"]

----

define `y2`
define `y3`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "y3", "y2"]

----

define `y2`
define `y3`
define `z3`
use `z3`
-- post defs: ["z3", "y3", "y2"]

----

define `y2`
define `y3`
define `z3`
use `y3`
-- post defs: ["z3", "y3", "y2"]

----

define `y2`
define `y3`
define `z3`
use `y2`
-- post defs: ["z3", "y3", "y2"]

----

define `y2`
define `y3`
use `y3`
define `x1`
-- post defs: ["x1", "y3", "y2"]

----

define `y2`
define `y3`
use `y3`
define `x2`
-- post defs: ["x2", "y3", "y2"]

----

define `y2`
define `y3`
use `y3`
define `x3`
-- post defs: ["x3", "y3", "y2"]

----

define `y2`
define `y3`
use `y3`
define `y1`
-- post defs: ["y1", "y3", "y2"]

----

define `y2`
define `y3`
use `y3`
define `z1`
-- post defs: ["z1", "y3", "y2"]

----

define `y2`
define `y3`
use `y3`
define `z2`
-- post defs: ["z2", "y3", "y2"]

----

define `y2`
define `y3`
use `y3`
define `z3`
-- post defs: ["z3", "y3", "y2"]

----

define `y2`
define `y3`
use `y3`
use `y3`
-- post defs: ["y3", "y2"]

----

define `y2`
define `y3`
use `y3`
use `y2`
-- post defs: ["y3", "y2"]

----

define `y2`
define `y3`
use `y2`
define `x1`
-- post defs: ["x1", "y3", "y2"]

----

define `y2`
define `y3`
use `y2`
define `x2`
-- post defs: ["x2", "y3", "y2"]

----

define `y2`
define `y3`
use `y2`
define `x3`
-- post defs: ["x3", "y3", "y2"]

----

define `y2`
define `y3`
use `y2`
define `y1`
-- post defs: ["y1", "y3", "y2"]

----

define `y2`
define `y3`
use `y2`
define `z1`
-- post defs: ["z1", "y3", "y2"]

----

define `y2`
define `y3`
use `y2`
define `z2`
-- post defs: ["z2", "y3", "y2"]

----

define `y2`
define `y3`
use `y2`
define `z3`
-- post defs: ["z3", "y3", "y2"]

----

define `y2`
define `y3`
use `y2`
use `y3`
-- post defs: ["y3", "y2"]

----

define `y2`
define `y3`
use `y2`
use `y2`
-- post defs: ["y3", "y2"]

----

define `y2`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "y2"]

----

define `y2`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "y2"]

----

define `y2`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "y2"]

----

define `y2`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "y2"]

----

define `y2`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "y2"]

----

define `y2`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "y2"]

----

define `y2`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "y2"]

----

define `y2`
define `z1`
use `z1`
-- post defs: ["z1", "y2"]

----

define `y2`
define `z1`
use `y2`
-- post defs: ["z1", "y2"]

----

define `y2`
define `z1`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "z1", "y2"]

----

define `y2`
define `z1`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "z1", "y2"]

----

define `y2`
define `z1`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "z1", "y2"]

----

define `y2`
define `z1`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "z1", "y2"]

----

define `y2`
define `z1`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "z1", "y2"]

----

define `y2`
define `z1`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "z1", "y2"]

----

define `y2`
define `z1`
define `x1`
use `x1`
-- post defs: ["x1", "z1", "y2"]

----

define `y2`
define `z1`
define `x1`
use `z1`
-- post defs: ["x1", "z1", "y2"]

----

define `y2`
define `z1`
define `x1`
use `y2`
-- post defs: ["x1", "z1", "y2"]

----

define `y2`
define `z1`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "z1", "y2"]

----

define `y2`
define `z1`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "z1", "y2"]

----

define `y2`
define `z1`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "z1", "y2"]

----

define `y2`
define `z1`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "z1", "y2"]

----

define `y2`
define `z1`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "z1", "y2"]

----

define `y2`
define `z1`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "z1", "y2"]

----

define `y2`
define `z1`
define `x2`
use `x2`
-- post defs: ["x2", "z1", "y2"]

----

define `y2`
define `z1`
define `x2`
use `z1`
-- post defs: ["x2", "z1", "y2"]

----

define `y2`
define `z1`
define `x2`
use `y2`
-- post defs: ["x2", "z1", "y2"]

----

define `y2`
define `z1`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "z1", "y2"]

----

define `y2`
define `z1`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "z1", "y2"]

----

define `y2`
define `z1`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "z1", "y2"]

----

define `y2`
define `z1`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "z1", "y2"]

----

define `y2`
define `z1`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "z1", "y2"]

----

define `y2`
define `z1`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "z1", "y2"]

----

define `y2`
define `z1`
define `x3`
use `x3`
-- post defs: ["x3", "z1", "y2"]

----

define `y2`
define `z1`
define `x3`
use `z1`
-- post defs: ["x3", "z1", "y2"]

----

define `y2`
define `z1`
define `x3`
use `y2`
-- post defs: ["x3", "z1", "y2"]

----

define `y2`
define `z1`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "z1", "y2"]

----

define `y2`
define `z1`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "z1", "y2"]

----

define `y2`
define `z1`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "z1", "y2"]

----

define `y2`
define `z1`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "z1", "y2"]

----

define `y2`
define `z1`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "z1", "y2"]

----

define `y2`
define `z1`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "z1", "y2"]

----

define `y2`
define `z1`
define `y1`
use `y1`
-- post defs: ["y1", "z1", "y2"]

----

define `y2`
define `z1`
define `y1`
use `z1`
-- post defs: ["y1", "z1", "y2"]

----

define `y2`
define `z1`
define `y1`
use `y2`
-- post defs: ["y1", "z1", "y2"]

----

define `y2`
define `z1`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "z1", "y2"]

----

define `y2`
define `z1`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "z1", "y2"]

----

define `y2`
define `z1`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "z1", "y2"]

----

define `y2`
define `z1`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "z1", "y2"]

----

define `y2`
define `z1`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "z1", "y2"]

----

define `y2`
define `z1`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "z1", "y2"]

----

define `y2`
define `z1`
define `y3`
use `y3`
-- post defs: ["y3", "z1", "y2"]

----

define `y2`
define `z1`
define `y3`
use `z1`
-- post defs: ["y3", "z1", "y2"]

----

define `y2`
define `z1`
define `y3`
use `y2`
-- post defs: ["y3", "z1", "y2"]

----

define `y2`
define `z1`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "z1", "y2"]

----

define `y2`
define `z1`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "z1", "y2"]

----

define `y2`
define `z1`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "z1", "y2"]

----

define `y2`
define `z1`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "z1", "y2"]

----

define `y2`
define `z1`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "z1", "y2"]

----

define `y2`
define `z1`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "z1", "y2"]

----

define `y2`
define `z1`
define `z2`
use `z2`
-- post defs: ["z2", "z1", "y2"]

----

define `y2`
define `z1`
define `z2`
use `z1`
-- post defs: ["z2", "z1", "y2"]

----

define `y2`
define `z1`
define `z2`
use `y2`
-- post defs: ["z2", "z1", "y2"]

----

define `y2`
define `z1`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "z1", "y2"]

----

define `y2`
define `z1`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "z1", "y2"]

----

define `y2`
define `z1`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "z1", "y2"]

----

define `y2`
define `z1`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "z1", "y2"]

----

define `y2`
define `z1`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "z1", "y2"]

----

define `y2`
define `z1`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "z1", "y2"]

----

define `y2`
define `z1`
define `z3`
use `z3`
-- post defs: ["z3", "z1", "y2"]

----

define `y2`
define `z1`
define `z3`
use `z1`
-- post defs: ["z3", "z1", "y2"]

----

define `y2`
define `z1`
define `z3`
use `y2`
-- post defs: ["z3", "z1", "y2"]

----

define `y2`
define `z1`
use `z1`
define `x1`
-- post defs: ["x1", "z1", "y2"]

----

define `y2`
define `z1`
use `z1`
define `x2`
-- post defs: ["x2", "z1", "y2"]

----

define `y2`
define `z1`
use `z1`
define `x3`
-- post defs: ["x3", "z1", "y2"]

----

define `y2`
define `z1`
use `z1`
define `y1`
-- post defs: ["y1", "z1", "y2"]

----

define `y2`
define `z1`
use `z1`
define `y3`
-- post defs: ["y3", "z1", "y2"]

----

define `y2`
define `z1`
use `z1`
define `z2`
-- post defs: ["z2", "z1", "y2"]

----

define `y2`
define `z1`
use `z1`
define `z3`
-- post defs: ["z3", "z1", "y2"]

----

define `y2`
define `z1`
use `z1`
use `z1`
-- post defs: ["z1", "y2"]

----

define `y2`
define `z1`
use `z1`
use `y2`
-- post defs: ["z1", "y2"]

----

define `y2`
define `z1`
use `y2`
define `x1`
-- post defs: ["x1", "z1", "y2"]

----

define `y2`
define `z1`
use `y2`
define `x2`
-- post defs: ["x2", "z1", "y2"]

----

define `y2`
define `z1`
use `y2`
define `x3`
-- post defs: ["x3", "z1", "y2"]

----

define `y2`
define `z1`
use `y2`
define `y1`
-- post defs: ["y1", "z1", "y2"]

----

define `y2`
define `z1`
use `y2`
define `y3`
-- post defs: ["y3", "z1", "y2"]

----

define `y2`
define `z1`
use `y2`
define `z2`
-- post defs: ["z2", "z1", "y2"]

----

define `y2`
define `z1`
use `y2`
define `z3`
-- post defs: ["z3", "z1", "y2"]

----

define `y2`
define `z1`
use `y2`
use `z1`
-- post defs: ["z1", "y2"]

----

define `y2`
define `z1`
use `y2`
use `y2`
-- post defs: ["z1", "y2"]

----

define `y2`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "y2"]

----

define `y2`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "y2"]

----

define `y2`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "y2"]

----

define `y2`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "y2"]

----

define `y2`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "y2"]

----

define `y2`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "y2"]

----

define `y2`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "y2"]

----

define `y2`
define `z2`
use `z2`
-- post defs: ["z2", "y2"]

----

define `y2`
define `z2`
use `y2`
-- post defs: ["z2", "y2"]

----

define `y2`
define `z2`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "z2", "y2"]

----

define `y2`
define `z2`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "z2", "y2"]

----

define `y2`
define `z2`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "z2", "y2"]

----

define `y2`
define `z2`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "z2", "y2"]

----

define `y2`
define `z2`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "z2", "y2"]

----

define `y2`
define `z2`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "z2", "y2"]

----

define `y2`
define `z2`
define `x1`
use `x1`
-- post defs: ["x1", "z2", "y2"]

----

define `y2`
define `z2`
define `x1`
use `z2`
-- post defs: ["x1", "z2", "y2"]

----

define `y2`
define `z2`
define `x1`
use `y2`
-- post defs: ["x1", "z2", "y2"]

----

define `y2`
define `z2`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "z2", "y2"]

----

define `y2`
define `z2`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "z2", "y2"]

----

define `y2`
define `z2`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "z2", "y2"]

----

define `y2`
define `z2`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "z2", "y2"]

----

define `y2`
define `z2`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "z2", "y2"]

----

define `y2`
define `z2`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "z2", "y2"]

----

define `y2`
define `z2`
define `x2`
use `x2`
-- post defs: ["x2", "z2", "y2"]

----

define `y2`
define `z2`
define `x2`
use `z2`
-- post defs: ["x2", "z2", "y2"]

----

define `y2`
define `z2`
define `x2`
use `y2`
-- post defs: ["x2", "z2", "y2"]

----

define `y2`
define `z2`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "z2", "y2"]

----

define `y2`
define `z2`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "z2", "y2"]

----

define `y2`
define `z2`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "z2", "y2"]

----

define `y2`
define `z2`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "z2", "y2"]

----

define `y2`
define `z2`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "z2", "y2"]

----

define `y2`
define `z2`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "z2", "y2"]

----

define `y2`
define `z2`
define `x3`
use `x3`
-- post defs: ["x3", "z2", "y2"]

----

define `y2`
define `z2`
define `x3`
use `z2`
-- post defs: ["x3", "z2", "y2"]

----

define `y2`
define `z2`
define `x3`
use `y2`
-- post defs: ["x3", "z2", "y2"]

----

define `y2`
define `z2`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "z2", "y2"]

----

define `y2`
define `z2`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "z2", "y2"]

----

define `y2`
define `z2`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "z2", "y2"]

----

define `y2`
define `z2`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "z2", "y2"]

----

define `y2`
define `z2`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "z2", "y2"]

----

define `y2`
define `z2`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "z2", "y2"]

----

define `y2`
define `z2`
define `y1`
use `y1`
-- post defs: ["y1", "z2", "y2"]

----

define `y2`
define `z2`
define `y1`
use `z2`
-- post defs: ["y1", "z2", "y2"]

----

define `y2`
define `z2`
define `y1`
use `y2`
-- post defs: ["y1", "z2", "y2"]

----

define `y2`
define `z2`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "z2", "y2"]

----

define `y2`
define `z2`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "z2", "y2"]

----

define `y2`
define `z2`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "z2", "y2"]

----

define `y2`
define `z2`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "z2", "y2"]

----

define `y2`
define `z2`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "z2", "y2"]

----

define `y2`
define `z2`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "z2", "y2"]

----

define `y2`
define `z2`
define `y3`
use `y3`
-- post defs: ["y3", "z2", "y2"]

----

define `y2`
define `z2`
define `y3`
use `z2`
-- post defs: ["y3", "z2", "y2"]

----

define `y2`
define `z2`
define `y3`
use `y2`
-- post defs: ["y3", "z2", "y2"]

----

define `y2`
define `z2`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "z2", "y2"]

----

define `y2`
define `z2`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "z2", "y2"]

----

define `y2`
define `z2`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "z2", "y2"]

----

define `y2`
define `z2`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "z2", "y2"]

----

define `y2`
define `z2`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "z2", "y2"]

----

define `y2`
define `z2`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "z2", "y2"]

----

define `y2`
define `z2`
define `z1`
use `z1`
-- post defs: ["z1", "z2", "y2"]

----

define `y2`
define `z2`
define `z1`
use `z2`
-- post defs: ["z1", "z2", "y2"]

----

define `y2`
define `z2`
define `z1`
use `y2`
-- post defs: ["z1", "z2", "y2"]

----

define `y2`
define `z2`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "z2", "y2"]

----

define `y2`
define `z2`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "z2", "y2"]

----

define `y2`
define `z2`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "z2", "y2"]

----

define `y2`
define `z2`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "z2", "y2"]

----

define `y2`
define `z2`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "z2", "y2"]

----

define `y2`
define `z2`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "z2", "y2"]

----

define `y2`
define `z2`
define `z3`
use `z3`
-- post defs: ["z3", "z2", "y2"]

----

define `y2`
define `z2`
define `z3`
use `z2`
-- post defs: ["z3", "z2", "y2"]

----

define `y2`
define `z2`
define `z3`
use `y2`
-- post defs: ["z3", "z2", "y2"]

----

define `y2`
define `z2`
use `z2`
define `x1`
-- post defs: ["x1", "z2", "y2"]

----

define `y2`
define `z2`
use `z2`
define `x2`
-- post defs: ["x2", "z2", "y2"]

----

define `y2`
define `z2`
use `z2`
define `x3`
-- post defs: ["x3", "z2", "y2"]

----

define `y2`
define `z2`
use `z2`
define `y1`
-- post defs: ["y1", "z2", "y2"]

----

define `y2`
define `z2`
use `z2`
define `y3`
-- post defs: ["y3", "z2", "y2"]

----

define `y2`
define `z2`
use `z2`
define `z1`
-- post defs: ["z1", "z2", "y2"]

----

define `y2`
define `z2`
use `z2`
define `z3`
-- post defs: ["z3", "z2", "y2"]

----

define `y2`
define `z2`
use `z2`
use `z2`
-- post defs: ["z2", "y2"]

----

define `y2`
define `z2`
use `z2`
use `y2`
-- post defs: ["z2", "y2"]

----

define `y2`
define `z2`
use `y2`
define `x1`
-- post defs: ["x1", "z2", "y2"]

----

define `y2`
define `z2`
use `y2`
define `x2`
-- post defs: ["x2", "z2", "y2"]

----

define `y2`
define `z2`
use `y2`
define `x3`
-- post defs: ["x3", "z2", "y2"]

----

define `y2`
define `z2`
use `y2`
define `y1`
-- post defs: ["y1", "z2", "y2"]

----

define `y2`
define `z2`
use `y2`
define `y3`
-- post defs: ["y3", "z2", "y2"]

----

define `y2`
define `z2`
use `y2`
define `z1`
-- post defs: ["z1", "z2", "y2"]

----

define `y2`
define `z2`
use `y2`
define `z3`
-- post defs: ["z3", "z2", "y2"]

----

define `y2`
define `z2`
use `y2`
use `z2`
-- post defs: ["z2", "y2"]

----

define `y2`
define `z2`
use `y2`
use `y2`
-- post defs: ["z2", "y2"]

----

define `y2`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "y2"]

----

define `y2`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "y2"]

----

define `y2`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "y2"]

----

define `y2`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "y2"]

----

define `y2`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "y2"]

----

define `y2`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "y2"]

----

define `y2`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "y2"]

----

define `y2`
define `z3`
use `z3`
-- post defs: ["z3", "y2"]

----

define `y2`
define `z3`
use `y2`
-- post defs: ["z3", "y2"]

----

define `y2`
define `z3`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "z3", "y2"]

----

define `y2`
define `z3`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "z3", "y2"]

----

define `y2`
define `z3`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "z3", "y2"]

----

define `y2`
define `z3`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "z3", "y2"]

----

define `y2`
define `z3`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "z3", "y2"]

----

define `y2`
define `z3`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "z3", "y2"]

----

define `y2`
define `z3`
define `x1`
use `x1`
-- post defs: ["x1", "z3", "y2"]

----

define `y2`
define `z3`
define `x1`
use `z3`
-- post defs: ["x1", "z3", "y2"]

----

define `y2`
define `z3`
define `x1`
use `y2`
-- post defs: ["x1", "z3", "y2"]

----

define `y2`
define `z3`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "z3", "y2"]

----

define `y2`
define `z3`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "z3", "y2"]

----

define `y2`
define `z3`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "z3", "y2"]

----

define `y2`
define `z3`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "z3", "y2"]

----

define `y2`
define `z3`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "z3", "y2"]

----

define `y2`
define `z3`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "z3", "y2"]

----

define `y2`
define `z3`
define `x2`
use `x2`
-- post defs: ["x2", "z3", "y2"]

----

define `y2`
define `z3`
define `x2`
use `z3`
-- post defs: ["x2", "z3", "y2"]

----

define `y2`
define `z3`
define `x2`
use `y2`
-- post defs: ["x2", "z3", "y2"]

----

define `y2`
define `z3`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "z3", "y2"]

----

define `y2`
define `z3`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "z3", "y2"]

----

define `y2`
define `z3`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "z3", "y2"]

----

define `y2`
define `z3`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "z3", "y2"]

----

define `y2`
define `z3`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "z3", "y2"]

----

define `y2`
define `z3`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "z3", "y2"]

----

define `y2`
define `z3`
define `x3`
use `x3`
-- post defs: ["x3", "z3", "y2"]

----

define `y2`
define `z3`
define `x3`
use `z3`
-- post defs: ["x3", "z3", "y2"]

----

define `y2`
define `z3`
define `x3`
use `y2`
-- post defs: ["x3", "z3", "y2"]

----

define `y2`
define `z3`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "z3", "y2"]

----

define `y2`
define `z3`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "z3", "y2"]

----

define `y2`
define `z3`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "z3", "y2"]

----

define `y2`
define `z3`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "z3", "y2"]

----

define `y2`
define `z3`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "z3", "y2"]

----

define `y2`
define `z3`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "z3", "y2"]

----

define `y2`
define `z3`
define `y1`
use `y1`
-- post defs: ["y1", "z3", "y2"]

----

define `y2`
define `z3`
define `y1`
use `z3`
-- post defs: ["y1", "z3", "y2"]

----

define `y2`
define `z3`
define `y1`
use `y2`
-- post defs: ["y1", "z3", "y2"]

----

define `y2`
define `z3`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "z3", "y2"]

----

define `y2`
define `z3`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "z3", "y2"]

----

define `y2`
define `z3`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "z3", "y2"]

----

define `y2`
define `z3`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "z3", "y2"]

----

define `y2`
define `z3`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "z3", "y2"]

----

define `y2`
define `z3`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "z3", "y2"]

----

define `y2`
define `z3`
define `y3`
use `y3`
-- post defs: ["y3", "z3", "y2"]

----

define `y2`
define `z3`
define `y3`
use `z3`
-- post defs: ["y3", "z3", "y2"]

----

define `y2`
define `z3`
define `y3`
use `y2`
-- post defs: ["y3", "z3", "y2"]

----

define `y2`
define `z3`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "z3", "y2"]

----

define `y2`
define `z3`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "z3", "y2"]

----

define `y2`
define `z3`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "z3", "y2"]

----

define `y2`
define `z3`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "z3", "y2"]

----

define `y2`
define `z3`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "z3", "y2"]

----

define `y2`
define `z3`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "z3", "y2"]

----

define `y2`
define `z3`
define `z1`
use `z1`
-- post defs: ["z1", "z3", "y2"]

----

define `y2`
define `z3`
define `z1`
use `z3`
-- post defs: ["z1", "z3", "y2"]

----

define `y2`
define `z3`
define `z1`
use `y2`
-- post defs: ["z1", "z3", "y2"]

----

define `y2`
define `z3`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "z3", "y2"]

----

define `y2`
define `z3`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "z3", "y2"]

----

define `y2`
define `z3`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "z3", "y2"]

----

define `y2`
define `z3`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "z3", "y2"]

----

define `y2`
define `z3`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "z3", "y2"]

----

define `y2`
define `z3`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "z3", "y2"]

----

define `y2`
define `z3`
define `z2`
use `z2`
-- post defs: ["z2", "z3", "y2"]

----

define `y2`
define `z3`
define `z2`
use `z3`
-- post defs: ["z2", "z3", "y2"]

----

define `y2`
define `z3`
define `z2`
use `y2`
-- post defs: ["z2", "z3", "y2"]

----

define `y2`
define `z3`
use `z3`
define `x1`
-- post defs: ["x1", "z3", "y2"]

----

define `y2`
define `z3`
use `z3`
define `x2`
-- post defs: ["x2", "z3", "y2"]

----

define `y2`
define `z3`
use `z3`
define `x3`
-- post defs: ["x3", "z3", "y2"]

----

define `y2`
define `z3`
use `z3`
define `y1`
-- post defs: ["y1", "z3", "y2"]

----

define `y2`
define `z3`
use `z3`
define `y3`
-- post defs: ["y3", "z3", "y2"]

----

define `y2`
define `z3`
use `z3`
define `z1`
-- post defs: ["z1", "z3", "y2"]

----

define `y2`
define `z3`
use `z3`
define `z2`
-- post defs: ["z2", "z3", "y2"]

----

define `y2`
define `z3`
use `z3`
use `z3`
-- post defs: ["z3", "y2"]

----

define `y2`
define `z3`
use `z3`
use `y2`
-- post defs: ["z3", "y2"]

----

define `y2`
define `z3`
use `y2`
define `x1`
-- post defs: ["x1", "z3", "y2"]

----

define `y2`
define `z3`
use `y2`
define `x2`
-- post defs: ["x2", "z3", "y2"]

----

define `y2`
define `z3`
use `y2`
define `x3`
-- post defs: ["x3", "z3", "y2"]

----

define `y2`
define `z3`
use `y2`
define `y1`
-- post defs: ["y1", "z3", "y2"]

----

define `y2`
define `z3`
use `y2`
define `y3`
-- post defs: ["y3", "z3", "y2"]

----

define `y2`
define `z3`
use `y2`
define `z1`
-- post defs: ["z1", "z3", "y2"]

----

define `y2`
define `z3`
use `y2`
define `z2`
-- post defs: ["z2", "z3", "y2"]

----

define `y2`
define `z3`
use `y2`
use `z3`
-- post defs: ["z3", "y2"]

----

define `y2`
define `z3`
use `y2`
use `y2`
-- post defs: ["z3", "y2"]

----

define `y2`
use `y2`
define `x1`
-- post defs: ["x1", "y2"]

----

define `y2`
use `y2`
define `x2`
-- post defs: ["x2", "y2"]

----

define `y2`
use `y2`
define `x3`
-- post defs: ["x3", "y2"]

----

define `y2`
use `y2`
define `y1`
-- post defs: ["y1", "y2"]

----

define `y2`
use `y2`
define `y3`
-- post defs: ["y3", "y2"]

----

define `y2`
use `y2`
define `z1`
-- post defs: ["z1", "y2"]

----

define `y2`
use `y2`
define `z2`
-- post defs: ["z2", "y2"]

----

define `y2`
use `y2`
define `z3`
-- post defs: ["z3", "y2"]

----

define `y2`
use `y2`
use `y2`
-- post defs: ["y2"]

----

define `y2`
use `y2`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "y2"]

----

define `y2`
use `y2`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "y2"]

----

define `y2`
use `y2`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "y2"]

----

define `y2`
use `y2`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "y2"]

----

define `y2`
use `y2`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "y2"]

----

define `y2`
use `y2`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "y2"]

----

define `y2`
use `y2`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "y2"]

----

define `y2`
use `y2`
define `x1`
use `x1`
-- post defs: ["x1", "y2"]

----

define `y2`
use `y2`
define `x1`
use `y2`
-- post defs: ["x1", "y2"]

----

define `y2`
use `y2`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "y2"]

----

define `y2`
use `y2`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "y2"]

----

define `y2`
use `y2`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "y2"]

----

define `y2`
use `y2`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "y2"]

----

define `y2`
use `y2`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "y2"]

----

define `y2`
use `y2`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "y2"]

----

define `y2`
use `y2`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "y2"]

----

define `y2`
use `y2`
define `x2`
use `x2`
-- post defs: ["x2", "y2"]

----

define `y2`
use `y2`
define `x2`
use `y2`
-- post defs: ["x2", "y2"]

----

define `y2`
use `y2`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "y2"]

----

define `y2`
use `y2`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "y2"]

----

define `y2`
use `y2`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "y2"]

----

define `y2`
use `y2`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "y2"]

----

define `y2`
use `y2`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "y2"]

----

define `y2`
use `y2`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "y2"]

----

define `y2`
use `y2`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "y2"]

----

define `y2`
use `y2`
define `x3`
use `x3`
-- post defs: ["x3", "y2"]

----

define `y2`
use `y2`
define `x3`
use `y2`
-- post defs: ["x3", "y2"]

----

define `y2`
use `y2`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "y2"]

----

define `y2`
use `y2`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "y2"]

----

define `y2`
use `y2`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "y2"]

----

define `y2`
use `y2`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "y2"]

----

define `y2`
use `y2`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "y2"]

----

define `y2`
use `y2`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "y2"]

----

define `y2`
use `y2`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "y2"]

----

define `y2`
use `y2`
define `y1`
use `y1`
-- post defs: ["y1", "y2"]

----

define `y2`
use `y2`
define `y1`
use `y2`
-- post defs: ["y1", "y2"]

----

define `y2`
use `y2`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "y2"]

----

define `y2`
use `y2`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "y2"]

----

define `y2`
use `y2`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "y2"]

----

define `y2`
use `y2`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "y2"]

----

define `y2`
use `y2`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "y2"]

----

define `y2`
use `y2`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "y2"]

----

define `y2`
use `y2`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "y2"]

----

define `y2`
use `y2`
define `y3`
use `y3`
-- post defs: ["y3", "y2"]

----

define `y2`
use `y2`
define `y3`
use `y2`
-- post defs: ["y3", "y2"]

----

define `y2`
use `y2`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "y2"]

----

define `y2`
use `y2`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "y2"]

----

define `y2`
use `y2`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "y2"]

----

define `y2`
use `y2`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "y2"]

----

define `y2`
use `y2`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "y2"]

----

define `y2`
use `y2`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "y2"]

----

define `y2`
use `y2`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "y2"]

----

define `y2`
use `y2`
define `z1`
use `z1`
-- post defs: ["z1", "y2"]

----

define `y2`
use `y2`
define `z1`
use `y2`
-- post defs: ["z1", "y2"]

----

define `y2`
use `y2`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "y2"]

----

define `y2`
use `y2`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "y2"]

----

define `y2`
use `y2`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "y2"]

----

define `y2`
use `y2`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "y2"]

----

define `y2`
use `y2`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "y2"]

----

define `y2`
use `y2`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "y2"]

----

define `y2`
use `y2`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "y2"]

----

define `y2`
use `y2`
define `z2`
use `z2`
-- post defs: ["z2", "y2"]

----

define `y2`
use `y2`
define `z2`
use `y2`
-- post defs: ["z2", "y2"]

----

define `y2`
use `y2`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "y2"]

----

define `y2`
use `y2`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "y2"]

----

define `y2`
use `y2`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "y2"]

----

define `y2`
use `y2`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "y2"]

----

define `y2`
use `y2`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "y2"]

----

define `y2`
use `y2`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "y2"]

----

define `y2`
use `y2`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "y2"]

----

define `y2`
use `y2`
define `z3`
use `z3`
-- post defs: ["z3", "y2"]

----

define `y2`
use `y2`
define `z3`
use `y2`
-- post defs: ["z3", "y2"]

----

define `y2`
use `y2`
use `y2`
define `x1`
-- post defs: ["x1", "y2"]

----

define `y2`
use `y2`
use `y2`
define `x2`
-- post defs: ["x2", "y2"]

----

define `y2`
use `y2`
use `y2`
define `x3`
-- post defs: ["x3", "y2"]

----

define `y2`
use `y2`
use `y2`
define `y1`
-- post defs: ["y1", "y2"]

----

define `y2`
use `y2`
use `y2`
define `y3`
-- post defs: ["y3", "y2"]

----

define `y2`
use `y2`
use `y2`
define `z1`
-- post defs: ["z1", "y2"]

----

define `y2`
use `y2`
use `y2`
define `z2`
-- post defs: ["z2", "y2"]

----

define `y2`
use `y2`
use `y2`
define `z3`
-- post defs: ["z3", "y2"]

----

define `y2`
use `y2`
use `y2`
use `y2`
-- post defs: ["y2"]

----

define `y3`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "y3"]

----

define `y3`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "y3"]

----

define `y3`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "y3"]

----

define `y3`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "y3"]

----

define `y3`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "y3"]

----

define `y3`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "y3"]

----

define `y3`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "y3"]

----

define `y3`
define `x1`
use `x1`
-- post defs: ["x1", "y3"]

----

define `y3`
define `x1`
use `y3`
-- post defs: ["x1", "y3"]

----

define `y3`
define `x1`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "x1", "y3"]

----

define `y3`
define `x1`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "x1", "y3"]

----

define `y3`
define `x1`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "x1", "y3"]

----

define `y3`
define `x1`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "x1", "y3"]

----

define `y3`
define `x1`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "x1", "y3"]

----

define `y3`
define `x1`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "x1", "y3"]

----

define `y3`
define `x1`
define `x2`
use `x2`
-- post defs: ["x2", "x1", "y3"]

----

define `y3`
define `x1`
define `x2`
use `x1`
-- post defs: ["x2", "x1", "y3"]

----

define `y3`
define `x1`
define `x2`
use `y3`
-- post defs: ["x2", "x1", "y3"]

----

define `y3`
define `x1`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "x1", "y3"]

----

define `y3`
define `x1`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "x1", "y3"]

----

define `y3`
define `x1`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "x1", "y3"]

----

define `y3`
define `x1`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "x1", "y3"]

----

define `y3`
define `x1`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "x1", "y3"]

----

define `y3`
define `x1`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "x1", "y3"]

----

define `y3`
define `x1`
define `x3`
use `x3`
-- post defs: ["x3", "x1", "y3"]

----

define `y3`
define `x1`
define `x3`
use `x1`
-- post defs: ["x3", "x1", "y3"]

----

define `y3`
define `x1`
define `x3`
use `y3`
-- post defs: ["x3", "x1", "y3"]

----

define `y3`
define `x1`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "x1", "y3"]

----

define `y3`
define `x1`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "x1", "y3"]

----

define `y3`
define `x1`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "x1", "y3"]

----

define `y3`
define `x1`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "x1", "y3"]

----

define `y3`
define `x1`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "x1", "y3"]

----

define `y3`
define `x1`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "x1", "y3"]

----

define `y3`
define `x1`
define `y1`
use `y1`
-- post defs: ["y1", "x1", "y3"]

----

define `y3`
define `x1`
define `y1`
use `x1`
-- post defs: ["y1", "x1", "y3"]

----

define `y3`
define `x1`
define `y1`
use `y3`
-- post defs: ["y1", "x1", "y3"]

----

define `y3`
define `x1`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "x1", "y3"]

----

define `y3`
define `x1`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "x1", "y3"]

----

define `y3`
define `x1`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "x1", "y3"]

----

define `y3`
define `x1`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "x1", "y3"]

----

define `y3`
define `x1`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "x1", "y3"]

----

define `y3`
define `x1`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "x1", "y3"]

----

define `y3`
define `x1`
define `y2`
use `y2`
-- post defs: ["y2", "x1", "y3"]

----

define `y3`
define `x1`
define `y2`
use `x1`
-- post defs: ["y2", "x1", "y3"]

----

define `y3`
define `x1`
define `y2`
use `y3`
-- post defs: ["y2", "x1", "y3"]

----

define `y3`
define `x1`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "x1", "y3"]

----

define `y3`
define `x1`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "x1", "y3"]

----

define `y3`
define `x1`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "x1", "y3"]

----

define `y3`
define `x1`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "x1", "y3"]

----

define `y3`
define `x1`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "x1", "y3"]

----

define `y3`
define `x1`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "x1", "y3"]

----

define `y3`
define `x1`
define `z1`
use `z1`
-- post defs: ["z1", "x1", "y3"]

----

define `y3`
define `x1`
define `z1`
use `x1`
-- post defs: ["z1", "x1", "y3"]

----

define `y3`
define `x1`
define `z1`
use `y3`
-- post defs: ["z1", "x1", "y3"]

----

define `y3`
define `x1`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "x1", "y3"]

----

define `y3`
define `x1`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "x1", "y3"]

----

define `y3`
define `x1`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "x1", "y3"]

----

define `y3`
define `x1`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "x1", "y3"]

----

define `y3`
define `x1`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "x1", "y3"]

----

define `y3`
define `x1`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "x1", "y3"]

----

define `y3`
define `x1`
define `z2`
use `z2`
-- post defs: ["z2", "x1", "y3"]

----

define `y3`
define `x1`
define `z2`
use `x1`
-- post defs: ["z2", "x1", "y3"]

----

define `y3`
define `x1`
define `z2`
use `y3`
-- post defs: ["z2", "x1", "y3"]

----

define `y3`
define `x1`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "x1", "y3"]

----

define `y3`
define `x1`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "x1", "y3"]

----

define `y3`
define `x1`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "x1", "y3"]

----

define `y3`
define `x1`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "x1", "y3"]

----

define `y3`
define `x1`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "x1", "y3"]

----

define `y3`
define `x1`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "x1", "y3"]

----

define `y3`
define `x1`
define `z3`
use `z3`
-- post defs: ["z3", "x1", "y3"]

----

define `y3`
define `x1`
define `z3`
use `x1`
-- post defs: ["z3", "x1", "y3"]

----

define `y3`
define `x1`
define `z3`
use `y3`
-- post defs: ["z3", "x1", "y3"]

----

define `y3`
define `x1`
use `x1`
define `x2`
-- post defs: ["x2", "x1", "y3"]

----

define `y3`
define `x1`
use `x1`
define `x3`
-- post defs: ["x3", "x1", "y3"]

----

define `y3`
define `x1`
use `x1`
define `y1`
-- post defs: ["y1", "x1", "y3"]

----

define `y3`
define `x1`
use `x1`
define `y2`
-- post defs: ["y2", "x1", "y3"]

----

define `y3`
define `x1`
use `x1`
define `z1`
-- post defs: ["z1", "x1", "y3"]

----

define `y3`
define `x1`
use `x1`
define `z2`
-- post defs: ["z2", "x1", "y3"]

----

define `y3`
define `x1`
use `x1`
define `z3`
-- post defs: ["z3", "x1", "y3"]

----

define `y3`
define `x1`
use `x1`
use `x1`
-- post defs: ["x1", "y3"]

----

define `y3`
define `x1`
use `x1`
use `y3`
-- post defs: ["x1", "y3"]

----

define `y3`
define `x1`
use `y3`
define `x2`
-- post defs: ["x2", "x1", "y3"]

----

define `y3`
define `x1`
use `y3`
define `x3`
-- post defs: ["x3", "x1", "y3"]

----

define `y3`
define `x1`
use `y3`
define `y1`
-- post defs: ["y1", "x1", "y3"]

----

define `y3`
define `x1`
use `y3`
define `y2`
-- post defs: ["y2", "x1", "y3"]

----

define `y3`
define `x1`
use `y3`
define `z1`
-- post defs: ["z1", "x1", "y3"]

----

define `y3`
define `x1`
use `y3`
define `z2`
-- post defs: ["z2", "x1", "y3"]

----

define `y3`
define `x1`
use `y3`
define `z3`
-- post defs: ["z3", "x1", "y3"]

----

define `y3`
define `x1`
use `y3`
use `x1`
-- post defs: ["x1", "y3"]

----

define `y3`
define `x1`
use `y3`
use `y3`
-- post defs: ["x1", "y3"]

----

define `y3`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "y3"]

----

define `y3`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "y3"]

----

define `y3`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "y3"]

----

define `y3`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "y3"]

----

define `y3`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "y3"]

----

define `y3`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "y3"]

----

define `y3`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "y3"]

----

define `y3`
define `x2`
use `x2`
-- post defs: ["x2", "y3"]

----

define `y3`
define `x2`
use `y3`
-- post defs: ["x2", "y3"]

----

define `y3`
define `x2`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "x2", "y3"]

----

define `y3`
define `x2`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "x2", "y3"]

----

define `y3`
define `x2`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "x2", "y3"]

----

define `y3`
define `x2`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "x2", "y3"]

----

define `y3`
define `x2`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "x2", "y3"]

----

define `y3`
define `x2`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "x2", "y3"]

----

define `y3`
define `x2`
define `x1`
use `x1`
-- post defs: ["x1", "x2", "y3"]

----

define `y3`
define `x2`
define `x1`
use `x2`
-- post defs: ["x1", "x2", "y3"]

----

define `y3`
define `x2`
define `x1`
use `y3`
-- post defs: ["x1", "x2", "y3"]

----

define `y3`
define `x2`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "x2", "y3"]

----

define `y3`
define `x2`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "x2", "y3"]

----

define `y3`
define `x2`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "x2", "y3"]

----

define `y3`
define `x2`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "x2", "y3"]

----

define `y3`
define `x2`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "x2", "y3"]

----

define `y3`
define `x2`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "x2", "y3"]

----

define `y3`
define `x2`
define `x3`
use `x3`
-- post defs: ["x3", "x2", "y3"]

----

define `y3`
define `x2`
define `x3`
use `x2`
-- post defs: ["x3", "x2", "y3"]

----

define `y3`
define `x2`
define `x3`
use `y3`
-- post defs: ["x3", "x2", "y3"]

----

define `y3`
define `x2`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "x2", "y3"]

----

define `y3`
define `x2`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "x2", "y3"]

----

define `y3`
define `x2`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "x2", "y3"]

----

define `y3`
define `x2`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "x2", "y3"]

----

define `y3`
define `x2`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "x2", "y3"]

----

define `y3`
define `x2`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "x2", "y3"]

----

define `y3`
define `x2`
define `y1`
use `y1`
-- post defs: ["y1", "x2", "y3"]

----

define `y3`
define `x2`
define `y1`
use `x2`
-- post defs: ["y1", "x2", "y3"]

----

define `y3`
define `x2`
define `y1`
use `y3`
-- post defs: ["y1", "x2", "y3"]

----

define `y3`
define `x2`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "x2", "y3"]

----

define `y3`
define `x2`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "x2", "y3"]

----

define `y3`
define `x2`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "x2", "y3"]

----

define `y3`
define `x2`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "x2", "y3"]

----

define `y3`
define `x2`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "x2", "y3"]

----

define `y3`
define `x2`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "x2", "y3"]

----

define `y3`
define `x2`
define `y2`
use `y2`
-- post defs: ["y2", "x2", "y3"]

----

define `y3`
define `x2`
define `y2`
use `x2`
-- post defs: ["y2", "x2", "y3"]

----

define `y3`
define `x2`
define `y2`
use `y3`
-- post defs: ["y2", "x2", "y3"]

----

define `y3`
define `x2`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "x2", "y3"]

----

define `y3`
define `x2`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "x2", "y3"]

----

define `y3`
define `x2`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "x2", "y3"]

----

define `y3`
define `x2`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "x2", "y3"]

----

define `y3`
define `x2`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "x2", "y3"]

----

define `y3`
define `x2`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "x2", "y3"]

----

define `y3`
define `x2`
define `z1`
use `z1`
-- post defs: ["z1", "x2", "y3"]

----

define `y3`
define `x2`
define `z1`
use `x2`
-- post defs: ["z1", "x2", "y3"]

----

define `y3`
define `x2`
define `z1`
use `y3`
-- post defs: ["z1", "x2", "y3"]

----

define `y3`
define `x2`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "x2", "y3"]

----

define `y3`
define `x2`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "x2", "y3"]

----

define `y3`
define `x2`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "x2", "y3"]

----

define `y3`
define `x2`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "x2", "y3"]

----

define `y3`
define `x2`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "x2", "y3"]

----

define `y3`
define `x2`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "x2", "y3"]

----

define `y3`
define `x2`
define `z2`
use `z2`
-- post defs: ["z2", "x2", "y3"]

----

define `y3`
define `x2`
define `z2`
use `x2`
-- post defs: ["z2", "x2", "y3"]

----

define `y3`
define `x2`
define `z2`
use `y3`
-- post defs: ["z2", "x2", "y3"]

----

define `y3`
define `x2`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "x2", "y3"]

----

define `y3`
define `x2`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "x2", "y3"]

----

define `y3`
define `x2`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "x2", "y3"]

----

define `y3`
define `x2`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "x2", "y3"]

----

define `y3`
define `x2`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "x2", "y3"]

----

define `y3`
define `x2`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "x2", "y3"]

----

define `y3`
define `x2`
define `z3`
use `z3`
-- post defs: ["z3", "x2", "y3"]

----

define `y3`
define `x2`
define `z3`
use `x2`
-- post defs: ["z3", "x2", "y3"]

----

define `y3`
define `x2`
define `z3`
use `y3`
-- post defs: ["z3", "x2", "y3"]

----

define `y3`
define `x2`
use `x2`
define `x1`
-- post defs: ["x1", "x2", "y3"]

----

define `y3`
define `x2`
use `x2`
define `x3`
-- post defs: ["x3", "x2", "y3"]

----

define `y3`
define `x2`
use `x2`
define `y1`
-- post defs: ["y1", "x2", "y3"]

----

define `y3`
define `x2`
use `x2`
define `y2`
-- post defs: ["y2", "x2", "y3"]

----

define `y3`
define `x2`
use `x2`
define `z1`
-- post defs: ["z1", "x2", "y3"]

----

define `y3`
define `x2`
use `x2`
define `z2`
-- post defs: ["z2", "x2", "y3"]

----

define `y3`
define `x2`
use `x2`
define `z3`
-- post defs: ["z3", "x2", "y3"]

----

define `y3`
define `x2`
use `x2`
use `x2`
-- post defs: ["x2", "y3"]

----

define `y3`
define `x2`
use `x2`
use `y3`
-- post defs: ["x2", "y3"]

----

define `y3`
define `x2`
use `y3`
define `x1`
-- post defs: ["x1", "x2", "y3"]

----

define `y3`
define `x2`
use `y3`
define `x3`
-- post defs: ["x3", "x2", "y3"]

----

define `y3`
define `x2`
use `y3`
define `y1`
-- post defs: ["y1", "x2", "y3"]

----

define `y3`
define `x2`
use `y3`
define `y2`
-- post defs: ["y2", "x2", "y3"]

----

define `y3`
define `x2`
use `y3`
define `z1`
-- post defs: ["z1", "x2", "y3"]

----

define `y3`
define `x2`
use `y3`
define `z2`
-- post defs: ["z2", "x2", "y3"]

----

define `y3`
define `x2`
use `y3`
define `z3`
-- post defs: ["z3", "x2", "y3"]

----

define `y3`
define `x2`
use `y3`
use `x2`
-- post defs: ["x2", "y3"]

----

define `y3`
define `x2`
use `y3`
use `y3`
-- post defs: ["x2", "y3"]

----

define `y3`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "y3"]

----

define `y3`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "y3"]

----

define `y3`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "y3"]

----

define `y3`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "y3"]

----

define `y3`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "y3"]

----

define `y3`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "y3"]

----

define `y3`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "y3"]

----

define `y3`
define `x3`
use `x3`
-- post defs: ["x3", "y3"]

----

define `y3`
define `x3`
use `y3`
-- post defs: ["x3", "y3"]

----

define `y3`
define `x3`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "x3", "y3"]

----

define `y3`
define `x3`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "x3", "y3"]

----

define `y3`
define `x3`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "x3", "y3"]

----

define `y3`
define `x3`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "x3", "y3"]

----

define `y3`
define `x3`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "x3", "y3"]

----

define `y3`
define `x3`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "x3", "y3"]

----

define `y3`
define `x3`
define `x1`
use `x1`
-- post defs: ["x1", "x3", "y3"]

----

define `y3`
define `x3`
define `x1`
use `x3`
-- post defs: ["x1", "x3", "y3"]

----

define `y3`
define `x3`
define `x1`
use `y3`
-- post defs: ["x1", "x3", "y3"]

----

define `y3`
define `x3`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "x3", "y3"]

----

define `y3`
define `x3`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "x3", "y3"]

----

define `y3`
define `x3`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "x3", "y3"]

----

define `y3`
define `x3`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "x3", "y3"]

----

define `y3`
define `x3`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "x3", "y3"]

----

define `y3`
define `x3`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "x3", "y3"]

----

define `y3`
define `x3`
define `x2`
use `x2`
-- post defs: ["x2", "x3", "y3"]

----

define `y3`
define `x3`
define `x2`
use `x3`
-- post defs: ["x2", "x3", "y3"]

----

define `y3`
define `x3`
define `x2`
use `y3`
-- post defs: ["x2", "x3", "y3"]

----

define `y3`
define `x3`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "x3", "y3"]

----

define `y3`
define `x3`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "x3", "y3"]

----

define `y3`
define `x3`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "x3", "y3"]

----

define `y3`
define `x3`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "x3", "y3"]

----

define `y3`
define `x3`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "x3", "y3"]

----

define `y3`
define `x3`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "x3", "y3"]

----

define `y3`
define `x3`
define `y1`
use `y1`
-- post defs: ["y1", "x3", "y3"]

----

define `y3`
define `x3`
define `y1`
use `x3`
-- post defs: ["y1", "x3", "y3"]

----

define `y3`
define `x3`
define `y1`
use `y3`
-- post defs: ["y1", "x3", "y3"]

----

define `y3`
define `x3`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "x3", "y3"]

----

define `y3`
define `x3`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "x3", "y3"]

----

define `y3`
define `x3`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "x3", "y3"]

----

define `y3`
define `x3`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "x3", "y3"]

----

define `y3`
define `x3`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "x3", "y3"]

----

define `y3`
define `x3`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "x3", "y3"]

----

define `y3`
define `x3`
define `y2`
use `y2`
-- post defs: ["y2", "x3", "y3"]

----

define `y3`
define `x3`
define `y2`
use `x3`
-- post defs: ["y2", "x3", "y3"]

----

define `y3`
define `x3`
define `y2`
use `y3`
-- post defs: ["y2", "x3", "y3"]

----

define `y3`
define `x3`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "x3", "y3"]

----

define `y3`
define `x3`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "x3", "y3"]

----

define `y3`
define `x3`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "x3", "y3"]

----

define `y3`
define `x3`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "x3", "y3"]

----

define `y3`
define `x3`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "x3", "y3"]

----

define `y3`
define `x3`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "x3", "y3"]

----

define `y3`
define `x3`
define `z1`
use `z1`
-- post defs: ["z1", "x3", "y3"]

----

define `y3`
define `x3`
define `z1`
use `x3`
-- post defs: ["z1", "x3", "y3"]

----

define `y3`
define `x3`
define `z1`
use `y3`
-- post defs: ["z1", "x3", "y3"]

----

define `y3`
define `x3`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "x3", "y3"]

----

define `y3`
define `x3`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "x3", "y3"]

----

define `y3`
define `x3`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "x3", "y3"]

----

define `y3`
define `x3`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "x3", "y3"]

----

define `y3`
define `x3`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "x3", "y3"]

----

define `y3`
define `x3`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "x3", "y3"]

----

define `y3`
define `x3`
define `z2`
use `z2`
-- post defs: ["z2", "x3", "y3"]

----

define `y3`
define `x3`
define `z2`
use `x3`
-- post defs: ["z2", "x3", "y3"]

----

define `y3`
define `x3`
define `z2`
use `y3`
-- post defs: ["z2", "x3", "y3"]

----

define `y3`
define `x3`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "x3", "y3"]

----

define `y3`
define `x3`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "x3", "y3"]

----

define `y3`
define `x3`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "x3", "y3"]

----

define `y3`
define `x3`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "x3", "y3"]

----

define `y3`
define `x3`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "x3", "y3"]

----

define `y3`
define `x3`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "x3", "y3"]

----

define `y3`
define `x3`
define `z3`
use `z3`
-- post defs: ["z3", "x3", "y3"]

----

define `y3`
define `x3`
define `z3`
use `x3`
-- post defs: ["z3", "x3", "y3"]

----

define `y3`
define `x3`
define `z3`
use `y3`
-- post defs: ["z3", "x3", "y3"]

----

define `y3`
define `x3`
use `x3`
define `x1`
-- post defs: ["x1", "x3", "y3"]

----

define `y3`
define `x3`
use `x3`
define `x2`
-- post defs: ["x2", "x3", "y3"]

----

define `y3`
define `x3`
use `x3`
define `y1`
-- post defs: ["y1", "x3", "y3"]

----

define `y3`
define `x3`
use `x3`
define `y2`
-- post defs: ["y2", "x3", "y3"]

----

define `y3`
define `x3`
use `x3`
define `z1`
-- post defs: ["z1", "x3", "y3"]

----

define `y3`
define `x3`
use `x3`
define `z2`
-- post defs: ["z2", "x3", "y3"]

----

define `y3`
define `x3`
use `x3`
define `z3`
-- post defs: ["z3", "x3", "y3"]

----

define `y3`
define `x3`
use `x3`
use `x3`
-- post defs: ["x3", "y3"]

----

define `y3`
define `x3`
use `x3`
use `y3`
-- post defs: ["x3", "y3"]

----

define `y3`
define `x3`
use `y3`
define `x1`
-- post defs: ["x1", "x3", "y3"]

----

define `y3`
define `x3`
use `y3`
define `x2`
-- post defs: ["x2", "x3", "y3"]

----

define `y3`
define `x3`
use `y3`
define `y1`
-- post defs: ["y1", "x3", "y3"]

----

define `y3`
define `x3`
use `y3`
define `y2`
-- post defs: ["y2", "x3", "y3"]

----

define `y3`
define `x3`
use `y3`
define `z1`
-- post defs: ["z1", "x3", "y3"]

----

define `y3`
define `x3`
use `y3`
define `z2`
-- post defs: ["z2", "x3", "y3"]

----

define `y3`
define `x3`
use `y3`
define `z3`
-- post defs: ["z3", "x3", "y3"]

----

define `y3`
define `x3`
use `y3`
use `x3`
-- post defs: ["x3", "y3"]

----

define `y3`
define `x3`
use `y3`
use `y3`
-- post defs: ["x3", "y3"]

----

define `y3`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "y3"]

----

define `y3`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "y3"]

----

define `y3`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "y3"]

----

define `y3`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "y3"]

----

define `y3`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "y3"]

----

define `y3`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "y3"]

----

define `y3`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "y3"]

----

define `y3`
define `y1`
use `y1`
-- post defs: ["y1", "y3"]

----

define `y3`
define `y1`
use `y3`
-- post defs: ["y1", "y3"]

----

define `y3`
define `y1`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "y1", "y3"]

----

define `y3`
define `y1`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "y1", "y3"]

----

define `y3`
define `y1`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "y1", "y3"]

----

define `y3`
define `y1`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "y1", "y3"]

----

define `y3`
define `y1`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "y1", "y3"]

----

define `y3`
define `y1`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "y1", "y3"]

----

define `y3`
define `y1`
define `x1`
use `x1`
-- post defs: ["x1", "y1", "y3"]

----

define `y3`
define `y1`
define `x1`
use `y1`
-- post defs: ["x1", "y1", "y3"]

----

define `y3`
define `y1`
define `x1`
use `y3`
-- post defs: ["x1", "y1", "y3"]

----

define `y3`
define `y1`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "y1", "y3"]

----

define `y3`
define `y1`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "y1", "y3"]

----

define `y3`
define `y1`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "y1", "y3"]

----

define `y3`
define `y1`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "y1", "y3"]

----

define `y3`
define `y1`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "y1", "y3"]

----

define `y3`
define `y1`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "y1", "y3"]

----

define `y3`
define `y1`
define `x2`
use `x2`
-- post defs: ["x2", "y1", "y3"]

----

define `y3`
define `y1`
define `x2`
use `y1`
-- post defs: ["x2", "y1", "y3"]

----

define `y3`
define `y1`
define `x2`
use `y3`
-- post defs: ["x2", "y1", "y3"]

----

define `y3`
define `y1`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "y1", "y3"]

----

define `y3`
define `y1`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "y1", "y3"]

----

define `y3`
define `y1`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "y1", "y3"]

----

define `y3`
define `y1`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "y1", "y3"]

----

define `y3`
define `y1`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "y1", "y3"]

----

define `y3`
define `y1`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "y1", "y3"]

----

define `y3`
define `y1`
define `x3`
use `x3`
-- post defs: ["x3", "y1", "y3"]

----

define `y3`
define `y1`
define `x3`
use `y1`
-- post defs: ["x3", "y1", "y3"]

----

define `y3`
define `y1`
define `x3`
use `y3`
-- post defs: ["x3", "y1", "y3"]

----

define `y3`
define `y1`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "y1", "y3"]

----

define `y3`
define `y1`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "y1", "y3"]

----

define `y3`
define `y1`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "y1", "y3"]

----

define `y3`
define `y1`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "y1", "y3"]

----

define `y3`
define `y1`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "y1", "y3"]

----

define `y3`
define `y1`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "y1", "y3"]

----

define `y3`
define `y1`
define `y2`
use `y2`
-- post defs: ["y2", "y1", "y3"]

----

define `y3`
define `y1`
define `y2`
use `y1`
-- post defs: ["y2", "y1", "y3"]

----

define `y3`
define `y1`
define `y2`
use `y3`
-- post defs: ["y2", "y1", "y3"]

----

define `y3`
define `y1`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "y1", "y3"]

----

define `y3`
define `y1`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "y1", "y3"]

----

define `y3`
define `y1`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "y1", "y3"]

----

define `y3`
define `y1`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "y1", "y3"]

----

define `y3`
define `y1`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "y1", "y3"]

----

define `y3`
define `y1`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "y1", "y3"]

----

define `y3`
define `y1`
define `z1`
use `z1`
-- post defs: ["z1", "y1", "y3"]

----

define `y3`
define `y1`
define `z1`
use `y1`
-- post defs: ["z1", "y1", "y3"]

----

define `y3`
define `y1`
define `z1`
use `y3`
-- post defs: ["z1", "y1", "y3"]

----

define `y3`
define `y1`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "y1", "y3"]

----

define `y3`
define `y1`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "y1", "y3"]

----

define `y3`
define `y1`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "y1", "y3"]

----

define `y3`
define `y1`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "y1", "y3"]

----

define `y3`
define `y1`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "y1", "y3"]

----

define `y3`
define `y1`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "y1", "y3"]

----

define `y3`
define `y1`
define `z2`
use `z2`
-- post defs: ["z2", "y1", "y3"]

----

define `y3`
define `y1`
define `z2`
use `y1`
-- post defs: ["z2", "y1", "y3"]

----

define `y3`
define `y1`
define `z2`
use `y3`
-- post defs: ["z2", "y1", "y3"]

----

define `y3`
define `y1`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "y1", "y3"]

----

define `y3`
define `y1`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "y1", "y3"]

----

define `y3`
define `y1`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "y1", "y3"]

----

define `y3`
define `y1`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "y1", "y3"]

----

define `y3`
define `y1`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "y1", "y3"]

----

define `y3`
define `y1`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "y1", "y3"]

----

define `y3`
define `y1`
define `z3`
use `z3`
-- post defs: ["z3", "y1", "y3"]

----

define `y3`
define `y1`
define `z3`
use `y1`
-- post defs: ["z3", "y1", "y3"]

----

define `y3`
define `y1`
define `z3`
use `y3`
-- post defs: ["z3", "y1", "y3"]

----

define `y3`
define `y1`
use `y1`
define `x1`
-- post defs: ["x1", "y1", "y3"]

----

define `y3`
define `y1`
use `y1`
define `x2`
-- post defs: ["x2", "y1", "y3"]

----

define `y3`
define `y1`
use `y1`
define `x3`
-- post defs: ["x3", "y1", "y3"]

----

define `y3`
define `y1`
use `y1`
define `y2`
-- post defs: ["y2", "y1", "y3"]

----

define `y3`
define `y1`
use `y1`
define `z1`
-- post defs: ["z1", "y1", "y3"]

----

define `y3`
define `y1`
use `y1`
define `z2`
-- post defs: ["z2", "y1", "y3"]

----

define `y3`
define `y1`
use `y1`
define `z3`
-- post defs: ["z3", "y1", "y3"]

----

define `y3`
define `y1`
use `y1`
use `y1`
-- post defs: ["y1", "y3"]

----

define `y3`
define `y1`
use `y1`
use `y3`
-- post defs: ["y1", "y3"]

----

define `y3`
define `y1`
use `y3`
define `x1`
-- post defs: ["x1", "y1", "y3"]

----

define `y3`
define `y1`
use `y3`
define `x2`
-- post defs: ["x2", "y1", "y3"]

----

define `y3`
define `y1`
use `y3`
define `x3`
-- post defs: ["x3", "y1", "y3"]

----

define `y3`
define `y1`
use `y3`
define `y2`
-- post defs: ["y2", "y1", "y3"]

----

define `y3`
define `y1`
use `y3`
define `z1`
-- post defs: ["z1", "y1", "y3"]

----

define `y3`
define `y1`
use `y3`
define `z2`
-- post defs: ["z2", "y1", "y3"]

----

define `y3`
define `y1`
use `y3`
define `z3`
-- post defs: ["z3", "y1", "y3"]

----

define `y3`
define `y1`
use `y3`
use `y1`
-- post defs: ["y1", "y3"]

----

define `y3`
define `y1`
use `y3`
use `y3`
-- post defs: ["y1", "y3"]

----

define `y3`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "y3"]

----

define `y3`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "y3"]

----

define `y3`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "y3"]

----

define `y3`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "y3"]

----

define `y3`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "y3"]

----

define `y3`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "y3"]

----

define `y3`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "y3"]

----

define `y3`
define `y2`
use `y2`
-- post defs: ["y2", "y3"]

----

define `y3`
define `y2`
use `y3`
-- post defs: ["y2", "y3"]

----

define `y3`
define `y2`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "y2", "y3"]

----

define `y3`
define `y2`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "y2", "y3"]

----

define `y3`
define `y2`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "y2", "y3"]

----

define `y3`
define `y2`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "y2", "y3"]

----

define `y3`
define `y2`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "y2", "y3"]

----

define `y3`
define `y2`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "y2", "y3"]

----

define `y3`
define `y2`
define `x1`
use `x1`
-- post defs: ["x1", "y2", "y3"]

----

define `y3`
define `y2`
define `x1`
use `y2`
-- post defs: ["x1", "y2", "y3"]

----

define `y3`
define `y2`
define `x1`
use `y3`
-- post defs: ["x1", "y2", "y3"]

----

define `y3`
define `y2`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "y2", "y3"]

----

define `y3`
define `y2`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "y2", "y3"]

----

define `y3`
define `y2`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "y2", "y3"]

----

define `y3`
define `y2`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "y2", "y3"]

----

define `y3`
define `y2`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "y2", "y3"]

----

define `y3`
define `y2`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "y2", "y3"]

----

define `y3`
define `y2`
define `x2`
use `x2`
-- post defs: ["x2", "y2", "y3"]

----

define `y3`
define `y2`
define `x2`
use `y2`
-- post defs: ["x2", "y2", "y3"]

----

define `y3`
define `y2`
define `x2`
use `y3`
-- post defs: ["x2", "y2", "y3"]

----

define `y3`
define `y2`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "y2", "y3"]

----

define `y3`
define `y2`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "y2", "y3"]

----

define `y3`
define `y2`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "y2", "y3"]

----

define `y3`
define `y2`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "y2", "y3"]

----

define `y3`
define `y2`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "y2", "y3"]

----

define `y3`
define `y2`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "y2", "y3"]

----

define `y3`
define `y2`
define `x3`
use `x3`
-- post defs: ["x3", "y2", "y3"]

----

define `y3`
define `y2`
define `x3`
use `y2`
-- post defs: ["x3", "y2", "y3"]

----

define `y3`
define `y2`
define `x3`
use `y3`
-- post defs: ["x3", "y2", "y3"]

----

define `y3`
define `y2`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "y2", "y3"]

----

define `y3`
define `y2`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "y2", "y3"]

----

define `y3`
define `y2`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "y2", "y3"]

----

define `y3`
define `y2`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "y2", "y3"]

----

define `y3`
define `y2`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "y2", "y3"]

----

define `y3`
define `y2`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "y2", "y3"]

----

define `y3`
define `y2`
define `y1`
use `y1`
-- post defs: ["y1", "y2", "y3"]

----

define `y3`
define `y2`
define `y1`
use `y2`
-- post defs: ["y1", "y2", "y3"]

----

define `y3`
define `y2`
define `y1`
use `y3`
-- post defs: ["y1", "y2", "y3"]

----

define `y3`
define `y2`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "y2", "y3"]

----

define `y3`
define `y2`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "y2", "y3"]

----

define `y3`
define `y2`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "y2", "y3"]

----

define `y3`
define `y2`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "y2", "y3"]

----

define `y3`
define `y2`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "y2", "y3"]

----

define `y3`
define `y2`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "y2", "y3"]

----

define `y3`
define `y2`
define `z1`
use `z1`
-- post defs: ["z1", "y2", "y3"]

----

define `y3`
define `y2`
define `z1`
use `y2`
-- post defs: ["z1", "y2", "y3"]

----

define `y3`
define `y2`
define `z1`
use `y3`
-- post defs: ["z1", "y2", "y3"]

----

define `y3`
define `y2`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "y2", "y3"]

----

define `y3`
define `y2`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "y2", "y3"]

----

define `y3`
define `y2`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "y2", "y3"]

----

define `y3`
define `y2`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "y2", "y3"]

----

define `y3`
define `y2`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "y2", "y3"]

----

define `y3`
define `y2`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "y2", "y3"]

----

define `y3`
define `y2`
define `z2`
use `z2`
-- post defs: ["z2", "y2", "y3"]

----

define `y3`
define `y2`
define `z2`
use `y2`
-- post defs: ["z2", "y2", "y3"]

----

define `y3`
define `y2`
define `z2`
use `y3`
-- post defs: ["z2", "y2", "y3"]

----

define `y3`
define `y2`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "y2", "y3"]

----

define `y3`
define `y2`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "y2", "y3"]

----

define `y3`
define `y2`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "y2", "y3"]

----

define `y3`
define `y2`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "y2", "y3"]

----

define `y3`
define `y2`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "y2", "y3"]

----

define `y3`
define `y2`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "y2", "y3"]

----

define `y3`
define `y2`
define `z3`
use `z3`
-- post defs: ["z3", "y2", "y3"]

----

define `y3`
define `y2`
define `z3`
use `y2`
-- post defs: ["z3", "y2", "y3"]

----

define `y3`
define `y2`
define `z3`
use `y3`
-- post defs: ["z3", "y2", "y3"]

----

define `y3`
define `y2`
use `y2`
define `x1`
-- post defs: ["x1", "y2", "y3"]

----

define `y3`
define `y2`
use `y2`
define `x2`
-- post defs: ["x2", "y2", "y3"]

----

define `y3`
define `y2`
use `y2`
define `x3`
-- post defs: ["x3", "y2", "y3"]

----

define `y3`
define `y2`
use `y2`
define `y1`
-- post defs: ["y1", "y2", "y3"]

----

define `y3`
define `y2`
use `y2`
define `z1`
-- post defs: ["z1", "y2", "y3"]

----

define `y3`
define `y2`
use `y2`
define `z2`
-- post defs: ["z2", "y2", "y3"]

----

define `y3`
define `y2`
use `y2`
define `z3`
-- post defs: ["z3", "y2", "y3"]

----

define `y3`
define `y2`
use `y2`
use `y2`
-- post defs: ["y2", "y3"]

----

define `y3`
define `y2`
use `y2`
use `y3`
-- post defs: ["y2", "y3"]

----

define `y3`
define `y2`
use `y3`
define `x1`
-- post defs: ["x1", "y2", "y3"]

----

define `y3`
define `y2`
use `y3`
define `x2`
-- post defs: ["x2", "y2", "y3"]

----

define `y3`
define `y2`
use `y3`
define `x3`
-- post defs: ["x3", "y2", "y3"]

----

define `y3`
define `y2`
use `y3`
define `y1`
-- post defs: ["y1", "y2", "y3"]

----

define `y3`
define `y2`
use `y3`
define `z1`
-- post defs: ["z1", "y2", "y3"]

----

define `y3`
define `y2`
use `y3`
define `z2`
-- post defs: ["z2", "y2", "y3"]

----

define `y3`
define `y2`
use `y3`
define `z3`
-- post defs: ["z3", "y2", "y3"]

----

define `y3`
define `y2`
use `y3`
use `y2`
-- post defs: ["y2", "y3"]

----

define `y3`
define `y2`
use `y3`
use `y3`
-- post defs: ["y2", "y3"]

----

define `y3`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "y3"]

----

define `y3`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "y3"]

----

define `y3`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "y3"]

----

define `y3`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "y3"]

----

define `y3`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "y3"]

----

define `y3`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "y3"]

----

define `y3`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "y3"]

----

define `y3`
define `z1`
use `z1`
-- post defs: ["z1", "y3"]

----

define `y3`
define `z1`
use `y3`
-- post defs: ["z1", "y3"]

----

define `y3`
define `z1`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "z1", "y3"]

----

define `y3`
define `z1`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "z1", "y3"]

----

define `y3`
define `z1`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "z1", "y3"]

----

define `y3`
define `z1`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "z1", "y3"]

----

define `y3`
define `z1`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "z1", "y3"]

----

define `y3`
define `z1`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "z1", "y3"]

----

define `y3`
define `z1`
define `x1`
use `x1`
-- post defs: ["x1", "z1", "y3"]

----

define `y3`
define `z1`
define `x1`
use `z1`
-- post defs: ["x1", "z1", "y3"]

----

define `y3`
define `z1`
define `x1`
use `y3`
-- post defs: ["x1", "z1", "y3"]

----

define `y3`
define `z1`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "z1", "y3"]

----

define `y3`
define `z1`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "z1", "y3"]

----

define `y3`
define `z1`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "z1", "y3"]

----

define `y3`
define `z1`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "z1", "y3"]

----

define `y3`
define `z1`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "z1", "y3"]

----

define `y3`
define `z1`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "z1", "y3"]

----

define `y3`
define `z1`
define `x2`
use `x2`
-- post defs: ["x2", "z1", "y3"]

----

define `y3`
define `z1`
define `x2`
use `z1`
-- post defs: ["x2", "z1", "y3"]

----

define `y3`
define `z1`
define `x2`
use `y3`
-- post defs: ["x2", "z1", "y3"]

----

define `y3`
define `z1`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "z1", "y3"]

----

define `y3`
define `z1`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "z1", "y3"]

----

define `y3`
define `z1`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "z1", "y3"]

----

define `y3`
define `z1`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "z1", "y3"]

----

define `y3`
define `z1`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "z1", "y3"]

----

define `y3`
define `z1`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "z1", "y3"]

----

define `y3`
define `z1`
define `x3`
use `x3`
-- post defs: ["x3", "z1", "y3"]

----

define `y3`
define `z1`
define `x3`
use `z1`
-- post defs: ["x3", "z1", "y3"]

----

define `y3`
define `z1`
define `x3`
use `y3`
-- post defs: ["x3", "z1", "y3"]

----

define `y3`
define `z1`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "z1", "y3"]

----

define `y3`
define `z1`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "z1", "y3"]

----

define `y3`
define `z1`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "z1", "y3"]

----

define `y3`
define `z1`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "z1", "y3"]

----

define `y3`
define `z1`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "z1", "y3"]

----

define `y3`
define `z1`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "z1", "y3"]

----

define `y3`
define `z1`
define `y1`
use `y1`
-- post defs: ["y1", "z1", "y3"]

----

define `y3`
define `z1`
define `y1`
use `z1`
-- post defs: ["y1", "z1", "y3"]

----

define `y3`
define `z1`
define `y1`
use `y3`
-- post defs: ["y1", "z1", "y3"]

----

define `y3`
define `z1`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "z1", "y3"]

----

define `y3`
define `z1`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "z1", "y3"]

----

define `y3`
define `z1`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "z1", "y3"]

----

define `y3`
define `z1`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "z1", "y3"]

----

define `y3`
define `z1`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "z1", "y3"]

----

define `y3`
define `z1`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "z1", "y3"]

----

define `y3`
define `z1`
define `y2`
use `y2`
-- post defs: ["y2", "z1", "y3"]

----

define `y3`
define `z1`
define `y2`
use `z1`
-- post defs: ["y2", "z1", "y3"]

----

define `y3`
define `z1`
define `y2`
use `y3`
-- post defs: ["y2", "z1", "y3"]

----

define `y3`
define `z1`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "z1", "y3"]

----

define `y3`
define `z1`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "z1", "y3"]

----

define `y3`
define `z1`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "z1", "y3"]

----

define `y3`
define `z1`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "z1", "y3"]

----

define `y3`
define `z1`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "z1", "y3"]

----

define `y3`
define `z1`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "z1", "y3"]

----

define `y3`
define `z1`
define `z2`
use `z2`
-- post defs: ["z2", "z1", "y3"]

----

define `y3`
define `z1`
define `z2`
use `z1`
-- post defs: ["z2", "z1", "y3"]

----

define `y3`
define `z1`
define `z2`
use `y3`
-- post defs: ["z2", "z1", "y3"]

----

define `y3`
define `z1`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "z1", "y3"]

----

define `y3`
define `z1`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "z1", "y3"]

----

define `y3`
define `z1`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "z1", "y3"]

----

define `y3`
define `z1`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "z1", "y3"]

----

define `y3`
define `z1`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "z1", "y3"]

----

define `y3`
define `z1`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "z1", "y3"]

----

define `y3`
define `z1`
define `z3`
use `z3`
-- post defs: ["z3", "z1", "y3"]

----

define `y3`
define `z1`
define `z3`
use `z1`
-- post defs: ["z3", "z1", "y3"]

----

define `y3`
define `z1`
define `z3`
use `y3`
-- post defs: ["z3", "z1", "y3"]

----

define `y3`
define `z1`
use `z1`
define `x1`
-- post defs: ["x1", "z1", "y3"]

----

define `y3`
define `z1`
use `z1`
define `x2`
-- post defs: ["x2", "z1", "y3"]

----

define `y3`
define `z1`
use `z1`
define `x3`
-- post defs: ["x3", "z1", "y3"]

----

define `y3`
define `z1`
use `z1`
define `y1`
-- post defs: ["y1", "z1", "y3"]

----

define `y3`
define `z1`
use `z1`
define `y2`
-- post defs: ["y2", "z1", "y3"]

----

define `y3`
define `z1`
use `z1`
define `z2`
-- post defs: ["z2", "z1", "y3"]

----

define `y3`
define `z1`
use `z1`
define `z3`
-- post defs: ["z3", "z1", "y3"]

----

define `y3`
define `z1`
use `z1`
use `z1`
-- post defs: ["z1", "y3"]

----

define `y3`
define `z1`
use `z1`
use `y3`
-- post defs: ["z1", "y3"]

----

define `y3`
define `z1`
use `y3`
define `x1`
-- post defs: ["x1", "z1", "y3"]

----

define `y3`
define `z1`
use `y3`
define `x2`
-- post defs: ["x2", "z1", "y3"]

----

define `y3`
define `z1`
use `y3`
define `x3`
-- post defs: ["x3", "z1", "y3"]

----

define `y3`
define `z1`
use `y3`
define `y1`
-- post defs: ["y1", "z1", "y3"]

----

define `y3`
define `z1`
use `y3`
define `y2`
-- post defs: ["y2", "z1", "y3"]

----

define `y3`
define `z1`
use `y3`
define `z2`
-- post defs: ["z2", "z1", "y3"]

----

define `y3`
define `z1`
use `y3`
define `z3`
-- post defs: ["z3", "z1", "y3"]

----

define `y3`
define `z1`
use `y3`
use `z1`
-- post defs: ["z1", "y3"]

----

define `y3`
define `z1`
use `y3`
use `y3`
-- post defs: ["z1", "y3"]

----

define `y3`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "y3"]

----

define `y3`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "y3"]

----

define `y3`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "y3"]

----

define `y3`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "y3"]

----

define `y3`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "y3"]

----

define `y3`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "y3"]

----

define `y3`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "y3"]

----

define `y3`
define `z2`
use `z2`
-- post defs: ["z2", "y3"]

----

define `y3`
define `z2`
use `y3`
-- post defs: ["z2", "y3"]

----

define `y3`
define `z2`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "z2", "y3"]

----

define `y3`
define `z2`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "z2", "y3"]

----

define `y3`
define `z2`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "z2", "y3"]

----

define `y3`
define `z2`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "z2", "y3"]

----

define `y3`
define `z2`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "z2", "y3"]

----

define `y3`
define `z2`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "z2", "y3"]

----

define `y3`
define `z2`
define `x1`
use `x1`
-- post defs: ["x1", "z2", "y3"]

----

define `y3`
define `z2`
define `x1`
use `z2`
-- post defs: ["x1", "z2", "y3"]

----

define `y3`
define `z2`
define `x1`
use `y3`
-- post defs: ["x1", "z2", "y3"]

----

define `y3`
define `z2`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "z2", "y3"]

----

define `y3`
define `z2`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "z2", "y3"]

----

define `y3`
define `z2`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "z2", "y3"]

----

define `y3`
define `z2`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "z2", "y3"]

----

define `y3`
define `z2`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "z2", "y3"]

----

define `y3`
define `z2`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "z2", "y3"]

----

define `y3`
define `z2`
define `x2`
use `x2`
-- post defs: ["x2", "z2", "y3"]

----

define `y3`
define `z2`
define `x2`
use `z2`
-- post defs: ["x2", "z2", "y3"]

----

define `y3`
define `z2`
define `x2`
use `y3`
-- post defs: ["x2", "z2", "y3"]

----

define `y3`
define `z2`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "z2", "y3"]

----

define `y3`
define `z2`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "z2", "y3"]

----

define `y3`
define `z2`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "z2", "y3"]

----

define `y3`
define `z2`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "z2", "y3"]

----

define `y3`
define `z2`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "z2", "y3"]

----

define `y3`
define `z2`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "z2", "y3"]

----

define `y3`
define `z2`
define `x3`
use `x3`
-- post defs: ["x3", "z2", "y3"]

----

define `y3`
define `z2`
define `x3`
use `z2`
-- post defs: ["x3", "z2", "y3"]

----

define `y3`
define `z2`
define `x3`
use `y3`
-- post defs: ["x3", "z2", "y3"]

----

define `y3`
define `z2`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "z2", "y3"]

----

define `y3`
define `z2`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "z2", "y3"]

----

define `y3`
define `z2`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "z2", "y3"]

----

define `y3`
define `z2`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "z2", "y3"]

----

define `y3`
define `z2`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "z2", "y3"]

----

define `y3`
define `z2`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "z2", "y3"]

----

define `y3`
define `z2`
define `y1`
use `y1`
-- post defs: ["y1", "z2", "y3"]

----

define `y3`
define `z2`
define `y1`
use `z2`
-- post defs: ["y1", "z2", "y3"]

----

define `y3`
define `z2`
define `y1`
use `y3`
-- post defs: ["y1", "z2", "y3"]

----

define `y3`
define `z2`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "z2", "y3"]

----

define `y3`
define `z2`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "z2", "y3"]

----

define `y3`
define `z2`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "z2", "y3"]

----

define `y3`
define `z2`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "z2", "y3"]

----

define `y3`
define `z2`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "z2", "y3"]

----

define `y3`
define `z2`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "z2", "y3"]

----

define `y3`
define `z2`
define `y2`
use `y2`
-- post defs: ["y2", "z2", "y3"]

----

define `y3`
define `z2`
define `y2`
use `z2`
-- post defs: ["y2", "z2", "y3"]

----

define `y3`
define `z2`
define `y2`
use `y3`
-- post defs: ["y2", "z2", "y3"]

----

define `y3`
define `z2`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "z2", "y3"]

----

define `y3`
define `z2`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "z2", "y3"]

----

define `y3`
define `z2`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "z2", "y3"]

----

define `y3`
define `z2`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "z2", "y3"]

----

define `y3`
define `z2`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "z2", "y3"]

----

define `y3`
define `z2`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "z2", "y3"]

----

define `y3`
define `z2`
define `z1`
use `z1`
-- post defs: ["z1", "z2", "y3"]

----

define `y3`
define `z2`
define `z1`
use `z2`
-- post defs: ["z1", "z2", "y3"]

----

define `y3`
define `z2`
define `z1`
use `y3`
-- post defs: ["z1", "z2", "y3"]

----

define `y3`
define `z2`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "z2", "y3"]

----

define `y3`
define `z2`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "z2", "y3"]

----

define `y3`
define `z2`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "z2", "y3"]

----

define `y3`
define `z2`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "z2", "y3"]

----

define `y3`
define `z2`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "z2", "y3"]

----

define `y3`
define `z2`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "z2", "y3"]

----

define `y3`
define `z2`
define `z3`
use `z3`
-- post defs: ["z3", "z2", "y3"]

----

define `y3`
define `z2`
define `z3`
use `z2`
-- post defs: ["z3", "z2", "y3"]

----

define `y3`
define `z2`
define `z3`
use `y3`
-- post defs: ["z3", "z2", "y3"]

----

define `y3`
define `z2`
use `z2`
define `x1`
-- post defs: ["x1", "z2", "y3"]

----

define `y3`
define `z2`
use `z2`
define `x2`
-- post defs: ["x2", "z2", "y3"]

----

define `y3`
define `z2`
use `z2`
define `x3`
-- post defs: ["x3", "z2", "y3"]

----

define `y3`
define `z2`
use `z2`
define `y1`
-- post defs: ["y1", "z2", "y3"]

----

define `y3`
define `z2`
use `z2`
define `y2`
-- post defs: ["y2", "z2", "y3"]

----

define `y3`
define `z2`
use `z2`
define `z1`
-- post defs: ["z1", "z2", "y3"]

----

define `y3`
define `z2`
use `z2`
define `z3`
-- post defs: ["z3", "z2", "y3"]

----

define `y3`
define `z2`
use `z2`
use `z2`
-- post defs: ["z2", "y3"]

----

define `y3`
define `z2`
use `z2`
use `y3`
-- post defs: ["z2", "y3"]

----

define `y3`
define `z2`
use `y3`
define `x1`
-- post defs: ["x1", "z2", "y3"]

----

define `y3`
define `z2`
use `y3`
define `x2`
-- post defs: ["x2", "z2", "y3"]

----

define `y3`
define `z2`
use `y3`
define `x3`
-- post defs: ["x3", "z2", "y3"]

----

define `y3`
define `z2`
use `y3`
define `y1`
-- post defs: ["y1", "z2", "y3"]

----

define `y3`
define `z2`
use `y3`
define `y2`
-- post defs: ["y2", "z2", "y3"]

----

define `y3`
define `z2`
use `y3`
define `z1`
-- post defs: ["z1", "z2", "y3"]

----

define `y3`
define `z2`
use `y3`
define `z3`
-- post defs: ["z3", "z2", "y3"]

----

define `y3`
define `z2`
use `y3`
use `z2`
-- post defs: ["z2", "y3"]

----

define `y3`
define `z2`
use `y3`
use `y3`
-- post defs: ["z2", "y3"]

----

define `y3`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "y3"]

----

define `y3`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "y3"]

----

define `y3`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "y3"]

----

define `y3`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "y3"]

----

define `y3`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "y3"]

----

define `y3`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "y3"]

----

define `y3`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "y3"]

----

define `y3`
define `z3`
use `z3`
-- post defs: ["z3", "y3"]

----

define `y3`
define `z3`
use `y3`
-- post defs: ["z3", "y3"]

----

define `y3`
define `z3`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "z3", "y3"]

----

define `y3`
define `z3`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "z3", "y3"]

----

define `y3`
define `z3`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "z3", "y3"]

----

define `y3`
define `z3`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "z3", "y3"]

----

define `y3`
define `z3`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "z3", "y3"]

----

define `y3`
define `z3`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "z3", "y3"]

----

define `y3`
define `z3`
define `x1`
use `x1`
-- post defs: ["x1", "z3", "y3"]

----

define `y3`
define `z3`
define `x1`
use `z3`
-- post defs: ["x1", "z3", "y3"]

----

define `y3`
define `z3`
define `x1`
use `y3`
-- post defs: ["x1", "z3", "y3"]

----

define `y3`
define `z3`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "z3", "y3"]

----

define `y3`
define `z3`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "z3", "y3"]

----

define `y3`
define `z3`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "z3", "y3"]

----

define `y3`
define `z3`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "z3", "y3"]

----

define `y3`
define `z3`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "z3", "y3"]

----

define `y3`
define `z3`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "z3", "y3"]

----

define `y3`
define `z3`
define `x2`
use `x2`
-- post defs: ["x2", "z3", "y3"]

----

define `y3`
define `z3`
define `x2`
use `z3`
-- post defs: ["x2", "z3", "y3"]

----

define `y3`
define `z3`
define `x2`
use `y3`
-- post defs: ["x2", "z3", "y3"]

----

define `y3`
define `z3`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "z3", "y3"]

----

define `y3`
define `z3`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "z3", "y3"]

----

define `y3`
define `z3`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "z3", "y3"]

----

define `y3`
define `z3`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "z3", "y3"]

----

define `y3`
define `z3`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "z3", "y3"]

----

define `y3`
define `z3`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "z3", "y3"]

----

define `y3`
define `z3`
define `x3`
use `x3`
-- post defs: ["x3", "z3", "y3"]

----

define `y3`
define `z3`
define `x3`
use `z3`
-- post defs: ["x3", "z3", "y3"]

----

define `y3`
define `z3`
define `x3`
use `y3`
-- post defs: ["x3", "z3", "y3"]

----

define `y3`
define `z3`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "z3", "y3"]

----

define `y3`
define `z3`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "z3", "y3"]

----

define `y3`
define `z3`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "z3", "y3"]

----

define `y3`
define `z3`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "z3", "y3"]

----

define `y3`
define `z3`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "z3", "y3"]

----

define `y3`
define `z3`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "z3", "y3"]

----

define `y3`
define `z3`
define `y1`
use `y1`
-- post defs: ["y1", "z3", "y3"]

----

define `y3`
define `z3`
define `y1`
use `z3`
-- post defs: ["y1", "z3", "y3"]

----

define `y3`
define `z3`
define `y1`
use `y3`
-- post defs: ["y1", "z3", "y3"]

----

define `y3`
define `z3`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "z3", "y3"]

----

define `y3`
define `z3`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "z3", "y3"]

----

define `y3`
define `z3`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "z3", "y3"]

----

define `y3`
define `z3`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "z3", "y3"]

----

define `y3`
define `z3`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "z3", "y3"]

----

define `y3`
define `z3`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "z3", "y3"]

----

define `y3`
define `z3`
define `y2`
use `y2`
-- post defs: ["y2", "z3", "y3"]

----

define `y3`
define `z3`
define `y2`
use `z3`
-- post defs: ["y2", "z3", "y3"]

----

define `y3`
define `z3`
define `y2`
use `y3`
-- post defs: ["y2", "z3", "y3"]

----

define `y3`
define `z3`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "z3", "y3"]

----

define `y3`
define `z3`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "z3", "y3"]

----

define `y3`
define `z3`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "z3", "y3"]

----

define `y3`
define `z3`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "z3", "y3"]

----

define `y3`
define `z3`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "z3", "y3"]

----

define `y3`
define `z3`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "z3", "y3"]

----

define `y3`
define `z3`
define `z1`
use `z1`
-- post defs: ["z1", "z3", "y3"]

----

define `y3`
define `z3`
define `z1`
use `z3`
-- post defs: ["z1", "z3", "y3"]

----

define `y3`
define `z3`
define `z1`
use `y3`
-- post defs: ["z1", "z3", "y3"]

----

define `y3`
define `z3`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "z3", "y3"]

----

define `y3`
define `z3`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "z3", "y3"]

----

define `y3`
define `z3`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "z3", "y3"]

----

define `y3`
define `z3`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "z3", "y3"]

----

define `y3`
define `z3`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "z3", "y3"]

----

define `y3`
define `z3`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "z3", "y3"]

----

define `y3`
define `z3`
define `z2`
use `z2`
-- post defs: ["z2", "z3", "y3"]

----

define `y3`
define `z3`
define `z2`
use `z3`
-- post defs: ["z2", "z3", "y3"]

----

define `y3`
define `z3`
define `z2`
use `y3`
-- post defs: ["z2", "z3", "y3"]

----

define `y3`
define `z3`
use `z3`
define `x1`
-- post defs: ["x1", "z3", "y3"]

----

define `y3`
define `z3`
use `z3`
define `x2`
-- post defs: ["x2", "z3", "y3"]

----

define `y3`
define `z3`
use `z3`
define `x3`
-- post defs: ["x3", "z3", "y3"]

----

define `y3`
define `z3`
use `z3`
define `y1`
-- post defs: ["y1", "z3", "y3"]

----

define `y3`
define `z3`
use `z3`
define `y2`
-- post defs: ["y2", "z3", "y3"]

----

define `y3`
define `z3`
use `z3`
define `z1`
-- post defs: ["z1", "z3", "y3"]

----

define `y3`
define `z3`
use `z3`
define `z2`
-- post defs: ["z2", "z3", "y3"]

----

define `y3`
define `z3`
use `z3`
use `z3`
-- post defs: ["z3", "y3"]

----

define `y3`
define `z3`
use `z3`
use `y3`
-- post defs: ["z3", "y3"]

----

define `y3`
define `z3`
use `y3`
define `x1`
-- post defs: ["x1", "z3", "y3"]

----

define `y3`
define `z3`
use `y3`
define `x2`
-- post defs: ["x2", "z3", "y3"]

----

define `y3`
define `z3`
use `y3`
define `x3`
-- post defs: ["x3", "z3", "y3"]

----

define `y3`
define `z3`
use `y3`
define `y1`
-- post defs: ["y1", "z3", "y3"]

----

define `y3`
define `z3`
use `y3`
define `y2`
-- post defs: ["y2", "z3", "y3"]

----

define `y3`
define `z3`
use `y3`
define `z1`
-- post defs: ["z1", "z3", "y3"]

----

define `y3`
define `z3`
use `y3`
define `z2`
-- post defs: ["z2", "z3", "y3"]

----

define `y3`
define `z3`
use `y3`
use `z3`
-- post defs: ["z3", "y3"]

----

define `y3`
define `z3`
use `y3`
use `y3`
-- post defs: ["z3", "y3"]

----

define `y3`
use `y3`
define `x1`
-- post defs: ["x1", "y3"]

----

define `y3`
use `y3`
define `x2`
-- post defs: ["x2", "y3"]

----

define `y3`
use `y3`
define `x3`
-- post defs: ["x3", "y3"]

----

define `y3`
use `y3`
define `y1`
-- post defs: ["y1", "y3"]

----

define `y3`
use `y3`
define `y2`
-- post defs: ["y2", "y3"]

----

define `y3`
use `y3`
define `z1`
-- post defs: ["z1", "y3"]

----

define `y3`
use `y3`
define `z2`
-- post defs: ["z2", "y3"]

----

define `y3`
use `y3`
define `z3`
-- post defs: ["z3", "y3"]

----

define `y3`
use `y3`
use `y3`
-- post defs: ["y3"]

----

define `y3`
use `y3`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "y3"]

----

define `y3`
use `y3`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "y3"]

----

define `y3`
use `y3`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "y3"]

----

define `y3`
use `y3`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "y3"]

----

define `y3`
use `y3`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "y3"]

----

define `y3`
use `y3`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "y3"]

----

define `y3`
use `y3`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "y3"]

----

define `y3`
use `y3`
define `x1`
use `x1`
-- post defs: ["x1", "y3"]

----

define `y3`
use `y3`
define `x1`
use `y3`
-- post defs: ["x1", "y3"]

----

define `y3`
use `y3`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "y3"]

----

define `y3`
use `y3`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "y3"]

----

define `y3`
use `y3`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "y3"]

----

define `y3`
use `y3`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "y3"]

----

define `y3`
use `y3`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "y3"]

----

define `y3`
use `y3`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "y3"]

----

define `y3`
use `y3`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "y3"]

----

define `y3`
use `y3`
define `x2`
use `x2`
-- post defs: ["x2", "y3"]

----

define `y3`
use `y3`
define `x2`
use `y3`
-- post defs: ["x2", "y3"]

----

define `y3`
use `y3`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "y3"]

----

define `y3`
use `y3`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "y3"]

----

define `y3`
use `y3`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "y3"]

----

define `y3`
use `y3`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "y3"]

----

define `y3`
use `y3`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "y3"]

----

define `y3`
use `y3`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "y3"]

----

define `y3`
use `y3`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "y3"]

----

define `y3`
use `y3`
define `x3`
use `x3`
-- post defs: ["x3", "y3"]

----

define `y3`
use `y3`
define `x3`
use `y3`
-- post defs: ["x3", "y3"]

----

define `y3`
use `y3`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "y3"]

----

define `y3`
use `y3`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "y3"]

----

define `y3`
use `y3`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "y3"]

----

define `y3`
use `y3`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "y3"]

----

define `y3`
use `y3`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "y3"]

----

define `y3`
use `y3`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "y3"]

----

define `y3`
use `y3`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "y3"]

----

define `y3`
use `y3`
define `y1`
use `y1`
-- post defs: ["y1", "y3"]

----

define `y3`
use `y3`
define `y1`
use `y3`
-- post defs: ["y1", "y3"]

----

define `y3`
use `y3`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "y3"]

----

define `y3`
use `y3`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "y3"]

----

define `y3`
use `y3`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "y3"]

----

define `y3`
use `y3`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "y3"]

----

define `y3`
use `y3`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "y3"]

----

define `y3`
use `y3`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "y3"]

----

define `y3`
use `y3`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "y3"]

----

define `y3`
use `y3`
define `y2`
use `y2`
-- post defs: ["y2", "y3"]

----

define `y3`
use `y3`
define `y2`
use `y3`
-- post defs: ["y2", "y3"]

----

define `y3`
use `y3`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "y3"]

----

define `y3`
use `y3`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "y3"]

----

define `y3`
use `y3`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "y3"]

----

define `y3`
use `y3`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "y3"]

----

define `y3`
use `y3`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "y3"]

----

define `y3`
use `y3`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "y3"]

----

define `y3`
use `y3`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "y3"]

----

define `y3`
use `y3`
define `z1`
use `z1`
-- post defs: ["z1", "y3"]

----

define `y3`
use `y3`
define `z1`
use `y3`
-- post defs: ["z1", "y3"]

----

define `y3`
use `y3`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "y3"]

----

define `y3`
use `y3`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "y3"]

----

define `y3`
use `y3`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "y3"]

----

define `y3`
use `y3`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "y3"]

----

define `y3`
use `y3`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "y3"]

----

define `y3`
use `y3`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "y3"]

----

define `y3`
use `y3`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "y3"]

----

define `y3`
use `y3`
define `z2`
use `z2`
-- post defs: ["z2", "y3"]

----

define `y3`
use `y3`
define `z2`
use `y3`
-- post defs: ["z2", "y3"]

----

define `y3`
use `y3`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "y3"]

----

define `y3`
use `y3`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "y3"]

----

define `y3`
use `y3`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "y3"]

----

define `y3`
use `y3`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "y3"]

----

define `y3`
use `y3`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "y3"]

----

define `y3`
use `y3`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "y3"]

----

define `y3`
use `y3`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "y3"]

----

define `y3`
use `y3`
define `z3`
use `z3`
-- post defs: ["z3", "y3"]

----

define `y3`
use `y3`
define `z3`
use `y3`
-- post defs: ["z3", "y3"]

----

define `y3`
use `y3`
use `y3`
define `x1`
-- post defs: ["x1", "y3"]

----

define `y3`
use `y3`
use `y3`
define `x2`
-- post defs: ["x2", "y3"]

----

define `y3`
use `y3`
use `y3`
define `x3`
-- post defs: ["x3", "y3"]

----

define `y3`
use `y3`
use `y3`
define `y1`
-- post defs: ["y1", "y3"]

----

define `y3`
use `y3`
use `y3`
define `y2`
-- post defs: ["y2", "y3"]

----

define `y3`
use `y3`
use `y3`
define `z1`
-- post defs: ["z1", "y3"]

----

define `y3`
use `y3`
use `y3`
define `z2`
-- post defs: ["z2", "y3"]

----

define `y3`
use `y3`
use `y3`
define `z3`
-- post defs: ["z3", "y3"]

----

define `y3`
use `y3`
use `y3`
use `y3`
-- post defs: ["y3"]

----

define `z1`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "z1"]

----

define `z1`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "z1"]

----

define `z1`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "z1"]

----

define `z1`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "z1"]

----

define `z1`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "z1"]

----

define `z1`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "z1"]

----

define `z1`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "z1"]

----

define `z1`
define `x1`
use `x1`
-- post defs: ["x1", "z1"]

----

define `z1`
define `x1`
use `z1`
-- post defs: ["x1", "z1"]

----

define `z1`
define `x1`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "x1", "z1"]

----

define `z1`
define `x1`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "x1", "z1"]

----

define `z1`
define `x1`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "x1", "z1"]

----

define `z1`
define `x1`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "x1", "z1"]

----

define `z1`
define `x1`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "x1", "z1"]

----

define `z1`
define `x1`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "x1", "z1"]

----

define `z1`
define `x1`
define `x2`
use `x2`
-- post defs: ["x2", "x1", "z1"]

----

define `z1`
define `x1`
define `x2`
use `x1`
-- post defs: ["x2", "x1", "z1"]

----

define `z1`
define `x1`
define `x2`
use `z1`
-- post defs: ["x2", "x1", "z1"]

----

define `z1`
define `x1`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "x1", "z1"]

----

define `z1`
define `x1`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "x1", "z1"]

----

define `z1`
define `x1`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "x1", "z1"]

----

define `z1`
define `x1`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "x1", "z1"]

----

define `z1`
define `x1`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "x1", "z1"]

----

define `z1`
define `x1`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "x1", "z1"]

----

define `z1`
define `x1`
define `x3`
use `x3`
-- post defs: ["x3", "x1", "z1"]

----

define `z1`
define `x1`
define `x3`
use `x1`
-- post defs: ["x3", "x1", "z1"]

----

define `z1`
define `x1`
define `x3`
use `z1`
-- post defs: ["x3", "x1", "z1"]

----

define `z1`
define `x1`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "x1", "z1"]

----

define `z1`
define `x1`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "x1", "z1"]

----

define `z1`
define `x1`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "x1", "z1"]

----

define `z1`
define `x1`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "x1", "z1"]

----

define `z1`
define `x1`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "x1", "z1"]

----

define `z1`
define `x1`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "x1", "z1"]

----

define `z1`
define `x1`
define `y1`
use `y1`
-- post defs: ["y1", "x1", "z1"]

----

define `z1`
define `x1`
define `y1`
use `x1`
-- post defs: ["y1", "x1", "z1"]

----

define `z1`
define `x1`
define `y1`
use `z1`
-- post defs: ["y1", "x1", "z1"]

----

define `z1`
define `x1`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "x1", "z1"]

----

define `z1`
define `x1`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "x1", "z1"]

----

define `z1`
define `x1`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "x1", "z1"]

----

define `z1`
define `x1`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "x1", "z1"]

----

define `z1`
define `x1`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "x1", "z1"]

----

define `z1`
define `x1`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "x1", "z1"]

----

define `z1`
define `x1`
define `y2`
use `y2`
-- post defs: ["y2", "x1", "z1"]

----

define `z1`
define `x1`
define `y2`
use `x1`
-- post defs: ["y2", "x1", "z1"]

----

define `z1`
define `x1`
define `y2`
use `z1`
-- post defs: ["y2", "x1", "z1"]

----

define `z1`
define `x1`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "x1", "z1"]

----

define `z1`
define `x1`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "x1", "z1"]

----

define `z1`
define `x1`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "x1", "z1"]

----

define `z1`
define `x1`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "x1", "z1"]

----

define `z1`
define `x1`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "x1", "z1"]

----

define `z1`
define `x1`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "x1", "z1"]

----

define `z1`
define `x1`
define `y3`
use `y3`
-- post defs: ["y3", "x1", "z1"]

----

define `z1`
define `x1`
define `y3`
use `x1`
-- post defs: ["y3", "x1", "z1"]

----

define `z1`
define `x1`
define `y3`
use `z1`
-- post defs: ["y3", "x1", "z1"]

----

define `z1`
define `x1`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "x1", "z1"]

----

define `z1`
define `x1`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "x1", "z1"]

----

define `z1`
define `x1`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "x1", "z1"]

----

define `z1`
define `x1`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "x1", "z1"]

----

define `z1`
define `x1`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "x1", "z1"]

----

define `z1`
define `x1`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "x1", "z1"]

----

define `z1`
define `x1`
define `z2`
use `z2`
-- post defs: ["z2", "x1", "z1"]

----

define `z1`
define `x1`
define `z2`
use `x1`
-- post defs: ["z2", "x1", "z1"]

----

define `z1`
define `x1`
define `z2`
use `z1`
-- post defs: ["z2", "x1", "z1"]

----

define `z1`
define `x1`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "x1", "z1"]

----

define `z1`
define `x1`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "x1", "z1"]

----

define `z1`
define `x1`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "x1", "z1"]

----

define `z1`
define `x1`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "x1", "z1"]

----

define `z1`
define `x1`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "x1", "z1"]

----

define `z1`
define `x1`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "x1", "z1"]

----

define `z1`
define `x1`
define `z3`
use `z3`
-- post defs: ["z3", "x1", "z1"]

----

define `z1`
define `x1`
define `z3`
use `x1`
-- post defs: ["z3", "x1", "z1"]

----

define `z1`
define `x1`
define `z3`
use `z1`
-- post defs: ["z3", "x1", "z1"]

----

define `z1`
define `x1`
use `x1`
define `x2`
-- post defs: ["x2", "x1", "z1"]

----

define `z1`
define `x1`
use `x1`
define `x3`
-- post defs: ["x3", "x1", "z1"]

----

define `z1`
define `x1`
use `x1`
define `y1`
-- post defs: ["y1", "x1", "z1"]

----

define `z1`
define `x1`
use `x1`
define `y2`
-- post defs: ["y2", "x1", "z1"]

----

define `z1`
define `x1`
use `x1`
define `y3`
-- post defs: ["y3", "x1", "z1"]

----

define `z1`
define `x1`
use `x1`
define `z2`
-- post defs: ["z2", "x1", "z1"]

----

define `z1`
define `x1`
use `x1`
define `z3`
-- post defs: ["z3", "x1", "z1"]

----

define `z1`
define `x1`
use `x1`
use `x1`
-- post defs: ["x1", "z1"]

----

define `z1`
define `x1`
use `x1`
use `z1`
-- post defs: ["x1", "z1"]

----

define `z1`
define `x1`
use `z1`
define `x2`
-- post defs: ["x2", "x1", "z1"]

----

define `z1`
define `x1`
use `z1`
define `x3`
-- post defs: ["x3", "x1", "z1"]

----

define `z1`
define `x1`
use `z1`
define `y1`
-- post defs: ["y1", "x1", "z1"]

----

define `z1`
define `x1`
use `z1`
define `y2`
-- post defs: ["y2", "x1", "z1"]

----

define `z1`
define `x1`
use `z1`
define `y3`
-- post defs: ["y3", "x1", "z1"]

----

define `z1`
define `x1`
use `z1`
define `z2`
-- post defs: ["z2", "x1", "z1"]

----

define `z1`
define `x1`
use `z1`
define `z3`
-- post defs: ["z3", "x1", "z1"]

----

define `z1`
define `x1`
use `z1`
use `x1`
-- post defs: ["x1", "z1"]

----

define `z1`
define `x1`
use `z1`
use `z1`
-- post defs: ["x1", "z1"]

----

define `z1`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "z1"]

----

define `z1`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "z1"]

----

define `z1`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "z1"]

----

define `z1`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "z1"]

----

define `z1`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "z1"]

----

define `z1`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "z1"]

----

define `z1`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "z1"]

----

define `z1`
define `x2`
use `x2`
-- post defs: ["x2", "z1"]

----

define `z1`
define `x2`
use `z1`
-- post defs: ["x2", "z1"]

----

define `z1`
define `x2`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "x2", "z1"]

----

define `z1`
define `x2`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "x2", "z1"]

----

define `z1`
define `x2`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "x2", "z1"]

----

define `z1`
define `x2`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "x2", "z1"]

----

define `z1`
define `x2`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "x2", "z1"]

----

define `z1`
define `x2`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "x2", "z1"]

----

define `z1`
define `x2`
define `x1`
use `x1`
-- post defs: ["x1", "x2", "z1"]

----

define `z1`
define `x2`
define `x1`
use `x2`
-- post defs: ["x1", "x2", "z1"]

----

define `z1`
define `x2`
define `x1`
use `z1`
-- post defs: ["x1", "x2", "z1"]

----

define `z1`
define `x2`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "x2", "z1"]

----

define `z1`
define `x2`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "x2", "z1"]

----

define `z1`
define `x2`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "x2", "z1"]

----

define `z1`
define `x2`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "x2", "z1"]

----

define `z1`
define `x2`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "x2", "z1"]

----

define `z1`
define `x2`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "x2", "z1"]

----

define `z1`
define `x2`
define `x3`
use `x3`
-- post defs: ["x3", "x2", "z1"]

----

define `z1`
define `x2`
define `x3`
use `x2`
-- post defs: ["x3", "x2", "z1"]

----

define `z1`
define `x2`
define `x3`
use `z1`
-- post defs: ["x3", "x2", "z1"]

----

define `z1`
define `x2`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "x2", "z1"]

----

define `z1`
define `x2`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "x2", "z1"]

----

define `z1`
define `x2`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "x2", "z1"]

----

define `z1`
define `x2`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "x2", "z1"]

----

define `z1`
define `x2`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "x2", "z1"]

----

define `z1`
define `x2`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "x2", "z1"]

----

define `z1`
define `x2`
define `y1`
use `y1`
-- post defs: ["y1", "x2", "z1"]

----

define `z1`
define `x2`
define `y1`
use `x2`
-- post defs: ["y1", "x2", "z1"]

----

define `z1`
define `x2`
define `y1`
use `z1`
-- post defs: ["y1", "x2", "z1"]

----

define `z1`
define `x2`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "x2", "z1"]

----

define `z1`
define `x2`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "x2", "z1"]

----

define `z1`
define `x2`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "x2", "z1"]

----

define `z1`
define `x2`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "x2", "z1"]

----

define `z1`
define `x2`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "x2", "z1"]

----

define `z1`
define `x2`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "x2", "z1"]

----

define `z1`
define `x2`
define `y2`
use `y2`
-- post defs: ["y2", "x2", "z1"]

----

define `z1`
define `x2`
define `y2`
use `x2`
-- post defs: ["y2", "x2", "z1"]

----

define `z1`
define `x2`
define `y2`
use `z1`
-- post defs: ["y2", "x2", "z1"]

----

define `z1`
define `x2`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "x2", "z1"]

----

define `z1`
define `x2`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "x2", "z1"]

----

define `z1`
define `x2`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "x2", "z1"]

----

define `z1`
define `x2`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "x2", "z1"]

----

define `z1`
define `x2`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "x2", "z1"]

----

define `z1`
define `x2`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "x2", "z1"]

----

define `z1`
define `x2`
define `y3`
use `y3`
-- post defs: ["y3", "x2", "z1"]

----

define `z1`
define `x2`
define `y3`
use `x2`
-- post defs: ["y3", "x2", "z1"]

----

define `z1`
define `x2`
define `y3`
use `z1`
-- post defs: ["y3", "x2", "z1"]

----

define `z1`
define `x2`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "x2", "z1"]

----

define `z1`
define `x2`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "x2", "z1"]

----

define `z1`
define `x2`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "x2", "z1"]

----

define `z1`
define `x2`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "x2", "z1"]

----

define `z1`
define `x2`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "x2", "z1"]

----

define `z1`
define `x2`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "x2", "z1"]

----

define `z1`
define `x2`
define `z2`
use `z2`
-- post defs: ["z2", "x2", "z1"]

----

define `z1`
define `x2`
define `z2`
use `x2`
-- post defs: ["z2", "x2", "z1"]

----

define `z1`
define `x2`
define `z2`
use `z1`
-- post defs: ["z2", "x2", "z1"]

----

define `z1`
define `x2`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "x2", "z1"]

----

define `z1`
define `x2`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "x2", "z1"]

----

define `z1`
define `x2`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "x2", "z1"]

----

define `z1`
define `x2`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "x2", "z1"]

----

define `z1`
define `x2`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "x2", "z1"]

----

define `z1`
define `x2`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "x2", "z1"]

----

define `z1`
define `x2`
define `z3`
use `z3`
-- post defs: ["z3", "x2", "z1"]

----

define `z1`
define `x2`
define `z3`
use `x2`
-- post defs: ["z3", "x2", "z1"]

----

define `z1`
define `x2`
define `z3`
use `z1`
-- post defs: ["z3", "x2", "z1"]

----

define `z1`
define `x2`
use `x2`
define `x1`
-- post defs: ["x1", "x2", "z1"]

----

define `z1`
define `x2`
use `x2`
define `x3`
-- post defs: ["x3", "x2", "z1"]

----

define `z1`
define `x2`
use `x2`
define `y1`
-- post defs: ["y1", "x2", "z1"]

----

define `z1`
define `x2`
use `x2`
define `y2`
-- post defs: ["y2", "x2", "z1"]

----

define `z1`
define `x2`
use `x2`
define `y3`
-- post defs: ["y3", "x2", "z1"]

----

define `z1`
define `x2`
use `x2`
define `z2`
-- post defs: ["z2", "x2", "z1"]

----

define `z1`
define `x2`
use `x2`
define `z3`
-- post defs: ["z3", "x2", "z1"]

----

define `z1`
define `x2`
use `x2`
use `x2`
-- post defs: ["x2", "z1"]

----

define `z1`
define `x2`
use `x2`
use `z1`
-- post defs: ["x2", "z1"]

----

define `z1`
define `x2`
use `z1`
define `x1`
-- post defs: ["x1", "x2", "z1"]

----

define `z1`
define `x2`
use `z1`
define `x3`
-- post defs: ["x3", "x2", "z1"]

----

define `z1`
define `x2`
use `z1`
define `y1`
-- post defs: ["y1", "x2", "z1"]

----

define `z1`
define `x2`
use `z1`
define `y2`
-- post defs: ["y2", "x2", "z1"]

----

define `z1`
define `x2`
use `z1`
define `y3`
-- post defs: ["y3", "x2", "z1"]

----

define `z1`
define `x2`
use `z1`
define `z2`
-- post defs: ["z2", "x2", "z1"]

----

define `z1`
define `x2`
use `z1`
define `z3`
-- post defs: ["z3", "x2", "z1"]

----

define `z1`
define `x2`
use `z1`
use `x2`
-- post defs: ["x2", "z1"]

----

define `z1`
define `x2`
use `z1`
use `z1`
-- post defs: ["x2", "z1"]

----

define `z1`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "z1"]

----

define `z1`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "z1"]

----

define `z1`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "z1"]

----

define `z1`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "z1"]

----

define `z1`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "z1"]

----

define `z1`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "z1"]

----

define `z1`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "z1"]

----

define `z1`
define `x3`
use `x3`
-- post defs: ["x3", "z1"]

----

define `z1`
define `x3`
use `z1`
-- post defs: ["x3", "z1"]

----

define `z1`
define `x3`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "x3", "z1"]

----

define `z1`
define `x3`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "x3", "z1"]

----

define `z1`
define `x3`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "x3", "z1"]

----

define `z1`
define `x3`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "x3", "z1"]

----

define `z1`
define `x3`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "x3", "z1"]

----

define `z1`
define `x3`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "x3", "z1"]

----

define `z1`
define `x3`
define `x1`
use `x1`
-- post defs: ["x1", "x3", "z1"]

----

define `z1`
define `x3`
define `x1`
use `x3`
-- post defs: ["x1", "x3", "z1"]

----

define `z1`
define `x3`
define `x1`
use `z1`
-- post defs: ["x1", "x3", "z1"]

----

define `z1`
define `x3`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "x3", "z1"]

----

define `z1`
define `x3`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "x3", "z1"]

----

define `z1`
define `x3`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "x3", "z1"]

----

define `z1`
define `x3`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "x3", "z1"]

----

define `z1`
define `x3`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "x3", "z1"]

----

define `z1`
define `x3`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "x3", "z1"]

----

define `z1`
define `x3`
define `x2`
use `x2`
-- post defs: ["x2", "x3", "z1"]

----

define `z1`
define `x3`
define `x2`
use `x3`
-- post defs: ["x2", "x3", "z1"]

----

define `z1`
define `x3`
define `x2`
use `z1`
-- post defs: ["x2", "x3", "z1"]

----

define `z1`
define `x3`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "x3", "z1"]

----

define `z1`
define `x3`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "x3", "z1"]

----

define `z1`
define `x3`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "x3", "z1"]

----

define `z1`
define `x3`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "x3", "z1"]

----

define `z1`
define `x3`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "x3", "z1"]

----

define `z1`
define `x3`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "x3", "z1"]

----

define `z1`
define `x3`
define `y1`
use `y1`
-- post defs: ["y1", "x3", "z1"]

----

define `z1`
define `x3`
define `y1`
use `x3`
-- post defs: ["y1", "x3", "z1"]

----

define `z1`
define `x3`
define `y1`
use `z1`
-- post defs: ["y1", "x3", "z1"]

----

define `z1`
define `x3`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "x3", "z1"]

----

define `z1`
define `x3`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "x3", "z1"]

----

define `z1`
define `x3`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "x3", "z1"]

----

define `z1`
define `x3`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "x3", "z1"]

----

define `z1`
define `x3`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "x3", "z1"]

----

define `z1`
define `x3`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "x3", "z1"]

----

define `z1`
define `x3`
define `y2`
use `y2`
-- post defs: ["y2", "x3", "z1"]

----

define `z1`
define `x3`
define `y2`
use `x3`
-- post defs: ["y2", "x3", "z1"]

----

define `z1`
define `x3`
define `y2`
use `z1`
-- post defs: ["y2", "x3", "z1"]

----

define `z1`
define `x3`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "x3", "z1"]

----

define `z1`
define `x3`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "x3", "z1"]

----

define `z1`
define `x3`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "x3", "z1"]

----

define `z1`
define `x3`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "x3", "z1"]

----

define `z1`
define `x3`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "x3", "z1"]

----

define `z1`
define `x3`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "x3", "z1"]

----

define `z1`
define `x3`
define `y3`
use `y3`
-- post defs: ["y3", "x3", "z1"]

----

define `z1`
define `x3`
define `y3`
use `x3`
-- post defs: ["y3", "x3", "z1"]

----

define `z1`
define `x3`
define `y3`
use `z1`
-- post defs: ["y3", "x3", "z1"]

----

define `z1`
define `x3`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "x3", "z1"]

----

define `z1`
define `x3`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "x3", "z1"]

----

define `z1`
define `x3`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "x3", "z1"]

----

define `z1`
define `x3`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "x3", "z1"]

----

define `z1`
define `x3`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "x3", "z1"]

----

define `z1`
define `x3`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "x3", "z1"]

----

define `z1`
define `x3`
define `z2`
use `z2`
-- post defs: ["z2", "x3", "z1"]

----

define `z1`
define `x3`
define `z2`
use `x3`
-- post defs: ["z2", "x3", "z1"]

----

define `z1`
define `x3`
define `z2`
use `z1`
-- post defs: ["z2", "x3", "z1"]

----

define `z1`
define `x3`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "x3", "z1"]

----

define `z1`
define `x3`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "x3", "z1"]

----

define `z1`
define `x3`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "x3", "z1"]

----

define `z1`
define `x3`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "x3", "z1"]

----

define `z1`
define `x3`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "x3", "z1"]

----

define `z1`
define `x3`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "x3", "z1"]

----

define `z1`
define `x3`
define `z3`
use `z3`
-- post defs: ["z3", "x3", "z1"]

----

define `z1`
define `x3`
define `z3`
use `x3`
-- post defs: ["z3", "x3", "z1"]

----

define `z1`
define `x3`
define `z3`
use `z1`
-- post defs: ["z3", "x3", "z1"]

----

define `z1`
define `x3`
use `x3`
define `x1`
-- post defs: ["x1", "x3", "z1"]

----

define `z1`
define `x3`
use `x3`
define `x2`
-- post defs: ["x2", "x3", "z1"]

----

define `z1`
define `x3`
use `x3`
define `y1`
-- post defs: ["y1", "x3", "z1"]

----

define `z1`
define `x3`
use `x3`
define `y2`
-- post defs: ["y2", "x3", "z1"]

----

define `z1`
define `x3`
use `x3`
define `y3`
-- post defs: ["y3", "x3", "z1"]

----

define `z1`
define `x3`
use `x3`
define `z2`
-- post defs: ["z2", "x3", "z1"]

----

define `z1`
define `x3`
use `x3`
define `z3`
-- post defs: ["z3", "x3", "z1"]

----

define `z1`
define `x3`
use `x3`
use `x3`
-- post defs: ["x3", "z1"]

----

define `z1`
define `x3`
use `x3`
use `z1`
-- post defs: ["x3", "z1"]

----

define `z1`
define `x3`
use `z1`
define `x1`
-- post defs: ["x1", "x3", "z1"]

----

define `z1`
define `x3`
use `z1`
define `x2`
-- post defs: ["x2", "x3", "z1"]

----

define `z1`
define `x3`
use `z1`
define `y1`
-- post defs: ["y1", "x3", "z1"]

----

define `z1`
define `x3`
use `z1`
define `y2`
-- post defs: ["y2", "x3", "z1"]

----

define `z1`
define `x3`
use `z1`
define `y3`
-- post defs: ["y3", "x3", "z1"]

----

define `z1`
define `x3`
use `z1`
define `z2`
-- post defs: ["z2", "x3", "z1"]

----

define `z1`
define `x3`
use `z1`
define `z3`
-- post defs: ["z3", "x3", "z1"]

----

define `z1`
define `x3`
use `z1`
use `x3`
-- post defs: ["x3", "z1"]

----

define `z1`
define `x3`
use `z1`
use `z1`
-- post defs: ["x3", "z1"]

----

define `z1`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "z1"]

----

define `z1`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "z1"]

----

define `z1`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "z1"]

----

define `z1`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "z1"]

----

define `z1`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "z1"]

----

define `z1`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "z1"]

----

define `z1`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "z1"]

----

define `z1`
define `y1`
use `y1`
-- post defs: ["y1", "z1"]

----

define `z1`
define `y1`
use `z1`
-- post defs: ["y1", "z1"]

----

define `z1`
define `y1`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "y1", "z1"]

----

define `z1`
define `y1`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "y1", "z1"]

----

define `z1`
define `y1`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "y1", "z1"]

----

define `z1`
define `y1`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "y1", "z1"]

----

define `z1`
define `y1`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "y1", "z1"]

----

define `z1`
define `y1`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "y1", "z1"]

----

define `z1`
define `y1`
define `x1`
use `x1`
-- post defs: ["x1", "y1", "z1"]

----

define `z1`
define `y1`
define `x1`
use `y1`
-- post defs: ["x1", "y1", "z1"]

----

define `z1`
define `y1`
define `x1`
use `z1`
-- post defs: ["x1", "y1", "z1"]

----

define `z1`
define `y1`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "y1", "z1"]

----

define `z1`
define `y1`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "y1", "z1"]

----

define `z1`
define `y1`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "y1", "z1"]

----

define `z1`
define `y1`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "y1", "z1"]

----

define `z1`
define `y1`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "y1", "z1"]

----

define `z1`
define `y1`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "y1", "z1"]

----

define `z1`
define `y1`
define `x2`
use `x2`
-- post defs: ["x2", "y1", "z1"]

----

define `z1`
define `y1`
define `x2`
use `y1`
-- post defs: ["x2", "y1", "z1"]

----

define `z1`
define `y1`
define `x2`
use `z1`
-- post defs: ["x2", "y1", "z1"]

----

define `z1`
define `y1`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "y1", "z1"]

----

define `z1`
define `y1`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "y1", "z1"]

----

define `z1`
define `y1`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "y1", "z1"]

----

define `z1`
define `y1`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "y1", "z1"]

----

define `z1`
define `y1`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "y1", "z1"]

----

define `z1`
define `y1`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "y1", "z1"]

----

define `z1`
define `y1`
define `x3`
use `x3`
-- post defs: ["x3", "y1", "z1"]

----

define `z1`
define `y1`
define `x3`
use `y1`
-- post defs: ["x3", "y1", "z1"]

----

define `z1`
define `y1`
define `x3`
use `z1`
-- post defs: ["x3", "y1", "z1"]

----

define `z1`
define `y1`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "y1", "z1"]

----

define `z1`
define `y1`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "y1", "z1"]

----

define `z1`
define `y1`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "y1", "z1"]

----

define `z1`
define `y1`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "y1", "z1"]

----

define `z1`
define `y1`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "y1", "z1"]

----

define `z1`
define `y1`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "y1", "z1"]

----

define `z1`
define `y1`
define `y2`
use `y2`
-- post defs: ["y2", "y1", "z1"]

----

define `z1`
define `y1`
define `y2`
use `y1`
-- post defs: ["y2", "y1", "z1"]

----

define `z1`
define `y1`
define `y2`
use `z1`
-- post defs: ["y2", "y1", "z1"]

----

define `z1`
define `y1`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "y1", "z1"]

----

define `z1`
define `y1`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "y1", "z1"]

----

define `z1`
define `y1`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "y1", "z1"]

----

define `z1`
define `y1`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "y1", "z1"]

----

define `z1`
define `y1`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "y1", "z1"]

----

define `z1`
define `y1`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "y1", "z1"]

----

define `z1`
define `y1`
define `y3`
use `y3`
-- post defs: ["y3", "y1", "z1"]

----

define `z1`
define `y1`
define `y3`
use `y1`
-- post defs: ["y3", "y1", "z1"]

----

define `z1`
define `y1`
define `y3`
use `z1`
-- post defs: ["y3", "y1", "z1"]

----

define `z1`
define `y1`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "y1", "z1"]

----

define `z1`
define `y1`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "y1", "z1"]

----

define `z1`
define `y1`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "y1", "z1"]

----

define `z1`
define `y1`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "y1", "z1"]

----

define `z1`
define `y1`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "y1", "z1"]

----

define `z1`
define `y1`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "y1", "z1"]

----

define `z1`
define `y1`
define `z2`
use `z2`
-- post defs: ["z2", "y1", "z1"]

----

define `z1`
define `y1`
define `z2`
use `y1`
-- post defs: ["z2", "y1", "z1"]

----

define `z1`
define `y1`
define `z2`
use `z1`
-- post defs: ["z2", "y1", "z1"]

----

define `z1`
define `y1`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "y1", "z1"]

----

define `z1`
define `y1`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "y1", "z1"]

----

define `z1`
define `y1`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "y1", "z1"]

----

define `z1`
define `y1`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "y1", "z1"]

----

define `z1`
define `y1`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "y1", "z1"]

----

define `z1`
define `y1`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "y1", "z1"]

----

define `z1`
define `y1`
define `z3`
use `z3`
-- post defs: ["z3", "y1", "z1"]

----

define `z1`
define `y1`
define `z3`
use `y1`
-- post defs: ["z3", "y1", "z1"]

----

define `z1`
define `y1`
define `z3`
use `z1`
-- post defs: ["z3", "y1", "z1"]

----

define `z1`
define `y1`
use `y1`
define `x1`
-- post defs: ["x1", "y1", "z1"]

----

define `z1`
define `y1`
use `y1`
define `x2`
-- post defs: ["x2", "y1", "z1"]

----

define `z1`
define `y1`
use `y1`
define `x3`
-- post defs: ["x3", "y1", "z1"]

----

define `z1`
define `y1`
use `y1`
define `y2`
-- post defs: ["y2", "y1", "z1"]

----

define `z1`
define `y1`
use `y1`
define `y3`
-- post defs: ["y3", "y1", "z1"]

----

define `z1`
define `y1`
use `y1`
define `z2`
-- post defs: ["z2", "y1", "z1"]

----

define `z1`
define `y1`
use `y1`
define `z3`
-- post defs: ["z3", "y1", "z1"]

----

define `z1`
define `y1`
use `y1`
use `y1`
-- post defs: ["y1", "z1"]

----

define `z1`
define `y1`
use `y1`
use `z1`
-- post defs: ["y1", "z1"]

----

define `z1`
define `y1`
use `z1`
define `x1`
-- post defs: ["x1", "y1", "z1"]

----

define `z1`
define `y1`
use `z1`
define `x2`
-- post defs: ["x2", "y1", "z1"]

----

define `z1`
define `y1`
use `z1`
define `x3`
-- post defs: ["x3", "y1", "z1"]

----

define `z1`
define `y1`
use `z1`
define `y2`
-- post defs: ["y2", "y1", "z1"]

----

define `z1`
define `y1`
use `z1`
define `y3`
-- post defs: ["y3", "y1", "z1"]

----

define `z1`
define `y1`
use `z1`
define `z2`
-- post defs: ["z2", "y1", "z1"]

----

define `z1`
define `y1`
use `z1`
define `z3`
-- post defs: ["z3", "y1", "z1"]

----

define `z1`
define `y1`
use `z1`
use `y1`
-- post defs: ["y1", "z1"]

----

define `z1`
define `y1`
use `z1`
use `z1`
-- post defs: ["y1", "z1"]

----

define `z1`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "z1"]

----

define `z1`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "z1"]

----

define `z1`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "z1"]

----

define `z1`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "z1"]

----

define `z1`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "z1"]

----

define `z1`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "z1"]

----

define `z1`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "z1"]

----

define `z1`
define `y2`
use `y2`
-- post defs: ["y2", "z1"]

----

define `z1`
define `y2`
use `z1`
-- post defs: ["y2", "z1"]

----

define `z1`
define `y2`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "y2", "z1"]

----

define `z1`
define `y2`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "y2", "z1"]

----

define `z1`
define `y2`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "y2", "z1"]

----

define `z1`
define `y2`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "y2", "z1"]

----

define `z1`
define `y2`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "y2", "z1"]

----

define `z1`
define `y2`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "y2", "z1"]

----

define `z1`
define `y2`
define `x1`
use `x1`
-- post defs: ["x1", "y2", "z1"]

----

define `z1`
define `y2`
define `x1`
use `y2`
-- post defs: ["x1", "y2", "z1"]

----

define `z1`
define `y2`
define `x1`
use `z1`
-- post defs: ["x1", "y2", "z1"]

----

define `z1`
define `y2`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "y2", "z1"]

----

define `z1`
define `y2`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "y2", "z1"]

----

define `z1`
define `y2`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "y2", "z1"]

----

define `z1`
define `y2`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "y2", "z1"]

----

define `z1`
define `y2`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "y2", "z1"]

----

define `z1`
define `y2`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "y2", "z1"]

----

define `z1`
define `y2`
define `x2`
use `x2`
-- post defs: ["x2", "y2", "z1"]

----

define `z1`
define `y2`
define `x2`
use `y2`
-- post defs: ["x2", "y2", "z1"]

----

define `z1`
define `y2`
define `x2`
use `z1`
-- post defs: ["x2", "y2", "z1"]

----

define `z1`
define `y2`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "y2", "z1"]

----

define `z1`
define `y2`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "y2", "z1"]

----

define `z1`
define `y2`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "y2", "z1"]

----

define `z1`
define `y2`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "y2", "z1"]

----

define `z1`
define `y2`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "y2", "z1"]

----

define `z1`
define `y2`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "y2", "z1"]

----

define `z1`
define `y2`
define `x3`
use `x3`
-- post defs: ["x3", "y2", "z1"]

----

define `z1`
define `y2`
define `x3`
use `y2`
-- post defs: ["x3", "y2", "z1"]

----

define `z1`
define `y2`
define `x3`
use `z1`
-- post defs: ["x3", "y2", "z1"]

----

define `z1`
define `y2`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "y2", "z1"]

----

define `z1`
define `y2`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "y2", "z1"]

----

define `z1`
define `y2`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "y2", "z1"]

----

define `z1`
define `y2`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "y2", "z1"]

----

define `z1`
define `y2`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "y2", "z1"]

----

define `z1`
define `y2`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "y2", "z1"]

----

define `z1`
define `y2`
define `y1`
use `y1`
-- post defs: ["y1", "y2", "z1"]

----

define `z1`
define `y2`
define `y1`
use `y2`
-- post defs: ["y1", "y2", "z1"]

----

define `z1`
define `y2`
define `y1`
use `z1`
-- post defs: ["y1", "y2", "z1"]

----

define `z1`
define `y2`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "y2", "z1"]

----

define `z1`
define `y2`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "y2", "z1"]

----

define `z1`
define `y2`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "y2", "z1"]

----

define `z1`
define `y2`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "y2", "z1"]

----

define `z1`
define `y2`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "y2", "z1"]

----

define `z1`
define `y2`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "y2", "z1"]

----

define `z1`
define `y2`
define `y3`
use `y3`
-- post defs: ["y3", "y2", "z1"]

----

define `z1`
define `y2`
define `y3`
use `y2`
-- post defs: ["y3", "y2", "z1"]

----

define `z1`
define `y2`
define `y3`
use `z1`
-- post defs: ["y3", "y2", "z1"]

----

define `z1`
define `y2`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "y2", "z1"]

----

define `z1`
define `y2`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "y2", "z1"]

----

define `z1`
define `y2`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "y2", "z1"]

----

define `z1`
define `y2`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "y2", "z1"]

----

define `z1`
define `y2`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "y2", "z1"]

----

define `z1`
define `y2`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "y2", "z1"]

----

define `z1`
define `y2`
define `z2`
use `z2`
-- post defs: ["z2", "y2", "z1"]

----

define `z1`
define `y2`
define `z2`
use `y2`
-- post defs: ["z2", "y2", "z1"]

----

define `z1`
define `y2`
define `z2`
use `z1`
-- post defs: ["z2", "y2", "z1"]

----

define `z1`
define `y2`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "y2", "z1"]

----

define `z1`
define `y2`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "y2", "z1"]

----

define `z1`
define `y2`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "y2", "z1"]

----

define `z1`
define `y2`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "y2", "z1"]

----

define `z1`
define `y2`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "y2", "z1"]

----

define `z1`
define `y2`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "y2", "z1"]

----

define `z1`
define `y2`
define `z3`
use `z3`
-- post defs: ["z3", "y2", "z1"]

----

define `z1`
define `y2`
define `z3`
use `y2`
-- post defs: ["z3", "y2", "z1"]

----

define `z1`
define `y2`
define `z3`
use `z1`
-- post defs: ["z3", "y2", "z1"]

----

define `z1`
define `y2`
use `y2`
define `x1`
-- post defs: ["x1", "y2", "z1"]

----

define `z1`
define `y2`
use `y2`
define `x2`
-- post defs: ["x2", "y2", "z1"]

----

define `z1`
define `y2`
use `y2`
define `x3`
-- post defs: ["x3", "y2", "z1"]

----

define `z1`
define `y2`
use `y2`
define `y1`
-- post defs: ["y1", "y2", "z1"]

----

define `z1`
define `y2`
use `y2`
define `y3`
-- post defs: ["y3", "y2", "z1"]

----

define `z1`
define `y2`
use `y2`
define `z2`
-- post defs: ["z2", "y2", "z1"]

----

define `z1`
define `y2`
use `y2`
define `z3`
-- post defs: ["z3", "y2", "z1"]

----

define `z1`
define `y2`
use `y2`
use `y2`
-- post defs: ["y2", "z1"]

----

define `z1`
define `y2`
use `y2`
use `z1`
-- post defs: ["y2", "z1"]

----

define `z1`
define `y2`
use `z1`
define `x1`
-- post defs: ["x1", "y2", "z1"]

----

define `z1`
define `y2`
use `z1`
define `x2`
-- post defs: ["x2", "y2", "z1"]

----

define `z1`
define `y2`
use `z1`
define `x3`
-- post defs: ["x3", "y2", "z1"]

----

define `z1`
define `y2`
use `z1`
define `y1`
-- post defs: ["y1", "y2", "z1"]

----

define `z1`
define `y2`
use `z1`
define `y3`
-- post defs: ["y3", "y2", "z1"]

----

define `z1`
define `y2`
use `z1`
define `z2`
-- post defs: ["z2", "y2", "z1"]

----

define `z1`
define `y2`
use `z1`
define `z3`
-- post defs: ["z3", "y2", "z1"]

----

define `z1`
define `y2`
use `z1`
use `y2`
-- post defs: ["y2", "z1"]

----

define `z1`
define `y2`
use `z1`
use `z1`
-- post defs: ["y2", "z1"]

----

define `z1`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "z1"]

----

define `z1`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "z1"]

----

define `z1`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "z1"]

----

define `z1`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "z1"]

----

define `z1`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "z1"]

----

define `z1`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "z1"]

----

define `z1`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "z1"]

----

define `z1`
define `y3`
use `y3`
-- post defs: ["y3", "z1"]

----

define `z1`
define `y3`
use `z1`
-- post defs: ["y3", "z1"]

----

define `z1`
define `y3`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "y3", "z1"]

----

define `z1`
define `y3`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "y3", "z1"]

----

define `z1`
define `y3`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "y3", "z1"]

----

define `z1`
define `y3`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "y3", "z1"]

----

define `z1`
define `y3`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "y3", "z1"]

----

define `z1`
define `y3`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "y3", "z1"]

----

define `z1`
define `y3`
define `x1`
use `x1`
-- post defs: ["x1", "y3", "z1"]

----

define `z1`
define `y3`
define `x1`
use `y3`
-- post defs: ["x1", "y3", "z1"]

----

define `z1`
define `y3`
define `x1`
use `z1`
-- post defs: ["x1", "y3", "z1"]

----

define `z1`
define `y3`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "y3", "z1"]

----

define `z1`
define `y3`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "y3", "z1"]

----

define `z1`
define `y3`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "y3", "z1"]

----

define `z1`
define `y3`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "y3", "z1"]

----

define `z1`
define `y3`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "y3", "z1"]

----

define `z1`
define `y3`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "y3", "z1"]

----

define `z1`
define `y3`
define `x2`
use `x2`
-- post defs: ["x2", "y3", "z1"]

----

define `z1`
define `y3`
define `x2`
use `y3`
-- post defs: ["x2", "y3", "z1"]

----

define `z1`
define `y3`
define `x2`
use `z1`
-- post defs: ["x2", "y3", "z1"]

----

define `z1`
define `y3`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "y3", "z1"]

----

define `z1`
define `y3`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "y3", "z1"]

----

define `z1`
define `y3`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "y3", "z1"]

----

define `z1`
define `y3`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "y3", "z1"]

----

define `z1`
define `y3`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "y3", "z1"]

----

define `z1`
define `y3`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "y3", "z1"]

----

define `z1`
define `y3`
define `x3`
use `x3`
-- post defs: ["x3", "y3", "z1"]

----

define `z1`
define `y3`
define `x3`
use `y3`
-- post defs: ["x3", "y3", "z1"]

----

define `z1`
define `y3`
define `x3`
use `z1`
-- post defs: ["x3", "y3", "z1"]

----

define `z1`
define `y3`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "y3", "z1"]

----

define `z1`
define `y3`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "y3", "z1"]

----

define `z1`
define `y3`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "y3", "z1"]

----

define `z1`
define `y3`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "y3", "z1"]

----

define `z1`
define `y3`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "y3", "z1"]

----

define `z1`
define `y3`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "y3", "z1"]

----

define `z1`
define `y3`
define `y1`
use `y1`
-- post defs: ["y1", "y3", "z1"]

----

define `z1`
define `y3`
define `y1`
use `y3`
-- post defs: ["y1", "y3", "z1"]

----

define `z1`
define `y3`
define `y1`
use `z1`
-- post defs: ["y1", "y3", "z1"]

----

define `z1`
define `y3`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "y3", "z1"]

----

define `z1`
define `y3`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "y3", "z1"]

----

define `z1`
define `y3`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "y3", "z1"]

----

define `z1`
define `y3`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "y3", "z1"]

----

define `z1`
define `y3`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "y3", "z1"]

----

define `z1`
define `y3`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "y3", "z1"]

----

define `z1`
define `y3`
define `y2`
use `y2`
-- post defs: ["y2", "y3", "z1"]

----

define `z1`
define `y3`
define `y2`
use `y3`
-- post defs: ["y2", "y3", "z1"]

----

define `z1`
define `y3`
define `y2`
use `z1`
-- post defs: ["y2", "y3", "z1"]

----

define `z1`
define `y3`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "y3", "z1"]

----

define `z1`
define `y3`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "y3", "z1"]

----

define `z1`
define `y3`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "y3", "z1"]

----

define `z1`
define `y3`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "y3", "z1"]

----

define `z1`
define `y3`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "y3", "z1"]

----

define `z1`
define `y3`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "y3", "z1"]

----

define `z1`
define `y3`
define `z2`
use `z2`
-- post defs: ["z2", "y3", "z1"]

----

define `z1`
define `y3`
define `z2`
use `y3`
-- post defs: ["z2", "y3", "z1"]

----

define `z1`
define `y3`
define `z2`
use `z1`
-- post defs: ["z2", "y3", "z1"]

----

define `z1`
define `y3`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "y3", "z1"]

----

define `z1`
define `y3`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "y3", "z1"]

----

define `z1`
define `y3`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "y3", "z1"]

----

define `z1`
define `y3`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "y3", "z1"]

----

define `z1`
define `y3`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "y3", "z1"]

----

define `z1`
define `y3`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "y3", "z1"]

----

define `z1`
define `y3`
define `z3`
use `z3`
-- post defs: ["z3", "y3", "z1"]

----

define `z1`
define `y3`
define `z3`
use `y3`
-- post defs: ["z3", "y3", "z1"]

----

define `z1`
define `y3`
define `z3`
use `z1`
-- post defs: ["z3", "y3", "z1"]

----

define `z1`
define `y3`
use `y3`
define `x1`
-- post defs: ["x1", "y3", "z1"]

----

define `z1`
define `y3`
use `y3`
define `x2`
-- post defs: ["x2", "y3", "z1"]

----

define `z1`
define `y3`
use `y3`
define `x3`
-- post defs: ["x3", "y3", "z1"]

----

define `z1`
define `y3`
use `y3`
define `y1`
-- post defs: ["y1", "y3", "z1"]

----

define `z1`
define `y3`
use `y3`
define `y2`
-- post defs: ["y2", "y3", "z1"]

----

define `z1`
define `y3`
use `y3`
define `z2`
-- post defs: ["z2", "y3", "z1"]

----

define `z1`
define `y3`
use `y3`
define `z3`
-- post defs: ["z3", "y3", "z1"]

----

define `z1`
define `y3`
use `y3`
use `y3`
-- post defs: ["y3", "z1"]

----

define `z1`
define `y3`
use `y3`
use `z1`
-- post defs: ["y3", "z1"]

----

define `z1`
define `y3`
use `z1`
define `x1`
-- post defs: ["x1", "y3", "z1"]

----

define `z1`
define `y3`
use `z1`
define `x2`
-- post defs: ["x2", "y3", "z1"]

----

define `z1`
define `y3`
use `z1`
define `x3`
-- post defs: ["x3", "y3", "z1"]

----

define `z1`
define `y3`
use `z1`
define `y1`
-- post defs: ["y1", "y3", "z1"]

----

define `z1`
define `y3`
use `z1`
define `y2`
-- post defs: ["y2", "y3", "z1"]

----

define `z1`
define `y3`
use `z1`
define `z2`
-- post defs: ["z2", "y3", "z1"]

----

define `z1`
define `y3`
use `z1`
define `z3`
-- post defs: ["z3", "y3", "z1"]

----

define `z1`
define `y3`
use `z1`
use `y3`
-- post defs: ["y3", "z1"]

----

define `z1`
define `y3`
use `z1`
use `z1`
-- post defs: ["y3", "z1"]

----

define `z1`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "z1"]

----

define `z1`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "z1"]

----

define `z1`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "z1"]

----

define `z1`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "z1"]

----

define `z1`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "z1"]

----

define `z1`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "z1"]

----

define `z1`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "z1"]

----

define `z1`
define `z2`
use `z2`
-- post defs: ["z2", "z1"]

----

define `z1`
define `z2`
use `z1`
-- post defs: ["z2", "z1"]

----

define `z1`
define `z2`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "z2", "z1"]

----

define `z1`
define `z2`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "z2", "z1"]

----

define `z1`
define `z2`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "z2", "z1"]

----

define `z1`
define `z2`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "z2", "z1"]

----

define `z1`
define `z2`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "z2", "z1"]

----

define `z1`
define `z2`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "z2", "z1"]

----

define `z1`
define `z2`
define `x1`
use `x1`
-- post defs: ["x1", "z2", "z1"]

----

define `z1`
define `z2`
define `x1`
use `z2`
-- post defs: ["x1", "z2", "z1"]

----

define `z1`
define `z2`
define `x1`
use `z1`
-- post defs: ["x1", "z2", "z1"]

----

define `z1`
define `z2`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "z2", "z1"]

----

define `z1`
define `z2`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "z2", "z1"]

----

define `z1`
define `z2`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "z2", "z1"]

----

define `z1`
define `z2`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "z2", "z1"]

----

define `z1`
define `z2`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "z2", "z1"]

----

define `z1`
define `z2`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "z2", "z1"]

----

define `z1`
define `z2`
define `x2`
use `x2`
-- post defs: ["x2", "z2", "z1"]

----

define `z1`
define `z2`
define `x2`
use `z2`
-- post defs: ["x2", "z2", "z1"]

----

define `z1`
define `z2`
define `x2`
use `z1`
-- post defs: ["x2", "z2", "z1"]

----

define `z1`
define `z2`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "z2", "z1"]

----

define `z1`
define `z2`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "z2", "z1"]

----

define `z1`
define `z2`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "z2", "z1"]

----

define `z1`
define `z2`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "z2", "z1"]

----

define `z1`
define `z2`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "z2", "z1"]

----

define `z1`
define `z2`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "z2", "z1"]

----

define `z1`
define `z2`
define `x3`
use `x3`
-- post defs: ["x3", "z2", "z1"]

----

define `z1`
define `z2`
define `x3`
use `z2`
-- post defs: ["x3", "z2", "z1"]

----

define `z1`
define `z2`
define `x3`
use `z1`
-- post defs: ["x3", "z2", "z1"]

----

define `z1`
define `z2`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "z2", "z1"]

----

define `z1`
define `z2`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "z2", "z1"]

----

define `z1`
define `z2`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "z2", "z1"]

----

define `z1`
define `z2`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "z2", "z1"]

----

define `z1`
define `z2`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "z2", "z1"]

----

define `z1`
define `z2`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "z2", "z1"]

----

define `z1`
define `z2`
define `y1`
use `y1`
-- post defs: ["y1", "z2", "z1"]

----

define `z1`
define `z2`
define `y1`
use `z2`
-- post defs: ["y1", "z2", "z1"]

----

define `z1`
define `z2`
define `y1`
use `z1`
-- post defs: ["y1", "z2", "z1"]

----

define `z1`
define `z2`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "z2", "z1"]

----

define `z1`
define `z2`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "z2", "z1"]

----

define `z1`
define `z2`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "z2", "z1"]

----

define `z1`
define `z2`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "z2", "z1"]

----

define `z1`
define `z2`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "z2", "z1"]

----

define `z1`
define `z2`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "z2", "z1"]

----

define `z1`
define `z2`
define `y2`
use `y2`
-- post defs: ["y2", "z2", "z1"]

----

define `z1`
define `z2`
define `y2`
use `z2`
-- post defs: ["y2", "z2", "z1"]

----

define `z1`
define `z2`
define `y2`
use `z1`
-- post defs: ["y2", "z2", "z1"]

----

define `z1`
define `z2`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "z2", "z1"]

----

define `z1`
define `z2`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "z2", "z1"]

----

define `z1`
define `z2`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "z2", "z1"]

----

define `z1`
define `z2`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "z2", "z1"]

----

define `z1`
define `z2`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "z2", "z1"]

----

define `z1`
define `z2`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "z2", "z1"]

----

define `z1`
define `z2`
define `y3`
use `y3`
-- post defs: ["y3", "z2", "z1"]

----

define `z1`
define `z2`
define `y3`
use `z2`
-- post defs: ["y3", "z2", "z1"]

----

define `z1`
define `z2`
define `y3`
use `z1`
-- post defs: ["y3", "z2", "z1"]

----

define `z1`
define `z2`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "z2", "z1"]

----

define `z1`
define `z2`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "z2", "z1"]

----

define `z1`
define `z2`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "z2", "z1"]

----

define `z1`
define `z2`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "z2", "z1"]

----

define `z1`
define `z2`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "z2", "z1"]

----

define `z1`
define `z2`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "z2", "z1"]

----

define `z1`
define `z2`
define `z3`
use `z3`
-- post defs: ["z3", "z2", "z1"]

----

define `z1`
define `z2`
define `z3`
use `z2`
-- post defs: ["z3", "z2", "z1"]

----

define `z1`
define `z2`
define `z3`
use `z1`
-- post defs: ["z3", "z2", "z1"]

----

define `z1`
define `z2`
use `z2`
define `x1`
-- post defs: ["x1", "z2", "z1"]

----

define `z1`
define `z2`
use `z2`
define `x2`
-- post defs: ["x2", "z2", "z1"]

----

define `z1`
define `z2`
use `z2`
define `x3`
-- post defs: ["x3", "z2", "z1"]

----

define `z1`
define `z2`
use `z2`
define `y1`
-- post defs: ["y1", "z2", "z1"]

----

define `z1`
define `z2`
use `z2`
define `y2`
-- post defs: ["y2", "z2", "z1"]

----

define `z1`
define `z2`
use `z2`
define `y3`
-- post defs: ["y3", "z2", "z1"]

----

define `z1`
define `z2`
use `z2`
define `z3`
-- post defs: ["z3", "z2", "z1"]

----

define `z1`
define `z2`
use `z2`
use `z2`
-- post defs: ["z2", "z1"]

----

define `z1`
define `z2`
use `z2`
use `z1`
-- post defs: ["z2", "z1"]

----

define `z1`
define `z2`
use `z1`
define `x1`
-- post defs: ["x1", "z2", "z1"]

----

define `z1`
define `z2`
use `z1`
define `x2`
-- post defs: ["x2", "z2", "z1"]

----

define `z1`
define `z2`
use `z1`
define `x3`
-- post defs: ["x3", "z2", "z1"]

----

define `z1`
define `z2`
use `z1`
define `y1`
-- post defs: ["y1", "z2", "z1"]

----

define `z1`
define `z2`
use `z1`
define `y2`
-- post defs: ["y2", "z2", "z1"]

----

define `z1`
define `z2`
use `z1`
define `y3`
-- post defs: ["y3", "z2", "z1"]

----

define `z1`
define `z2`
use `z1`
define `z3`
-- post defs: ["z3", "z2", "z1"]

----

define `z1`
define `z2`
use `z1`
use `z2`
-- post defs: ["z2", "z1"]

----

define `z1`
define `z2`
use `z1`
use `z1`
-- post defs: ["z2", "z1"]

----

define `z1`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "z1"]

----

define `z1`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "z1"]

----

define `z1`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "z1"]

----

define `z1`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "z1"]

----

define `z1`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "z1"]

----

define `z1`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "z1"]

----

define `z1`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "z1"]

----

define `z1`
define `z3`
use `z3`
-- post defs: ["z3", "z1"]

----

define `z1`
define `z3`
use `z1`
-- post defs: ["z3", "z1"]

----

define `z1`
define `z3`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "z3", "z1"]

----

define `z1`
define `z3`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "z3", "z1"]

----

define `z1`
define `z3`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "z3", "z1"]

----

define `z1`
define `z3`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "z3", "z1"]

----

define `z1`
define `z3`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "z3", "z1"]

----

define `z1`
define `z3`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "z3", "z1"]

----

define `z1`
define `z3`
define `x1`
use `x1`
-- post defs: ["x1", "z3", "z1"]

----

define `z1`
define `z3`
define `x1`
use `z3`
-- post defs: ["x1", "z3", "z1"]

----

define `z1`
define `z3`
define `x1`
use `z1`
-- post defs: ["x1", "z3", "z1"]

----

define `z1`
define `z3`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "z3", "z1"]

----

define `z1`
define `z3`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "z3", "z1"]

----

define `z1`
define `z3`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "z3", "z1"]

----

define `z1`
define `z3`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "z3", "z1"]

----

define `z1`
define `z3`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "z3", "z1"]

----

define `z1`
define `z3`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "z3", "z1"]

----

define `z1`
define `z3`
define `x2`
use `x2`
-- post defs: ["x2", "z3", "z1"]

----

define `z1`
define `z3`
define `x2`
use `z3`
-- post defs: ["x2", "z3", "z1"]

----

define `z1`
define `z3`
define `x2`
use `z1`
-- post defs: ["x2", "z3", "z1"]

----

define `z1`
define `z3`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "z3", "z1"]

----

define `z1`
define `z3`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "z3", "z1"]

----

define `z1`
define `z3`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "z3", "z1"]

----

define `z1`
define `z3`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "z3", "z1"]

----

define `z1`
define `z3`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "z3", "z1"]

----

define `z1`
define `z3`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "z3", "z1"]

----

define `z1`
define `z3`
define `x3`
use `x3`
-- post defs: ["x3", "z3", "z1"]

----

define `z1`
define `z3`
define `x3`
use `z3`
-- post defs: ["x3", "z3", "z1"]

----

define `z1`
define `z3`
define `x3`
use `z1`
-- post defs: ["x3", "z3", "z1"]

----

define `z1`
define `z3`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "z3", "z1"]

----

define `z1`
define `z3`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "z3", "z1"]

----

define `z1`
define `z3`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "z3", "z1"]

----

define `z1`
define `z3`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "z3", "z1"]

----

define `z1`
define `z3`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "z3", "z1"]

----

define `z1`
define `z3`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "z3", "z1"]

----

define `z1`
define `z3`
define `y1`
use `y1`
-- post defs: ["y1", "z3", "z1"]

----

define `z1`
define `z3`
define `y1`
use `z3`
-- post defs: ["y1", "z3", "z1"]

----

define `z1`
define `z3`
define `y1`
use `z1`
-- post defs: ["y1", "z3", "z1"]

----

define `z1`
define `z3`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "z3", "z1"]

----

define `z1`
define `z3`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "z3", "z1"]

----

define `z1`
define `z3`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "z3", "z1"]

----

define `z1`
define `z3`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "z3", "z1"]

----

define `z1`
define `z3`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "z3", "z1"]

----

define `z1`
define `z3`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "z3", "z1"]

----

define `z1`
define `z3`
define `y2`
use `y2`
-- post defs: ["y2", "z3", "z1"]

----

define `z1`
define `z3`
define `y2`
use `z3`
-- post defs: ["y2", "z3", "z1"]

----

define `z1`
define `z3`
define `y2`
use `z1`
-- post defs: ["y2", "z3", "z1"]

----

define `z1`
define `z3`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "z3", "z1"]

----

define `z1`
define `z3`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "z3", "z1"]

----

define `z1`
define `z3`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "z3", "z1"]

----

define `z1`
define `z3`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "z3", "z1"]

----

define `z1`
define `z3`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "z3", "z1"]

----

define `z1`
define `z3`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "z3", "z1"]

----

define `z1`
define `z3`
define `y3`
use `y3`
-- post defs: ["y3", "z3", "z1"]

----

define `z1`
define `z3`
define `y3`
use `z3`
-- post defs: ["y3", "z3", "z1"]

----

define `z1`
define `z3`
define `y3`
use `z1`
-- post defs: ["y3", "z3", "z1"]

----

define `z1`
define `z3`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "z3", "z1"]

----

define `z1`
define `z3`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "z3", "z1"]

----

define `z1`
define `z3`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "z3", "z1"]

----

define `z1`
define `z3`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "z3", "z1"]

----

define `z1`
define `z3`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "z3", "z1"]

----

define `z1`
define `z3`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "z3", "z1"]

----

define `z1`
define `z3`
define `z2`
use `z2`
-- post defs: ["z2", "z3", "z1"]

----

define `z1`
define `z3`
define `z2`
use `z3`
-- post defs: ["z2", "z3", "z1"]

----

define `z1`
define `z3`
define `z2`
use `z1`
-- post defs: ["z2", "z3", "z1"]

----

define `z1`
define `z3`
use `z3`
define `x1`
-- post defs: ["x1", "z3", "z1"]

----

define `z1`
define `z3`
use `z3`
define `x2`
-- post defs: ["x2", "z3", "z1"]

----

define `z1`
define `z3`
use `z3`
define `x3`
-- post defs: ["x3", "z3", "z1"]

----

define `z1`
define `z3`
use `z3`
define `y1`
-- post defs: ["y1", "z3", "z1"]

----

define `z1`
define `z3`
use `z3`
define `y2`
-- post defs: ["y2", "z3", "z1"]

----

define `z1`
define `z3`
use `z3`
define `y3`
-- post defs: ["y3", "z3", "z1"]

----

define `z1`
define `z3`
use `z3`
define `z2`
-- post defs: ["z2", "z3", "z1"]

----

define `z1`
define `z3`
use `z3`
use `z3`
-- post defs: ["z3", "z1"]

----

define `z1`
define `z3`
use `z3`
use `z1`
-- post defs: ["z3", "z1"]

----

define `z1`
define `z3`
use `z1`
define `x1`
-- post defs: ["x1", "z3", "z1"]

----

define `z1`
define `z3`
use `z1`
define `x2`
-- post defs: ["x2", "z3", "z1"]

----

define `z1`
define `z3`
use `z1`
define `x3`
-- post defs: ["x3", "z3", "z1"]

----

define `z1`
define `z3`
use `z1`
define `y1`
-- post defs: ["y1", "z3", "z1"]

----

define `z1`
define `z3`
use `z1`
define `y2`
-- post defs: ["y2", "z3", "z1"]

----

define `z1`
define `z3`
use `z1`
define `y3`
-- post defs: ["y3", "z3", "z1"]

----

define `z1`
define `z3`
use `z1`
define `z2`
-- post defs: ["z2", "z3", "z1"]

----

define `z1`
define `z3`
use `z1`
use `z3`
-- post defs: ["z3", "z1"]

----

define `z1`
define `z3`
use `z1`
use `z1`
-- post defs: ["z3", "z1"]

----

define `z1`
use `z1`
define `x1`
-- post defs: ["x1", "z1"]

----

define `z1`
use `z1`
define `x2`
-- post defs: ["x2", "z1"]

----

define `z1`
use `z1`
define `x3`
-- post defs: ["x3", "z1"]

----

define `z1`
use `z1`
define `y1`
-- post defs: ["y1", "z1"]

----

define `z1`
use `z1`
define `y2`
-- post defs: ["y2", "z1"]

----

define `z1`
use `z1`
define `y3`
-- post defs: ["y3", "z1"]

----

define `z1`
use `z1`
define `z2`
-- post defs: ["z2", "z1"]

----

define `z1`
use `z1`
define `z3`
-- post defs: ["z3", "z1"]

----

define `z1`
use `z1`
use `z1`
-- post defs: ["z1"]

----

define `z1`
use `z1`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "z1"]

----

define `z1`
use `z1`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "z1"]

----

define `z1`
use `z1`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "z1"]

----

define `z1`
use `z1`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "z1"]

----

define `z1`
use `z1`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "z1"]

----

define `z1`
use `z1`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "z1"]

----

define `z1`
use `z1`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "z1"]

----

define `z1`
use `z1`
define `x1`
use `x1`
-- post defs: ["x1", "z1"]

----

define `z1`
use `z1`
define `x1`
use `z1`
-- post defs: ["x1", "z1"]

----

define `z1`
use `z1`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "z1"]

----

define `z1`
use `z1`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "z1"]

----

define `z1`
use `z1`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "z1"]

----

define `z1`
use `z1`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "z1"]

----

define `z1`
use `z1`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "z1"]

----

define `z1`
use `z1`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "z1"]

----

define `z1`
use `z1`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "z1"]

----

define `z1`
use `z1`
define `x2`
use `x2`
-- post defs: ["x2", "z1"]

----

define `z1`
use `z1`
define `x2`
use `z1`
-- post defs: ["x2", "z1"]

----

define `z1`
use `z1`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "z1"]

----

define `z1`
use `z1`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "z1"]

----

define `z1`
use `z1`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "z1"]

----

define `z1`
use `z1`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "z1"]

----

define `z1`
use `z1`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "z1"]

----

define `z1`
use `z1`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "z1"]

----

define `z1`
use `z1`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "z1"]

----

define `z1`
use `z1`
define `x3`
use `x3`
-- post defs: ["x3", "z1"]

----

define `z1`
use `z1`
define `x3`
use `z1`
-- post defs: ["x3", "z1"]

----

define `z1`
use `z1`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "z1"]

----

define `z1`
use `z1`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "z1"]

----

define `z1`
use `z1`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "z1"]

----

define `z1`
use `z1`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "z1"]

----

define `z1`
use `z1`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "z1"]

----

define `z1`
use `z1`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "z1"]

----

define `z1`
use `z1`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "z1"]

----

define `z1`
use `z1`
define `y1`
use `y1`
-- post defs: ["y1", "z1"]

----

define `z1`
use `z1`
define `y1`
use `z1`
-- post defs: ["y1", "z1"]

----

define `z1`
use `z1`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "z1"]

----

define `z1`
use `z1`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "z1"]

----

define `z1`
use `z1`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "z1"]

----

define `z1`
use `z1`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "z1"]

----

define `z1`
use `z1`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "z1"]

----

define `z1`
use `z1`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "z1"]

----

define `z1`
use `z1`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "z1"]

----

define `z1`
use `z1`
define `y2`
use `y2`
-- post defs: ["y2", "z1"]

----

define `z1`
use `z1`
define `y2`
use `z1`
-- post defs: ["y2", "z1"]

----

define `z1`
use `z1`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "z1"]

----

define `z1`
use `z1`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "z1"]

----

define `z1`
use `z1`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "z1"]

----

define `z1`
use `z1`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "z1"]

----

define `z1`
use `z1`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "z1"]

----

define `z1`
use `z1`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "z1"]

----

define `z1`
use `z1`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "z1"]

----

define `z1`
use `z1`
define `y3`
use `y3`
-- post defs: ["y3", "z1"]

----

define `z1`
use `z1`
define `y3`
use `z1`
-- post defs: ["y3", "z1"]

----

define `z1`
use `z1`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "z1"]

----

define `z1`
use `z1`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "z1"]

----

define `z1`
use `z1`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "z1"]

----

define `z1`
use `z1`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "z1"]

----

define `z1`
use `z1`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "z1"]

----

define `z1`
use `z1`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "z1"]

----

define `z1`
use `z1`
define `z2`
define `z3`
-- post defs: ["z3", "z2", "z1"]

----

define `z1`
use `z1`
define `z2`
use `z2`
-- post defs: ["z2", "z1"]

----

define `z1`
use `z1`
define `z2`
use `z1`
-- post defs: ["z2", "z1"]

----

define `z1`
use `z1`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "z1"]

----

define `z1`
use `z1`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "z1"]

----

define `z1`
use `z1`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "z1"]

----

define `z1`
use `z1`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "z1"]

----

define `z1`
use `z1`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "z1"]

----

define `z1`
use `z1`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "z1"]

----

define `z1`
use `z1`
define `z3`
define `z2`
-- post defs: ["z2", "z3", "z1"]

----

define `z1`
use `z1`
define `z3`
use `z3`
-- post defs: ["z3", "z1"]

----

define `z1`
use `z1`
define `z3`
use `z1`
-- post defs: ["z3", "z1"]

----

define `z1`
use `z1`
use `z1`
define `x1`
-- post defs: ["x1", "z1"]

----

define `z1`
use `z1`
use `z1`
define `x2`
-- post defs: ["x2", "z1"]

----

define `z1`
use `z1`
use `z1`
define `x3`
-- post defs: ["x3", "z1"]

----

define `z1`
use `z1`
use `z1`
define `y1`
-- post defs: ["y1", "z1"]

----

define `z1`
use `z1`
use `z1`
define `y2`
-- post defs: ["y2", "z1"]

----

define `z1`
use `z1`
use `z1`
define `y3`
-- post defs: ["y3", "z1"]

----

define `z1`
use `z1`
use `z1`
define `z2`
-- post defs: ["z2", "z1"]

----

define `z1`
use `z1`
use `z1`
define `z3`
-- post defs: ["z3", "z1"]

----

define `z1`
use `z1`
use `z1`
use `z1`
-- post defs: ["z1"]

----

define `z2`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "z2"]

----

define `z2`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "z2"]

----

define `z2`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "z2"]

----

define `z2`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "z2"]

----

define `z2`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "z2"]

----

define `z2`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "z2"]

----

define `z2`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "z2"]

----

define `z2`
define `x1`
use `x1`
-- post defs: ["x1", "z2"]

----

define `z2`
define `x1`
use `z2`
-- post defs: ["x1", "z2"]

----

define `z2`
define `x1`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "x1", "z2"]

----

define `z2`
define `x1`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "x1", "z2"]

----

define `z2`
define `x1`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "x1", "z2"]

----

define `z2`
define `x1`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "x1", "z2"]

----

define `z2`
define `x1`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "x1", "z2"]

----

define `z2`
define `x1`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "x1", "z2"]

----

define `z2`
define `x1`
define `x2`
use `x2`
-- post defs: ["x2", "x1", "z2"]

----

define `z2`
define `x1`
define `x2`
use `x1`
-- post defs: ["x2", "x1", "z2"]

----

define `z2`
define `x1`
define `x2`
use `z2`
-- post defs: ["x2", "x1", "z2"]

----

define `z2`
define `x1`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "x1", "z2"]

----

define `z2`
define `x1`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "x1", "z2"]

----

define `z2`
define `x1`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "x1", "z2"]

----

define `z2`
define `x1`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "x1", "z2"]

----

define `z2`
define `x1`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "x1", "z2"]

----

define `z2`
define `x1`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "x1", "z2"]

----

define `z2`
define `x1`
define `x3`
use `x3`
-- post defs: ["x3", "x1", "z2"]

----

define `z2`
define `x1`
define `x3`
use `x1`
-- post defs: ["x3", "x1", "z2"]

----

define `z2`
define `x1`
define `x3`
use `z2`
-- post defs: ["x3", "x1", "z2"]

----

define `z2`
define `x1`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "x1", "z2"]

----

define `z2`
define `x1`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "x1", "z2"]

----

define `z2`
define `x1`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "x1", "z2"]

----

define `z2`
define `x1`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "x1", "z2"]

----

define `z2`
define `x1`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "x1", "z2"]

----

define `z2`
define `x1`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "x1", "z2"]

----

define `z2`
define `x1`
define `y1`
use `y1`
-- post defs: ["y1", "x1", "z2"]

----

define `z2`
define `x1`
define `y1`
use `x1`
-- post defs: ["y1", "x1", "z2"]

----

define `z2`
define `x1`
define `y1`
use `z2`
-- post defs: ["y1", "x1", "z2"]

----

define `z2`
define `x1`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "x1", "z2"]

----

define `z2`
define `x1`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "x1", "z2"]

----

define `z2`
define `x1`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "x1", "z2"]

----

define `z2`
define `x1`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "x1", "z2"]

----

define `z2`
define `x1`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "x1", "z2"]

----

define `z2`
define `x1`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "x1", "z2"]

----

define `z2`
define `x1`
define `y2`
use `y2`
-- post defs: ["y2", "x1", "z2"]

----

define `z2`
define `x1`
define `y2`
use `x1`
-- post defs: ["y2", "x1", "z2"]

----

define `z2`
define `x1`
define `y2`
use `z2`
-- post defs: ["y2", "x1", "z2"]

----

define `z2`
define `x1`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "x1", "z2"]

----

define `z2`
define `x1`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "x1", "z2"]

----

define `z2`
define `x1`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "x1", "z2"]

----

define `z2`
define `x1`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "x1", "z2"]

----

define `z2`
define `x1`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "x1", "z2"]

----

define `z2`
define `x1`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "x1", "z2"]

----

define `z2`
define `x1`
define `y3`
use `y3`
-- post defs: ["y3", "x1", "z2"]

----

define `z2`
define `x1`
define `y3`
use `x1`
-- post defs: ["y3", "x1", "z2"]

----

define `z2`
define `x1`
define `y3`
use `z2`
-- post defs: ["y3", "x1", "z2"]

----

define `z2`
define `x1`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "x1", "z2"]

----

define `z2`
define `x1`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "x1", "z2"]

----

define `z2`
define `x1`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "x1", "z2"]

----

define `z2`
define `x1`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "x1", "z2"]

----

define `z2`
define `x1`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "x1", "z2"]

----

define `z2`
define `x1`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "x1", "z2"]

----

define `z2`
define `x1`
define `z1`
use `z1`
-- post defs: ["z1", "x1", "z2"]

----

define `z2`
define `x1`
define `z1`
use `x1`
-- post defs: ["z1", "x1", "z2"]

----

define `z2`
define `x1`
define `z1`
use `z2`
-- post defs: ["z1", "x1", "z2"]

----

define `z2`
define `x1`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "x1", "z2"]

----

define `z2`
define `x1`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "x1", "z2"]

----

define `z2`
define `x1`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "x1", "z2"]

----

define `z2`
define `x1`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "x1", "z2"]

----

define `z2`
define `x1`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "x1", "z2"]

----

define `z2`
define `x1`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "x1", "z2"]

----

define `z2`
define `x1`
define `z3`
use `z3`
-- post defs: ["z3", "x1", "z2"]

----

define `z2`
define `x1`
define `z3`
use `x1`
-- post defs: ["z3", "x1", "z2"]

----

define `z2`
define `x1`
define `z3`
use `z2`
-- post defs: ["z3", "x1", "z2"]

----

define `z2`
define `x1`
use `x1`
define `x2`
-- post defs: ["x2", "x1", "z2"]

----

define `z2`
define `x1`
use `x1`
define `x3`
-- post defs: ["x3", "x1", "z2"]

----

define `z2`
define `x1`
use `x1`
define `y1`
-- post defs: ["y1", "x1", "z2"]

----

define `z2`
define `x1`
use `x1`
define `y2`
-- post defs: ["y2", "x1", "z2"]

----

define `z2`
define `x1`
use `x1`
define `y3`
-- post defs: ["y3", "x1", "z2"]

----

define `z2`
define `x1`
use `x1`
define `z1`
-- post defs: ["z1", "x1", "z2"]

----

define `z2`
define `x1`
use `x1`
define `z3`
-- post defs: ["z3", "x1", "z2"]

----

define `z2`
define `x1`
use `x1`
use `x1`
-- post defs: ["x1", "z2"]

----

define `z2`
define `x1`
use `x1`
use `z2`
-- post defs: ["x1", "z2"]

----

define `z2`
define `x1`
use `z2`
define `x2`
-- post defs: ["x2", "x1", "z2"]

----

define `z2`
define `x1`
use `z2`
define `x3`
-- post defs: ["x3", "x1", "z2"]

----

define `z2`
define `x1`
use `z2`
define `y1`
-- post defs: ["y1", "x1", "z2"]

----

define `z2`
define `x1`
use `z2`
define `y2`
-- post defs: ["y2", "x1", "z2"]

----

define `z2`
define `x1`
use `z2`
define `y3`
-- post defs: ["y3", "x1", "z2"]

----

define `z2`
define `x1`
use `z2`
define `z1`
-- post defs: ["z1", "x1", "z2"]

----

define `z2`
define `x1`
use `z2`
define `z3`
-- post defs: ["z3", "x1", "z2"]

----

define `z2`
define `x1`
use `z2`
use `x1`
-- post defs: ["x1", "z2"]

----

define `z2`
define `x1`
use `z2`
use `z2`
-- post defs: ["x1", "z2"]

----

define `z2`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "z2"]

----

define `z2`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "z2"]

----

define `z2`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "z2"]

----

define `z2`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "z2"]

----

define `z2`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "z2"]

----

define `z2`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "z2"]

----

define `z2`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "z2"]

----

define `z2`
define `x2`
use `x2`
-- post defs: ["x2", "z2"]

----

define `z2`
define `x2`
use `z2`
-- post defs: ["x2", "z2"]

----

define `z2`
define `x2`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "x2", "z2"]

----

define `z2`
define `x2`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "x2", "z2"]

----

define `z2`
define `x2`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "x2", "z2"]

----

define `z2`
define `x2`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "x2", "z2"]

----

define `z2`
define `x2`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "x2", "z2"]

----

define `z2`
define `x2`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "x2", "z2"]

----

define `z2`
define `x2`
define `x1`
use `x1`
-- post defs: ["x1", "x2", "z2"]

----

define `z2`
define `x2`
define `x1`
use `x2`
-- post defs: ["x1", "x2", "z2"]

----

define `z2`
define `x2`
define `x1`
use `z2`
-- post defs: ["x1", "x2", "z2"]

----

define `z2`
define `x2`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "x2", "z2"]

----

define `z2`
define `x2`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "x2", "z2"]

----

define `z2`
define `x2`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "x2", "z2"]

----

define `z2`
define `x2`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "x2", "z2"]

----

define `z2`
define `x2`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "x2", "z2"]

----

define `z2`
define `x2`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "x2", "z2"]

----

define `z2`
define `x2`
define `x3`
use `x3`
-- post defs: ["x3", "x2", "z2"]

----

define `z2`
define `x2`
define `x3`
use `x2`
-- post defs: ["x3", "x2", "z2"]

----

define `z2`
define `x2`
define `x3`
use `z2`
-- post defs: ["x3", "x2", "z2"]

----

define `z2`
define `x2`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "x2", "z2"]

----

define `z2`
define `x2`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "x2", "z2"]

----

define `z2`
define `x2`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "x2", "z2"]

----

define `z2`
define `x2`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "x2", "z2"]

----

define `z2`
define `x2`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "x2", "z2"]

----

define `z2`
define `x2`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "x2", "z2"]

----

define `z2`
define `x2`
define `y1`
use `y1`
-- post defs: ["y1", "x2", "z2"]

----

define `z2`
define `x2`
define `y1`
use `x2`
-- post defs: ["y1", "x2", "z2"]

----

define `z2`
define `x2`
define `y1`
use `z2`
-- post defs: ["y1", "x2", "z2"]

----

define `z2`
define `x2`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "x2", "z2"]

----

define `z2`
define `x2`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "x2", "z2"]

----

define `z2`
define `x2`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "x2", "z2"]

----

define `z2`
define `x2`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "x2", "z2"]

----

define `z2`
define `x2`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "x2", "z2"]

----

define `z2`
define `x2`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "x2", "z2"]

----

define `z2`
define `x2`
define `y2`
use `y2`
-- post defs: ["y2", "x2", "z2"]

----

define `z2`
define `x2`
define `y2`
use `x2`
-- post defs: ["y2", "x2", "z2"]

----

define `z2`
define `x2`
define `y2`
use `z2`
-- post defs: ["y2", "x2", "z2"]

----

define `z2`
define `x2`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "x2", "z2"]

----

define `z2`
define `x2`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "x2", "z2"]

----

define `z2`
define `x2`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "x2", "z2"]

----

define `z2`
define `x2`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "x2", "z2"]

----

define `z2`
define `x2`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "x2", "z2"]

----

define `z2`
define `x2`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "x2", "z2"]

----

define `z2`
define `x2`
define `y3`
use `y3`
-- post defs: ["y3", "x2", "z2"]

----

define `z2`
define `x2`
define `y3`
use `x2`
-- post defs: ["y3", "x2", "z2"]

----

define `z2`
define `x2`
define `y3`
use `z2`
-- post defs: ["y3", "x2", "z2"]

----

define `z2`
define `x2`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "x2", "z2"]

----

define `z2`
define `x2`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "x2", "z2"]

----

define `z2`
define `x2`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "x2", "z2"]

----

define `z2`
define `x2`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "x2", "z2"]

----

define `z2`
define `x2`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "x2", "z2"]

----

define `z2`
define `x2`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "x2", "z2"]

----

define `z2`
define `x2`
define `z1`
use `z1`
-- post defs: ["z1", "x2", "z2"]

----

define `z2`
define `x2`
define `z1`
use `x2`
-- post defs: ["z1", "x2", "z2"]

----

define `z2`
define `x2`
define `z1`
use `z2`
-- post defs: ["z1", "x2", "z2"]

----

define `z2`
define `x2`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "x2", "z2"]

----

define `z2`
define `x2`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "x2", "z2"]

----

define `z2`
define `x2`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "x2", "z2"]

----

define `z2`
define `x2`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "x2", "z2"]

----

define `z2`
define `x2`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "x2", "z2"]

----

define `z2`
define `x2`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "x2", "z2"]

----

define `z2`
define `x2`
define `z3`
use `z3`
-- post defs: ["z3", "x2", "z2"]

----

define `z2`
define `x2`
define `z3`
use `x2`
-- post defs: ["z3", "x2", "z2"]

----

define `z2`
define `x2`
define `z3`
use `z2`
-- post defs: ["z3", "x2", "z2"]

----

define `z2`
define `x2`
use `x2`
define `x1`
-- post defs: ["x1", "x2", "z2"]

----

define `z2`
define `x2`
use `x2`
define `x3`
-- post defs: ["x3", "x2", "z2"]

----

define `z2`
define `x2`
use `x2`
define `y1`
-- post defs: ["y1", "x2", "z2"]

----

define `z2`
define `x2`
use `x2`
define `y2`
-- post defs: ["y2", "x2", "z2"]

----

define `z2`
define `x2`
use `x2`
define `y3`
-- post defs: ["y3", "x2", "z2"]

----

define `z2`
define `x2`
use `x2`
define `z1`
-- post defs: ["z1", "x2", "z2"]

----

define `z2`
define `x2`
use `x2`
define `z3`
-- post defs: ["z3", "x2", "z2"]

----

define `z2`
define `x2`
use `x2`
use `x2`
-- post defs: ["x2", "z2"]

----

define `z2`
define `x2`
use `x2`
use `z2`
-- post defs: ["x2", "z2"]

----

define `z2`
define `x2`
use `z2`
define `x1`
-- post defs: ["x1", "x2", "z2"]

----

define `z2`
define `x2`
use `z2`
define `x3`
-- post defs: ["x3", "x2", "z2"]

----

define `z2`
define `x2`
use `z2`
define `y1`
-- post defs: ["y1", "x2", "z2"]

----

define `z2`
define `x2`
use `z2`
define `y2`
-- post defs: ["y2", "x2", "z2"]

----

define `z2`
define `x2`
use `z2`
define `y3`
-- post defs: ["y3", "x2", "z2"]

----

define `z2`
define `x2`
use `z2`
define `z1`
-- post defs: ["z1", "x2", "z2"]

----

define `z2`
define `x2`
use `z2`
define `z3`
-- post defs: ["z3", "x2", "z2"]

----

define `z2`
define `x2`
use `z2`
use `x2`
-- post defs: ["x2", "z2"]

----

define `z2`
define `x2`
use `z2`
use `z2`
-- post defs: ["x2", "z2"]

----

define `z2`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "z2"]

----

define `z2`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "z2"]

----

define `z2`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "z2"]

----

define `z2`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "z2"]

----

define `z2`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "z2"]

----

define `z2`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "z2"]

----

define `z2`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "z2"]

----

define `z2`
define `x3`
use `x3`
-- post defs: ["x3", "z2"]

----

define `z2`
define `x3`
use `z2`
-- post defs: ["x3", "z2"]

----

define `z2`
define `x3`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "x3", "z2"]

----

define `z2`
define `x3`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "x3", "z2"]

----

define `z2`
define `x3`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "x3", "z2"]

----

define `z2`
define `x3`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "x3", "z2"]

----

define `z2`
define `x3`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "x3", "z2"]

----

define `z2`
define `x3`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "x3", "z2"]

----

define `z2`
define `x3`
define `x1`
use `x1`
-- post defs: ["x1", "x3", "z2"]

----

define `z2`
define `x3`
define `x1`
use `x3`
-- post defs: ["x1", "x3", "z2"]

----

define `z2`
define `x3`
define `x1`
use `z2`
-- post defs: ["x1", "x3", "z2"]

----

define `z2`
define `x3`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "x3", "z2"]

----

define `z2`
define `x3`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "x3", "z2"]

----

define `z2`
define `x3`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "x3", "z2"]

----

define `z2`
define `x3`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "x3", "z2"]

----

define `z2`
define `x3`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "x3", "z2"]

----

define `z2`
define `x3`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "x3", "z2"]

----

define `z2`
define `x3`
define `x2`
use `x2`
-- post defs: ["x2", "x3", "z2"]

----

define `z2`
define `x3`
define `x2`
use `x3`
-- post defs: ["x2", "x3", "z2"]

----

define `z2`
define `x3`
define `x2`
use `z2`
-- post defs: ["x2", "x3", "z2"]

----

define `z2`
define `x3`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "x3", "z2"]

----

define `z2`
define `x3`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "x3", "z2"]

----

define `z2`
define `x3`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "x3", "z2"]

----

define `z2`
define `x3`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "x3", "z2"]

----

define `z2`
define `x3`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "x3", "z2"]

----

define `z2`
define `x3`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "x3", "z2"]

----

define `z2`
define `x3`
define `y1`
use `y1`
-- post defs: ["y1", "x3", "z2"]

----

define `z2`
define `x3`
define `y1`
use `x3`
-- post defs: ["y1", "x3", "z2"]

----

define `z2`
define `x3`
define `y1`
use `z2`
-- post defs: ["y1", "x3", "z2"]

----

define `z2`
define `x3`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "x3", "z2"]

----

define `z2`
define `x3`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "x3", "z2"]

----

define `z2`
define `x3`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "x3", "z2"]

----

define `z2`
define `x3`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "x3", "z2"]

----

define `z2`
define `x3`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "x3", "z2"]

----

define `z2`
define `x3`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "x3", "z2"]

----

define `z2`
define `x3`
define `y2`
use `y2`
-- post defs: ["y2", "x3", "z2"]

----

define `z2`
define `x3`
define `y2`
use `x3`
-- post defs: ["y2", "x3", "z2"]

----

define `z2`
define `x3`
define `y2`
use `z2`
-- post defs: ["y2", "x3", "z2"]

----

define `z2`
define `x3`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "x3", "z2"]

----

define `z2`
define `x3`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "x3", "z2"]

----

define `z2`
define `x3`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "x3", "z2"]

----

define `z2`
define `x3`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "x3", "z2"]

----

define `z2`
define `x3`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "x3", "z2"]

----

define `z2`
define `x3`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "x3", "z2"]

----

define `z2`
define `x3`
define `y3`
use `y3`
-- post defs: ["y3", "x3", "z2"]

----

define `z2`
define `x3`
define `y3`
use `x3`
-- post defs: ["y3", "x3", "z2"]

----

define `z2`
define `x3`
define `y3`
use `z2`
-- post defs: ["y3", "x3", "z2"]

----

define `z2`
define `x3`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "x3", "z2"]

----

define `z2`
define `x3`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "x3", "z2"]

----

define `z2`
define `x3`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "x3", "z2"]

----

define `z2`
define `x3`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "x3", "z2"]

----

define `z2`
define `x3`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "x3", "z2"]

----

define `z2`
define `x3`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "x3", "z2"]

----

define `z2`
define `x3`
define `z1`
use `z1`
-- post defs: ["z1", "x3", "z2"]

----

define `z2`
define `x3`
define `z1`
use `x3`
-- post defs: ["z1", "x3", "z2"]

----

define `z2`
define `x3`
define `z1`
use `z2`
-- post defs: ["z1", "x3", "z2"]

----

define `z2`
define `x3`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "x3", "z2"]

----

define `z2`
define `x3`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "x3", "z2"]

----

define `z2`
define `x3`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "x3", "z2"]

----

define `z2`
define `x3`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "x3", "z2"]

----

define `z2`
define `x3`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "x3", "z2"]

----

define `z2`
define `x3`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "x3", "z2"]

----

define `z2`
define `x3`
define `z3`
use `z3`
-- post defs: ["z3", "x3", "z2"]

----

define `z2`
define `x3`
define `z3`
use `x3`
-- post defs: ["z3", "x3", "z2"]

----

define `z2`
define `x3`
define `z3`
use `z2`
-- post defs: ["z3", "x3", "z2"]

----

define `z2`
define `x3`
use `x3`
define `x1`
-- post defs: ["x1", "x3", "z2"]

----

define `z2`
define `x3`
use `x3`
define `x2`
-- post defs: ["x2", "x3", "z2"]

----

define `z2`
define `x3`
use `x3`
define `y1`
-- post defs: ["y1", "x3", "z2"]

----

define `z2`
define `x3`
use `x3`
define `y2`
-- post defs: ["y2", "x3", "z2"]

----

define `z2`
define `x3`
use `x3`
define `y3`
-- post defs: ["y3", "x3", "z2"]

----

define `z2`
define `x3`
use `x3`
define `z1`
-- post defs: ["z1", "x3", "z2"]

----

define `z2`
define `x3`
use `x3`
define `z3`
-- post defs: ["z3", "x3", "z2"]

----

define `z2`
define `x3`
use `x3`
use `x3`
-- post defs: ["x3", "z2"]

----

define `z2`
define `x3`
use `x3`
use `z2`
-- post defs: ["x3", "z2"]

----

define `z2`
define `x3`
use `z2`
define `x1`
-- post defs: ["x1", "x3", "z2"]

----

define `z2`
define `x3`
use `z2`
define `x2`
-- post defs: ["x2", "x3", "z2"]

----

define `z2`
define `x3`
use `z2`
define `y1`
-- post defs: ["y1", "x3", "z2"]

----

define `z2`
define `x3`
use `z2`
define `y2`
-- post defs: ["y2", "x3", "z2"]

----

define `z2`
define `x3`
use `z2`
define `y3`
-- post defs: ["y3", "x3", "z2"]

----

define `z2`
define `x3`
use `z2`
define `z1`
-- post defs: ["z1", "x3", "z2"]

----

define `z2`
define `x3`
use `z2`
define `z3`
-- post defs: ["z3", "x3", "z2"]

----

define `z2`
define `x3`
use `z2`
use `x3`
-- post defs: ["x3", "z2"]

----

define `z2`
define `x3`
use `z2`
use `z2`
-- post defs: ["x3", "z2"]

----

define `z2`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "z2"]

----

define `z2`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "z2"]

----

define `z2`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "z2"]

----

define `z2`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "z2"]

----

define `z2`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "z2"]

----

define `z2`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "z2"]

----

define `z2`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "z2"]

----

define `z2`
define `y1`
use `y1`
-- post defs: ["y1", "z2"]

----

define `z2`
define `y1`
use `z2`
-- post defs: ["y1", "z2"]

----

define `z2`
define `y1`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "y1", "z2"]

----

define `z2`
define `y1`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "y1", "z2"]

----

define `z2`
define `y1`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "y1", "z2"]

----

define `z2`
define `y1`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "y1", "z2"]

----

define `z2`
define `y1`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "y1", "z2"]

----

define `z2`
define `y1`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "y1", "z2"]

----

define `z2`
define `y1`
define `x1`
use `x1`
-- post defs: ["x1", "y1", "z2"]

----

define `z2`
define `y1`
define `x1`
use `y1`
-- post defs: ["x1", "y1", "z2"]

----

define `z2`
define `y1`
define `x1`
use `z2`
-- post defs: ["x1", "y1", "z2"]

----

define `z2`
define `y1`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "y1", "z2"]

----

define `z2`
define `y1`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "y1", "z2"]

----

define `z2`
define `y1`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "y1", "z2"]

----

define `z2`
define `y1`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "y1", "z2"]

----

define `z2`
define `y1`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "y1", "z2"]

----

define `z2`
define `y1`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "y1", "z2"]

----

define `z2`
define `y1`
define `x2`
use `x2`
-- post defs: ["x2", "y1", "z2"]

----

define `z2`
define `y1`
define `x2`
use `y1`
-- post defs: ["x2", "y1", "z2"]

----

define `z2`
define `y1`
define `x2`
use `z2`
-- post defs: ["x2", "y1", "z2"]

----

define `z2`
define `y1`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "y1", "z2"]

----

define `z2`
define `y1`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "y1", "z2"]

----

define `z2`
define `y1`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "y1", "z2"]

----

define `z2`
define `y1`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "y1", "z2"]

----

define `z2`
define `y1`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "y1", "z2"]

----

define `z2`
define `y1`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "y1", "z2"]

----

define `z2`
define `y1`
define `x3`
use `x3`
-- post defs: ["x3", "y1", "z2"]

----

define `z2`
define `y1`
define `x3`
use `y1`
-- post defs: ["x3", "y1", "z2"]

----

define `z2`
define `y1`
define `x3`
use `z2`
-- post defs: ["x3", "y1", "z2"]

----

define `z2`
define `y1`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "y1", "z2"]

----

define `z2`
define `y1`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "y1", "z2"]

----

define `z2`
define `y1`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "y1", "z2"]

----

define `z2`
define `y1`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "y1", "z2"]

----

define `z2`
define `y1`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "y1", "z2"]

----

define `z2`
define `y1`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "y1", "z2"]

----

define `z2`
define `y1`
define `y2`
use `y2`
-- post defs: ["y2", "y1", "z2"]

----

define `z2`
define `y1`
define `y2`
use `y1`
-- post defs: ["y2", "y1", "z2"]

----

define `z2`
define `y1`
define `y2`
use `z2`
-- post defs: ["y2", "y1", "z2"]

----

define `z2`
define `y1`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "y1", "z2"]

----

define `z2`
define `y1`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "y1", "z2"]

----

define `z2`
define `y1`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "y1", "z2"]

----

define `z2`
define `y1`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "y1", "z2"]

----

define `z2`
define `y1`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "y1", "z2"]

----

define `z2`
define `y1`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "y1", "z2"]

----

define `z2`
define `y1`
define `y3`
use `y3`
-- post defs: ["y3", "y1", "z2"]

----

define `z2`
define `y1`
define `y3`
use `y1`
-- post defs: ["y3", "y1", "z2"]

----

define `z2`
define `y1`
define `y3`
use `z2`
-- post defs: ["y3", "y1", "z2"]

----

define `z2`
define `y1`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "y1", "z2"]

----

define `z2`
define `y1`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "y1", "z2"]

----

define `z2`
define `y1`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "y1", "z2"]

----

define `z2`
define `y1`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "y1", "z2"]

----

define `z2`
define `y1`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "y1", "z2"]

----

define `z2`
define `y1`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "y1", "z2"]

----

define `z2`
define `y1`
define `z1`
use `z1`
-- post defs: ["z1", "y1", "z2"]

----

define `z2`
define `y1`
define `z1`
use `y1`
-- post defs: ["z1", "y1", "z2"]

----

define `z2`
define `y1`
define `z1`
use `z2`
-- post defs: ["z1", "y1", "z2"]

----

define `z2`
define `y1`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "y1", "z2"]

----

define `z2`
define `y1`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "y1", "z2"]

----

define `z2`
define `y1`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "y1", "z2"]

----

define `z2`
define `y1`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "y1", "z2"]

----

define `z2`
define `y1`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "y1", "z2"]

----

define `z2`
define `y1`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "y1", "z2"]

----

define `z2`
define `y1`
define `z3`
use `z3`
-- post defs: ["z3", "y1", "z2"]

----

define `z2`
define `y1`
define `z3`
use `y1`
-- post defs: ["z3", "y1", "z2"]

----

define `z2`
define `y1`
define `z3`
use `z2`
-- post defs: ["z3", "y1", "z2"]

----

define `z2`
define `y1`
use `y1`
define `x1`
-- post defs: ["x1", "y1", "z2"]

----

define `z2`
define `y1`
use `y1`
define `x2`
-- post defs: ["x2", "y1", "z2"]

----

define `z2`
define `y1`
use `y1`
define `x3`
-- post defs: ["x3", "y1", "z2"]

----

define `z2`
define `y1`
use `y1`
define `y2`
-- post defs: ["y2", "y1", "z2"]

----

define `z2`
define `y1`
use `y1`
define `y3`
-- post defs: ["y3", "y1", "z2"]

----

define `z2`
define `y1`
use `y1`
define `z1`
-- post defs: ["z1", "y1", "z2"]

----

define `z2`
define `y1`
use `y1`
define `z3`
-- post defs: ["z3", "y1", "z2"]

----

define `z2`
define `y1`
use `y1`
use `y1`
-- post defs: ["y1", "z2"]

----

define `z2`
define `y1`
use `y1`
use `z2`
-- post defs: ["y1", "z2"]

----

define `z2`
define `y1`
use `z2`
define `x1`
-- post defs: ["x1", "y1", "z2"]

----

define `z2`
define `y1`
use `z2`
define `x2`
-- post defs: ["x2", "y1", "z2"]

----

define `z2`
define `y1`
use `z2`
define `x3`
-- post defs: ["x3", "y1", "z2"]

----

define `z2`
define `y1`
use `z2`
define `y2`
-- post defs: ["y2", "y1", "z2"]

----

define `z2`
define `y1`
use `z2`
define `y3`
-- post defs: ["y3", "y1", "z2"]

----

define `z2`
define `y1`
use `z2`
define `z1`
-- post defs: ["z1", "y1", "z2"]

----

define `z2`
define `y1`
use `z2`
define `z3`
-- post defs: ["z3", "y1", "z2"]

----

define `z2`
define `y1`
use `z2`
use `y1`
-- post defs: ["y1", "z2"]

----

define `z2`
define `y1`
use `z2`
use `z2`
-- post defs: ["y1", "z2"]

----

define `z2`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "z2"]

----

define `z2`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "z2"]

----

define `z2`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "z2"]

----

define `z2`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "z2"]

----

define `z2`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "z2"]

----

define `z2`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "z2"]

----

define `z2`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "z2"]

----

define `z2`
define `y2`
use `y2`
-- post defs: ["y2", "z2"]

----

define `z2`
define `y2`
use `z2`
-- post defs: ["y2", "z2"]

----

define `z2`
define `y2`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "y2", "z2"]

----

define `z2`
define `y2`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "y2", "z2"]

----

define `z2`
define `y2`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "y2", "z2"]

----

define `z2`
define `y2`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "y2", "z2"]

----

define `z2`
define `y2`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "y2", "z2"]

----

define `z2`
define `y2`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "y2", "z2"]

----

define `z2`
define `y2`
define `x1`
use `x1`
-- post defs: ["x1", "y2", "z2"]

----

define `z2`
define `y2`
define `x1`
use `y2`
-- post defs: ["x1", "y2", "z2"]

----

define `z2`
define `y2`
define `x1`
use `z2`
-- post defs: ["x1", "y2", "z2"]

----

define `z2`
define `y2`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "y2", "z2"]

----

define `z2`
define `y2`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "y2", "z2"]

----

define `z2`
define `y2`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "y2", "z2"]

----

define `z2`
define `y2`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "y2", "z2"]

----

define `z2`
define `y2`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "y2", "z2"]

----

define `z2`
define `y2`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "y2", "z2"]

----

define `z2`
define `y2`
define `x2`
use `x2`
-- post defs: ["x2", "y2", "z2"]

----

define `z2`
define `y2`
define `x2`
use `y2`
-- post defs: ["x2", "y2", "z2"]

----

define `z2`
define `y2`
define `x2`
use `z2`
-- post defs: ["x2", "y2", "z2"]

----

define `z2`
define `y2`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "y2", "z2"]

----

define `z2`
define `y2`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "y2", "z2"]

----

define `z2`
define `y2`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "y2", "z2"]

----

define `z2`
define `y2`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "y2", "z2"]

----

define `z2`
define `y2`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "y2", "z2"]

----

define `z2`
define `y2`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "y2", "z2"]

----

define `z2`
define `y2`
define `x3`
use `x3`
-- post defs: ["x3", "y2", "z2"]

----

define `z2`
define `y2`
define `x3`
use `y2`
-- post defs: ["x3", "y2", "z2"]

----

define `z2`
define `y2`
define `x3`
use `z2`
-- post defs: ["x3", "y2", "z2"]

----

define `z2`
define `y2`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "y2", "z2"]

----

define `z2`
define `y2`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "y2", "z2"]

----

define `z2`
define `y2`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "y2", "z2"]

----

define `z2`
define `y2`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "y2", "z2"]

----

define `z2`
define `y2`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "y2", "z2"]

----

define `z2`
define `y2`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "y2", "z2"]

----

define `z2`
define `y2`
define `y1`
use `y1`
-- post defs: ["y1", "y2", "z2"]

----

define `z2`
define `y2`
define `y1`
use `y2`
-- post defs: ["y1", "y2", "z2"]

----

define `z2`
define `y2`
define `y1`
use `z2`
-- post defs: ["y1", "y2", "z2"]

----

define `z2`
define `y2`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "y2", "z2"]

----

define `z2`
define `y2`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "y2", "z2"]

----

define `z2`
define `y2`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "y2", "z2"]

----

define `z2`
define `y2`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "y2", "z2"]

----

define `z2`
define `y2`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "y2", "z2"]

----

define `z2`
define `y2`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "y2", "z2"]

----

define `z2`
define `y2`
define `y3`
use `y3`
-- post defs: ["y3", "y2", "z2"]

----

define `z2`
define `y2`
define `y3`
use `y2`
-- post defs: ["y3", "y2", "z2"]

----

define `z2`
define `y2`
define `y3`
use `z2`
-- post defs: ["y3", "y2", "z2"]

----

define `z2`
define `y2`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "y2", "z2"]

----

define `z2`
define `y2`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "y2", "z2"]

----

define `z2`
define `y2`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "y2", "z2"]

----

define `z2`
define `y2`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "y2", "z2"]

----

define `z2`
define `y2`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "y2", "z2"]

----

define `z2`
define `y2`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "y2", "z2"]

----

define `z2`
define `y2`
define `z1`
use `z1`
-- post defs: ["z1", "y2", "z2"]

----

define `z2`
define `y2`
define `z1`
use `y2`
-- post defs: ["z1", "y2", "z2"]

----

define `z2`
define `y2`
define `z1`
use `z2`
-- post defs: ["z1", "y2", "z2"]

----

define `z2`
define `y2`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "y2", "z2"]

----

define `z2`
define `y2`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "y2", "z2"]

----

define `z2`
define `y2`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "y2", "z2"]

----

define `z2`
define `y2`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "y2", "z2"]

----

define `z2`
define `y2`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "y2", "z2"]

----

define `z2`
define `y2`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "y2", "z2"]

----

define `z2`
define `y2`
define `z3`
use `z3`
-- post defs: ["z3", "y2", "z2"]

----

define `z2`
define `y2`
define `z3`
use `y2`
-- post defs: ["z3", "y2", "z2"]

----

define `z2`
define `y2`
define `z3`
use `z2`
-- post defs: ["z3", "y2", "z2"]

----

define `z2`
define `y2`
use `y2`
define `x1`
-- post defs: ["x1", "y2", "z2"]

----

define `z2`
define `y2`
use `y2`
define `x2`
-- post defs: ["x2", "y2", "z2"]

----

define `z2`
define `y2`
use `y2`
define `x3`
-- post defs: ["x3", "y2", "z2"]

----

define `z2`
define `y2`
use `y2`
define `y1`
-- post defs: ["y1", "y2", "z2"]

----

define `z2`
define `y2`
use `y2`
define `y3`
-- post defs: ["y3", "y2", "z2"]

----

define `z2`
define `y2`
use `y2`
define `z1`
-- post defs: ["z1", "y2", "z2"]

----

define `z2`
define `y2`
use `y2`
define `z3`
-- post defs: ["z3", "y2", "z2"]

----

define `z2`
define `y2`
use `y2`
use `y2`
-- post defs: ["y2", "z2"]

----

define `z2`
define `y2`
use `y2`
use `z2`
-- post defs: ["y2", "z2"]

----

define `z2`
define `y2`
use `z2`
define `x1`
-- post defs: ["x1", "y2", "z2"]

----

define `z2`
define `y2`
use `z2`
define `x2`
-- post defs: ["x2", "y2", "z2"]

----

define `z2`
define `y2`
use `z2`
define `x3`
-- post defs: ["x3", "y2", "z2"]

----

define `z2`
define `y2`
use `z2`
define `y1`
-- post defs: ["y1", "y2", "z2"]

----

define `z2`
define `y2`
use `z2`
define `y3`
-- post defs: ["y3", "y2", "z2"]

----

define `z2`
define `y2`
use `z2`
define `z1`
-- post defs: ["z1", "y2", "z2"]

----

define `z2`
define `y2`
use `z2`
define `z3`
-- post defs: ["z3", "y2", "z2"]

----

define `z2`
define `y2`
use `z2`
use `y2`
-- post defs: ["y2", "z2"]

----

define `z2`
define `y2`
use `z2`
use `z2`
-- post defs: ["y2", "z2"]

----

define `z2`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "z2"]

----

define `z2`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "z2"]

----

define `z2`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "z2"]

----

define `z2`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "z2"]

----

define `z2`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "z2"]

----

define `z2`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "z2"]

----

define `z2`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "z2"]

----

define `z2`
define `y3`
use `y3`
-- post defs: ["y3", "z2"]

----

define `z2`
define `y3`
use `z2`
-- post defs: ["y3", "z2"]

----

define `z2`
define `y3`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "y3", "z2"]

----

define `z2`
define `y3`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "y3", "z2"]

----

define `z2`
define `y3`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "y3", "z2"]

----

define `z2`
define `y3`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "y3", "z2"]

----

define `z2`
define `y3`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "y3", "z2"]

----

define `z2`
define `y3`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "y3", "z2"]

----

define `z2`
define `y3`
define `x1`
use `x1`
-- post defs: ["x1", "y3", "z2"]

----

define `z2`
define `y3`
define `x1`
use `y3`
-- post defs: ["x1", "y3", "z2"]

----

define `z2`
define `y3`
define `x1`
use `z2`
-- post defs: ["x1", "y3", "z2"]

----

define `z2`
define `y3`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "y3", "z2"]

----

define `z2`
define `y3`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "y3", "z2"]

----

define `z2`
define `y3`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "y3", "z2"]

----

define `z2`
define `y3`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "y3", "z2"]

----

define `z2`
define `y3`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "y3", "z2"]

----

define `z2`
define `y3`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "y3", "z2"]

----

define `z2`
define `y3`
define `x2`
use `x2`
-- post defs: ["x2", "y3", "z2"]

----

define `z2`
define `y3`
define `x2`
use `y3`
-- post defs: ["x2", "y3", "z2"]

----

define `z2`
define `y3`
define `x2`
use `z2`
-- post defs: ["x2", "y3", "z2"]

----

define `z2`
define `y3`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "y3", "z2"]

----

define `z2`
define `y3`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "y3", "z2"]

----

define `z2`
define `y3`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "y3", "z2"]

----

define `z2`
define `y3`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "y3", "z2"]

----

define `z2`
define `y3`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "y3", "z2"]

----

define `z2`
define `y3`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "y3", "z2"]

----

define `z2`
define `y3`
define `x3`
use `x3`
-- post defs: ["x3", "y3", "z2"]

----

define `z2`
define `y3`
define `x3`
use `y3`
-- post defs: ["x3", "y3", "z2"]

----

define `z2`
define `y3`
define `x3`
use `z2`
-- post defs: ["x3", "y3", "z2"]

----

define `z2`
define `y3`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "y3", "z2"]

----

define `z2`
define `y3`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "y3", "z2"]

----

define `z2`
define `y3`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "y3", "z2"]

----

define `z2`
define `y3`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "y3", "z2"]

----

define `z2`
define `y3`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "y3", "z2"]

----

define `z2`
define `y3`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "y3", "z2"]

----

define `z2`
define `y3`
define `y1`
use `y1`
-- post defs: ["y1", "y3", "z2"]

----

define `z2`
define `y3`
define `y1`
use `y3`
-- post defs: ["y1", "y3", "z2"]

----

define `z2`
define `y3`
define `y1`
use `z2`
-- post defs: ["y1", "y3", "z2"]

----

define `z2`
define `y3`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "y3", "z2"]

----

define `z2`
define `y3`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "y3", "z2"]

----

define `z2`
define `y3`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "y3", "z2"]

----

define `z2`
define `y3`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "y3", "z2"]

----

define `z2`
define `y3`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "y3", "z2"]

----

define `z2`
define `y3`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "y3", "z2"]

----

define `z2`
define `y3`
define `y2`
use `y2`
-- post defs: ["y2", "y3", "z2"]

----

define `z2`
define `y3`
define `y2`
use `y3`
-- post defs: ["y2", "y3", "z2"]

----

define `z2`
define `y3`
define `y2`
use `z2`
-- post defs: ["y2", "y3", "z2"]

----

define `z2`
define `y3`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "y3", "z2"]

----

define `z2`
define `y3`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "y3", "z2"]

----

define `z2`
define `y3`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "y3", "z2"]

----

define `z2`
define `y3`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "y3", "z2"]

----

define `z2`
define `y3`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "y3", "z2"]

----

define `z2`
define `y3`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "y3", "z2"]

----

define `z2`
define `y3`
define `z1`
use `z1`
-- post defs: ["z1", "y3", "z2"]

----

define `z2`
define `y3`
define `z1`
use `y3`
-- post defs: ["z1", "y3", "z2"]

----

define `z2`
define `y3`
define `z1`
use `z2`
-- post defs: ["z1", "y3", "z2"]

----

define `z2`
define `y3`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "y3", "z2"]

----

define `z2`
define `y3`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "y3", "z2"]

----

define `z2`
define `y3`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "y3", "z2"]

----

define `z2`
define `y3`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "y3", "z2"]

----

define `z2`
define `y3`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "y3", "z2"]

----

define `z2`
define `y3`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "y3", "z2"]

----

define `z2`
define `y3`
define `z3`
use `z3`
-- post defs: ["z3", "y3", "z2"]

----

define `z2`
define `y3`
define `z3`
use `y3`
-- post defs: ["z3", "y3", "z2"]

----

define `z2`
define `y3`
define `z3`
use `z2`
-- post defs: ["z3", "y3", "z2"]

----

define `z2`
define `y3`
use `y3`
define `x1`
-- post defs: ["x1", "y3", "z2"]

----

define `z2`
define `y3`
use `y3`
define `x2`
-- post defs: ["x2", "y3", "z2"]

----

define `z2`
define `y3`
use `y3`
define `x3`
-- post defs: ["x3", "y3", "z2"]

----

define `z2`
define `y3`
use `y3`
define `y1`
-- post defs: ["y1", "y3", "z2"]

----

define `z2`
define `y3`
use `y3`
define `y2`
-- post defs: ["y2", "y3", "z2"]

----

define `z2`
define `y3`
use `y3`
define `z1`
-- post defs: ["z1", "y3", "z2"]

----

define `z2`
define `y3`
use `y3`
define `z3`
-- post defs: ["z3", "y3", "z2"]

----

define `z2`
define `y3`
use `y3`
use `y3`
-- post defs: ["y3", "z2"]

----

define `z2`
define `y3`
use `y3`
use `z2`
-- post defs: ["y3", "z2"]

----

define `z2`
define `y3`
use `z2`
define `x1`
-- post defs: ["x1", "y3", "z2"]

----

define `z2`
define `y3`
use `z2`
define `x2`
-- post defs: ["x2", "y3", "z2"]

----

define `z2`
define `y3`
use `z2`
define `x3`
-- post defs: ["x3", "y3", "z2"]

----

define `z2`
define `y3`
use `z2`
define `y1`
-- post defs: ["y1", "y3", "z2"]

----

define `z2`
define `y3`
use `z2`
define `y2`
-- post defs: ["y2", "y3", "z2"]

----

define `z2`
define `y3`
use `z2`
define `z1`
-- post defs: ["z1", "y3", "z2"]

----

define `z2`
define `y3`
use `z2`
define `z3`
-- post defs: ["z3", "y3", "z2"]

----

define `z2`
define `y3`
use `z2`
use `y3`
-- post defs: ["y3", "z2"]

----

define `z2`
define `y3`
use `z2`
use `z2`
-- post defs: ["y3", "z2"]

----

define `z2`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "z2"]

----

define `z2`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "z2"]

----

define `z2`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "z2"]

----

define `z2`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "z2"]

----

define `z2`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "z2"]

----

define `z2`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "z2"]

----

define `z2`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "z2"]

----

define `z2`
define `z1`
use `z1`
-- post defs: ["z1", "z2"]

----

define `z2`
define `z1`
use `z2`
-- post defs: ["z1", "z2"]

----

define `z2`
define `z1`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "z1", "z2"]

----

define `z2`
define `z1`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "z1", "z2"]

----

define `z2`
define `z1`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "z1", "z2"]

----

define `z2`
define `z1`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "z1", "z2"]

----

define `z2`
define `z1`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "z1", "z2"]

----

define `z2`
define `z1`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "z1", "z2"]

----

define `z2`
define `z1`
define `x1`
use `x1`
-- post defs: ["x1", "z1", "z2"]

----

define `z2`
define `z1`
define `x1`
use `z1`
-- post defs: ["x1", "z1", "z2"]

----

define `z2`
define `z1`
define `x1`
use `z2`
-- post defs: ["x1", "z1", "z2"]

----

define `z2`
define `z1`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "z1", "z2"]

----

define `z2`
define `z1`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "z1", "z2"]

----

define `z2`
define `z1`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "z1", "z2"]

----

define `z2`
define `z1`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "z1", "z2"]

----

define `z2`
define `z1`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "z1", "z2"]

----

define `z2`
define `z1`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "z1", "z2"]

----

define `z2`
define `z1`
define `x2`
use `x2`
-- post defs: ["x2", "z1", "z2"]

----

define `z2`
define `z1`
define `x2`
use `z1`
-- post defs: ["x2", "z1", "z2"]

----

define `z2`
define `z1`
define `x2`
use `z2`
-- post defs: ["x2", "z1", "z2"]

----

define `z2`
define `z1`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "z1", "z2"]

----

define `z2`
define `z1`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "z1", "z2"]

----

define `z2`
define `z1`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "z1", "z2"]

----

define `z2`
define `z1`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "z1", "z2"]

----

define `z2`
define `z1`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "z1", "z2"]

----

define `z2`
define `z1`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "z1", "z2"]

----

define `z2`
define `z1`
define `x3`
use `x3`
-- post defs: ["x3", "z1", "z2"]

----

define `z2`
define `z1`
define `x3`
use `z1`
-- post defs: ["x3", "z1", "z2"]

----

define `z2`
define `z1`
define `x3`
use `z2`
-- post defs: ["x3", "z1", "z2"]

----

define `z2`
define `z1`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "z1", "z2"]

----

define `z2`
define `z1`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "z1", "z2"]

----

define `z2`
define `z1`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "z1", "z2"]

----

define `z2`
define `z1`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "z1", "z2"]

----

define `z2`
define `z1`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "z1", "z2"]

----

define `z2`
define `z1`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "z1", "z2"]

----

define `z2`
define `z1`
define `y1`
use `y1`
-- post defs: ["y1", "z1", "z2"]

----

define `z2`
define `z1`
define `y1`
use `z1`
-- post defs: ["y1", "z1", "z2"]

----

define `z2`
define `z1`
define `y1`
use `z2`
-- post defs: ["y1", "z1", "z2"]

----

define `z2`
define `z1`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "z1", "z2"]

----

define `z2`
define `z1`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "z1", "z2"]

----

define `z2`
define `z1`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "z1", "z2"]

----

define `z2`
define `z1`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "z1", "z2"]

----

define `z2`
define `z1`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "z1", "z2"]

----

define `z2`
define `z1`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "z1", "z2"]

----

define `z2`
define `z1`
define `y2`
use `y2`
-- post defs: ["y2", "z1", "z2"]

----

define `z2`
define `z1`
define `y2`
use `z1`
-- post defs: ["y2", "z1", "z2"]

----

define `z2`
define `z1`
define `y2`
use `z2`
-- post defs: ["y2", "z1", "z2"]

----

define `z2`
define `z1`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "z1", "z2"]

----

define `z2`
define `z1`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "z1", "z2"]

----

define `z2`
define `z1`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "z1", "z2"]

----

define `z2`
define `z1`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "z1", "z2"]

----

define `z2`
define `z1`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "z1", "z2"]

----

define `z2`
define `z1`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "z1", "z2"]

----

define `z2`
define `z1`
define `y3`
use `y3`
-- post defs: ["y3", "z1", "z2"]

----

define `z2`
define `z1`
define `y3`
use `z1`
-- post defs: ["y3", "z1", "z2"]

----

define `z2`
define `z1`
define `y3`
use `z2`
-- post defs: ["y3", "z1", "z2"]

----

define `z2`
define `z1`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "z1", "z2"]

----

define `z2`
define `z1`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "z1", "z2"]

----

define `z2`
define `z1`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "z1", "z2"]

----

define `z2`
define `z1`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "z1", "z2"]

----

define `z2`
define `z1`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "z1", "z2"]

----

define `z2`
define `z1`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "z1", "z2"]

----

define `z2`
define `z1`
define `z3`
use `z3`
-- post defs: ["z3", "z1", "z2"]

----

define `z2`
define `z1`
define `z3`
use `z1`
-- post defs: ["z3", "z1", "z2"]

----

define `z2`
define `z1`
define `z3`
use `z2`
-- post defs: ["z3", "z1", "z2"]

----

define `z2`
define `z1`
use `z1`
define `x1`
-- post defs: ["x1", "z1", "z2"]

----

define `z2`
define `z1`
use `z1`
define `x2`
-- post defs: ["x2", "z1", "z2"]

----

define `z2`
define `z1`
use `z1`
define `x3`
-- post defs: ["x3", "z1", "z2"]

----

define `z2`
define `z1`
use `z1`
define `y1`
-- post defs: ["y1", "z1", "z2"]

----

define `z2`
define `z1`
use `z1`
define `y2`
-- post defs: ["y2", "z1", "z2"]

----

define `z2`
define `z1`
use `z1`
define `y3`
-- post defs: ["y3", "z1", "z2"]

----

define `z2`
define `z1`
use `z1`
define `z3`
-- post defs: ["z3", "z1", "z2"]

----

define `z2`
define `z1`
use `z1`
use `z1`
-- post defs: ["z1", "z2"]

----

define `z2`
define `z1`
use `z1`
use `z2`
-- post defs: ["z1", "z2"]

----

define `z2`
define `z1`
use `z2`
define `x1`
-- post defs: ["x1", "z1", "z2"]

----

define `z2`
define `z1`
use `z2`
define `x2`
-- post defs: ["x2", "z1", "z2"]

----

define `z2`
define `z1`
use `z2`
define `x3`
-- post defs: ["x3", "z1", "z2"]

----

define `z2`
define `z1`
use `z2`
define `y1`
-- post defs: ["y1", "z1", "z2"]

----

define `z2`
define `z1`
use `z2`
define `y2`
-- post defs: ["y2", "z1", "z2"]

----

define `z2`
define `z1`
use `z2`
define `y3`
-- post defs: ["y3", "z1", "z2"]

----

define `z2`
define `z1`
use `z2`
define `z3`
-- post defs: ["z3", "z1", "z2"]

----

define `z2`
define `z1`
use `z2`
use `z1`
-- post defs: ["z1", "z2"]

----

define `z2`
define `z1`
use `z2`
use `z2`
-- post defs: ["z1", "z2"]

----

define `z2`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "z2"]

----

define `z2`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "z2"]

----

define `z2`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "z2"]

----

define `z2`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "z2"]

----

define `z2`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "z2"]

----

define `z2`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "z2"]

----

define `z2`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "z2"]

----

define `z2`
define `z3`
use `z3`
-- post defs: ["z3", "z2"]

----

define `z2`
define `z3`
use `z2`
-- post defs: ["z3", "z2"]

----

define `z2`
define `z3`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "z3", "z2"]

----

define `z2`
define `z3`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "z3", "z2"]

----

define `z2`
define `z3`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "z3", "z2"]

----

define `z2`
define `z3`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "z3", "z2"]

----

define `z2`
define `z3`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "z3", "z2"]

----

define `z2`
define `z3`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "z3", "z2"]

----

define `z2`
define `z3`
define `x1`
use `x1`
-- post defs: ["x1", "z3", "z2"]

----

define `z2`
define `z3`
define `x1`
use `z3`
-- post defs: ["x1", "z3", "z2"]

----

define `z2`
define `z3`
define `x1`
use `z2`
-- post defs: ["x1", "z3", "z2"]

----

define `z2`
define `z3`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "z3", "z2"]

----

define `z2`
define `z3`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "z3", "z2"]

----

define `z2`
define `z3`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "z3", "z2"]

----

define `z2`
define `z3`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "z3", "z2"]

----

define `z2`
define `z3`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "z3", "z2"]

----

define `z2`
define `z3`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "z3", "z2"]

----

define `z2`
define `z3`
define `x2`
use `x2`
-- post defs: ["x2", "z3", "z2"]

----

define `z2`
define `z3`
define `x2`
use `z3`
-- post defs: ["x2", "z3", "z2"]

----

define `z2`
define `z3`
define `x2`
use `z2`
-- post defs: ["x2", "z3", "z2"]

----

define `z2`
define `z3`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "z3", "z2"]

----

define `z2`
define `z3`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "z3", "z2"]

----

define `z2`
define `z3`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "z3", "z2"]

----

define `z2`
define `z3`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "z3", "z2"]

----

define `z2`
define `z3`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "z3", "z2"]

----

define `z2`
define `z3`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "z3", "z2"]

----

define `z2`
define `z3`
define `x3`
use `x3`
-- post defs: ["x3", "z3", "z2"]

----

define `z2`
define `z3`
define `x3`
use `z3`
-- post defs: ["x3", "z3", "z2"]

----

define `z2`
define `z3`
define `x3`
use `z2`
-- post defs: ["x3", "z3", "z2"]

----

define `z2`
define `z3`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "z3", "z2"]

----

define `z2`
define `z3`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "z3", "z2"]

----

define `z2`
define `z3`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "z3", "z2"]

----

define `z2`
define `z3`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "z3", "z2"]

----

define `z2`
define `z3`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "z3", "z2"]

----

define `z2`
define `z3`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "z3", "z2"]

----

define `z2`
define `z3`
define `y1`
use `y1`
-- post defs: ["y1", "z3", "z2"]

----

define `z2`
define `z3`
define `y1`
use `z3`
-- post defs: ["y1", "z3", "z2"]

----

define `z2`
define `z3`
define `y1`
use `z2`
-- post defs: ["y1", "z3", "z2"]

----

define `z2`
define `z3`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "z3", "z2"]

----

define `z2`
define `z3`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "z3", "z2"]

----

define `z2`
define `z3`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "z3", "z2"]

----

define `z2`
define `z3`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "z3", "z2"]

----

define `z2`
define `z3`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "z3", "z2"]

----

define `z2`
define `z3`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "z3", "z2"]

----

define `z2`
define `z3`
define `y2`
use `y2`
-- post defs: ["y2", "z3", "z2"]

----

define `z2`
define `z3`
define `y2`
use `z3`
-- post defs: ["y2", "z3", "z2"]

----

define `z2`
define `z3`
define `y2`
use `z2`
-- post defs: ["y2", "z3", "z2"]

----

define `z2`
define `z3`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "z3", "z2"]

----

define `z2`
define `z3`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "z3", "z2"]

----

define `z2`
define `z3`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "z3", "z2"]

----

define `z2`
define `z3`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "z3", "z2"]

----

define `z2`
define `z3`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "z3", "z2"]

----

define `z2`
define `z3`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "z3", "z2"]

----

define `z2`
define `z3`
define `y3`
use `y3`
-- post defs: ["y3", "z3", "z2"]

----

define `z2`
define `z3`
define `y3`
use `z3`
-- post defs: ["y3", "z3", "z2"]

----

define `z2`
define `z3`
define `y3`
use `z2`
-- post defs: ["y3", "z3", "z2"]

----

define `z2`
define `z3`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "z3", "z2"]

----

define `z2`
define `z3`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "z3", "z2"]

----

define `z2`
define `z3`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "z3", "z2"]

----

define `z2`
define `z3`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "z3", "z2"]

----

define `z2`
define `z3`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "z3", "z2"]

----

define `z2`
define `z3`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "z3", "z2"]

----

define `z2`
define `z3`
define `z1`
use `z1`
-- post defs: ["z1", "z3", "z2"]

----

define `z2`
define `z3`
define `z1`
use `z3`
-- post defs: ["z1", "z3", "z2"]

----

define `z2`
define `z3`
define `z1`
use `z2`
-- post defs: ["z1", "z3", "z2"]

----

define `z2`
define `z3`
use `z3`
define `x1`
-- post defs: ["x1", "z3", "z2"]

----

define `z2`
define `z3`
use `z3`
define `x2`
-- post defs: ["x2", "z3", "z2"]

----

define `z2`
define `z3`
use `z3`
define `x3`
-- post defs: ["x3", "z3", "z2"]

----

define `z2`
define `z3`
use `z3`
define `y1`
-- post defs: ["y1", "z3", "z2"]

----

define `z2`
define `z3`
use `z3`
define `y2`
-- post defs: ["y2", "z3", "z2"]

----

define `z2`
define `z3`
use `z3`
define `y3`
-- post defs: ["y3", "z3", "z2"]

----

define `z2`
define `z3`
use `z3`
define `z1`
-- post defs: ["z1", "z3", "z2"]

----

define `z2`
define `z3`
use `z3`
use `z3`
-- post defs: ["z3", "z2"]

----

define `z2`
define `z3`
use `z3`
use `z2`
-- post defs: ["z3", "z2"]

----

define `z2`
define `z3`
use `z2`
define `x1`
-- post defs: ["x1", "z3", "z2"]

----

define `z2`
define `z3`
use `z2`
define `x2`
-- post defs: ["x2", "z3", "z2"]

----

define `z2`
define `z3`
use `z2`
define `x3`
-- post defs: ["x3", "z3", "z2"]

----

define `z2`
define `z3`
use `z2`
define `y1`
-- post defs: ["y1", "z3", "z2"]

----

define `z2`
define `z3`
use `z2`
define `y2`
-- post defs: ["y2", "z3", "z2"]

----

define `z2`
define `z3`
use `z2`
define `y3`
-- post defs: ["y3", "z3", "z2"]

----

define `z2`
define `z3`
use `z2`
define `z1`
-- post defs: ["z1", "z3", "z2"]

----

define `z2`
define `z3`
use `z2`
use `z3`
-- post defs: ["z3", "z2"]

----

define `z2`
define `z3`
use `z2`
use `z2`
-- post defs: ["z3", "z2"]

----

define `z2`
use `z2`
define `x1`
-- post defs: ["x1", "z2"]

----

define `z2`
use `z2`
define `x2`
-- post defs: ["x2", "z2"]

----

define `z2`
use `z2`
define `x3`
-- post defs: ["x3", "z2"]

----

define `z2`
use `z2`
define `y1`
-- post defs: ["y1", "z2"]

----

define `z2`
use `z2`
define `y2`
-- post defs: ["y2", "z2"]

----

define `z2`
use `z2`
define `y3`
-- post defs: ["y3", "z2"]

----

define `z2`
use `z2`
define `z1`
-- post defs: ["z1", "z2"]

----

define `z2`
use `z2`
define `z3`
-- post defs: ["z3", "z2"]

----

define `z2`
use `z2`
use `z2`
-- post defs: ["z2"]

----

define `z2`
use `z2`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "z2"]

----

define `z2`
use `z2`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "z2"]

----

define `z2`
use `z2`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "z2"]

----

define `z2`
use `z2`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "z2"]

----

define `z2`
use `z2`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "z2"]

----

define `z2`
use `z2`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "z2"]

----

define `z2`
use `z2`
define `x1`
define `z3`
-- post defs: ["z3", "x1", "z2"]

----

define `z2`
use `z2`
define `x1`
use `x1`
-- post defs: ["x1", "z2"]

----

define `z2`
use `z2`
define `x1`
use `z2`
-- post defs: ["x1", "z2"]

----

define `z2`
use `z2`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "z2"]

----

define `z2`
use `z2`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "z2"]

----

define `z2`
use `z2`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "z2"]

----

define `z2`
use `z2`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "z2"]

----

define `z2`
use `z2`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "z2"]

----

define `z2`
use `z2`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "z2"]

----

define `z2`
use `z2`
define `x2`
define `z3`
-- post defs: ["z3", "x2", "z2"]

----

define `z2`
use `z2`
define `x2`
use `x2`
-- post defs: ["x2", "z2"]

----

define `z2`
use `z2`
define `x2`
use `z2`
-- post defs: ["x2", "z2"]

----

define `z2`
use `z2`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "z2"]

----

define `z2`
use `z2`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "z2"]

----

define `z2`
use `z2`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "z2"]

----

define `z2`
use `z2`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "z2"]

----

define `z2`
use `z2`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "z2"]

----

define `z2`
use `z2`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "z2"]

----

define `z2`
use `z2`
define `x3`
define `z3`
-- post defs: ["z3", "x3", "z2"]

----

define `z2`
use `z2`
define `x3`
use `x3`
-- post defs: ["x3", "z2"]

----

define `z2`
use `z2`
define `x3`
use `z2`
-- post defs: ["x3", "z2"]

----

define `z2`
use `z2`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "z2"]

----

define `z2`
use `z2`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "z2"]

----

define `z2`
use `z2`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "z2"]

----

define `z2`
use `z2`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "z2"]

----

define `z2`
use `z2`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "z2"]

----

define `z2`
use `z2`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "z2"]

----

define `z2`
use `z2`
define `y1`
define `z3`
-- post defs: ["z3", "y1", "z2"]

----

define `z2`
use `z2`
define `y1`
use `y1`
-- post defs: ["y1", "z2"]

----

define `z2`
use `z2`
define `y1`
use `z2`
-- post defs: ["y1", "z2"]

----

define `z2`
use `z2`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "z2"]

----

define `z2`
use `z2`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "z2"]

----

define `z2`
use `z2`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "z2"]

----

define `z2`
use `z2`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "z2"]

----

define `z2`
use `z2`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "z2"]

----

define `z2`
use `z2`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "z2"]

----

define `z2`
use `z2`
define `y2`
define `z3`
-- post defs: ["z3", "y2", "z2"]

----

define `z2`
use `z2`
define `y2`
use `y2`
-- post defs: ["y2", "z2"]

----

define `z2`
use `z2`
define `y2`
use `z2`
-- post defs: ["y2", "z2"]

----

define `z2`
use `z2`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "z2"]

----

define `z2`
use `z2`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "z2"]

----

define `z2`
use `z2`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "z2"]

----

define `z2`
use `z2`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "z2"]

----

define `z2`
use `z2`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "z2"]

----

define `z2`
use `z2`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "z2"]

----

define `z2`
use `z2`
define `y3`
define `z3`
-- post defs: ["z3", "y3", "z2"]

----

define `z2`
use `z2`
define `y3`
use `y3`
-- post defs: ["y3", "z2"]

----

define `z2`
use `z2`
define `y3`
use `z2`
-- post defs: ["y3", "z2"]

----

define `z2`
use `z2`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "z2"]

----

define `z2`
use `z2`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "z2"]

----

define `z2`
use `z2`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "z2"]

----

define `z2`
use `z2`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "z2"]

----

define `z2`
use `z2`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "z2"]

----

define `z2`
use `z2`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "z2"]

----

define `z2`
use `z2`
define `z1`
define `z3`
-- post defs: ["z3", "z1", "z2"]

----

define `z2`
use `z2`
define `z1`
use `z1`
-- post defs: ["z1", "z2"]

----

define `z2`
use `z2`
define `z1`
use `z2`
-- post defs: ["z1", "z2"]

----

define `z2`
use `z2`
define `z3`
define `x1`
-- post defs: ["x1", "z3", "z2"]

----

define `z2`
use `z2`
define `z3`
define `x2`
-- post defs: ["x2", "z3", "z2"]

----

define `z2`
use `z2`
define `z3`
define `x3`
-- post defs: ["x3", "z3", "z2"]

----

define `z2`
use `z2`
define `z3`
define `y1`
-- post defs: ["y1", "z3", "z2"]

----

define `z2`
use `z2`
define `z3`
define `y2`
-- post defs: ["y2", "z3", "z2"]

----

define `z2`
use `z2`
define `z3`
define `y3`
-- post defs: ["y3", "z3", "z2"]

----

define `z2`
use `z2`
define `z3`
define `z1`
-- post defs: ["z1", "z3", "z2"]

----

define `z2`
use `z2`
define `z3`
use `z3`
-- post defs: ["z3", "z2"]

----

define `z2`
use `z2`
define `z3`
use `z2`
-- post defs: ["z3", "z2"]

----

define `z2`
use `z2`
use `z2`
define `x1`
-- post defs: ["x1", "z2"]

----

define `z2`
use `z2`
use `z2`
define `x2`
-- post defs: ["x2", "z2"]

----

define `z2`
use `z2`
use `z2`
define `x3`
-- post defs: ["x3", "z2"]

----

define `z2`
use `z2`
use `z2`
define `y1`
-- post defs: ["y1", "z2"]

----

define `z2`
use `z2`
use `z2`
define `y2`
-- post defs: ["y2", "z2"]

----

define `z2`
use `z2`
use `z2`
define `y3`
-- post defs: ["y3", "z2"]

----

define `z2`
use `z2`
use `z2`
define `z1`
-- post defs: ["z1", "z2"]

----

define `z2`
use `z2`
use `z2`
define `z3`
-- post defs: ["z3", "z2"]

----

define `z2`
use `z2`
use `z2`
use `z2`
-- post defs: ["z2"]

----

define `z3`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "z3"]

----

define `z3`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "z3"]

----

define `z3`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "z3"]

----

define `z3`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "z3"]

----

define `z3`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "z3"]

----

define `z3`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "z3"]

----

define `z3`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "z3"]

----

define `z3`
define `x1`
use `x1`
-- post defs: ["x1", "z3"]

----

define `z3`
define `x1`
use `z3`
-- post defs: ["x1", "z3"]

----

define `z3`
define `x1`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "x1", "z3"]

----

define `z3`
define `x1`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "x1", "z3"]

----

define `z3`
define `x1`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "x1", "z3"]

----

define `z3`
define `x1`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "x1", "z3"]

----

define `z3`
define `x1`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "x1", "z3"]

----

define `z3`
define `x1`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "x1", "z3"]

----

define `z3`
define `x1`
define `x2`
use `x2`
-- post defs: ["x2", "x1", "z3"]

----

define `z3`
define `x1`
define `x2`
use `x1`
-- post defs: ["x2", "x1", "z3"]

----

define `z3`
define `x1`
define `x2`
use `z3`
-- post defs: ["x2", "x1", "z3"]

----

define `z3`
define `x1`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "x1", "z3"]

----

define `z3`
define `x1`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "x1", "z3"]

----

define `z3`
define `x1`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "x1", "z3"]

----

define `z3`
define `x1`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "x1", "z3"]

----

define `z3`
define `x1`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "x1", "z3"]

----

define `z3`
define `x1`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "x1", "z3"]

----

define `z3`
define `x1`
define `x3`
use `x3`
-- post defs: ["x3", "x1", "z3"]

----

define `z3`
define `x1`
define `x3`
use `x1`
-- post defs: ["x3", "x1", "z3"]

----

define `z3`
define `x1`
define `x3`
use `z3`
-- post defs: ["x3", "x1", "z3"]

----

define `z3`
define `x1`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "x1", "z3"]

----

define `z3`
define `x1`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "x1", "z3"]

----

define `z3`
define `x1`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "x1", "z3"]

----

define `z3`
define `x1`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "x1", "z3"]

----

define `z3`
define `x1`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "x1", "z3"]

----

define `z3`
define `x1`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "x1", "z3"]

----

define `z3`
define `x1`
define `y1`
use `y1`
-- post defs: ["y1", "x1", "z3"]

----

define `z3`
define `x1`
define `y1`
use `x1`
-- post defs: ["y1", "x1", "z3"]

----

define `z3`
define `x1`
define `y1`
use `z3`
-- post defs: ["y1", "x1", "z3"]

----

define `z3`
define `x1`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "x1", "z3"]

----

define `z3`
define `x1`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "x1", "z3"]

----

define `z3`
define `x1`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "x1", "z3"]

----

define `z3`
define `x1`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "x1", "z3"]

----

define `z3`
define `x1`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "x1", "z3"]

----

define `z3`
define `x1`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "x1", "z3"]

----

define `z3`
define `x1`
define `y2`
use `y2`
-- post defs: ["y2", "x1", "z3"]

----

define `z3`
define `x1`
define `y2`
use `x1`
-- post defs: ["y2", "x1", "z3"]

----

define `z3`
define `x1`
define `y2`
use `z3`
-- post defs: ["y2", "x1", "z3"]

----

define `z3`
define `x1`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "x1", "z3"]

----

define `z3`
define `x1`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "x1", "z3"]

----

define `z3`
define `x1`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "x1", "z3"]

----

define `z3`
define `x1`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "x1", "z3"]

----

define `z3`
define `x1`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "x1", "z3"]

----

define `z3`
define `x1`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "x1", "z3"]

----

define `z3`
define `x1`
define `y3`
use `y3`
-- post defs: ["y3", "x1", "z3"]

----

define `z3`
define `x1`
define `y3`
use `x1`
-- post defs: ["y3", "x1", "z3"]

----

define `z3`
define `x1`
define `y3`
use `z3`
-- post defs: ["y3", "x1", "z3"]

----

define `z3`
define `x1`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "x1", "z3"]

----

define `z3`
define `x1`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "x1", "z3"]

----

define `z3`
define `x1`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "x1", "z3"]

----

define `z3`
define `x1`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "x1", "z3"]

----

define `z3`
define `x1`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "x1", "z3"]

----

define `z3`
define `x1`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "x1", "z3"]

----

define `z3`
define `x1`
define `z1`
use `z1`
-- post defs: ["z1", "x1", "z3"]

----

define `z3`
define `x1`
define `z1`
use `x1`
-- post defs: ["z1", "x1", "z3"]

----

define `z3`
define `x1`
define `z1`
use `z3`
-- post defs: ["z1", "x1", "z3"]

----

define `z3`
define `x1`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "x1", "z3"]

----

define `z3`
define `x1`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "x1", "z3"]

----

define `z3`
define `x1`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "x1", "z3"]

----

define `z3`
define `x1`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "x1", "z3"]

----

define `z3`
define `x1`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "x1", "z3"]

----

define `z3`
define `x1`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "x1", "z3"]

----

define `z3`
define `x1`
define `z2`
use `z2`
-- post defs: ["z2", "x1", "z3"]

----

define `z3`
define `x1`
define `z2`
use `x1`
-- post defs: ["z2", "x1", "z3"]

----

define `z3`
define `x1`
define `z2`
use `z3`
-- post defs: ["z2", "x1", "z3"]

----

define `z3`
define `x1`
use `x1`
define `x2`
-- post defs: ["x2", "x1", "z3"]

----

define `z3`
define `x1`
use `x1`
define `x3`
-- post defs: ["x3", "x1", "z3"]

----

define `z3`
define `x1`
use `x1`
define `y1`
-- post defs: ["y1", "x1", "z3"]

----

define `z3`
define `x1`
use `x1`
define `y2`
-- post defs: ["y2", "x1", "z3"]

----

define `z3`
define `x1`
use `x1`
define `y3`
-- post defs: ["y3", "x1", "z3"]

----

define `z3`
define `x1`
use `x1`
define `z1`
-- post defs: ["z1", "x1", "z3"]

----

define `z3`
define `x1`
use `x1`
define `z2`
-- post defs: ["z2", "x1", "z3"]

----

define `z3`
define `x1`
use `x1`
use `x1`
-- post defs: ["x1", "z3"]

----

define `z3`
define `x1`
use `x1`
use `z3`
-- post defs: ["x1", "z3"]

----

define `z3`
define `x1`
use `z3`
define `x2`
-- post defs: ["x2", "x1", "z3"]

----

define `z3`
define `x1`
use `z3`
define `x3`
-- post defs: ["x3", "x1", "z3"]

----

define `z3`
define `x1`
use `z3`
define `y1`
-- post defs: ["y1", "x1", "z3"]

----

define `z3`
define `x1`
use `z3`
define `y2`
-- post defs: ["y2", "x1", "z3"]

----

define `z3`
define `x1`
use `z3`
define `y3`
-- post defs: ["y3", "x1", "z3"]

----

define `z3`
define `x1`
use `z3`
define `z1`
-- post defs: ["z1", "x1", "z3"]

----

define `z3`
define `x1`
use `z3`
define `z2`
-- post defs: ["z2", "x1", "z3"]

----

define `z3`
define `x1`
use `z3`
use `x1`
-- post defs: ["x1", "z3"]

----

define `z3`
define `x1`
use `z3`
use `z3`
-- post defs: ["x1", "z3"]

----

define `z3`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "z3"]

----

define `z3`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "z3"]

----

define `z3`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "z3"]

----

define `z3`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "z3"]

----

define `z3`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "z3"]

----

define `z3`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "z3"]

----

define `z3`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "z3"]

----

define `z3`
define `x2`
use `x2`
-- post defs: ["x2", "z3"]

----

define `z3`
define `x2`
use `z3`
-- post defs: ["x2", "z3"]

----

define `z3`
define `x2`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "x2", "z3"]

----

define `z3`
define `x2`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "x2", "z3"]

----

define `z3`
define `x2`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "x2", "z3"]

----

define `z3`
define `x2`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "x2", "z3"]

----

define `z3`
define `x2`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "x2", "z3"]

----

define `z3`
define `x2`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "x2", "z3"]

----

define `z3`
define `x2`
define `x1`
use `x1`
-- post defs: ["x1", "x2", "z3"]

----

define `z3`
define `x2`
define `x1`
use `x2`
-- post defs: ["x1", "x2", "z3"]

----

define `z3`
define `x2`
define `x1`
use `z3`
-- post defs: ["x1", "x2", "z3"]

----

define `z3`
define `x2`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "x2", "z3"]

----

define `z3`
define `x2`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "x2", "z3"]

----

define `z3`
define `x2`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "x2", "z3"]

----

define `z3`
define `x2`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "x2", "z3"]

----

define `z3`
define `x2`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "x2", "z3"]

----

define `z3`
define `x2`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "x2", "z3"]

----

define `z3`
define `x2`
define `x3`
use `x3`
-- post defs: ["x3", "x2", "z3"]

----

define `z3`
define `x2`
define `x3`
use `x2`
-- post defs: ["x3", "x2", "z3"]

----

define `z3`
define `x2`
define `x3`
use `z3`
-- post defs: ["x3", "x2", "z3"]

----

define `z3`
define `x2`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "x2", "z3"]

----

define `z3`
define `x2`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "x2", "z3"]

----

define `z3`
define `x2`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "x2", "z3"]

----

define `z3`
define `x2`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "x2", "z3"]

----

define `z3`
define `x2`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "x2", "z3"]

----

define `z3`
define `x2`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "x2", "z3"]

----

define `z3`
define `x2`
define `y1`
use `y1`
-- post defs: ["y1", "x2", "z3"]

----

define `z3`
define `x2`
define `y1`
use `x2`
-- post defs: ["y1", "x2", "z3"]

----

define `z3`
define `x2`
define `y1`
use `z3`
-- post defs: ["y1", "x2", "z3"]

----

define `z3`
define `x2`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "x2", "z3"]

----

define `z3`
define `x2`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "x2", "z3"]

----

define `z3`
define `x2`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "x2", "z3"]

----

define `z3`
define `x2`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "x2", "z3"]

----

define `z3`
define `x2`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "x2", "z3"]

----

define `z3`
define `x2`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "x2", "z3"]

----

define `z3`
define `x2`
define `y2`
use `y2`
-- post defs: ["y2", "x2", "z3"]

----

define `z3`
define `x2`
define `y2`
use `x2`
-- post defs: ["y2", "x2", "z3"]

----

define `z3`
define `x2`
define `y2`
use `z3`
-- post defs: ["y2", "x2", "z3"]

----

define `z3`
define `x2`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "x2", "z3"]

----

define `z3`
define `x2`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "x2", "z3"]

----

define `z3`
define `x2`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "x2", "z3"]

----

define `z3`
define `x2`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "x2", "z3"]

----

define `z3`
define `x2`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "x2", "z3"]

----

define `z3`
define `x2`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "x2", "z3"]

----

define `z3`
define `x2`
define `y3`
use `y3`
-- post defs: ["y3", "x2", "z3"]

----

define `z3`
define `x2`
define `y3`
use `x2`
-- post defs: ["y3", "x2", "z3"]

----

define `z3`
define `x2`
define `y3`
use `z3`
-- post defs: ["y3", "x2", "z3"]

----

define `z3`
define `x2`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "x2", "z3"]

----

define `z3`
define `x2`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "x2", "z3"]

----

define `z3`
define `x2`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "x2", "z3"]

----

define `z3`
define `x2`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "x2", "z3"]

----

define `z3`
define `x2`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "x2", "z3"]

----

define `z3`
define `x2`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "x2", "z3"]

----

define `z3`
define `x2`
define `z1`
use `z1`
-- post defs: ["z1", "x2", "z3"]

----

define `z3`
define `x2`
define `z1`
use `x2`
-- post defs: ["z1", "x2", "z3"]

----

define `z3`
define `x2`
define `z1`
use `z3`
-- post defs: ["z1", "x2", "z3"]

----

define `z3`
define `x2`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "x2", "z3"]

----

define `z3`
define `x2`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "x2", "z3"]

----

define `z3`
define `x2`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "x2", "z3"]

----

define `z3`
define `x2`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "x2", "z3"]

----

define `z3`
define `x2`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "x2", "z3"]

----

define `z3`
define `x2`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "x2", "z3"]

----

define `z3`
define `x2`
define `z2`
use `z2`
-- post defs: ["z2", "x2", "z3"]

----

define `z3`
define `x2`
define `z2`
use `x2`
-- post defs: ["z2", "x2", "z3"]

----

define `z3`
define `x2`
define `z2`
use `z3`
-- post defs: ["z2", "x2", "z3"]

----

define `z3`
define `x2`
use `x2`
define `x1`
-- post defs: ["x1", "x2", "z3"]

----

define `z3`
define `x2`
use `x2`
define `x3`
-- post defs: ["x3", "x2", "z3"]

----

define `z3`
define `x2`
use `x2`
define `y1`
-- post defs: ["y1", "x2", "z3"]

----

define `z3`
define `x2`
use `x2`
define `y2`
-- post defs: ["y2", "x2", "z3"]

----

define `z3`
define `x2`
use `x2`
define `y3`
-- post defs: ["y3", "x2", "z3"]

----

define `z3`
define `x2`
use `x2`
define `z1`
-- post defs: ["z1", "x2", "z3"]

----

define `z3`
define `x2`
use `x2`
define `z2`
-- post defs: ["z2", "x2", "z3"]

----

define `z3`
define `x2`
use `x2`
use `x2`
-- post defs: ["x2", "z3"]

----

define `z3`
define `x2`
use `x2`
use `z3`
-- post defs: ["x2", "z3"]

----

define `z3`
define `x2`
use `z3`
define `x1`
-- post defs: ["x1", "x2", "z3"]

----

define `z3`
define `x2`
use `z3`
define `x3`
-- post defs: ["x3", "x2", "z3"]

----

define `z3`
define `x2`
use `z3`
define `y1`
-- post defs: ["y1", "x2", "z3"]

----

define `z3`
define `x2`
use `z3`
define `y2`
-- post defs: ["y2", "x2", "z3"]

----

define `z3`
define `x2`
use `z3`
define `y3`
-- post defs: ["y3", "x2", "z3"]

----

define `z3`
define `x2`
use `z3`
define `z1`
-- post defs: ["z1", "x2", "z3"]

----

define `z3`
define `x2`
use `z3`
define `z2`
-- post defs: ["z2", "x2", "z3"]

----

define `z3`
define `x2`
use `z3`
use `x2`
-- post defs: ["x2", "z3"]

----

define `z3`
define `x2`
use `z3`
use `z3`
-- post defs: ["x2", "z3"]

----

define `z3`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "z3"]

----

define `z3`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "z3"]

----

define `z3`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "z3"]

----

define `z3`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "z3"]

----

define `z3`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "z3"]

----

define `z3`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "z3"]

----

define `z3`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "z3"]

----

define `z3`
define `x3`
use `x3`
-- post defs: ["x3", "z3"]

----

define `z3`
define `x3`
use `z3`
-- post defs: ["x3", "z3"]

----

define `z3`
define `x3`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "x3", "z3"]

----

define `z3`
define `x3`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "x3", "z3"]

----

define `z3`
define `x3`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "x3", "z3"]

----

define `z3`
define `x3`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "x3", "z3"]

----

define `z3`
define `x3`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "x3", "z3"]

----

define `z3`
define `x3`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "x3", "z3"]

----

define `z3`
define `x3`
define `x1`
use `x1`
-- post defs: ["x1", "x3", "z3"]

----

define `z3`
define `x3`
define `x1`
use `x3`
-- post defs: ["x1", "x3", "z3"]

----

define `z3`
define `x3`
define `x1`
use `z3`
-- post defs: ["x1", "x3", "z3"]

----

define `z3`
define `x3`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "x3", "z3"]

----

define `z3`
define `x3`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "x3", "z3"]

----

define `z3`
define `x3`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "x3", "z3"]

----

define `z3`
define `x3`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "x3", "z3"]

----

define `z3`
define `x3`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "x3", "z3"]

----

define `z3`
define `x3`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "x3", "z3"]

----

define `z3`
define `x3`
define `x2`
use `x2`
-- post defs: ["x2", "x3", "z3"]

----

define `z3`
define `x3`
define `x2`
use `x3`
-- post defs: ["x2", "x3", "z3"]

----

define `z3`
define `x3`
define `x2`
use `z3`
-- post defs: ["x2", "x3", "z3"]

----

define `z3`
define `x3`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "x3", "z3"]

----

define `z3`
define `x3`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "x3", "z3"]

----

define `z3`
define `x3`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "x3", "z3"]

----

define `z3`
define `x3`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "x3", "z3"]

----

define `z3`
define `x3`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "x3", "z3"]

----

define `z3`
define `x3`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "x3", "z3"]

----

define `z3`
define `x3`
define `y1`
use `y1`
-- post defs: ["y1", "x3", "z3"]

----

define `z3`
define `x3`
define `y1`
use `x3`
-- post defs: ["y1", "x3", "z3"]

----

define `z3`
define `x3`
define `y1`
use `z3`
-- post defs: ["y1", "x3", "z3"]

----

define `z3`
define `x3`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "x3", "z3"]

----

define `z3`
define `x3`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "x3", "z3"]

----

define `z3`
define `x3`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "x3", "z3"]

----

define `z3`
define `x3`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "x3", "z3"]

----

define `z3`
define `x3`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "x3", "z3"]

----

define `z3`
define `x3`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "x3", "z3"]

----

define `z3`
define `x3`
define `y2`
use `y2`
-- post defs: ["y2", "x3", "z3"]

----

define `z3`
define `x3`
define `y2`
use `x3`
-- post defs: ["y2", "x3", "z3"]

----

define `z3`
define `x3`
define `y2`
use `z3`
-- post defs: ["y2", "x3", "z3"]

----

define `z3`
define `x3`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "x3", "z3"]

----

define `z3`
define `x3`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "x3", "z3"]

----

define `z3`
define `x3`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "x3", "z3"]

----

define `z3`
define `x3`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "x3", "z3"]

----

define `z3`
define `x3`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "x3", "z3"]

----

define `z3`
define `x3`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "x3", "z3"]

----

define `z3`
define `x3`
define `y3`
use `y3`
-- post defs: ["y3", "x3", "z3"]

----

define `z3`
define `x3`
define `y3`
use `x3`
-- post defs: ["y3", "x3", "z3"]

----

define `z3`
define `x3`
define `y3`
use `z3`
-- post defs: ["y3", "x3", "z3"]

----

define `z3`
define `x3`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "x3", "z3"]

----

define `z3`
define `x3`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "x3", "z3"]

----

define `z3`
define `x3`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "x3", "z3"]

----

define `z3`
define `x3`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "x3", "z3"]

----

define `z3`
define `x3`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "x3", "z3"]

----

define `z3`
define `x3`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "x3", "z3"]

----

define `z3`
define `x3`
define `z1`
use `z1`
-- post defs: ["z1", "x3", "z3"]

----

define `z3`
define `x3`
define `z1`
use `x3`
-- post defs: ["z1", "x3", "z3"]

----

define `z3`
define `x3`
define `z1`
use `z3`
-- post defs: ["z1", "x3", "z3"]

----

define `z3`
define `x3`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "x3", "z3"]

----

define `z3`
define `x3`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "x3", "z3"]

----

define `z3`
define `x3`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "x3", "z3"]

----

define `z3`
define `x3`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "x3", "z3"]

----

define `z3`
define `x3`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "x3", "z3"]

----

define `z3`
define `x3`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "x3", "z3"]

----

define `z3`
define `x3`
define `z2`
use `z2`
-- post defs: ["z2", "x3", "z3"]

----

define `z3`
define `x3`
define `z2`
use `x3`
-- post defs: ["z2", "x3", "z3"]

----

define `z3`
define `x3`
define `z2`
use `z3`
-- post defs: ["z2", "x3", "z3"]

----

define `z3`
define `x3`
use `x3`
define `x1`
-- post defs: ["x1", "x3", "z3"]

----

define `z3`
define `x3`
use `x3`
define `x2`
-- post defs: ["x2", "x3", "z3"]

----

define `z3`
define `x3`
use `x3`
define `y1`
-- post defs: ["y1", "x3", "z3"]

----

define `z3`
define `x3`
use `x3`
define `y2`
-- post defs: ["y2", "x3", "z3"]

----

define `z3`
define `x3`
use `x3`
define `y3`
-- post defs: ["y3", "x3", "z3"]

----

define `z3`
define `x3`
use `x3`
define `z1`
-- post defs: ["z1", "x3", "z3"]

----

define `z3`
define `x3`
use `x3`
define `z2`
-- post defs: ["z2", "x3", "z3"]

----

define `z3`
define `x3`
use `x3`
use `x3`
-- post defs: ["x3", "z3"]

----

define `z3`
define `x3`
use `x3`
use `z3`
-- post defs: ["x3", "z3"]

----

define `z3`
define `x3`
use `z3`
define `x1`
-- post defs: ["x1", "x3", "z3"]

----

define `z3`
define `x3`
use `z3`
define `x2`
-- post defs: ["x2", "x3", "z3"]

----

define `z3`
define `x3`
use `z3`
define `y1`
-- post defs: ["y1", "x3", "z3"]

----

define `z3`
define `x3`
use `z3`
define `y2`
-- post defs: ["y2", "x3", "z3"]

----

define `z3`
define `x3`
use `z3`
define `y3`
-- post defs: ["y3", "x3", "z3"]

----

define `z3`
define `x3`
use `z3`
define `z1`
-- post defs: ["z1", "x3", "z3"]

----

define `z3`
define `x3`
use `z3`
define `z2`
-- post defs: ["z2", "x3", "z3"]

----

define `z3`
define `x3`
use `z3`
use `x3`
-- post defs: ["x3", "z3"]

----

define `z3`
define `x3`
use `z3`
use `z3`
-- post defs: ["x3", "z3"]

----

define `z3`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "z3"]

----

define `z3`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "z3"]

----

define `z3`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "z3"]

----

define `z3`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "z3"]

----

define `z3`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "z3"]

----

define `z3`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "z3"]

----

define `z3`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "z3"]

----

define `z3`
define `y1`
use `y1`
-- post defs: ["y1", "z3"]

----

define `z3`
define `y1`
use `z3`
-- post defs: ["y1", "z3"]

----

define `z3`
define `y1`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "y1", "z3"]

----

define `z3`
define `y1`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "y1", "z3"]

----

define `z3`
define `y1`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "y1", "z3"]

----

define `z3`
define `y1`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "y1", "z3"]

----

define `z3`
define `y1`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "y1", "z3"]

----

define `z3`
define `y1`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "y1", "z3"]

----

define `z3`
define `y1`
define `x1`
use `x1`
-- post defs: ["x1", "y1", "z3"]

----

define `z3`
define `y1`
define `x1`
use `y1`
-- post defs: ["x1", "y1", "z3"]

----

define `z3`
define `y1`
define `x1`
use `z3`
-- post defs: ["x1", "y1", "z3"]

----

define `z3`
define `y1`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "y1", "z3"]

----

define `z3`
define `y1`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "y1", "z3"]

----

define `z3`
define `y1`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "y1", "z3"]

----

define `z3`
define `y1`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "y1", "z3"]

----

define `z3`
define `y1`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "y1", "z3"]

----

define `z3`
define `y1`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "y1", "z3"]

----

define `z3`
define `y1`
define `x2`
use `x2`
-- post defs: ["x2", "y1", "z3"]

----

define `z3`
define `y1`
define `x2`
use `y1`
-- post defs: ["x2", "y1", "z3"]

----

define `z3`
define `y1`
define `x2`
use `z3`
-- post defs: ["x2", "y1", "z3"]

----

define `z3`
define `y1`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "y1", "z3"]

----

define `z3`
define `y1`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "y1", "z3"]

----

define `z3`
define `y1`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "y1", "z3"]

----

define `z3`
define `y1`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "y1", "z3"]

----

define `z3`
define `y1`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "y1", "z3"]

----

define `z3`
define `y1`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "y1", "z3"]

----

define `z3`
define `y1`
define `x3`
use `x3`
-- post defs: ["x3", "y1", "z3"]

----

define `z3`
define `y1`
define `x3`
use `y1`
-- post defs: ["x3", "y1", "z3"]

----

define `z3`
define `y1`
define `x3`
use `z3`
-- post defs: ["x3", "y1", "z3"]

----

define `z3`
define `y1`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "y1", "z3"]

----

define `z3`
define `y1`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "y1", "z3"]

----

define `z3`
define `y1`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "y1", "z3"]

----

define `z3`
define `y1`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "y1", "z3"]

----

define `z3`
define `y1`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "y1", "z3"]

----

define `z3`
define `y1`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "y1", "z3"]

----

define `z3`
define `y1`
define `y2`
use `y2`
-- post defs: ["y2", "y1", "z3"]

----

define `z3`
define `y1`
define `y2`
use `y1`
-- post defs: ["y2", "y1", "z3"]

----

define `z3`
define `y1`
define `y2`
use `z3`
-- post defs: ["y2", "y1", "z3"]

----

define `z3`
define `y1`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "y1", "z3"]

----

define `z3`
define `y1`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "y1", "z3"]

----

define `z3`
define `y1`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "y1", "z3"]

----

define `z3`
define `y1`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "y1", "z3"]

----

define `z3`
define `y1`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "y1", "z3"]

----

define `z3`
define `y1`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "y1", "z3"]

----

define `z3`
define `y1`
define `y3`
use `y3`
-- post defs: ["y3", "y1", "z3"]

----

define `z3`
define `y1`
define `y3`
use `y1`
-- post defs: ["y3", "y1", "z3"]

----

define `z3`
define `y1`
define `y3`
use `z3`
-- post defs: ["y3", "y1", "z3"]

----

define `z3`
define `y1`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "y1", "z3"]

----

define `z3`
define `y1`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "y1", "z3"]

----

define `z3`
define `y1`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "y1", "z3"]

----

define `z3`
define `y1`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "y1", "z3"]

----

define `z3`
define `y1`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "y1", "z3"]

----

define `z3`
define `y1`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "y1", "z3"]

----

define `z3`
define `y1`
define `z1`
use `z1`
-- post defs: ["z1", "y1", "z3"]

----

define `z3`
define `y1`
define `z1`
use `y1`
-- post defs: ["z1", "y1", "z3"]

----

define `z3`
define `y1`
define `z1`
use `z3`
-- post defs: ["z1", "y1", "z3"]

----

define `z3`
define `y1`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "y1", "z3"]

----

define `z3`
define `y1`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "y1", "z3"]

----

define `z3`
define `y1`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "y1", "z3"]

----

define `z3`
define `y1`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "y1", "z3"]

----

define `z3`
define `y1`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "y1", "z3"]

----

define `z3`
define `y1`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "y1", "z3"]

----

define `z3`
define `y1`
define `z2`
use `z2`
-- post defs: ["z2", "y1", "z3"]

----

define `z3`
define `y1`
define `z2`
use `y1`
-- post defs: ["z2", "y1", "z3"]

----

define `z3`
define `y1`
define `z2`
use `z3`
-- post defs: ["z2", "y1", "z3"]

----

define `z3`
define `y1`
use `y1`
define `x1`
-- post defs: ["x1", "y1", "z3"]

----

define `z3`
define `y1`
use `y1`
define `x2`
-- post defs: ["x2", "y1", "z3"]

----

define `z3`
define `y1`
use `y1`
define `x3`
-- post defs: ["x3", "y1", "z3"]

----

define `z3`
define `y1`
use `y1`
define `y2`
-- post defs: ["y2", "y1", "z3"]

----

define `z3`
define `y1`
use `y1`
define `y3`
-- post defs: ["y3", "y1", "z3"]

----

define `z3`
define `y1`
use `y1`
define `z1`
-- post defs: ["z1", "y1", "z3"]

----

define `z3`
define `y1`
use `y1`
define `z2`
-- post defs: ["z2", "y1", "z3"]

----

define `z3`
define `y1`
use `y1`
use `y1`
-- post defs: ["y1", "z3"]

----

define `z3`
define `y1`
use `y1`
use `z3`
-- post defs: ["y1", "z3"]

----

define `z3`
define `y1`
use `z3`
define `x1`
-- post defs: ["x1", "y1", "z3"]

----

define `z3`
define `y1`
use `z3`
define `x2`
-- post defs: ["x2", "y1", "z3"]

----

define `z3`
define `y1`
use `z3`
define `x3`
-- post defs: ["x3", "y1", "z3"]

----

define `z3`
define `y1`
use `z3`
define `y2`
-- post defs: ["y2", "y1", "z3"]

----

define `z3`
define `y1`
use `z3`
define `y3`
-- post defs: ["y3", "y1", "z3"]

----

define `z3`
define `y1`
use `z3`
define `z1`
-- post defs: ["z1", "y1", "z3"]

----

define `z3`
define `y1`
use `z3`
define `z2`
-- post defs: ["z2", "y1", "z3"]

----

define `z3`
define `y1`
use `z3`
use `y1`
-- post defs: ["y1", "z3"]

----

define `z3`
define `y1`
use `z3`
use `z3`
-- post defs: ["y1", "z3"]

----

define `z3`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "z3"]

----

define `z3`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "z3"]

----

define `z3`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "z3"]

----

define `z3`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "z3"]

----

define `z3`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "z3"]

----

define `z3`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "z3"]

----

define `z3`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "z3"]

----

define `z3`
define `y2`
use `y2`
-- post defs: ["y2", "z3"]

----

define `z3`
define `y2`
use `z3`
-- post defs: ["y2", "z3"]

----

define `z3`
define `y2`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "y2", "z3"]

----

define `z3`
define `y2`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "y2", "z3"]

----

define `z3`
define `y2`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "y2", "z3"]

----

define `z3`
define `y2`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "y2", "z3"]

----

define `z3`
define `y2`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "y2", "z3"]

----

define `z3`
define `y2`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "y2", "z3"]

----

define `z3`
define `y2`
define `x1`
use `x1`
-- post defs: ["x1", "y2", "z3"]

----

define `z3`
define `y2`
define `x1`
use `y2`
-- post defs: ["x1", "y2", "z3"]

----

define `z3`
define `y2`
define `x1`
use `z3`
-- post defs: ["x1", "y2", "z3"]

----

define `z3`
define `y2`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "y2", "z3"]

----

define `z3`
define `y2`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "y2", "z3"]

----

define `z3`
define `y2`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "y2", "z3"]

----

define `z3`
define `y2`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "y2", "z3"]

----

define `z3`
define `y2`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "y2", "z3"]

----

define `z3`
define `y2`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "y2", "z3"]

----

define `z3`
define `y2`
define `x2`
use `x2`
-- post defs: ["x2", "y2", "z3"]

----

define `z3`
define `y2`
define `x2`
use `y2`
-- post defs: ["x2", "y2", "z3"]

----

define `z3`
define `y2`
define `x2`
use `z3`
-- post defs: ["x2", "y2", "z3"]

----

define `z3`
define `y2`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "y2", "z3"]

----

define `z3`
define `y2`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "y2", "z3"]

----

define `z3`
define `y2`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "y2", "z3"]

----

define `z3`
define `y2`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "y2", "z3"]

----

define `z3`
define `y2`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "y2", "z3"]

----

define `z3`
define `y2`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "y2", "z3"]

----

define `z3`
define `y2`
define `x3`
use `x3`
-- post defs: ["x3", "y2", "z3"]

----

define `z3`
define `y2`
define `x3`
use `y2`
-- post defs: ["x3", "y2", "z3"]

----

define `z3`
define `y2`
define `x3`
use `z3`
-- post defs: ["x3", "y2", "z3"]

----

define `z3`
define `y2`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "y2", "z3"]

----

define `z3`
define `y2`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "y2", "z3"]

----

define `z3`
define `y2`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "y2", "z3"]

----

define `z3`
define `y2`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "y2", "z3"]

----

define `z3`
define `y2`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "y2", "z3"]

----

define `z3`
define `y2`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "y2", "z3"]

----

define `z3`
define `y2`
define `y1`
use `y1`
-- post defs: ["y1", "y2", "z3"]

----

define `z3`
define `y2`
define `y1`
use `y2`
-- post defs: ["y1", "y2", "z3"]

----

define `z3`
define `y2`
define `y1`
use `z3`
-- post defs: ["y1", "y2", "z3"]

----

define `z3`
define `y2`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "y2", "z3"]

----

define `z3`
define `y2`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "y2", "z3"]

----

define `z3`
define `y2`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "y2", "z3"]

----

define `z3`
define `y2`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "y2", "z3"]

----

define `z3`
define `y2`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "y2", "z3"]

----

define `z3`
define `y2`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "y2", "z3"]

----

define `z3`
define `y2`
define `y3`
use `y3`
-- post defs: ["y3", "y2", "z3"]

----

define `z3`
define `y2`
define `y3`
use `y2`
-- post defs: ["y3", "y2", "z3"]

----

define `z3`
define `y2`
define `y3`
use `z3`
-- post defs: ["y3", "y2", "z3"]

----

define `z3`
define `y2`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "y2", "z3"]

----

define `z3`
define `y2`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "y2", "z3"]

----

define `z3`
define `y2`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "y2", "z3"]

----

define `z3`
define `y2`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "y2", "z3"]

----

define `z3`
define `y2`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "y2", "z3"]

----

define `z3`
define `y2`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "y2", "z3"]

----

define `z3`
define `y2`
define `z1`
use `z1`
-- post defs: ["z1", "y2", "z3"]

----

define `z3`
define `y2`
define `z1`
use `y2`
-- post defs: ["z1", "y2", "z3"]

----

define `z3`
define `y2`
define `z1`
use `z3`
-- post defs: ["z1", "y2", "z3"]

----

define `z3`
define `y2`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "y2", "z3"]

----

define `z3`
define `y2`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "y2", "z3"]

----

define `z3`
define `y2`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "y2", "z3"]

----

define `z3`
define `y2`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "y2", "z3"]

----

define `z3`
define `y2`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "y2", "z3"]

----

define `z3`
define `y2`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "y2", "z3"]

----

define `z3`
define `y2`
define `z2`
use `z2`
-- post defs: ["z2", "y2", "z3"]

----

define `z3`
define `y2`
define `z2`
use `y2`
-- post defs: ["z2", "y2", "z3"]

----

define `z3`
define `y2`
define `z2`
use `z3`
-- post defs: ["z2", "y2", "z3"]

----

define `z3`
define `y2`
use `y2`
define `x1`
-- post defs: ["x1", "y2", "z3"]

----

define `z3`
define `y2`
use `y2`
define `x2`
-- post defs: ["x2", "y2", "z3"]

----

define `z3`
define `y2`
use `y2`
define `x3`
-- post defs: ["x3", "y2", "z3"]

----

define `z3`
define `y2`
use `y2`
define `y1`
-- post defs: ["y1", "y2", "z3"]

----

define `z3`
define `y2`
use `y2`
define `y3`
-- post defs: ["y3", "y2", "z3"]

----

define `z3`
define `y2`
use `y2`
define `z1`
-- post defs: ["z1", "y2", "z3"]

----

define `z3`
define `y2`
use `y2`
define `z2`
-- post defs: ["z2", "y2", "z3"]

----

define `z3`
define `y2`
use `y2`
use `y2`
-- post defs: ["y2", "z3"]

----

define `z3`
define `y2`
use `y2`
use `z3`
-- post defs: ["y2", "z3"]

----

define `z3`
define `y2`
use `z3`
define `x1`
-- post defs: ["x1", "y2", "z3"]

----

define `z3`
define `y2`
use `z3`
define `x2`
-- post defs: ["x2", "y2", "z3"]

----

define `z3`
define `y2`
use `z3`
define `x3`
-- post defs: ["x3", "y2", "z3"]

----

define `z3`
define `y2`
use `z3`
define `y1`
-- post defs: ["y1", "y2", "z3"]

----

define `z3`
define `y2`
use `z3`
define `y3`
-- post defs: ["y3", "y2", "z3"]

----

define `z3`
define `y2`
use `z3`
define `z1`
-- post defs: ["z1", "y2", "z3"]

----

define `z3`
define `y2`
use `z3`
define `z2`
-- post defs: ["z2", "y2", "z3"]

----

define `z3`
define `y2`
use `z3`
use `y2`
-- post defs: ["y2", "z3"]

----

define `z3`
define `y2`
use `z3`
use `z3`
-- post defs: ["y2", "z3"]

----

define `z3`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "z3"]

----

define `z3`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "z3"]

----

define `z3`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "z3"]

----

define `z3`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "z3"]

----

define `z3`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "z3"]

----

define `z3`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "z3"]

----

define `z3`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "z3"]

----

define `z3`
define `y3`
use `y3`
-- post defs: ["y3", "z3"]

----

define `z3`
define `y3`
use `z3`
-- post defs: ["y3", "z3"]

----

define `z3`
define `y3`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "y3", "z3"]

----

define `z3`
define `y3`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "y3", "z3"]

----

define `z3`
define `y3`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "y3", "z3"]

----

define `z3`
define `y3`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "y3", "z3"]

----

define `z3`
define `y3`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "y3", "z3"]

----

define `z3`
define `y3`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "y3", "z3"]

----

define `z3`
define `y3`
define `x1`
use `x1`
-- post defs: ["x1", "y3", "z3"]

----

define `z3`
define `y3`
define `x1`
use `y3`
-- post defs: ["x1", "y3", "z3"]

----

define `z3`
define `y3`
define `x1`
use `z3`
-- post defs: ["x1", "y3", "z3"]

----

define `z3`
define `y3`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "y3", "z3"]

----

define `z3`
define `y3`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "y3", "z3"]

----

define `z3`
define `y3`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "y3", "z3"]

----

define `z3`
define `y3`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "y3", "z3"]

----

define `z3`
define `y3`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "y3", "z3"]

----

define `z3`
define `y3`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "y3", "z3"]

----

define `z3`
define `y3`
define `x2`
use `x2`
-- post defs: ["x2", "y3", "z3"]

----

define `z3`
define `y3`
define `x2`
use `y3`
-- post defs: ["x2", "y3", "z3"]

----

define `z3`
define `y3`
define `x2`
use `z3`
-- post defs: ["x2", "y3", "z3"]

----

define `z3`
define `y3`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "y3", "z3"]

----

define `z3`
define `y3`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "y3", "z3"]

----

define `z3`
define `y3`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "y3", "z3"]

----

define `z3`
define `y3`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "y3", "z3"]

----

define `z3`
define `y3`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "y3", "z3"]

----

define `z3`
define `y3`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "y3", "z3"]

----

define `z3`
define `y3`
define `x3`
use `x3`
-- post defs: ["x3", "y3", "z3"]

----

define `z3`
define `y3`
define `x3`
use `y3`
-- post defs: ["x3", "y3", "z3"]

----

define `z3`
define `y3`
define `x3`
use `z3`
-- post defs: ["x3", "y3", "z3"]

----

define `z3`
define `y3`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "y3", "z3"]

----

define `z3`
define `y3`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "y3", "z3"]

----

define `z3`
define `y3`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "y3", "z3"]

----

define `z3`
define `y3`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "y3", "z3"]

----

define `z3`
define `y3`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "y3", "z3"]

----

define `z3`
define `y3`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "y3", "z3"]

----

define `z3`
define `y3`
define `y1`
use `y1`
-- post defs: ["y1", "y3", "z3"]

----

define `z3`
define `y3`
define `y1`
use `y3`
-- post defs: ["y1", "y3", "z3"]

----

define `z3`
define `y3`
define `y1`
use `z3`
-- post defs: ["y1", "y3", "z3"]

----

define `z3`
define `y3`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "y3", "z3"]

----

define `z3`
define `y3`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "y3", "z3"]

----

define `z3`
define `y3`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "y3", "z3"]

----

define `z3`
define `y3`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "y3", "z3"]

----

define `z3`
define `y3`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "y3", "z3"]

----

define `z3`
define `y3`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "y3", "z3"]

----

define `z3`
define `y3`
define `y2`
use `y2`
-- post defs: ["y2", "y3", "z3"]

----

define `z3`
define `y3`
define `y2`
use `y3`
-- post defs: ["y2", "y3", "z3"]

----

define `z3`
define `y3`
define `y2`
use `z3`
-- post defs: ["y2", "y3", "z3"]

----

define `z3`
define `y3`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "y3", "z3"]

----

define `z3`
define `y3`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "y3", "z3"]

----

define `z3`
define `y3`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "y3", "z3"]

----

define `z3`
define `y3`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "y3", "z3"]

----

define `z3`
define `y3`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "y3", "z3"]

----

define `z3`
define `y3`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "y3", "z3"]

----

define `z3`
define `y3`
define `z1`
use `z1`
-- post defs: ["z1", "y3", "z3"]

----

define `z3`
define `y3`
define `z1`
use `y3`
-- post defs: ["z1", "y3", "z3"]

----

define `z3`
define `y3`
define `z1`
use `z3`
-- post defs: ["z1", "y3", "z3"]

----

define `z3`
define `y3`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "y3", "z3"]

----

define `z3`
define `y3`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "y3", "z3"]

----

define `z3`
define `y3`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "y3", "z3"]

----

define `z3`
define `y3`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "y3", "z3"]

----

define `z3`
define `y3`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "y3", "z3"]

----

define `z3`
define `y3`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "y3", "z3"]

----

define `z3`
define `y3`
define `z2`
use `z2`
-- post defs: ["z2", "y3", "z3"]

----

define `z3`
define `y3`
define `z2`
use `y3`
-- post defs: ["z2", "y3", "z3"]

----

define `z3`
define `y3`
define `z2`
use `z3`
-- post defs: ["z2", "y3", "z3"]

----

define `z3`
define `y3`
use `y3`
define `x1`
-- post defs: ["x1", "y3", "z3"]

----

define `z3`
define `y3`
use `y3`
define `x2`
-- post defs: ["x2", "y3", "z3"]

----

define `z3`
define `y3`
use `y3`
define `x3`
-- post defs: ["x3", "y3", "z3"]

----

define `z3`
define `y3`
use `y3`
define `y1`
-- post defs: ["y1", "y3", "z3"]

----

define `z3`
define `y3`
use `y3`
define `y2`
-- post defs: ["y2", "y3", "z3"]

----

define `z3`
define `y3`
use `y3`
define `z1`
-- post defs: ["z1", "y3", "z3"]

----

define `z3`
define `y3`
use `y3`
define `z2`
-- post defs: ["z2", "y3", "z3"]

----

define `z3`
define `y3`
use `y3`
use `y3`
-- post defs: ["y3", "z3"]

----

define `z3`
define `y3`
use `y3`
use `z3`
-- post defs: ["y3", "z3"]

----

define `z3`
define `y3`
use `z3`
define `x1`
-- post defs: ["x1", "y3", "z3"]

----

define `z3`
define `y3`
use `z3`
define `x2`
-- post defs: ["x2", "y3", "z3"]

----

define `z3`
define `y3`
use `z3`
define `x3`
-- post defs: ["x3", "y3", "z3"]

----

define `z3`
define `y3`
use `z3`
define `y1`
-- post defs: ["y1", "y3", "z3"]

----

define `z3`
define `y3`
use `z3`
define `y2`
-- post defs: ["y2", "y3", "z3"]

----

define `z3`
define `y3`
use `z3`
define `z1`
-- post defs: ["z1", "y3", "z3"]

----

define `z3`
define `y3`
use `z3`
define `z2`
-- post defs: ["z2", "y3", "z3"]

----

define `z3`
define `y3`
use `z3`
use `y3`
-- post defs: ["y3", "z3"]

----

define `z3`
define `y3`
use `z3`
use `z3`
-- post defs: ["y3", "z3"]

----

define `z3`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "z3"]

----

define `z3`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "z3"]

----

define `z3`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "z3"]

----

define `z3`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "z3"]

----

define `z3`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "z3"]

----

define `z3`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "z3"]

----

define `z3`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "z3"]

----

define `z3`
define `z1`
use `z1`
-- post defs: ["z1", "z3"]

----

define `z3`
define `z1`
use `z3`
-- post defs: ["z1", "z3"]

----

define `z3`
define `z1`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "z1", "z3"]

----

define `z3`
define `z1`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "z1", "z3"]

----

define `z3`
define `z1`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "z1", "z3"]

----

define `z3`
define `z1`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "z1", "z3"]

----

define `z3`
define `z1`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "z1", "z3"]

----

define `z3`
define `z1`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "z1", "z3"]

----

define `z3`
define `z1`
define `x1`
use `x1`
-- post defs: ["x1", "z1", "z3"]

----

define `z3`
define `z1`
define `x1`
use `z1`
-- post defs: ["x1", "z1", "z3"]

----

define `z3`
define `z1`
define `x1`
use `z3`
-- post defs: ["x1", "z1", "z3"]

----

define `z3`
define `z1`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "z1", "z3"]

----

define `z3`
define `z1`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "z1", "z3"]

----

define `z3`
define `z1`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "z1", "z3"]

----

define `z3`
define `z1`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "z1", "z3"]

----

define `z3`
define `z1`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "z1", "z3"]

----

define `z3`
define `z1`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "z1", "z3"]

----

define `z3`
define `z1`
define `x2`
use `x2`
-- post defs: ["x2", "z1", "z3"]

----

define `z3`
define `z1`
define `x2`
use `z1`
-- post defs: ["x2", "z1", "z3"]

----

define `z3`
define `z1`
define `x2`
use `z3`
-- post defs: ["x2", "z1", "z3"]

----

define `z3`
define `z1`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "z1", "z3"]

----

define `z3`
define `z1`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "z1", "z3"]

----

define `z3`
define `z1`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "z1", "z3"]

----

define `z3`
define `z1`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "z1", "z3"]

----

define `z3`
define `z1`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "z1", "z3"]

----

define `z3`
define `z1`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "z1", "z3"]

----

define `z3`
define `z1`
define `x3`
use `x3`
-- post defs: ["x3", "z1", "z3"]

----

define `z3`
define `z1`
define `x3`
use `z1`
-- post defs: ["x3", "z1", "z3"]

----

define `z3`
define `z1`
define `x3`
use `z3`
-- post defs: ["x3", "z1", "z3"]

----

define `z3`
define `z1`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "z1", "z3"]

----

define `z3`
define `z1`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "z1", "z3"]

----

define `z3`
define `z1`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "z1", "z3"]

----

define `z3`
define `z1`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "z1", "z3"]

----

define `z3`
define `z1`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "z1", "z3"]

----

define `z3`
define `z1`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "z1", "z3"]

----

define `z3`
define `z1`
define `y1`
use `y1`
-- post defs: ["y1", "z1", "z3"]

----

define `z3`
define `z1`
define `y1`
use `z1`
-- post defs: ["y1", "z1", "z3"]

----

define `z3`
define `z1`
define `y1`
use `z3`
-- post defs: ["y1", "z1", "z3"]

----

define `z3`
define `z1`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "z1", "z3"]

----

define `z3`
define `z1`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "z1", "z3"]

----

define `z3`
define `z1`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "z1", "z3"]

----

define `z3`
define `z1`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "z1", "z3"]

----

define `z3`
define `z1`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "z1", "z3"]

----

define `z3`
define `z1`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "z1", "z3"]

----

define `z3`
define `z1`
define `y2`
use `y2`
-- post defs: ["y2", "z1", "z3"]

----

define `z3`
define `z1`
define `y2`
use `z1`
-- post defs: ["y2", "z1", "z3"]

----

define `z3`
define `z1`
define `y2`
use `z3`
-- post defs: ["y2", "z1", "z3"]

----

define `z3`
define `z1`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "z1", "z3"]

----

define `z3`
define `z1`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "z1", "z3"]

----

define `z3`
define `z1`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "z1", "z3"]

----

define `z3`
define `z1`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "z1", "z3"]

----

define `z3`
define `z1`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "z1", "z3"]

----

define `z3`
define `z1`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "z1", "z3"]

----

define `z3`
define `z1`
define `y3`
use `y3`
-- post defs: ["y3", "z1", "z3"]

----

define `z3`
define `z1`
define `y3`
use `z1`
-- post defs: ["y3", "z1", "z3"]

----

define `z3`
define `z1`
define `y3`
use `z3`
-- post defs: ["y3", "z1", "z3"]

----

define `z3`
define `z1`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "z1", "z3"]

----

define `z3`
define `z1`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "z1", "z3"]

----

define `z3`
define `z1`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "z1", "z3"]

----

define `z3`
define `z1`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "z1", "z3"]

----

define `z3`
define `z1`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "z1", "z3"]

----

define `z3`
define `z1`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "z1", "z3"]

----

define `z3`
define `z1`
define `z2`
use `z2`
-- post defs: ["z2", "z1", "z3"]

----

define `z3`
define `z1`
define `z2`
use `z1`
-- post defs: ["z2", "z1", "z3"]

----

define `z3`
define `z1`
define `z2`
use `z3`
-- post defs: ["z2", "z1", "z3"]

----

define `z3`
define `z1`
use `z1`
define `x1`
-- post defs: ["x1", "z1", "z3"]

----

define `z3`
define `z1`
use `z1`
define `x2`
-- post defs: ["x2", "z1", "z3"]

----

define `z3`
define `z1`
use `z1`
define `x3`
-- post defs: ["x3", "z1", "z3"]

----

define `z3`
define `z1`
use `z1`
define `y1`
-- post defs: ["y1", "z1", "z3"]

----

define `z3`
define `z1`
use `z1`
define `y2`
-- post defs: ["y2", "z1", "z3"]

----

define `z3`
define `z1`
use `z1`
define `y3`
-- post defs: ["y3", "z1", "z3"]

----

define `z3`
define `z1`
use `z1`
define `z2`
-- post defs: ["z2", "z1", "z3"]

----

define `z3`
define `z1`
use `z1`
use `z1`
-- post defs: ["z1", "z3"]

----

define `z3`
define `z1`
use `z1`
use `z3`
-- post defs: ["z1", "z3"]

----

define `z3`
define `z1`
use `z3`
define `x1`
-- post defs: ["x1", "z1", "z3"]

----

define `z3`
define `z1`
use `z3`
define `x2`
-- post defs: ["x2", "z1", "z3"]

----

define `z3`
define `z1`
use `z3`
define `x3`
-- post defs: ["x3", "z1", "z3"]

----

define `z3`
define `z1`
use `z3`
define `y1`
-- post defs: ["y1", "z1", "z3"]

----

define `z3`
define `z1`
use `z3`
define `y2`
-- post defs: ["y2", "z1", "z3"]

----

define `z3`
define `z1`
use `z3`
define `y3`
-- post defs: ["y3", "z1", "z3"]

----

define `z3`
define `z1`
use `z3`
define `z2`
-- post defs: ["z2", "z1", "z3"]

----

define `z3`
define `z1`
use `z3`
use `z1`
-- post defs: ["z1", "z3"]

----

define `z3`
define `z1`
use `z3`
use `z3`
-- post defs: ["z1", "z3"]

----

define `z3`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "z3"]

----

define `z3`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "z3"]

----

define `z3`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "z3"]

----

define `z3`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "z3"]

----

define `z3`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "z3"]

----

define `z3`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "z3"]

----

define `z3`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "z3"]

----

define `z3`
define `z2`
use `z2`
-- post defs: ["z2", "z3"]

----

define `z3`
define `z2`
use `z3`
-- post defs: ["z2", "z3"]

----

define `z3`
define `z2`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "z2", "z3"]

----

define `z3`
define `z2`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "z2", "z3"]

----

define `z3`
define `z2`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "z2", "z3"]

----

define `z3`
define `z2`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "z2", "z3"]

----

define `z3`
define `z2`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "z2", "z3"]

----

define `z3`
define `z2`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "z2", "z3"]

----

define `z3`
define `z2`
define `x1`
use `x1`
-- post defs: ["x1", "z2", "z3"]

----

define `z3`
define `z2`
define `x1`
use `z2`
-- post defs: ["x1", "z2", "z3"]

----

define `z3`
define `z2`
define `x1`
use `z3`
-- post defs: ["x1", "z2", "z3"]

----

define `z3`
define `z2`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "z2", "z3"]

----

define `z3`
define `z2`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "z2", "z3"]

----

define `z3`
define `z2`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "z2", "z3"]

----

define `z3`
define `z2`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "z2", "z3"]

----

define `z3`
define `z2`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "z2", "z3"]

----

define `z3`
define `z2`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "z2", "z3"]

----

define `z3`
define `z2`
define `x2`
use `x2`
-- post defs: ["x2", "z2", "z3"]

----

define `z3`
define `z2`
define `x2`
use `z2`
-- post defs: ["x2", "z2", "z3"]

----

define `z3`
define `z2`
define `x2`
use `z3`
-- post defs: ["x2", "z2", "z3"]

----

define `z3`
define `z2`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "z2", "z3"]

----

define `z3`
define `z2`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "z2", "z3"]

----

define `z3`
define `z2`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "z2", "z3"]

----

define `z3`
define `z2`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "z2", "z3"]

----

define `z3`
define `z2`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "z2", "z3"]

----

define `z3`
define `z2`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "z2", "z3"]

----

define `z3`
define `z2`
define `x3`
use `x3`
-- post defs: ["x3", "z2", "z3"]

----

define `z3`
define `z2`
define `x3`
use `z2`
-- post defs: ["x3", "z2", "z3"]

----

define `z3`
define `z2`
define `x3`
use `z3`
-- post defs: ["x3", "z2", "z3"]

----

define `z3`
define `z2`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "z2", "z3"]

----

define `z3`
define `z2`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "z2", "z3"]

----

define `z3`
define `z2`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "z2", "z3"]

----

define `z3`
define `z2`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "z2", "z3"]

----

define `z3`
define `z2`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "z2", "z3"]

----

define `z3`
define `z2`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "z2", "z3"]

----

define `z3`
define `z2`
define `y1`
use `y1`
-- post defs: ["y1", "z2", "z3"]

----

define `z3`
define `z2`
define `y1`
use `z2`
-- post defs: ["y1", "z2", "z3"]

----

define `z3`
define `z2`
define `y1`
use `z3`
-- post defs: ["y1", "z2", "z3"]

----

define `z3`
define `z2`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "z2", "z3"]

----

define `z3`
define `z2`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "z2", "z3"]

----

define `z3`
define `z2`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "z2", "z3"]

----

define `z3`
define `z2`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "z2", "z3"]

----

define `z3`
define `z2`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "z2", "z3"]

----

define `z3`
define `z2`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "z2", "z3"]

----

define `z3`
define `z2`
define `y2`
use `y2`
-- post defs: ["y2", "z2", "z3"]

----

define `z3`
define `z2`
define `y2`
use `z2`
-- post defs: ["y2", "z2", "z3"]

----

define `z3`
define `z2`
define `y2`
use `z3`
-- post defs: ["y2", "z2", "z3"]

----

define `z3`
define `z2`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "z2", "z3"]

----

define `z3`
define `z2`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "z2", "z3"]

----

define `z3`
define `z2`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "z2", "z3"]

----

define `z3`
define `z2`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "z2", "z3"]

----

define `z3`
define `z2`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "z2", "z3"]

----

define `z3`
define `z2`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "z2", "z3"]

----

define `z3`
define `z2`
define `y3`
use `y3`
-- post defs: ["y3", "z2", "z3"]

----

define `z3`
define `z2`
define `y3`
use `z2`
-- post defs: ["y3", "z2", "z3"]

----

define `z3`
define `z2`
define `y3`
use `z3`
-- post defs: ["y3", "z2", "z3"]

----

define `z3`
define `z2`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "z2", "z3"]

----

define `z3`
define `z2`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "z2", "z3"]

----

define `z3`
define `z2`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "z2", "z3"]

----

define `z3`
define `z2`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "z2", "z3"]

----

define `z3`
define `z2`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "z2", "z3"]

----

define `z3`
define `z2`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "z2", "z3"]

----

define `z3`
define `z2`
define `z1`
use `z1`
-- post defs: ["z1", "z2", "z3"]

----

define `z3`
define `z2`
define `z1`
use `z2`
-- post defs: ["z1", "z2", "z3"]

----

define `z3`
define `z2`
define `z1`
use `z3`
-- post defs: ["z1", "z2", "z3"]

----

define `z3`
define `z2`
use `z2`
define `x1`
-- post defs: ["x1", "z2", "z3"]

----

define `z3`
define `z2`
use `z2`
define `x2`
-- post defs: ["x2", "z2", "z3"]

----

define `z3`
define `z2`
use `z2`
define `x3`
-- post defs: ["x3", "z2", "z3"]

----

define `z3`
define `z2`
use `z2`
define `y1`
-- post defs: ["y1", "z2", "z3"]

----

define `z3`
define `z2`
use `z2`
define `y2`
-- post defs: ["y2", "z2", "z3"]

----

define `z3`
define `z2`
use `z2`
define `y3`
-- post defs: ["y3", "z2", "z3"]

----

define `z3`
define `z2`
use `z2`
define `z1`
-- post defs: ["z1", "z2", "z3"]

----

define `z3`
define `z2`
use `z2`
use `z2`
-- post defs: ["z2", "z3"]

----

define `z3`
define `z2`
use `z2`
use `z3`
-- post defs: ["z2", "z3"]

----

define `z3`
define `z2`
use `z3`
define `x1`
-- post defs: ["x1", "z2", "z3"]

----

define `z3`
define `z2`
use `z3`
define `x2`
-- post defs: ["x2", "z2", "z3"]

----

define `z3`
define `z2`
use `z3`
define `x3`
-- post defs: ["x3", "z2", "z3"]

----

define `z3`
define `z2`
use `z3`
define `y1`
-- post defs: ["y1", "z2", "z3"]

----

define `z3`
define `z2`
use `z3`
define `y2`
-- post defs: ["y2", "z2", "z3"]

----

define `z3`
define `z2`
use `z3`
define `y3`
-- post defs: ["y3", "z2", "z3"]

----

define `z3`
define `z2`
use `z3`
define `z1`
-- post defs: ["z1", "z2", "z3"]

----

define `z3`
define `z2`
use `z3`
use `z2`
-- post defs: ["z2", "z3"]

----

define `z3`
define `z2`
use `z3`
use `z3`
-- post defs: ["z2", "z3"]

----

define `z3`
use `z3`
define `x1`
-- post defs: ["x1", "z3"]

----

define `z3`
use `z3`
define `x2`
-- post defs: ["x2", "z3"]

----

define `z3`
use `z3`
define `x3`
-- post defs: ["x3", "z3"]

----

define `z3`
use `z3`
define `y1`
-- post defs: ["y1", "z3"]

----

define `z3`
use `z3`
define `y2`
-- post defs: ["y2", "z3"]

----

define `z3`
use `z3`
define `y3`
-- post defs: ["y3", "z3"]

----

define `z3`
use `z3`
define `z1`
-- post defs: ["z1", "z3"]

----

define `z3`
use `z3`
define `z2`
-- post defs: ["z2", "z3"]

----

define `z3`
use `z3`
use `z3`
-- post defs: ["z3"]

----

define `z3`
use `z3`
define `x1`
define `x2`
-- post defs: ["x2", "x1", "z3"]

----

define `z3`
use `z3`
define `x1`
define `x3`
-- post defs: ["x3", "x1", "z3"]

----

define `z3`
use `z3`
define `x1`
define `y1`
-- post defs: ["y1", "x1", "z3"]

----

define `z3`
use `z3`
define `x1`
define `y2`
-- post defs: ["y2", "x1", "z3"]

----

define `z3`
use `z3`
define `x1`
define `y3`
-- post defs: ["y3", "x1", "z3"]

----

define `z3`
use `z3`
define `x1`
define `z1`
-- post defs: ["z1", "x1", "z3"]

----

define `z3`
use `z3`
define `x1`
define `z2`
-- post defs: ["z2", "x1", "z3"]

----

define `z3`
use `z3`
define `x1`
use `x1`
-- post defs: ["x1", "z3"]

----

define `z3`
use `z3`
define `x1`
use `z3`
-- post defs: ["x1", "z3"]

----

define `z3`
use `z3`
define `x2`
define `x1`
-- post defs: ["x1", "x2", "z3"]

----

define `z3`
use `z3`
define `x2`
define `x3`
-- post defs: ["x3", "x2", "z3"]

----

define `z3`
use `z3`
define `x2`
define `y1`
-- post defs: ["y1", "x2", "z3"]

----

define `z3`
use `z3`
define `x2`
define `y2`
-- post defs: ["y2", "x2", "z3"]

----

define `z3`
use `z3`
define `x2`
define `y3`
-- post defs: ["y3", "x2", "z3"]

----

define `z3`
use `z3`
define `x2`
define `z1`
-- post defs: ["z1", "x2", "z3"]

----

define `z3`
use `z3`
define `x2`
define `z2`
-- post defs: ["z2", "x2", "z3"]

----

define `z3`
use `z3`
define `x2`
use `x2`
-- post defs: ["x2", "z3"]

----

define `z3`
use `z3`
define `x2`
use `z3`
-- post defs: ["x2", "z3"]

----

define `z3`
use `z3`
define `x3`
define `x1`
-- post defs: ["x1", "x3", "z3"]

----

define `z3`
use `z3`
define `x3`
define `x2`
-- post defs: ["x2", "x3", "z3"]

----

define `z3`
use `z3`
define `x3`
define `y1`
-- post defs: ["y1", "x3", "z3"]

----

define `z3`
use `z3`
define `x3`
define `y2`
-- post defs: ["y2", "x3", "z3"]

----

define `z3`
use `z3`
define `x3`
define `y3`
-- post defs: ["y3", "x3", "z3"]

----

define `z3`
use `z3`
define `x3`
define `z1`
-- post defs: ["z1", "x3", "z3"]

----

define `z3`
use `z3`
define `x3`
define `z2`
-- post defs: ["z2", "x3", "z3"]

----

define `z3`
use `z3`
define `x3`
use `x3`
-- post defs: ["x3", "z3"]

----

define `z3`
use `z3`
define `x3`
use `z3`
-- post defs: ["x3", "z3"]

----

define `z3`
use `z3`
define `y1`
define `x1`
-- post defs: ["x1", "y1", "z3"]

----

define `z3`
use `z3`
define `y1`
define `x2`
-- post defs: ["x2", "y1", "z3"]

----

define `z3`
use `z3`
define `y1`
define `x3`
-- post defs: ["x3", "y1", "z3"]

----

define `z3`
use `z3`
define `y1`
define `y2`
-- post defs: ["y2", "y1", "z3"]

----

define `z3`
use `z3`
define `y1`
define `y3`
-- post defs: ["y3", "y1", "z3"]

----

define `z3`
use `z3`
define `y1`
define `z1`
-- post defs: ["z1", "y1", "z3"]

----

define `z3`
use `z3`
define `y1`
define `z2`
-- post defs: ["z2", "y1", "z3"]

----

define `z3`
use `z3`
define `y1`
use `y1`
-- post defs: ["y1", "z3"]

----

define `z3`
use `z3`
define `y1`
use `z3`
-- post defs: ["y1", "z3"]

----

define `z3`
use `z3`
define `y2`
define `x1`
-- post defs: ["x1", "y2", "z3"]

----

define `z3`
use `z3`
define `y2`
define `x2`
-- post defs: ["x2", "y2", "z3"]

----

define `z3`
use `z3`
define `y2`
define `x3`
-- post defs: ["x3", "y2", "z3"]

----

define `z3`
use `z3`
define `y2`
define `y1`
-- post defs: ["y1", "y2", "z3"]

----

define `z3`
use `z3`
define `y2`
define `y3`
-- post defs: ["y3", "y2", "z3"]

----

define `z3`
use `z3`
define `y2`
define `z1`
-- post defs: ["z1", "y2", "z3"]

----

define `z3`
use `z3`
define `y2`
define `z2`
-- post defs: ["z2", "y2", "z3"]

----

define `z3`
use `z3`
define `y2`
use `y2`
-- post defs: ["y2", "z3"]

----

define `z3`
use `z3`
define `y2`
use `z3`
-- post defs: ["y2", "z3"]

----

define `z3`
use `z3`
define `y3`
define `x1`
-- post defs: ["x1", "y3", "z3"]

----

define `z3`
use `z3`
define `y3`
define `x2`
-- post defs: ["x2", "y3", "z3"]

----

define `z3`
use `z3`
define `y3`
define `x3`
-- post defs: ["x3", "y3", "z3"]

----

define `z3`
use `z3`
define `y3`
define `y1`
-- post defs: ["y1", "y3", "z3"]

----

define `z3`
use `z3`
define `y3`
define `y2`
-- post defs: ["y2", "y3", "z3"]

----

define `z3`
use `z3`
define `y3`
define `z1`
-- post defs: ["z1", "y3", "z3"]

----

define `z3`
use `z3`
define `y3`
define `z2`
-- post defs: ["z2", "y3", "z3"]

----

define `z3`
use `z3`
define `y3`
use `y3`
-- post defs: ["y3", "z3"]

----

define `z3`
use `z3`
define `y3`
use `z3`
-- post defs: ["y3", "z3"]

----

define `z3`
use `z3`
define `z1`
define `x1`
-- post defs: ["x1", "z1", "z3"]

----

define `z3`
use `z3`
define `z1`
define `x2`
-- post defs: ["x2", "z1", "z3"]

----

define `z3`
use `z3`
define `z1`
define `x3`
-- post defs: ["x3", "z1", "z3"]

----

define `z3`
use `z3`
define `z1`
define `y1`
-- post defs: ["y1", "z1", "z3"]

----

define `z3`
use `z3`
define `z1`
define `y2`
-- post defs: ["y2", "z1", "z3"]

----

define `z3`
use `z3`
define `z1`
define `y3`
-- post defs: ["y3", "z1", "z3"]

----

define `z3`
use `z3`
define `z1`
define `z2`
-- post defs: ["z2", "z1", "z3"]

----

define `z3`
use `z3`
define `z1`
use `z1`
-- post defs: ["z1", "z3"]

----

define `z3`
use `z3`
define `z1`
use `z3`
-- post defs: ["z1", "z3"]

----

define `z3`
use `z3`
define `z2`
define `x1`
-- post defs: ["x1", "z2", "z3"]

----

define `z3`
use `z3`
define `z2`
define `x2`
-- post defs: ["x2", "z2", "z3"]

----

define `z3`
use `z3`
define `z2`
define `x3`
-- post defs: ["x3", "z2", "z3"]

----

define `z3`
use `z3`
define `z2`
define `y1`
-- post defs: ["y1", "z2", "z3"]

----

define `z3`
use `z3`
define `z2`
define `y2`
-- post defs: ["y2", "z2", "z3"]

----

define `z3`
use `z3`
define `z2`
define `y3`
-- post defs: ["y3", "z2", "z3"]

----

define `z3`
use `z3`
define `z2`
define `z1`
-- post defs: ["z1", "z2", "z3"]

----

define `z3`
use `z3`
define `z2`
use `z2`
-- post defs: ["z2", "z3"]

----

define `z3`
use `z3`
define `z2`
use `z3`
-- post defs: ["z2", "z3"]

----

define `z3`
use `z3`
use `z3`
define `x1`
-- post defs: ["x1", "z3"]

----

define `z3`
use `z3`
use `z3`
define `x2`
-- post defs: ["x2", "z3"]

----

define `z3`
use `z3`
use `z3`
define `x3`
-- post defs: ["x3", "z3"]

----

define `z3`
use `z3`
use `z3`
define `y1`
-- post defs: ["y1", "z3"]

----

define `z3`
use `z3`
use `z3`
define `y2`
-- post defs: ["y2", "z3"]

----

define `z3`
use `z3`
use `z3`
define `y3`
-- post defs: ["y3", "z3"]

----

define `z3`
use `z3`
use `z3`
define `z1`
-- post defs: ["z1", "z3"]

----

define `z3`
use `z3`
use `z3`
define `z2`
-- post defs: ["z2", "z3"]

----

define `z3`
use `z3`
use `z3`
use `z3`
-- post defs: ["z3"]

----

